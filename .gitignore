.beads/
.claude-octopus/
.metaswarm/
*.tmp

# Internal design specs / probe notes (not for publication)
docs/superpowers/

# Local agent-instruction files (not for publication)
AGENTS.md
CLAUDE.md

# Local agent/dev tooling caches (not part of the plugin)
.agents/
.codex/
.headroom/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
.claude/settings.json
.claude/settings.local.json
.worktrees/

# GSD isolation sentinel (run-scoped, not project state)
.gsd/
