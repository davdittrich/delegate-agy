# Context (DOC intel)

No documents classified as DOC were present in this ingest set. All six classified documents
in `.planning/intel/classifications/` are typed SPEC. No entries to extract.

Absent — architectural facts recurring across the SPEC set (PATH-shadowing of `gemini`,
SIGTERM handling) are captured, correctly typed, in `constraints.md`. A separate, non-ingested
codebase map exists at `.planning/codebase/` (7 documents; `CONCERNS.md` is current and
evidence-backed) for verifying claims against present reality — it was not classified or
synthesized here because it is not part of the ingest manifest.
