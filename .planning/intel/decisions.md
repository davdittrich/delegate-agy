# Decisions (ADR intel)

No documents classified as ADR (locked or proposed) were present in this ingest set. All six
classified documents in `.planning/intel/classifications/` are typed SPEC. No entries to extract.

Absent — do not infer ADR-equivalent decisions from SPEC design rationale; that content is
captured, correctly typed, in `constraints.md` instead.
