---
phase: quick-260822-m01
plan: 01
subsystem: infra
tags: [mcp, tokensave, lean-ctx, install-sh, agy-bridge, policy]

requires: []
provides:
  - "install.sh/uninstall.sh no longer know about a tokensave MCP server; they only detect and hint lean-ctx"
  - "agy-bridge's TOOL PREFERENCE stanza and its 60-min cache are keyed on a single lean_ctx value"
  - "every FORBIDDEN allowlist catch-all and PERMITTED list in config/policies/*.md is free of the retired tool name"
affects: []

actuals:
  tokens: 16251
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "RETIRED-named test literals convention: any surviving mention of a decommissioned name in a test fixture is bound to a variable prefixed _<TESTID>_RETIRED_* so residual-reference greps can allowlist it explicitly instead of by accident"

key-files:
  created: []
  modified:
    - tests/run-tests.sh
    - scripts/install.sh
    - scripts/uninstall.sh
    - scripts/agy_bridge.sh
    - config/policies/code.md
    - config/policies/implement.md
    - config/policies/review-analysis.md
    - config/policies/search.md
    - config/policies/shim-default.md
    - config/policies/shim-sandbox.md
    - config/policies/shim-yolo.md
    - config/provider.md
    - agents/agy-delegate-code.md
    - README.md
    - .claude/commands/agy-setup.md
    - .claude/commands/agy-uninstall.md
    - .gitignore

key-decisions:
  - "D-02 overturned per explicit user instruction mid-plan: the FORBIDDEN allowlist catch-all's mention of the retired tool name is removed from all four search/shim policies and provider.md's catch-all bullet too, not left in place as originally planned (the catch-all's own leading default-deny clause covers the retired tool implicitly either way, so this is a legibility change with zero permission-boundary effect, per the plan's re-assessed T-QT01-02)"
  - "install.sh's _agy_detect collapses from a two-value (lean_ctx, tokensave) probe to a single lean_ctx probe; the availability hint file drops the tokensave key entirely rather than keeping it hardcoded false"
  - "agy_bridge.sh's MCP cache file collapses from two lines (lean_ctx, tokensave) to one; the TOOL PREFERENCE stanza's OR-condition collapses to a single equality check"
  - ".claude/commands/agy-uninstall.md gets a one-sentence migration breadcrumb telling anyone who used the retired AGY_SETUP_REGISTER_TOKENSAVE=1 opt-in to remove the tokensave entry from ~/.gemini/antigravity-cli/mcp_config.json by hand, since the uninstaller no longer manages it (D-05)"

requirements-completed: [QT-01]

coverage:
  - id: D1
    description: "install.sh never writes to the agy MCP config; AGY_SETUP_REGISTER_TOKENSAVE=1 is inert; the hint carries exactly {lean_ctx: bool}"
    requirement: "QT-01"
    verification:
      - kind: unit
        ref: "tests/run-tests.sh#I9 install never mutates the agy MCP config (retired opt-in is inert)"
        status: pass
      - kind: unit
        ref: "tests/run-tests.sh#I11 availability hint carries exactly the lean_ctx key"
        status: pass
    human_judgment: false
  - id: D2
    description: "uninstall.sh removes the availability hint unconditionally, no env flag, never touches the agy MCP config"
    requirement: "QT-01"
    verification:
      - kind: unit
        ref: "tests/run-tests.sh#I13b uninstall removes the availability hint"
        status: pass
    human_judgment: false
  - id: D3
    description: "every policy FORBIDDEN catch-all and PERMITTED list is free of the retired tool name, while the four catch-all files stay byte-identical to each other"
    requirement: "QT-01"
    verification:
      - kind: unit
        ref: "tests/run-tests.sh#ST1 catch-all line byte-identical across search+shim policies"
        status: pass
      - kind: unit
        ref: "tests/run-tests.sh#ST2 catch-all covers ctx_call + mcp__ and names no retired server"
        status: pass
      - kind: unit
        ref: "tests/run-tests.sh#ST3 MCP policies permit read-only ctx, never exec/edit tools"
        status: pass
    human_judgment: false

duration: ~25min
completed: 2026-08-22
status: complete
---

# Quick Task 260822-m01: Remove tokensave from the plugin, completely Summary

**Deleted the tokensave MCP-server registration mechanism from install.sh/uninstall.sh/agy_bridge.sh and scrubbed every remaining mention of the retired tool from policy files, the code-review agent, README, and the setup/uninstall command docs — full suite green at PASS=164 FAIL=0.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-08-22T14:39:54Z
- **Tasks:** 3
- **Files modified:** 17

## Accomplishments
- Rewrote 17 test cases (ST2, ST3, T7 fixtures M1/M2/SH1, I9/I10/I11/I13b) to assert absence of the retired tokensave surface instead of its presence; deleted I9b outright (RED tracer, exactly 5 expected FAILs)
- Removed `_register_tokensave`, `TOKENSAVE_BIN`, the interactive/env-flag opt-in prompt, and the two-value MCP-config write from install.sh; `_agy_detect`/the hint writer now only know about `lean_ctx`
- Removed the tokensave de-registration block and `AGY_UNINSTALL_TOKENSAVE=1` gate from uninstall.sh; it now unconditionally removes the availability hint
- Collapsed agy_bridge.sh's MCP-availability cache from a two-line (lean_ctx, tokensave) format to one line, and its stanza-gating condition from an OR to a single check
- Removed every catch-all and PERMITTED-list mention of the retired tool from all four search/shim policy files (kept byte-identical to each other) and provider.md, per the user's mid-plan D-02 override
- Removed tokensave from agents/agy-delegate-code.md's tools[] frontmatter and prose, README.md's opt-in/uninstall paragraphs, and both `/agy-setup`/`/agy-uninstall` command docs (with a one-sentence migration breadcrumb for anyone who used the old opt-in)

## Task Commits

Each task was committed atomically:

1. **Task 1: Retarget test suite off tokensave (RED tracer)** - `13f7000` (test)
2. **Task 2: Remove tokensave MCP registration mechanism** - `46ead1b` (feat)
3. **Task 3: Remove docs/policy mentions of tokensave (suite goes green)** - `2b6b9b1` (docs)

_Note: Task 1 is a deliberate RED tracer — it left the suite at PASS=159 FAIL=5 (ST2, ST3, I9, I11, I13b) before any source change, confirming the new assertions actually exercise the not-yet-changed code._

## Files Created/Modified
- `tests/run-tests.sh` - ST2/ST3 assert absence of the retired tool; T7 fixtures use single-key `lean_ctx` hints; I9/I10/I11/I13b repurposed to assert install/uninstall never touch the agy MCP config; I9b deleted
- `scripts/install.sh` - `_agy_detect` now probes lean-ctx only and never mutates `mcp_config.json`; opt-in registration code and its env flag removed
- `scripts/uninstall.sh` - de-registration block and its env flag removed; hint removal is now unconditional
- `scripts/agy_bridge.sh` - single-value MCP cache and stanza-gating condition; help text and stanza wording de-tokensaved
- `config/policies/{code,implement,review-analysis}.md` - PERMITTED lists drop `tokensave_context`
- `config/policies/{search,shim-default,shim-sandbox,shim-yolo}.md` - FORBIDDEN catch-all drops the `tokensave_*` clause, still byte-identical across all four
- `config/provider.md` - MCP Integration bullet, catch-all bullet, and tool-preference stanza description de-tokensaved
- `agents/agy-delegate-code.md` - tools[] frontmatter and "gather context" bullet drop tokensave
- `README.md` - opt-in and uninstall paragraphs drop the tokensave register/de-register clauses
- `.claude/commands/agy-setup.md` - register-tokensave section deleted; migration breadcrumb added
- `.claude/commands/agy-uninstall.md` - flag mentions dropped from frontmatter/body/de-register section; migration breadcrumb added
- `.gitignore` - `.tokensave/` line removed
- `.claude/settings.local.json` - `mcp__tokensave__tokensave_status` permission entry removed (edited locally; file is gitignored, not committed — see Issues Encountered)

## Decisions Made
- D-02 overturned mid-plan per explicit user instruction: catch-all mentions of the retired name are removed too, not just PERMITTED-list mentions (see key-decisions in frontmatter)
- Collapsed the two-value lean_ctx/tokensave probe and cache format to a single value throughout install.sh, uninstall.sh, and agy_bridge.sh rather than keeping a dead second field
- Migration breadcrumbs (one sentence each) added to `/agy-setup` and `/agy-uninstall` docs so anyone who used the retired opt-in knows to clean up `~/.gemini/antigravity-cli/mcp_config.json` by hand

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] ST3's negative probe used a bare `tokensave` literal instead of a RETIRED-named variable**
- **Found during:** Task 3's residual-grep verification sweep (`grep -rniE 'tokensave' ... | grep -vE 'RETIRED|previously registered'`)
- **Issue:** The plan's own audit convention allowlists only `RETIRED`-named test literals and the `previously registered` migration-breadcrumb wording; my initial ST3 edit (Task 1) used a raw `grep -q 'tokensave' "$ROOT/config/policies/$f.md"` literal, which the sweep correctly flagged as an unmarked survivor
- **Fix:** Bound the literal to `_ST3_RETIRED_TOOL="tokensave"` (matching the `_ST2_RETIRED_TOOL`/`_I9_RETIRED_BIN` convention already used elsewhere in the same edit pass) and referenced the variable in the grep
- **Files modified:** tests/run-tests.sh
- **Verification:** Residual sweep re-run empty; full suite still PASS=164 FAIL=0
- **Committed in:** `2b6b9b1` (part of Task 3 commit, since the fix was discovered during Task 3's verification, after Task 1's commit had already landed)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug in my own prior test edit)
**Impact on plan:** No scope creep; fix was required for the plan's own stated verification command to pass clean.

## Issues Encountered
`.claude/settings.local.json` is listed in the plan's `files_modified` and was edited (removed the `mcp__tokensave__tokensave_status` permission entry, JSON validated), but the file is gitignored per this repo's `.gitignore` and was never tracked — `git add` refuses it without `-f`. Per CLAUDE.md's git-hygiene rule (audit tracked files, don't force-add gitignored dev artifacts) I left it untracked/local-only rather than force-adding it. The edit is present on disk for local functionality; it will not appear in any commit or diff.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Nothing blocked. Full suite green (PASS=164 FAIL=0, exactly one below the 165 baseline since I9b was deleted with no replacement). Residual grep for `tokensave` across all shipped paths (README, AGENTS.md, GEMINI.md, CLAUDE.md, .gitignore, scripts/, agents/, skills/, hooks/, config/, .claude/commands/, .claude/settings.local.json, tests/run-tests.sh) is empty except the two intentionally-allowlisted categories (RETIRED-named test literals, migration-breadcrumb prose).

---
*Phase: quick-260822-m01*
*Completed: 2026-08-22*

## Self-Check: PASSED

All 12 files verified present on disk; all 3 commit hashes (`13f7000`, `46ead1b`, `2b6b9b1`) verified present in `git log --oneline --all`.
