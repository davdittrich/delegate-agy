---
beads_epic: delegate-agy-byv
phase: 03-the-exit-code-contract
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/fake-agy.sh
  - tests/run-tests.sh
  - scripts/agy_bridge.sh
autonomous: true
requirements: [R5]
beads: [delegate-agy-v5a]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "When agy writes zero bytes to stderr, the bridge's external-kill message ends after the word `kill` with no trailing separator, in the plain-text form and inside the JSON envelope's `error` string. (edge:R5/empty — explicit, from D-03)"
    - "When agy writes bytes to stderr, the bridge's external-kill message ends with a colon, a space, and those bytes, in both output forms. (edge:R5/empty — explicit, from D-03)"
    - "Emptiness is byte-emptiness after trailing-newline stripping: a stderr file holding only newline characters counts as empty and drops the suffix, identically in the plain-text and JSON forms. Before this plan the two forms disagreed here — `$(cat ...)` strips the trailing newline while `open(...).read()` keeps it — and the single shared guard removes that disagreement. (edge:R5/encoding — explicit)"
    - "agy's stderr bytes reach `printf` only as a `%s` argument and reach `python3` only as a positional `sys.argv` slot; they are never spliced into a printf format string nor into python3 source text."
  artifacts:
    - "scripts/agy_bridge.sh — `EC_KILL9_TAIL` defined exactly once, referenced by both the plain-text and the JSON form of the external-kill branch"
    - "tests/run-tests.sh — cases EC01 (plain text) and EC02 (JSON), each asserting both the empty-stderr and the non-empty-stderr result"
    - "tests/fake-agy.sh — the `FAKE_AGY_PRINT_KILL9` path emits `FAKE_AGY_STDERR` before exiting 137"
  key_links:
    - "tests/fake-agy.sh `FAKE_AGY_PRINT_KILL9` path -> the bridge's `EXIT_CODE -eq 137 && DURATION -lt TIMEOUT` branch: without the fake writing stderr on that path, the non-empty half of every assertion below is unreachable and the cases pass vacuously"
    - "one guarded suffix expansion -> both the printf argument list and the python3 argv list: a second, independent guard in either place reintroduces the divergence this plan removes"
  prohibitions:
    - "An error message emitted by either entry point must never end in a separator with nothing after it."
    - "Captured agy stderr must never be interpolated into python3 source text; it crosses into python3 only as a positional argv element."
    - "Captured agy stderr must never become part of a printf format string; a `%` in agy's output must not be read as a format directive."
    - "No existing assertion in tests/run-tests.sh may be weakened, deleted, or made conditional in order to let this plan's change pass."
  flagged_assumptions:
    - "edge:R5/precision — elapsed time is integer seconds from bash's `SECONDS`, truncated toward zero. Sub-second resolution does not exist, so a SIGKILL arriving in the final second of the bound can read as `DURATION < TIMEOUT` and be classified an external kill. This is shipped behavior (Phase 1 D-02), unchanged and untested here; stated as a ceiling, not closed."
---

<objective>
Close the dangling-separator defect (`delegate-agy-v5a`) on the bridge's external-kill branch, in both the plain-text and the JSON output form, and prove it with runtime byte-exact assertions.

This is the phase's tracer: the thinnest slice that touches every layer the phase will modify — the fake that provokes the condition, the shipped script that formats the message, and the suite that pins the bytes. Plans 02-04 expand outward from it to the sibling script, the sibling branch, README, and the remaining exit codes.

Purpose: an operator whose agy was OOM-killed with no stderr currently reads a message that trails off into a colon and nothing. Criterion 3 of the phase forbids it, in either output form.
Output: a guarded suffix built once per branch, one shared literal, and two suite cases that fail if either form regresses.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-the-exit-code-contract/03-CONTEXT.md
@.planning/phases/03-the-exit-code-contract/03-PATTERNS.md
</context>

## Alternatives Considered

Mechanism choice: how to append `": <stderr>"` only when stderr is non-empty, in a way that one piece of code serves both the plain-text and the JSON form.

- **Bash alternate-value parameter expansion `${var:+word}`** — `bash(1)` §"Shell Parameter Expansion", GNU Bash Reference Manual 5.3 (2025), <https://www.gnu.org/software/bash/manual/bash.html#Shell-Parameter-Expansion>. One expansion produces the whole suffix or the empty string; the same expansion is passed as a `printf` `%s` argument and as a `python3` argv element, so the guard exists in exactly one place. Verified against empty, single-word, multi-word, tab-bearing, and format-specifier-bearing content before this plan was written.
- **Explicit `[[ -n "$x" ]] && _suffix=": $x"` guard building a temporary variable** — `.planning/phases/03-the-exit-code-contract/03-PATTERNS.md` §"D-03's target shape" (2026). Functionally identical; three lines instead of one expansion, and the extra variable is written once and read twice with no other purpose.
- **Conditional inside the `json.dumps` call (Python-side `if`)** — Python `json` module docs, <https://docs.python.org/3/library/json.html> (2025), applied to the shipped shape at `scripts/agy_bridge.sh:724-728` (2026). Rejected outright: it duplicates the emptiness test in a second language, which is the exact structure that let the same bug ship in four places.

**Decided by: LOC.** All three are correct and none has a performance, ecosystem, or maintenance edge over the others; the parameter expansion is one line where the alternatives are three lines and two languages respectively.

<artifacts_this_phase_produces>
New symbols introduced by this plan (they do not exist on `master` and must not be treated as drift):

| Symbol | Kind | Location |
|--------|------|----------|
| `EC_KILL9_TAIL` | shell constant | `scripts/agy_bridge.sh` (external-kill branch) |
| `_err_txt` | shell local | `scripts/agy_bridge.sh` (external-kill branch) |
| `_EC_KILL9_TAIL` | test-local literal | `tests/run-tests.sh` |
| `EC01`, `EC02` | test case ids | `tests/run-tests.sh` |

No new files. `EC_KILL9_TAIL` also appears in `scripts/gemini_shim.sh`, introduced by plan `03-02`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — the bridge's plain-text external-kill message drops its separator on empty stderr</name>
  <beads-id>delegate-agy-byv.1</beads-id>
  <files>tests/fake-agy.sh, tests/run-tests.sh, scripts/agy_bridge.sh</files>
  <reversibility rating="reversible">A guard around an existing separator plus one hoisted constant, local to one branch of one file; reverting is a single-hunk revert (03-CONTEXT.md D-01, D-03).</reversibility>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-byv.1` -->
</task>

<task type="auto" tdd="true">
  <name>Task 2: The same message inside the JSON envelope</name>
  <beads-id>delegate-agy-byv.2</beads-id>
  <files>tests/run-tests.sh, scripts/agy_bridge.sh</files>
  <reversibility rating="reversible">Two argv slots and one Python concatenation; reverting restores the previous string exactly (03-CONTEXT.md D-02).</reversibility>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-byv.2` -->
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agy subprocess -> `$STDERR_FILE` -> the bridge | agy is an external binary; every byte it writes to stderr is untrusted input to the message-formatting code this plan edits. |
| the bridge -> `python3 -c` argv | the untrusted stderr bytes cross a language boundary into a Python interpreter. |
| the bridge -> the caller's terminal / JSON consumer | Metaswarm and Octopus parse the envelope; the operator reads the text form. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information disclosure | external-kill message, both forms | low | accept | Relaying agy's stderr verbatim is the requirement (R6 "with agy's own stderr as the reason"). The bridge adds no new source of data; it only decides whether to prefix a separator. Accepted as designed. |
| T-03-02 | Tampering / injection | `python3 -c` call at the external-kill branch | high | mitigate | Task 2 keeps every value positional in `sys.argv`; the Python source string gains no `$`-expansion of captured data. Enforced by the last two `<acceptance_criteria>` of task 2 and by the pre-existing five-site convention documented in 03-PATTERNS.md §"JSON envelope construction". |
| T-03-03 | Tampering | JSON envelope structure | medium | mitigate | Escaping stays `json.dumps`'s job; the change adds a string concatenation of two argv elements and introduces no manual quoting. EC02 parses the envelope with a real JSON parser, so a structurally broken envelope fails the case rather than passing a substring check. |
| T-03-04 | Tampering | `printf` format string, plain-text arm | medium | mitigate | agy's stderr must remain a `%s` argument. Reproduced 2026-08-20: content of `100%s%d` renders as `1000` if spliced into the format and renders literally if passed as an argument. Pinned by task 1's format-specifier acceptance criterion. |
| T-03-05 | Supply chain | package installs | n/a | n/a | This plan installs no npm/pip/cargo package and adds no dependency; no legitimacy gate applies. |
</threat_model>

<verification>
- Full suite green: `bash tests/run-tests.sh` ends `FAIL=0`, `PASS` above the 145 baseline.
- `EC01` and `EC02` both report `ok`, and each was observed reporting `FAIL` before its corresponding script change (recorded in the SUMMARY).
- No pre-existing case id changed status; in particular `T5`, `B4`, `R9e` and `RB03` still report `ok`.
- Manual probe of all four scenarios (empty, non-empty, newline-only, format-specifier) on both output forms, output pasted into the SUMMARY.
</verification>

<success_criteria>
The bridge's external-kill branch emits no dangling separator in either output form, the guard exists in exactly one place per branch, agy's stderr never reaches a format string or Python source, and two suite cases fail if any of that regresses.
</success_criteria>

<output>
Create `.planning/phases/03-the-exit-code-contract/03-01-SUMMARY.md` when done. Record: the observed RED output for EC01 and EC02; the decided JSON trailing-newline change; the baseline and final `PASS=`/`FAIL=` counts.
</output>
