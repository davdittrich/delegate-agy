---
beads_epic: delegate-agy-byv
phase: 03-the-exit-code-contract
plan: 04
type: tdd
wave: 4
depends_on: ["03-03"]
files_modified:
  - tests/run-tests.sh
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [R5, R6]
beads: [delegate-agy-v5a, delegate-agy-6q1]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "EC07 ITSELF provokes and asserts the exact numeric value of FOUR codes: 3, 124 and 137 on both entry points, and 2 on the bridge (the shim has no model-list path). This is what the case executes, and this is the limit of what it claims to execute. (R5 acceptance, criterion 1)"
    - "EC07 ITSELF asserts cause-fragment exclusivity over exactly those four captured messages — including exit 2's degraded-model-list message, which is part of the exclusivity loop even though its wording remains `R8`'s responsibility per D-10. Each captured message contains exactly one of the four cause fragments and none of the other three: a timeout is never reported as a kill, an empty output is never reported as a timeout. (criterion 1, criterion 2)"
    - "Exit 127 is NOT provoked by EC07 and EC07 makes no exclusivity claim about it. Provoking it requires the generated-launcher fixture that `I16` and `RB29` build, and rebuilding that fixture to re-assert a code those cases already pin exactly would be duplicated cost for no new evidence. What EC07 does instead is pin the CITATION against rot with two source assertions over `tests/run-tests.sh` itself, each verified to match exactly once on the unmodified tree: exactly one line matching `-eq 127 ]]` followed by `|| I16_OK=0` (currently `tests/run-tests.sh:3597`), and exactly one matching `-eq 127 ]]` followed by `|| RB29_OK=0` (currently `:3924`). If either cited case is loosened from an exact-code assertion, EC07 fails rather than continuing to cite a case that no longer proves what it is cited for. (Codex review finding, HIGH — the plan's five-code claim is narrowed to the four codes EC07 runs, and the fifth's delegation is made load-bearing instead of decorative.)"
    - "The negative half of exit 127's coverage is likewise cited, not re-asserted, and likewise pinned: `I16` asserts the stale-pin run produces empty stdout (`tests/run-tests.sh:3598`, `[[ -z \"$OUT_STALE\" ]]`) and `RB29` asserts the same (`:3925`, `[[ -z \"$RB29_SOUT\" ]]`), each matching exactly once today. Neither cited case asserts absence of the timeout/kill/empty-output fragments, so EC07 does not claim that property for 127; the gap is stated here rather than implied away. (Codex review finding, HIGH)"
    - "The 137-versus-124 discrimination is strictly `elapsed < bound`, so a kill whose elapsed duration equals the bound exactly is reported as a timeout, not as an external kill. (edge:R5/adjacency — explicit, source assertion)"
    - "The error branch's arm order is fixed: early-137, then 124-or-137, then generic-nonzero, then empty-stdout; a run matching more than one condition takes the earliest arm. (edge:R5/ordering — explicit, source assertion)"
    - "agy exiting 0 with a strictly zero-byte stdout yields exit 3 with a zero-byte stdout in text mode on both entry points. (edge:R6/empty — explicit, from R6 acceptance)"
    - "In JSON mode both entry points emit an error envelope with no `response` key: the bridge's flat envelope carries `error` and `error_class`, the shim's nested envelope carries `error.message` and `error.class`. (edge:R6/empty — explicit, from R6 acceptance)"
    - "Empty stdout is byte-emptiness as `test -s` decides it: a stdout of exactly one newline byte is a non-empty response, passes through as success with exit 0, and does not trigger exit 3. (edge:R6/encoding — explicit)"
    - "For that one-newline stdout, the JSON behavior is asserted rather than assumed, in all four success shapes. Verified 2026-08-20 against live source: the bridge's JSON success arm reads the file in Python (`scripts/agy_bridge.sh:793`, `'response':open(sys.argv[4]).read()`) and the shim's reads it on stdin (`scripts/gemini_shim.sh:716-724`, `'response': sys.stdin.read()` with `< \"$STDOUT_FILE\"`), so BOTH preserve the byte and both envelopes carry `response` equal to a single newline — not `\"\"`, not an absent key. The text arms also round-trip it: the bridge `cat`s the file (`:796`) and the shim's `printf '%s\\n' \"$RESPONSE\"` (`:726`) reprints the newline that `RESPONSE=$(cat ...)` at `:709` stripped. EC08 asserts all four, so a refactor that turns a one-byte response into an empty one fails the case. (edge:R6/encoding — explicit; Codex review finding, MEDIUM)"
    - "When agy's stderr is also empty, the exit-3 reason is the fixed fallback sentence rather than an empty string, in all four output shapes. (edge:R6/empty — explicit)"
  artifacts:
    - "tests/run-tests.sh — case EC07 (exact exit codes and cause-fragment exclusivity across the five documented codes, plus the two source assertions)"
    - "tests/run-tests.sh — case EC08 (R6's payload shapes on both entry points, both output modes, pinned so a failure payload can never become success-shaped)"
    - ".planning/REQUIREMENTS.md — R5 and R6 traceability rows moved off `partial` / `needs regression coverage`, with R5's row retaining the integer-second precision ceiling as a named residue rather than reading as unqualifiedly met"
  key_links:
    - "EC08's zero-byte-stdout assertion requires stdout and stderr captured SEPARATELY; the suite's `_run` helper merges them with `2>&1` and is therefore unusable for this case — using it would make the assertion vacuous"
    - "exit 127 is provoked only through the generated launcher wrapper, which needs the installer fixture `I16`/`RB29` build; EC07 cross-references those cases rather than rebuilding the fixture — and the cross-reference is only worth anything because EC07 also pins that the cited lines still assert `-eq 127`. Citation without that pin is the failure mode this plan was reviewed for: a claim that decays silently the day someone loosens the case it points at"
  prohibitions:
    - "A test must never be weakened, deleted, or made conditional to accommodate a change; the suite header states this and D-12 makes it live."
    - "The pre-existing loose cases `B2`, `S2`, `T4`, `T5`, `SH4`, `SH5` and `SH6` must not be retrofitted to the exact-code standard; new cases are added beside them."
    - "A failure payload must never be success-shaped: no `response` key and no truthy `success` value may appear in an exit-3 envelope from either entry point."
    - "The four-way error branch's arm order must not be changed by this plan; it is asserted, not edited."
    - "EC07 must not state, in its comment or its `ok`/`bad` label, that it asserts five exit codes. It asserts four and cites the fifth; the label says so. A case whose name overclaims is a case a later reader trusts for something it never did."
    - "The deliberate mutations in tasks 1 and 2 must be reverted by editing the mutated hunk back, never with `git checkout`, `git restore`, `git reset` or `git stash` — the worktree may be dirty with unrelated work. Capture `git status --porcelain` before the first mutation and again after the last revert and require them to match. (Codex review finding, MEDIUM — cross-plan)"
  flagged_assumptions:
    - "edge:R5/precision — elapsed time is integer seconds from bash's `SECONDS`, truncated toward zero. Sub-second resolution does not exist, so a SIGKILL arriving in the final second of the bound can read as `DURATION < TIMEOUT` and be classified an external kill rather than a timeout. This is shipped Phase-1 behavior (01-CONTEXT.md D-02), it is stated as a known ceiling rather than closed, and no test in this plan pins sub-second behavior because none can."
    - "edge:R5/boundary — the 137-versus-124 threshold itself (a kill landing well inside the bound versus at/after it) is already covered by the shipped cases `T4`/`T5` and `SH4`/`SH6`; this plan verifies the threshold's strictness as a source assertion rather than re-provoking it at wall-clock cost. Backstop, not a new runtime probe."
---

<objective>
Close the two coverage gaps criterion 1 and criterion 5 name: exit 3 and exit 124 are provoked in the suite today but asserted only as "non-zero", and R6's payload shapes — zero-byte stdout in text mode, a `response`-free envelope in JSON mode — are pinned nowhere on either entry point.

Purpose: `REQUIREMENTS.md` records R5 as `partial` and R6 as "shipped on branch, needs regression coverage". A code asserted as non-zero is a code whose contract the suite cannot defend; a failure payload with no shape test is one refactor away from becoming success-shaped, which is the specific outcome R6 exists to prevent and which a live agy was observed producing on 2026-08-19.
Output: two cases that pin the five codes and both failure payloads, and the two requirement rows updated to say so.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-the-exit-code-contract/03-CONTEXT.md
@.planning/phases/03-the-exit-code-contract/03-PATTERNS.md
@.planning/phases/03-the-exit-code-contract/03-03-SUMMARY.md
</context>

## Alternatives Considered

Mechanism choice: how a documented message is held to the documentation that quotes it, for the messages this phase adds or touches (03-CONTEXT.md D-11 leaves the exact shape to discretion, bounded to "matching the existing `RB03` pattern").

- **Static source-and-README `grep -cF`/`grep -qF` pinning** — `tests/run-tests.sh:2383-2440` (`RB03`, 2026), whose own comment at `:2385-2391` states the discipline. Fewest lines and no wall-clock cost. Its ceiling: it proves what the *source text* contains, not what the program *prints*. A format string can be restructured — a field reordered, a separator moved, a `%s` turned into a concatenation — while the pinned literal survives untouched inside it, and the check stays green over drifted output.
- **Runtime provocation with byte-exact comparison** — the suite's own `_run` harness at `tests/run-tests.sh:105-113` and case `R8` at `:497-505` (2026). Costs one process spawn per scenario and, for the timeout codes, roughly the bound in wall-clock. It observes the actual bytes an operator would see, which is the property criterion 4 is written about.
- **Golden-file snapshot fixtures under `tests/fixtures/`** — the mechanism established for captured real-`agy` output in `.planning/phases/01.5-contract-check-against-a-real-agy/01.5-CONTEXT.md` §D-14/D-14a (2026). Rejected on two counts: it adds new files and a diff harness to pin eight short strings, and D-14a deliberately reserves that directory for *captured* real-agy output, so putting synthetic expectations there erodes the captured-versus-synthetic separation that phase established.

**Decided by: maintenance.** The three are close enough on performance and ecosystem that neither decides it, and the static form wins on raw LOC. Maintenance decides against it: a static-only pin goes stale the moment a format string is restructured without the literal changing, and re-detecting that requires a manual re-audit — which is precisely how `delegate-agy-6q1` reached a shipped release. This phase therefore uses the static pin for provenance (plans `03-02` `EC05`, `03-03` `EC06`) and runtime provocation for output shape (`EC01`-`EC04` and this plan's `EC07`/`EC08`), paying the wall-clock only where a static check is structurally blind.

<artifacts_this_phase_produces>
New symbols introduced by this plan (they do not exist on `master`):

| Symbol | Kind | Location |
|--------|------|----------|
| `_ec_run_split` | test helper function (captures stdout and stderr into separate variables) | `tests/run-tests.sh` |
| `EC07`, `EC08` | test case ids | `tests/run-tests.sh` |

No new files, no changes to any shipped script.
</artifacts_this_phase_produces>

<review_dispositions>
Cross-AI review (`03-REVIEWS.md`, codex). Findings owned by this plan.

**Codex HIGH — "EC07's stated scope conflicts with its mechanism: its truth says every one of the five codes is asserted and never conflated, but exit 2 and 127 are delegated to existing `R8`, `I16` and `RB29`". ACCEPTED. The claim is narrowed to what runs, and the surviving delegation is made load-bearing.**
Checked what the pre-review plan actually specified before narrowing anything, because the two delegated codes turned out not to be alike:

- *Exit 2 was never really delegated.* EC07 already provokes it directly — `FAKE_AGY_MODELS_GARBAGE=1` with the model cache removed, bridge only — and asserts `rc -eq 2`. What was delegated was only the MESSAGE wording, to `R8` (`tests/run-tests.sh:497-505`), per D-10's explicit scoping of exit-2 coverage to that one site. So exit 2 stays in EC07's own assertion set, and this revision additionally makes explicit that its captured message participates in the cause-fragment exclusivity loop. Codex's concern does not survive contact with the mechanism here; recorded rather than silently dropped.
- *Exit 127 genuinely was delegated, and Codex is right that a bare cross-reference proves nothing.* `I16` (`:3597`) and `RB29` (`:3924`) each assert `-eq 127` exactly, and each pairs it with an empty-stdout assertion (`:3598`, `:3925`) — but neither asserts absence of the other codes' cause fragments, so the pre-review truth's "never conflated" claim covered 127 without evidence. Two changes: (a) the truth is narrowed to name 127 as cited-not-provoked, with the exclusivity gap stated outright; (b) EC07 gains two source assertions that the cited lines still assert `-eq 127`, verified to match exactly once each on the unmodified tree, so loosening either cited case fails EC07 instead of quietly hollowing out its citation. Codex's alternative — re-provoke 127 inside EC07 — was rejected: it means rebuilding the installer/launcher fixture `I16` and `RB29` already build, to re-derive a code they already pin exactly, at real setup cost and with a second fixture to keep in sync.

**Codex MEDIUM — "the newline-success case may expose invalid JSON behavior; state and test the actual expected JSON behavior rather than merely asserting exit 0". ACCEPTED and answered against source.**
A reasonable suspicion that the live code does not bear out. Read 2026-08-20: the bridge's JSON success arm is `'response':open(sys.argv[4]).read()` (`scripts/agy_bridge.sh:793`) and the shim's is `'response': sys.stdin.read()` with `< "$STDOUT_FILE"` (`scripts/gemini_shim.sh:716-724`) — both read the file, neither routes it through a shell variable, so neither loses the byte, and both envelopes carry `response` equal to a single newline. The shim's TEXT arm does strip it (`RESPONSE=$(cat "$STDOUT_FILE")`, `:709`) and then restores it (`printf '%s\n' "$RESPONSE"`, `:726`), which round-trips for this input by construction. EC08 now asserts the concrete expected value in all four shapes rather than asserting exit 0 and moving on, so the property is pinned instead of merely being true today.

**Codex MEDIUM — "source assertions for control flow are fragile; runtime boundary tests remain stronger evidence". ACCEPTED as already-scoped, no mechanism change.**
Agreed and already the plan's stated position: `flagged_assumptions` records `edge:R5/boundary` as a backstop rather than a probe, and the runtime boundary cases `T4`/`T5` and `SH4`/`SH6` remain untouched under D-12 and continue to carry the well-inside-versus-at/after-bound evidence. The source assertions add an ordering guard those runtime cases do not provide; they do not replace them. Codex's "use source checks only as supplementary ordering guards" is exactly the role assigned.

**Codex LOW — "requirement closure is premature if the precision ceiling remains undocumented; the traceability row should retain this known limitation". ACCEPTED.**
Task 3 must not write an unqualified `met` for R5: its row names the integer-second `SECONDS` resolution ceiling as a residue, in the same sentence that records the coverage now in place. Reflected in this plan's `artifacts`.

**Codex MEDIUM (cross-plan) — mutation mechanics. ACCEPTED**, added to this plan's prohibitions for tasks 1 and 2.

**Codex MEDIUM (cross-plan) — collapse the waves. REJECTED**; reasoning in plan `03-01`'s `<review_dispositions>`.

**Codex LOW (cross-plan) — "tests depend heavily on static grep provenance and should not be described as proof of runtime output; EC05/EC06 still overstate it". ACCEPTED**, owned by plans `03-02` and `03-03`; this plan's `## Alternatives Considered` already records the static-for-provenance / runtime-for-shape split as the deliberate phase-wide policy and is unchanged.
</review_dispositions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EC07 — four exit codes provoked and asserted exactly, the fifth cited with its citation pinned, none conflated</name>
  <beads-id>delegate-agy-byv.9</beads-id>
  <files>tests/run-tests.sh</files>
  <reversibility rating="reversible">Additive test cases only; no shipped code changes (03-CONTEXT.md D-11, D-12).</reversibility>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-byv.9` -->
</task>

<task type="auto" tdd="true">
  <name>Task 2: EC08 — the empty-success failure payload can never become success-shaped</name>
  <beads-id>delegate-agy-byv.10</beads-id>
  <files>tests/run-tests.sh</files>
  <reversibility rating="reversible">Additive test case and one test-local helper; no shipped code changes (R6, criterion 5).</reversibility>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-byv.10` -->
</task>

<task type="auto">
  <name>Task 3: Move R5 and R6 off provisional in the traceability table, and close the two tickets</name>
  <beads-id>delegate-agy-byv.11</beads-id>
  <files>.planning/REQUIREMENTS.md</files>
  <reversibility rating="reversible">Documentation and tracker bookkeeping (03-CONTEXT.md D-04).</reversibility>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-byv.11` -->
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the bridge/shim -> a JSON-consuming caller (Metaswarm, Octopus) | An error envelope that a caller mistakes for a success envelope causes that caller to merge unchanged state and report success. This is the boundary R6 exists to defend and the one EC08 pins. |
| the test harness's own capture -> the assertions it feeds | A merged stdout/stderr capture makes a zero-byte-stdout assertion pass unconditionally; the harness itself is the place this case can go silently vacuous. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Spoofing | exit-3 JSON envelope from either entry point | high | mitigate | A failure payload carrying a `response` key, or a truthy `success`, would be read by a downstream agent as a successful empty result — the live 2026-08-19 observation that grounds R6. EC08 parses both envelopes with a real JSON parser and asserts the key's absence, and is required to have been observed red under a deliberately reintroduced `response` key. |
| T-03-13 | Tampering | EC08 going vacuous through a merged capture | high | mitigate | The case uses its own split-stream helper, never `_run`; a comment above the helper names `_run` and states why. The byte count is taken with `wc -c` on the captured file rather than from a command substitution, which would strip the trailing newline and destroy the byte-emptiness scenario. Both properties are demonstrated red under mutation. |
| T-03-14 | Denial of service | suite wall-clock growth from hang-driven provocations | low | accept | EC07 uses the 2-second bound `T4`/`SH4` already use; the growth is bounded to roughly fifteen seconds on a suite already running about two and a half minutes, and is checked as an acceptance criterion. Accepted. |
| T-03-15 | Repudiation | a requirement row claiming more than the evidence supports | medium | mitigate | Task 3 forbids writing `met` over an open sub-item and requires every residue to carry a `bd` id that is still open; the roadmap's Phase 6 gate reads these rows and would otherwise pass over a live gap. |
| T-03-05 | Supply chain | package installs | n/a | n/a | This plan installs no npm/pip/cargo package and adds no dependency; no legitimacy gate applies. |
</threat_model>

<verification>
- Full suite green: `bash tests/run-tests.sh` ends `FAIL=0`; `EC01`-`EC08` all `ok`.
- Every documented exit code is asserted by exact value: 2 (`EC07` for the code, `R8` for the message wording per D-10), 3 (`EC07`), 124 (`EC07`), 137 (`EC07`, and byte-exactly by `EC01`/`EC03`), 127 (`I16:3597`, `RB29:3924` — cited, not re-provoked, with EC07 asserting those two lines still pin the exact code).
- EC07's `ok`/`bad` label and its comment state four provoked codes plus one cited code; neither claims five provoked codes.
- EC07 and EC08 were each observed red under deliberate mutation — three mutations in total — and green after every revert, with `git status` clean for the shipped scripts.
- No pre-existing case changed status or text.
- `delegate-agy-v5a` and `delegate-agy-6q1` are closed with evidence; any residue is named with an open ticket id.
</verification>

<success_criteria>
Every documented exit code is defended by an exact assertion — four of them provoked by EC07 itself, the fifth by two cited cases whose exactness EC07 pins — no provoked code's message can be mistaken for another's, R6's failure payload is pinned in both output modes on both entry points against becoming success-shaped, and the requirements record states exactly what is closed and what is not, including R5's surviving integer-second precision ceiling.

The plan's own words match its mechanism: nothing here claims coverage EC07 does not execute or cite with a rot-proof pin.
</success_criteria>

<output>
Create `.planning/phases/03-the-exit-code-contract/03-04-SUMMARY.md` when done. Record: EC07's mutation-red and reverted-green output; EC08's two mutation-red and reverted-green outputs; the final `PASS=`/`FAIL=` counts against the 145 baseline; R5/R6's new status text with any residue and its ticket ids.
</output>
