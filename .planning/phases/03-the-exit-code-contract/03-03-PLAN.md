---
phase: 03-the-exit-code-contract
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - README.md
  - tests/run-tests.sh
  - scripts/agy_bridge.sh
autonomous: true
requirements: [R5, R6]
beads: [delegate-agy-6q1]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README's exit-3 row quotes the bracketed-class message shape both entry points actually print, names the three class values, and states that the bridge's and the shim's JSON envelopes are shaped differently. (D-07)"
    - "README's exit-124 row quotes the timeout message as emitted rather than describing it. (D-05, criterion 4)"
    - "README's exit-2 row still quotes the degraded-model-list message byte-for-byte, and names that only `agy-bridge` emits it — the shim degrades quietly on the same input. (D-05, D-06, D-08)"
    - "README's exit-127 row has been re-checked against the generated launcher's own text in `scripts/install.sh`; either it already matched, or the mismatch is recorded as a Phase-4 finding rather than fixed here. (D-05, out-of-scope boundary)"
    - "The bridge's and the shim's differing generic-nonzero wording is named once in README with the reason, and is not unified. (D-06)"
    - "Every `exit 2` site in `scripts/agy_bridge.sh` has been read and given a recorded verdict; no site was skipped and no site was rewritten without a stated inconsistency. (D-09)"
    - "README keeps exactly one exemplar row for exit 2; no rows were added for the other exit-2 causes. (D-08)"
  artifacts:
    - "README.md — troubleshooting rows for exit 2, 3 and 124 restated; a one-line generic-nonzero divergence note beneath the table"
    - "tests/run-tests.sh — case EC06, pinning the exit-3, exit-124 and exit-2 literals across both scripts and README"
    - ".planning/phases/03-the-exit-code-contract/03-03-SUMMARY.md — the per-site exit-2 verdict table and the exit-127 verdict"
  key_links:
    - "README's quoted exit-3 message -> both scripts' `printf 'ERROR: agy returned empty output [%s]: %s\\n'` -> EC06's grep pair: the message is identical in both scripts today, so EC06 must assert it in BOTH and not assume the bridge speaks for the shim"
    - "the exit-2 exemplar row -> `scripts/agy_bridge.sh`'s degraded-list message only: the shim has no matching site, so a shared row without that clause would document behavior the shim does not have"
  prohibitions:
    - "No exit-2 call site may be rewritten unless the D-09 pass records a specific inconsistency at that site; a tidy-up with no finding behind it is out of scope."
    - "`scripts/install.sh` must not be edited by this plan; exit 127 is Phase 4's surface and this plan's contact with it is read-only."
    - "README must not gain rows for additional exit-2 causes beyond the single existing exemplar."
    - "README must not gain any occurrence of the word `unbounded`; case RB03 fails the suite on the first one."
    - "No existing assertion in tests/run-tests.sh may be weakened, deleted, or made conditional in order to let this plan's change pass."
  flagged_assumptions:
    - "edge:R5/ordering — the four-way error branch's ORDER is part of the contract: early-137 is tested before 124-or-137, before generic-nonzero, before empty-stdout, and a run matching more than one condition takes the earliest arm. This plan documents exit codes on the assumption that ordering is fixed; plan 03-04 pins it as a source assertion. Neither plan reorders it."
    - "delegate-agy-b7g is a known exit-2 wording gap at `scripts/agy_bridge.sh:530` — a zero-byte `agy models` reply with no cache reports the generic retrieval failure rather than the degraded-list message. It is reached through the model-fetch control flow, which is Phase 2's surface (S1), not this phase's message-text surface, and its exit code is already correct. The D-09 pass must record an explicit verdict citing the ticket rather than silently passing over the site; the default disposition is to leave it for the Phase 6 gate."
---

<objective>
Audit all five documented exit-code rows in README's troubleshooting table against what the code prints today, restate the three that are stale or silent, name the one bridge-vs-shim divergence the table does not yet mention, and read every `exit 2` site in the bridge for message consistency.

Purpose: criterion 4 says an operator matching their terminal output against the docs must find their string. Three of the five rows currently fail that: exit 3 summarises a message it does not quote, exit 124 quotes nothing at all, and no row mentions that the bridge and the shim word an arbitrary nonzero agy exit differently. `delegate-agy-6q1` happened because a row and a message drifted apart with nothing watching; this plan closes the rows and adds the watcher.
Output: an accurate table, one divergence note, one provenance case, and a recorded verdict for every exit-2 site.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-the-exit-code-contract/03-CONTEXT.md
@.planning/phases/03-the-exit-code-contract/03-PATTERNS.md
@.planning/phases/03-the-exit-code-contract/03-02-SUMMARY.md
</context>

## Alternatives Considered

N/A — no mechanism choice. Task 1 and task 3 are documentation and a read-and-verify pass over existing code; neither selects a library, framework, or algorithm. Task 2 reuses the provenance mechanism plan `03-02` already established and 03-CONTEXT.md's Claude's-Discretion note already bounds to "matching the existing `RB03` pattern rather than inventing a new mechanism". Inventing a comparison here to satisfy a gate would be padding.

<artifacts_this_phase_produces>
New symbols introduced by this plan (they do not exist on `master`):

| Symbol | Kind | Location |
|--------|------|----------|
| `_EC_EMPTY_OUTPUT_LITERAL` | test-local literal | `tests/run-tests.sh` |
| `_EC_TIMEOUT_LITERAL` | test-local literal | `tests/run-tests.sh` |
| `_EC_DEGRADED_LITERAL` | test-local literal | `tests/run-tests.sh` |
| `EC06` | test case id | `tests/run-tests.sh` |

No new files, no new shipped-script symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Restate the exit-3, exit-124 and exit-2 rows; re-verify exit 127; name the generic-nonzero divergence</name>

  <precondition>The exit-137 row already carries its stderr-suffix statement and `EC05` reports `ok` (plan `03-02`, task 3). Assert with `grep -c 'stderr' README.md` returning at least `1` on the 137 row before starting; the 137 row is not this task's work and must not be touched again.</precondition>

  <read_first>
    - `README.md` lines 220-244 — the troubleshooting table in full plus the "Running the tests" and "Contract check" sections beneath it. The contract-check exit-code table at `:246-250` is a DIFFERENT, disjoint numbering space (`0`/`10`/`11`) and is explicitly out of scope (03-CONTEXT.md canonical_refs); do not edit it and do not merge it.
    - `scripts/agy_bridge.sh` lines 753-775 — the empty-stdout branch: the `_reason` capture and its fallback string, the `case` block assigning `_class`, the JSON arm's flat envelope with its `error` and `error_class` keys, and the text arm's `printf`.
    - `scripts/gemini_shim.sh` lines 686-707 — the same branch on the shim: identical `_reason`/`_class` logic, identical text `printf`, but a NESTED JSON envelope. Note also that in JSON mode the shim emits BOTH the envelope on stdout and the text line on stderr, whereas the bridge emits only the envelope.
    - `scripts/agy_bridge.sh` line 740 and `scripts/gemini_shim.sh` line 677 — the two timeout messages. Confirm they are byte-identical before writing a shared row.
    - `scripts/agy_bridge.sh` lines 538-543 — the degraded-model-list message, two lines, the first of which README's exit-2 row quotes.
    - `scripts/gemini_shim.sh` `load_models()` — confirm the shim has no equivalent exit-2 path for a degraded list (Phase 2 decision D-05: the shim degrades silently because it shadows `gemini` box-wide).
    - `scripts/install.sh` lines 130-140 and 155-175 — the generated launcher's two exit-127 messages. READ ONLY; this plan does not edit this file.
    - `scripts/agy_bridge.sh` line 750 and `scripts/gemini_shim.sh` lines 679-681 — the two generic-nonzero arms, for the divergence note's wording.
  </read_first>

  <files>README.md</files>

  <action>
Four row edits and one note. Everything below is written into `README.md` only.

**Exit 3 (currently line 233).** Replace the flat summary with the message as printed. Both scripts emit the identical text form `ERROR: agy returned empty output [<class>]: <reason>`; quote that shape in the left cell with the three possible `<class>` values named — `empty_output`, `quota`, `auth` — and state what `<reason>` is: agy's full stderr, or the fixed fallback sentence `agy returned empty output (exit 0, no stdout)` when agy wrote no stderr either. Keep the existing right-cell guidance (quota `RESOURCE_EXHAUSTED (429)`, wait or re-auth, both entry points fail loud rather than reporting empty success) — it is accurate. Add, per 03-CONTEXT.md D-07, one short aside naming the JSON divergence: `agy-bridge --json` emits a flat envelope carrying `error` and `error_class` at the top level, while the `gemini` shim's `--output-format json` emits a nested `error` object carrying `message` and `class`. State both key sets explicitly; an operator writing a parser needs the key names, not the word "differently".

**Exit 124 (currently line 231).** The row today describes the outcome and quotes nothing. Add the emitted message to the left cell in backticks, in the same `Exit code N (\`…\`)` shape the exit-2 and exit-137 rows already use, with the seconds value shown as a placeholder the way the 137 row shows `Ns`/`Ms`. Both scripts emit identical text here, so one shared row stands; keep the existing right-cell advice.

**Exit 2 (currently line 230).** The quoted message already matches `scripts/agy_bridge.sh:540` byte-for-byte — verify that character by character rather than assuming, and leave the quotation alone if it holds. Add one clause to the right cell stating that this message comes from `agy-bridge`; the `gemini` shim, on the same degraded model list, falls back to a good cached list or proceeds silently rather than exiting 2, because it shadows `gemini` for every caller on the box (Phase 2's recorded decision). Do NOT add rows for any of the other exit-2 causes — the single exemplar is 03-CONTEXT.md D-08's locked decision.

**Exit 127 (currently line 226, plus the related "moved or was updated" row at line 225).** Read-only re-verification against the generated wrapper's own text in `scripts/install.sh`. Check both messages character by character against what the wrapper prints. If they match — including the elision the row uses — change nothing and record "verified accurate, no edit" plus the compared strings in the SUMMARY. If they do not match, still change nothing here: `scripts/install.sh` is Phase 4's file and editing it is prohibited by this plan. Instead file a `bd` issue naming the exact mismatch and the Phase-4 mapping, and record the issue id in the SUMMARY.

**Generic-nonzero divergence note.** Add one short line immediately beneath the troubleshooting table (not a table row) stating that for any other nonzero code agy returns, `agy-bridge` prefixes agy's stderr with `ERROR: agy exit N:` while the `gemini` shim relays agy's stderr unchanged and exits with agy's own code — and give the reason in the same line: the shim must look like `gemini` to a caller that has never heard of agy. This satisfies 03-CONTEXT.md D-06 for the one place the two entry points genuinely word the same condition differently. Format it to match the file's existing prose conventions around the table; 03-CONTEXT.md leaves the exact form (aside, sub-bullet, or cell) to discretion.

Throughout: hyphens stay hyphens. Do not let `--` become an en or em dash in any quoted message — plan `03-02`'s `EC05` and this plan's `EC06` grep README for exact bytes. Do not write the word `unbounded` anywhere; `RB03` turns the suite red on the first occurrence.
  </action>

  <verify>
    <automated>bash tests/run-tests.sh > /tmp/p3-03-t1.out 2>&1; tail -1 /tmp/p3-03-t1.out; grep -c 'unbounded' README.md; grep -cF 'ERROR: agy returned empty output [' README.md; grep -cF 'ERROR: agy timeout after' README.md</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` ends with `FAIL=0` and `RB03`, `CC05`, `EC01`-`EC05` all still report `ok`.
    - `grep -ic 'unbounded' README.md` returns `0`.
    - `grep -cF 'ERROR: agy returned empty output [' README.md` returns at least `1`.
    - `grep -cF 'ERROR: agy timeout after' README.md` returns at least `1`.
    - `grep -cF "ERROR: agy model list contains no 'gemini-' ids; agy may be unauthenticated" README.md` returns `1`, and the same command against `scripts/agy_bridge.sh` returns `1` — the two are byte-identical.
    - The exit-3 row names all three class values: `grep -c 'empty_output' README.md`, `grep -c 'quota' README.md` and `grep -c 'auth' README.md` each return at least `1`.
    - The exit-3 row names both envelope shapes: README contains `error_class` (bridge, flat) and contains the shim's nested `message` and `class` key names.
    - `git diff --stat` names exactly one file: `README.md`. `scripts/install.sh` appears in no diff.
    - `git diff README.md` modifies at most four table rows (exit 2, exit 3, exit 124, and no other) and adds one line beneath the table. The exit-137 row and the `WARNING: timeout/gtimeout not found` row appear in no `-` line.
    - The troubleshooting table's row count is unchanged: `sed -n '222,236p' README.md | grep -c '^| '` returns the same value as before this task.
    - The contract-check exit-code table (`0`/`10`/`11`) appears in no diff hunk.
  </acceptance_criteria>

  <reversibility rating="reversible">Documentation only; a single-file revert restores the previous table (03-CONTEXT.md D-05, D-06, D-07).</reversibility>

  <done>All five exit-code rows have been checked against the code; three are restated to quote what is printed, one is verified unchanged, one is verified read-only with its verdict recorded; the generic-nonzero divergence is named once with its reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EC06 — pin the exit-3, exit-124 and exit-2 literals to both scripts and README</name>

  <read_first>
    - `tests/run-tests.sh` — the `EC` section as plans `03-01` and `03-02` left it, especially `EC05`'s comment-filtered `grep -cF` loop and its README `grep -qF` check. This case copies that structure with three literals instead of one.
    - `tests/run-tests.sh` lines 2385-2440 — `RB03`, for the "expected bytes are written HERE" discipline and the positive/negative pairing.
    - `README.md` — the rows as task 1 left them.
    - `scripts/agy_bridge.sh` line 773, `scripts/gemini_shim.sh` line 705 — the two identical empty-output `printf` format strings.
    - `scripts/agy_bridge.sh` line 740, `scripts/gemini_shim.sh` line 677 — the two timeout messages. Note they are NOT the same construction: the bridge uses `printf` with a `%d`, the shim uses `echo` with an inline expansion. The invariant text they share is the prefix `ERROR: agy timeout after`; that prefix, not a whole line, is what can be pinned in both.
    - `scripts/agy_bridge.sh` line 540 — the degraded-model-list message.
  </read_first>

  <behavior>
    Test EC06 asserts, for each of three literals defined in the test file itself:
    - `ERROR: agy returned empty output [` — present in `scripts/agy_bridge.sh`, present in `scripts/gemini_shim.sh`, present in `README.md`. Comment lines filtered before counting in both scripts.
    - `ERROR: agy timeout after` — present in both scripts and in `README.md`.
    - `ERROR: agy model list contains no 'gemini-' ids; agy may be unauthenticated` — present in `scripts/agy_bridge.sh` and in `README.md`, and ABSENT from `scripts/gemini_shim.sh` (the shim has no such site; its presence would mean the quiet-degrade decision was reversed without the row being updated).
    - Runtime agreement for the empty-output message: the bridge and the shim, both driven to exit 3 with the same empty stderr, print byte-identical text lines. Otherwise a shared README row is documenting two different strings.
    - Runtime agreement for the timeout message: same, driven to exit 124 with the same bound.
  </behavior>

  <files>tests/run-tests.sh</files>

  <action>
Add EC06 to the `EC` section, after `EC05`, following the accumulate-then-report shape (`EC06_OK`, `EC06_DETAIL`, one `ok`/`bad`). Define the three literals as test-local variables in the test file — never extract a string from a script and then search that same script with it.

For the two-script presence checks, filter comment lines with `grep -v '^[[:space:]]*#'` before `grep -cF` and require a count of at least `1`, not exactly `1`: unlike `EC05`'s hoisted constant, these literals appear inside format strings whose count is an implementation detail. Fail with a detail token naming which file and which literal.

The degraded-list literal gets the opposite treatment on the shim: assert the comment-filtered count is exactly `0`. This is the negative half `RB03`'s comment calls equally load-bearing — the README row task 1 wrote says only the bridge emits it, and this is what makes that claim falsifiable.

For the two runtime-agreement checks, drive both entry points and compare the captured strings for equality:
- exit 3: `FAKE_AGY_EXIT=0 FAKE_AGY_STDOUT=""` through `bash "$BRIDGE" --type code -- "empty"` and `bash "$SHIM" -p "empty"`, comparing stderr only (redirect stdout away, since the shim's JSON path writes to stdout and the text path must leave stdout at zero bytes).
- exit 124: `FAKE_AGY_PRINT_HANG=1` with a small equal bound on both — `--timeout 2` on the bridge and `GEMINI_SHIM_TIMEOUT=2` on the shim — comparing the captured lines. Use the same small bound `T4`/`SH4` already use so the case adds no meaningful wall-clock; note in a comment that these two invocations each cost roughly the bound plus the kill escalation.

Report one `ok`/`bad` naming which literal and which surface drifted.

Demonstrate EC06 can fail before accepting it: change one character of the empty-output format string in `scripts/gemini_shim.sh`, re-run, observe `FAIL - EC06` with the shim named, revert. Record both outputs in the SUMMARY.
  </action>

  <verify>
    <automated>bash tests/run-tests.sh > /tmp/p3-03-t2.out 2>&1; tail -1 /tmp/p3-03-t2.out; grep -E '^ok   - EC0[123456]' /tmp/p3-03-t2.out</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` ends with `FAIL=0`; `EC01` through `EC06` all appear as `ok` lines.
    - EC06 was observed reporting `FAIL` under the deliberate one-character mutation of the shim's empty-output format string, with the shim named in its detail line, and `ok` again after revert. Both outputs pasted into the SUMMARY.
    - `git diff --stat` names exactly one file: `tests/run-tests.sh`.
    - `git diff tests/run-tests.sh` contains no deletions of pre-existing `ok`/`bad`/`[[ ` assertion lines — only additions.
    - Every `grep -cF` EC06 performs against a shipped script is preceded by a `grep -v '^[[:space:]]*#'` filter in the same pipeline: `grep -n 'grep -cF' tests/run-tests.sh` shows no EC06 line lacking the filter.
    - The suite's total wall-clock has not grown by more than roughly ten seconds against the plan `03-02` run — the two hang-driven invocations use a 2-second bound.
  </acceptance_criteria>

  <reversibility rating="reversible">One additive test case (03-CONTEXT.md D-11).</reversibility>

  <done>Every exit-code message README now quotes is pinned to the script or scripts that emit it, the shim's absence from the exit-2 path is pinned as an absence, and the two shared rows are proven to describe byte-identical output on both entry points.</done>
</task>

<task type="auto">
  <name>Task 3: The exit-2 consistency pass (D-09)</name>

  <read_first>
    - `scripts/agy_bridge.sh` — every `exit 2` site. Enumerate them yourself rather than trusting this list, with `grep -n 'exit 2' scripts/agy_bridge.sh`; at mapping time (2026-08-20, current `master`) they were at lines 21, 44, 371, 374, 377, 378, 381, 382, 385, 388, 389, 397, 406, 407, 451, 460, 530, 542, 551, 554, 585, 597, 600, 604 and 641. Read each site's message and the condition that reaches it.
    - `.planning/phases/03-the-exit-code-contract/03-PATTERNS.md` §"`ERROR:`-prefix convention" — the style the pass measures against: every site opens `ERROR: `, is imperative, and names the offending flag or value in single quotes.
    - 03-CONTEXT.md §D-09 and §D-10 — this is a read-and-verify pass, not a rewrite mandate, and it does not extend to adding reachability tests for these sites.
    - `bd show delegate-agy-b7g` — the known wording gap at the model-fetch site, whose disposition this task must record.
  </read_first>

  <files>scripts/agy_bridge.sh</files>

  <action>
Read every `exit 2` site in `scripts/agy_bridge.sh` and record a verdict for each in a table in this plan's SUMMARY: line number, the message text, and one of `consistent` / `inconsistent: <what>` / `left for another phase: <ticket>`.

Judge each site against four questions, and state the answer per site rather than a blanket "all fine":
1. Does the message begin with the literal `ERROR: `?
2. Does it name the specific offending flag, value, or file — not just the category?
3. Could an operator confuse it with any other exit-2 message in the same script? Two messages that could plausibly be produced by the same user mistake and read the same are the finding this pass exists to catch.
4. Does the message name a cause NARROWER than the condition that reaches it? A message asserting one specific reason on a branch that fires for several is a message that will mislead — this is the same class of defect as `delegate-agy-b7g`, and it is the finding most likely to be present.

Fix only sites where a specific answer above is a finding. A site that merely reads slightly differently from its neighbours is not a finding; do not rewrite for uniformity. Every changed line must trace to a recorded finding. If the pass finds nothing, change nothing — that is a legitimate and expected outcome, and the SUMMARY says so with the table as evidence.

**`delegate-agy-b7g`.** The site at `:530` reports `ERROR: failed to retrieve model list from agy` when a zero-byte `agy models` reply with no cache would be more accurately described by the degraded-list message at `:540`. Record a verdict for it explicitly rather than passing over it. The default disposition is `left for another phase: delegate-agy-b7g` — the exit code is already correct, the fix is in the model-fetch control flow rather than in message text, that flow is Phase 2's surface under requirement S1, and ROADMAP.md's Phase 6 gate already holds the ticket. If you conclude otherwise, state the reasoning in the SUMMARY and say plainly that it widens this phase's scope; do not fix it silently.

If any site IS changed, it inherits 03-CONTEXT.md D-11's bar: the new text gets pinned by a test in the manner of `EC06`, and if README quotes it, README is updated in the same commit. If no site is changed, no test is added — do not invent a case for a fix that did not happen.

Add a `bd` comment recording the pass's outcome on the phase's tracking issue rather than a standalone file, per this repository's convention.
  </action>

  <verify>
    <automated>bash tests/run-tests.sh > /tmp/p3-03-t3.out 2>&1; tail -1 /tmp/p3-03-t3.out; grep -c "^| " .planning/phases/03-the-exit-code-contract/03-03-SUMMARY.md</automated>
    <human-check>Read the SUMMARY's verdict table and confirm every enumerated `exit 2` line number appears in it exactly once.</human-check>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` ends with `FAIL=0`.
    - The SUMMARY contains a verdict table whose row count equals the count returned by `grep -c 'exit 2' scripts/agy_bridge.sh`, with every line number from that enumeration present exactly once.
    - Each verdict cell reads `consistent`, or `inconsistent:` followed by which of the four questions failed, or `left for another phase:` followed by a `bd` issue id.
    - `delegate-agy-b7g` appears by id in the verdict for the `:530` site.
    - `git diff scripts/agy_bridge.sh` is either empty, or every changed line is traceable to a row in the verdict table marked `inconsistent:`.
    - If `scripts/agy_bridge.sh` changed, README and `tests/run-tests.sh` changed in the same commit for any changed message that README quotes; if it did not change, neither did they.
    - `bd show` on the phase's tracking issue lists a new comment recording the pass outcome.
    - README's troubleshooting table still has exactly one exit-2 row.
  </acceptance_criteria>

  <reversibility rating="reversible">A read-and-verify pass; any edit it produces is a single-message change with a recorded finding behind it (03-CONTEXT.md D-09).</reversibility>

  <done>Every exit-2 site in the bridge has been read and given a recorded verdict, `delegate-agy-b7g` has an explicit disposition, and any message changed has a finding, a test and a doc update behind it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> operator's shell | An operator matches a printed string against the table. A paraphrased or stale row sends them debugging the wrong subsystem — the failure `delegate-agy-6q1` records. |
| `scripts/install.sh`'s generated wrapper -> README's exit-127 row | The row describes text produced by a file this plan may not edit; the only safe contact is read-only comparison. |
| agy models reply -> the bridge's exit-2 messages | Untrusted external output selects which exit-2 message an operator sees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Repudiation | README rows for exit 2, 3, 124 | medium | mitigate | EC06 pins each quoted literal to the script or scripts that emit it, and pins the shim's absence from the exit-2 path as an absence. The case must be observed red under a one-character mutation before it is trusted. |
| T-03-09 | Information disclosure | exit-3 row naming both JSON envelope key sets | low | accept | The key names are already in the shipped output that every Metaswarm/Octopus caller receives; documenting them exposes nothing new and is required for a caller to write a correct parser (R6). Accepted. |
| T-03-10 | Tampering | editing `scripts/install.sh` while "just fixing a row" | medium | mitigate | Prohibited by this plan's `must_haves.prohibitions` and enforced by a `git diff --stat` acceptance criterion on task 1; a mismatch becomes a Phase-4 `bd` issue rather than an out-of-phase edit. |
| T-03-11 | Tampering | an exit-2 site rewritten for tidiness, changing operator-visible behavior with no finding behind it | low | mitigate | Task 3 requires every changed line to trace to a numbered finding in the verdict table, and the acceptance criteria check the diff against that table. |
| T-03-05 | Supply chain | package installs | n/a | n/a | This plan installs no npm/pip/cargo package and adds no dependency; no legitimacy gate applies. |
</threat_model>

<verification>
- Full suite green: `bash tests/run-tests.sh` ends `FAIL=0`; `EC01`-`EC06`, `RB03` and `CC05` all `ok`.
- EC06 observed red under a deliberate mutation and green after revert, both recorded.
- `grep -ic 'unbounded' README.md` returns `0`.
- Every exit-code message README quotes is present byte-for-byte in the script that emits it.
- The SUMMARY carries a complete exit-2 verdict table and an explicit exit-127 verdict.
- `scripts/install.sh` is unchanged.
</verification>

<success_criteria>
An operator can match any of the five documented exit codes' output against README and find their string; the two places the entry points genuinely differ are named with reasons; a test fails if any quoted message drifts; and every exit-2 site has been read with its verdict on the record.
</success_criteria>

<output>
Create `.planning/phases/03-the-exit-code-contract/03-03-SUMMARY.md` when done. Record: the exit-2 verdict table (one row per site); the exit-127 read-only verdict with the compared strings; `delegate-agy-b7g`'s disposition; EC06's mutation-red and reverted-green output.
</output>
