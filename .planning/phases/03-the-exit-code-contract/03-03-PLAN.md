---
beads_epic: delegate-agy-byv
phase: 03-the-exit-code-contract
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - README.md
  - tests/run-tests.sh
  - scripts/agy_bridge.sh
autonomous: true
requirements: [R5, R6]
beads: [delegate-agy-6q1]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README's exit-3 row quotes the bracketed-class message shape both entry points actually print, names the three class values, and states that the bridge's and the shim's JSON envelopes are shaped differently. (D-07)"
    - "README's exit-124 row quotes the timeout message as emitted rather than describing it. (D-05, criterion 4)"
    - "README's exit-2 row still quotes the degraded-model-list message byte-for-byte, and names that only `agy-bridge` emits it — the shim degrades quietly on the same input. (D-05, D-06, D-08)"
    - "README's exit-127 row has been re-checked against the generated launcher's own text in `scripts/install.sh`; either it already matched, or the mismatch is recorded as a Phase-4 finding rather than fixed here. (D-05, out-of-scope boundary)"
    - "The bridge's and the shim's differing generic-nonzero wording is named once in README with the reason, and is not unified. (D-06)"
    - "Every `exit 2` site in `scripts/agy_bridge.sh` has been read and given a recorded verdict; no site was skipped and no site was rewritten without a stated inconsistency. (D-09)"
    - "README keeps exactly one exemplar row for exit 2; no rows were added for the other exit-2 causes. (D-08)"
  artifacts:
    - "README.md — troubleshooting rows for exit 2, 3 and 124 restated; a one-line generic-nonzero divergence note beneath the table"
    - "tests/run-tests.sh — case EC06, pinning the exit-3, exit-124 and exit-2 literals across both scripts and README"
    - ".planning/phases/03-the-exit-code-contract/03-03-SUMMARY.md — the per-site exit-2 verdict table and the exit-127 verdict"
  key_links:
    - "README's quoted exit-3 message -> both scripts' `printf 'ERROR: agy returned empty output [%s]: %s\\n'` -> EC06's grep pair: the message is identical in both scripts today, so EC06 must assert it in BOTH and not assume the bridge speaks for the shim"
    - "the exit-2 exemplar row -> `scripts/agy_bridge.sh`'s degraded-list message only: the shim has no matching site, so a shared row without that clause would document behavior the shim does not have"
  prohibitions:
    - "No exit-2 call site may be rewritten unless the D-09 pass records a specific inconsistency at that site; a tidy-up with no finding behind it is out of scope."
    - "`scripts/install.sh` must not be edited by this plan; exit 127 is Phase 4's surface and this plan's contact with it is read-only."
    - "README must not gain rows for additional exit-2 causes beyond the single existing exemplar."
    - "README must not gain any occurrence of the word `unbounded`; case RB03 fails the suite on the first one."
    - "No existing assertion in tests/run-tests.sh may be weakened, deleted, or made conditional in order to let this plan's change pass."
  flagged_assumptions:
    - "edge:R5/ordering — the four-way error branch's ORDER is part of the contract: early-137 is tested before 124-or-137, before generic-nonzero, before empty-stdout, and a run matching more than one condition takes the earliest arm. This plan documents exit codes on the assumption that ordering is fixed; plan 03-04 pins it as a source assertion. Neither plan reorders it."
    - "delegate-agy-b7g is a known exit-2 wording gap at `scripts/agy_bridge.sh:530` — a zero-byte `agy models` reply with no cache reports the generic retrieval failure rather than the degraded-list message. It is reached through the model-fetch control flow, which is Phase 2's surface (S1), not this phase's message-text surface, and its exit code is already correct. The D-09 pass must record an explicit verdict citing the ticket rather than silently passing over the site; the default disposition is to leave it for the Phase 6 gate."
---

<objective>
Audit all five documented exit-code rows in README's troubleshooting table against what the code prints today, restate the three that are stale or silent, name the one bridge-vs-shim divergence the table does not yet mention, and read every `exit 2` site in the bridge for message consistency.

Purpose: criterion 4 says an operator matching their terminal output against the docs must find their string. Three of the five rows currently fail that: exit 3 summarises a message it does not quote, exit 124 quotes nothing at all, and no row mentions that the bridge and the shim word an arbitrary nonzero agy exit differently. `delegate-agy-6q1` happened because a row and a message drifted apart with nothing watching; this plan closes the rows and adds the watcher.
Output: an accurate table, one divergence note, one provenance case, and a recorded verdict for every exit-2 site.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-the-exit-code-contract/03-CONTEXT.md
@.planning/phases/03-the-exit-code-contract/03-PATTERNS.md
@.planning/phases/03-the-exit-code-contract/03-02-SUMMARY.md
</context>

## Alternatives Considered

N/A — no mechanism choice. Task 1 and task 3 are documentation and a read-and-verify pass over existing code; neither selects a library, framework, or algorithm. Task 2 reuses the provenance mechanism plan `03-02` already established and 03-CONTEXT.md's Claude's-Discretion note already bounds to "matching the existing `RB03` pattern rather than inventing a new mechanism". Inventing a comparison here to satisfy a gate would be padding.

<artifacts_this_phase_produces>
New symbols introduced by this plan (they do not exist on `master`):

| Symbol | Kind | Location |
|--------|------|----------|
| `_EC_EMPTY_OUTPUT_LITERAL` | test-local literal | `tests/run-tests.sh` |
| `_EC_TIMEOUT_LITERAL` | test-local literal | `tests/run-tests.sh` |
| `_EC_DEGRADED_LITERAL` | test-local literal | `tests/run-tests.sh` |
| `EC06` | test case id | `tests/run-tests.sh` |

No new files, no new shipped-script symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Restate the exit-3, exit-124 and exit-2 rows; re-verify exit 127; name the generic-nonzero divergence</name>
  <beads-id>delegate-agy-byv.6</beads-id>
  <files>README.md</files>
  <reversibility rating="reversible">Documentation only; a single-file revert restores the previous table (03-CONTEXT.md D-05, D-06, D-07).</reversibility>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-byv.6` -->
</task>

<task type="auto" tdd="true">
  <name>Task 2: EC06 — pin the exit-3, exit-124 and exit-2 literals to both scripts and README</name>
  <beads-id>delegate-agy-byv.7</beads-id>
  <files>tests/run-tests.sh</files>
  <reversibility rating="reversible">One additive test case (03-CONTEXT.md D-11).</reversibility>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-byv.7` -->
</task>

<task type="auto">
  <name>Task 3: The exit-2 consistency pass (D-09)</name>
  <beads-id>delegate-agy-byv.8</beads-id>
  <files>scripts/agy_bridge.sh</files>
  <reversibility rating="reversible">A read-and-verify pass; any edit it produces is a single-message change with a recorded finding behind it (03-CONTEXT.md D-09).</reversibility>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-byv.8` -->
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> operator's shell | An operator matches a printed string against the table. A paraphrased or stale row sends them debugging the wrong subsystem — the failure `delegate-agy-6q1` records. |
| `scripts/install.sh`'s generated wrapper -> README's exit-127 row | The row describes text produced by a file this plan may not edit; the only safe contact is read-only comparison. |
| agy models reply -> the bridge's exit-2 messages | Untrusted external output selects which exit-2 message an operator sees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Repudiation | README rows for exit 2, 3, 124 | medium | mitigate | EC06 pins each quoted literal to the script or scripts that emit it, and pins the shim's absence from the exit-2 path as an absence. The case must be observed red under a one-character mutation before it is trusted. |
| T-03-09 | Information disclosure | exit-3 row naming both JSON envelope key sets | low | accept | The key names are already in the shipped output that every Metaswarm/Octopus caller receives; documenting them exposes nothing new and is required for a caller to write a correct parser (R6). Accepted. |
| T-03-10 | Tampering | editing `scripts/install.sh` while "just fixing a row" | medium | mitigate | Prohibited by this plan's `must_haves.prohibitions` and enforced by a `git diff --stat` acceptance criterion on task 1; a mismatch becomes a Phase-4 `bd` issue rather than an out-of-phase edit. |
| T-03-11 | Tampering | an exit-2 site rewritten for tidiness, changing operator-visible behavior with no finding behind it | low | mitigate | Task 3 requires every changed line to trace to a numbered finding in the verdict table, and the acceptance criteria check the diff against that table. |
| T-03-05 | Supply chain | package installs | n/a | n/a | This plan installs no npm/pip/cargo package and adds no dependency; no legitimacy gate applies. |
</threat_model>

<verification>
- Full suite green: `bash tests/run-tests.sh` ends `FAIL=0`; `EC01`-`EC06`, `RB03` and `CC05` all `ok`.
- EC06 observed red under a deliberate mutation and green after revert, both recorded.
- `grep -ic 'unbounded' README.md` returns `0`.
- Every exit-code message README quotes is present byte-for-byte in the script that emits it.
- The SUMMARY carries a complete exit-2 verdict table and an explicit exit-127 verdict.
- `scripts/install.sh` is unchanged.
</verification>

<success_criteria>
An operator can match any of the five documented exit codes' output against README and find their string; the two places the entry points genuinely differ are named with reasons; a test fails if any quoted message drifts; and every exit-2 site has been read with its verdict on the record.
</success_criteria>

<output>
Create `.planning/phases/03-the-exit-code-contract/03-03-SUMMARY.md` when done. Record: the exit-2 verdict table (one row per site); the exit-127 read-only verdict with the compared strings; `delegate-agy-b7g`'s disposition; EC06's mutation-red and reverted-green output.
</output>
