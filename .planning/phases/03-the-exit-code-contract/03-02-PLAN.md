---
phase: 03-the-exit-code-contract
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/gemini_shim.sh
  - scripts/agy_bridge.sh
  - tests/run-tests.sh
  - README.md
autonomous: true
requirements: [R5]
beads: [delegate-agy-v5a, delegate-agy-6q1]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The shim's external-kill message and the bridge's are byte-identical for the same duration, bound, and stderr content — including the presence or absence of the separator. (D-01)"
    - "The bridge's generic-nonzero message ends after the exit code with no separator when agy wrote nothing to stderr, and carries `: <stderr>` when it did. (edge:R5/empty — explicit, from D-03)"
    - "The shim's generic-nonzero branch relays agy's raw stderr with no prefix and no separator of its own, and stays that way — it is not a defect site and is not unified with the bridge's shape. (03-CONTEXT.md D-06)"
    - "The invariant tail of the external-kill sentence is defined exactly once in each shipped script, the two definitions are byte-identical, and README quotes those same bytes verbatim. (D-11)"
    - "README's exit-137 row states that agy's stderr is appended after a colon when non-empty and that nothing is appended when empty. (delegate-agy-6q1)"
  artifacts:
    - "scripts/gemini_shim.sh — `EC_KILL9_TAIL` defined once, byte-identical to the bridge's definition, referenced by the external-kill printf"
    - "tests/run-tests.sh — EC03 (shim external kill), EC04 (bridge generic nonzero), EC05 (cross-script and README provenance for the tail literal)"
    - "README.md — the exit-137 troubleshooting row, restated to include the appended-when-non-empty stderr suffix"
  key_links:
    - "the single tail literal -> both scripts' definitions -> README's quoted row: EC05 is the only thing that fails when one of the three drifts, so it must check all three and not two"
    - "EC05's `grep -cF` counts must filter comment lines before counting, or a comment naming the literal inflates the count and the guard reports a false failure"
  prohibitions:
    - "An error message emitted by either entry point must never end in a separator with nothing after it."
    - "Captured agy stderr must never become part of a printf format string; a `%` in agy's output must not be read as a format directive."
    - "The shim's generic-nonzero branch must not gain the bridge's `ERROR: agy exit N:` prefix; the documented asymmetry is named, not removed."
    - "No existing assertion in tests/run-tests.sh may be weakened, deleted, or made conditional in order to let this plan's change pass."
    - "README must not gain any occurrence of the word `unbounded`; case RB03 fails the suite on the first one."
  flagged_assumptions:
    - "edge:R5/precision — carried forward from plan 03-01: elapsed time is integer seconds from bash's `SECONDS`, truncated toward zero, so a SIGKILL in the final second of the bound can classify as an external kill. Shipped behavior, unchanged and untested here."
---

<objective>
Expand plan 01's proven slice sideways: the same guard on the shim's external-kill branch and on the bridge's generic-nonzero branch, then pin the shared literal across both scripts and README so doc and code cannot drift apart again.

Purpose: `delegate-agy-v5a` was filed against the bridge only. The shim carries the identical defect and shadows `gemini` for every PATH caller on the box, so leaving it is leaving the worse half (03-CONTEXT.md D-01). `delegate-agy-6q1` is the docs half of the same defect.
Output: two more guarded sites, a cross-script provenance case, and a README row that quotes what the code now actually prints.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-the-exit-code-contract/03-CONTEXT.md
@.planning/phases/03-the-exit-code-contract/03-PATTERNS.md
@.planning/phases/03-the-exit-code-contract/03-01-SUMMARY.md
</context>

## Alternatives Considered

N/A — no mechanism choice. Every task here applies the mechanism plan `03-01` already decided and verified (bash alternate-value expansion `${var:+word}`, guard in bash only, untrusted bytes as arguments) to two sibling call sites, and copies `RB03`'s existing provenance shape verbatim per 03-CONTEXT.md's Claude's-Discretion note ("subject to matching the existing `RB03` pattern rather than inventing a new mechanism"). Introducing a second mechanism for the sibling sites is precisely the divergence this plan exists to prevent.

<artifacts_this_phase_produces>
New symbols introduced by this plan (they do not exist on `master`):

| Symbol | Kind | Location |
|--------|------|----------|
| `EC_KILL9_TAIL` | shell constant | `scripts/gemini_shim.sh` (mirror of the bridge's, added by `03-01`) |
| `_err_txt` | shell local | `scripts/gemini_shim.sh`, and the bridge's generic-nonzero branch |
| `_EC_README` | test-local path variable | `tests/run-tests.sh` |
| `EC03`, `EC04`, `EC05` | test case ids | `tests/run-tests.sh` |

No new files.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The shim's external-kill message, mirrored byte-for-byte</name>

  <precondition>`scripts/agy_bridge.sh` already defines `EC_KILL9_TAIL` and both its external-kill arms reference it (plan `03-01`, tasks 1 and 2). Assert with `grep -c '\$EC_KILL9_TAIL' scripts/agy_bridge.sh` returning `2`; halt if it does not — the byte-identity assertion in task 3 has no fixed point without it.</precondition>

  <read_first>
    - `scripts/gemini_shim.sh` lines 666-682 — the whole error branch. Note that unlike the bridge, none of the 137 / 124 / generic-nonzero arms consult `$OUTPUT_FORMAT`; only the empty-stdout branch at `:687-706` does. There is therefore no JSON arm to fix here.
    - `scripts/gemini_shim.sh` lines 679-681 — the generic-nonzero arm, `cat "$STDERR_FILE" >&2` then `exit "$EXIT_CODE"`. Read it to confirm it adds no separator of its own and is not a fix site.
    - `scripts/agy_bridge.sh` — the external-kill branch as plan `03-01` left it, and the `EC_KILL9_TAIL` definition. This task copies both.
    - `tests/run-tests.sh` — the `EC` section and case `EC01` from plan `03-01`; this task's case reuses its literals and its shape.
    - `tests/run-tests.sh` lines 1138-1147 — case `SH6`, the existing loose shim 137 case. It stays exactly as written (D-12); read it for the invocation shape only.
  </read_first>

  <behavior>
    Test EC03, driving `bash "$SHIM" -p "oom check"` under `FAKE_AGY_PRINT_KILL9=1 GEMINI_SHIM_TIMEOUT=60`:
    - Empty stderr: rc is 137 and the emitted line is exactly `ERROR: agy killed (signal 9) after 0s, before its 60s bound ` + the tail literal, with nothing after the tail.
    - Non-empty stderr (`FAKE_AGY_STDERR='FAKE-AGY-KILL9-STDERR'`): the same line followed by `: FAKE-AGY-KILL9-STDERR`.
    - Newline-only stderr: byte-identical to the empty case.
    - Format-specifier stderr (`FAKE_AGY_STDERR='100%s%d'`): those characters appear literally.
    - Cross-entry-point identity: for each of the four scenarios, the shim's line and the bridge's line (same duration, same bound) are equal as strings.
  </behavior>

  <files>tests/run-tests.sh, scripts/gemini_shim.sh</files>

  <action>
RED first: add EC03 to the `EC` section beside EC01/EC02, reusing the test-local tail literal plan `03-01` defined — do not define a second copy. Drive the four scenarios through `_run OUT RC bash "$SHIM" -p "oom check"` with the `FAKE_AGY_PRINT_KILL9=1 GEMINI_SHIM_TIMEOUT=60` prefix, using the same accumulate-then-report shape (`EC03_OK`/`EC03_DETAIL`, one `ok`/`bad` at the end). Add the cross-entry-point identity check as a fifth scenario: run the same input through `$BRIDGE --type code --timeout 60` and assert the two captured strings are equal.

`GEMINI_SHIM_TIMEOUT=60` fixes the printed bound at `60s` and `FAKE_AGY_PRINT_KILL9` returns in well under a second, so the printed duration is a fixed `0s`; both are safe to hard-code into the expected bytes, exactly as EC01 does.

Confirm EC03 is red on the empty-stderr and newline-only scenarios. Verified 2026-08-20 on the unmodified tree, the shim prints `ERROR: agy killed (signal 9) after 0s, before its 60s bound -- possible OOM or external kill: ` with the separator present and nothing after it.

GREEN, in `scripts/gemini_shim.sh` only:

(a) Define `EC_KILL9_TAIL` with the same single-quoted literal the bridge uses, byte-for-byte, placed with the shim's other top-level constants. It is referenced once in this file rather than twice; it exists anyway so that the twin-file identity assertion in task 3 has something to compare, and because twin-site divergence is this project's recurring failure mode (`delegate-agy-u1z`, and Phase 2's CR-01/WR-01 round).

(b) Rewrite the external-kill `printf` at `:673-674` to the bridge's shape: capture the stderr file into a local `_err_txt` (free name, verified), and pass four fields — duration, bound, `"$EC_KILL9_TAIL"`, and the guarded suffix `"${_err_txt:+: $_err_txt}"`. The tail and the suffix are ARGUMENTS; neither may be spliced into the format string.

Leave the generic-nonzero arm at `:679-681` untouched. It relays raw stderr with no prefix and no separator, which is the deliberate bridge-vs-shim asymmetry 03-CONTEXT.md D-06 says to name in README, not to remove.

Re-run the suite; EC03 must report `ok`, EC01/EC02 must stay `ok`, and the final line must read `FAIL=0`.
  </action>

  <verify>
    <automated>bash tests/run-tests.sh > /tmp/p3-02-t1.out 2>&1; tail -1 /tmp/p3-02-t1.out; grep -E '^ok   - EC0[123]' /tmp/p3-02-t1.out</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` ends with `FAIL=0`; `EC01`, `EC02`, `EC03` all appear as `ok` lines.
    - `FAKE_AGY_PRINT_KILL9=1 GEMINI_SHIM_TIMEOUT=60 bash scripts/gemini_shim.sh -p "x" 2>&1 >/dev/null | cat -A` prints a line whose last three characters before `$` are `i`, `l`, `l` — the sentence ends on the word `kill`. (Today the same command prints `... kill: $`.)
    - The same command with `FAKE_AGY_STDERR=boom` prints a line ending `kill: boom`.
    - The same command with `FAKE_AGY_STDERR='100%s%d'` prints a line whose tail is those six characters verbatim.
    - The shim's and the bridge's external-kill lines are equal: `diff <(FAKE_AGY_PRINT_KILL9=1 GEMINI_SHIM_TIMEOUT=60 bash scripts/gemini_shim.sh -p x 2>&1 >/dev/null) <(FAKE_AGY_PRINT_KILL9=1 bash scripts/agy_bridge.sh --type code --timeout 60 -- x 2>&1 >/dev/null)` exits 0 with no output.
    - `grep -v '^[[:space:]]*#' scripts/gemini_shim.sh | grep -c 'EC_KILL9_TAIL='` returns `1`.
    - `sed -n '/^elif \[\[ "$EXIT_CODE" -ne 0 \]\]/,/^fi/p' scripts/gemini_shim.sh` still shows `cat "$STDERR_FILE" >&2` with no added prefix string.
    - `git diff --stat` names exactly two files: `tests/run-tests.sh`, `scripts/gemini_shim.sh`.
  </acceptance_criteria>

  <reversibility rating="reversible">One guard and one hoisted constant in one branch of one file (03-CONTEXT.md D-01, D-03).</reversibility>

  <done>The shim's external-kill message is byte-identical to the bridge's in all four stderr scenarios; the shim's generic-nonzero relay is unchanged; EC03 fails if either fact regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The bridge's generic-nonzero message drops its separator too</name>

  <read_first>
    - `scripts/agy_bridge.sh` lines 743-752 — the generic-nonzero arm as plan `03-01` left it: the JSON arm at `:744-748` (whose `error` is the whole stderr file, no separator — NOT a fix site) and the plain-text `printf` at `:750` (the fix site).
    - `scripts/agy_bridge.sh` — the external-kill branch as plans `03-01` and this plan's task 1 left it, for the exact guard shape to copy.
    - `tests/run-tests.sh` lines 246-252 — case `B3`, which already drives `FAKE_AGY_EXIT=1 FAKE_AGY_STDERR=...` through this same branch with a substring check. It stays exactly as written (D-12); read it to confirm this task's new case does not duplicate it and to reuse its invocation shape.
  </read_first>

  <behavior>
    Test EC04, driving `bash "$BRIDGE" --type code -- "generic nonzero"` under `FAKE_AGY_EXIT=7`:
    - Empty stderr: rc is 7 and the emitted line is exactly `ERROR: agy exit 7` — nothing after the code.
    - Non-empty stderr (`FAKE_AGY_STDERR='FAKE-AGY-GENERIC-STDERR'`): rc is 7 and the line is exactly `ERROR: agy exit 7: FAKE-AGY-GENERIC-STDERR`.
    - Newline-only stderr: byte-identical to the empty case.
    - Format-specifier stderr (`FAKE_AGY_STDERR='100%s%d'`): those characters appear literally.
    - The JSON arm of this same branch is unchanged: `--json` with `FAKE_AGY_EXIT=7 FAKE_AGY_STDERR='x'` still yields an envelope whose `error` is agy's stderr with no prefix and no separator.
  </behavior>

  <files>tests/run-tests.sh, scripts/agy_bridge.sh</files>

  <action>
RED first: add EC04 beside EC03, same accumulate-then-report shape. Expected bytes are written in the test file — `ERROR: agy exit 7` and `ERROR: agy exit 7: FAKE-AGY-GENERIC-STDERR` — as full-string equality, not substring containment.

Confirm EC04 is red on the empty-stderr and newline-only scenarios. Verified 2026-08-20 on the unmodified tree: `FAKE_AGY_EXIT=7` with empty stderr prints `ERROR: agy exit 7: ` with the separator present and nothing after it, and with `FAKE_AGY_STDERR=boom` prints `ERROR: agy exit 7: boom`.

GREEN: in the plain-text arm at `:750`, capture the stderr file into `_err_txt` and emit with `printf` taking two fields — the exit code as `%d` and the guarded suffix `"${_err_txt:+: $_err_txt}"` as `%s`. The literal `: ` disappears from the format string; the separator now lives only inside the guarded expansion. As everywhere else in this phase, the stderr bytes are an ARGUMENT.

Do not touch the JSON arm at `:744-748`. Its `error` value is `open(sys.argv[4]).read()` — the whole stderr file, with no separator prepended and therefore nothing to guard. 03-PATTERNS.md flags this explicitly as a site that is not broken; the fifth scenario of EC04 exists to keep a future reader from "fixing" it.

Re-run the suite; EC04 must report `ok`, EC01-EC03 must stay `ok`, `B3` must stay `ok`, and the final line must read `FAIL=0`.
  </action>

  <verify>
    <automated>bash tests/run-tests.sh > /tmp/p3-02-t2.out 2>&1; tail -1 /tmp/p3-02-t2.out; grep -E '^ok   - (EC0[1234]|B3)' /tmp/p3-02-t2.out</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` ends with `FAIL=0`; `EC01` through `EC04` all appear as `ok` lines and `B3` still appears as `ok`.
    - `FAKE_AGY_EXIT=7 bash scripts/agy_bridge.sh --type code -- x 2>&1 >/dev/null | cat -A` prints exactly `ERROR: agy exit 7$`.
    - `FAKE_AGY_EXIT=7 FAKE_AGY_STDERR=boom bash scripts/agy_bridge.sh --type code -- x 2>&1 >/dev/null | cat -A` prints exactly `ERROR: agy exit 7: boom$`.
    - `FAKE_AGY_EXIT=7 FAKE_AGY_STDERR='100%s%d' bash scripts/agy_bridge.sh --type code -- x 2>&1 >/dev/null` prints a line whose tail is those six characters verbatim.
    - `FAKE_AGY_EXIT=7 FAKE_AGY_STDERR=xyz bash scripts/agy_bridge.sh --type code --json -- x 2>/dev/null | python3 -c 'import json,sys; print(repr(json.load(sys.stdin)["error"]))'` prints `'xyz'` — no prefix, no separator, unchanged from before this task.
    - `grep -cF "ERROR: agy exit %d: %s" scripts/agy_bridge.sh` returns `0`.
    - `git diff scripts/agy_bridge.sh` touches only the plain-text arm of the generic-nonzero branch; the `python3 -c` block at `:744-748` appears in no `+`/`-` line.
    - `git diff --stat` names exactly two files: `tests/run-tests.sh`, `scripts/agy_bridge.sh`.
  </acceptance_criteria>

  <reversibility rating="reversible">A separator moved from a format string into a guarded expansion, one line (03-CONTEXT.md D-03).</reversibility>

  <done>All four defect sites named by 03-CONTEXT.md D-01/D-02/D-03 are closed; the one site that only looked broken is verified unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the tail literal across both scripts and README, and restate the exit-137 row</name>

  <read_first>
    - `README.md` lines 220-234 — the whole troubleshooting table. Read every row; this task edits one of them and must not disturb the others (those are plan `03-03`'s work).
    - `README.md` line 232 — the current exit-137 row, which omits the stderr suffix entirely (`delegate-agy-6q1`).
    - `tests/run-tests.sh` lines 2385-2440 — `RB03` in full, including the comment block explaining the comment-filtered `grep -cF` count, the README `grep -qF` presence check, and the negative half. Copy this shape; do not invent a new one.
    - `tests/run-tests.sh` line 2434 — `grep -qiF 'unbounded' "$_RB_README"`, the standing negative check. Any README wording written here that contains that word turns the suite red.
    - `scripts/agy_bridge.sh` and `scripts/gemini_shim.sh` — both `EC_KILL9_TAIL` definitions, as left by plan `03-01` and this plan's task 1.
  </read_first>

  <files>tests/run-tests.sh, README.md</files>

  <action>
Two edits, one case.

**README.** Restate the exit-137 row so it quotes the message as the code now prints it, both halves. Keep the row's existing left-cell shape (`Exit code 137 (`…`)`) and its existing right-cell advice text — the diagnosis and the "raising the bound will not help" guidance are accurate and stay. Add to the left cell, after the backticked message: a statement that agy's own stderr is appended after a colon and a space when agy wrote any, and that nothing is appended when it wrote none. Add to the right cell one short clause stating that `agy-bridge` and the `gemini` shim emit this line with identical wording, so the row covers both entry points and needs no divergence aside (03-CONTEXT.md D-06: keep shared rows shared where the wording actually matches — task 1's cross-entry-point identity assertion is what makes that true).

The backticked message text must still contain the tail literal byte-for-byte, hyphens not em dashes, because the provenance case below greps README for exactly those bytes. Do not let a Markdown editor or an autocorrect turn `--` into an en/em dash. Do not use the word `unbounded` anywhere in the new wording.

**Provenance case EC05**, appended to the `EC` section. Copy `RB03`'s structure:

- Reuse the test-local tail literal already defined by plan `03-01` (one copy, in the test file, not extracted from either script).
- For each of `$BRIDGE` and `$SHIM`: filter comment lines with `grep -v '^[[:space:]]*#'`, then `grep -cF` the full assignment form (constant name, `=`, the single-quoted literal) and assert the count is exactly `1`. Filtering first is not optional — an unfiltered count is inflated by any comment that names the constant, which is the self-invalidating shape `RB03`'s own comment warns about.
- Assert the two scripts' definitions are byte-identical by extracting each definition line and comparing the extracted strings, not by asserting each matches the test literal twice.
- `grep -qF` the tail literal in `README.md` and fail with a distinct detail token if absent.
- Negative half, mirroring `RB03`'s: assert that neither script contains the old unconditional forms any more — the external-kill format string ending in a colon-space-`%s`, and the generic-nonzero `ERROR: agy exit %d: %s` form. Build these expected-absent strings in the test file from separator-free fragments concatenated in the test's own code, so the probe text is not itself a match for the scan reading the file it lives beside (the shape plan 01.5-02 used for `CC03m`).

Report with one `ok`/`bad` naming which of the three surfaces — bridge, shim, README — drifted.
  </action>

  <verify>
    <automated>bash tests/run-tests.sh > /tmp/p3-02-t3.out 2>&1; tail -1 /tmp/p3-02-t3.out; grep -E '^ok   - (EC0[12345]|RB03)' /tmp/p3-02-t3.out</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` ends with `FAIL=0`; `EC01` through `EC05` all appear as `ok` lines, and `RB03` still appears as `ok`.
    - `grep -ic 'unbounded' README.md` returns `0`.
    - The exit-137 row of README's troubleshooting table contains the tail literal verbatim: piping the row through `grep -cF -- '-- possible OOM or external kill'` returns `1`.
    - The exit-137 row's left cell mentions the appended stderr and the empty case: `sed -n '232p' README.md | grep -c 'stderr'` returns at least `1`.
    - EC05 is demonstrated able to fail: temporarily change one character of the tail literal in `scripts/gemini_shim.sh`, re-run the suite, observe `FAIL - EC05` with the shim named in its detail line, then revert. Paste both the red output and the reverted-green output into the SUMMARY. (A provenance case that has never been seen red is a case whose scan may be vacuous — the lesson recorded in STATE.md for `RB01`.)
    - `grep -v '^[[:space:]]*#' scripts/agy_bridge.sh | grep -c 'EC_KILL9_TAIL='` and the same command against `scripts/gemini_shim.sh` both return `1`.
    - `git diff --stat` names exactly two files: `tests/run-tests.sh`, `README.md`.
    - `git diff README.md` shows exactly one modified table row.
  </acceptance_criteria>

  <reversibility rating="reversible">One documentation row and one additive test case (03-CONTEXT.md D-06, D-11).</reversibility>

  <done>The tail literal exists once per shipped script, byte-identical across the twins, quoted verbatim in README; EC05 has been observed failing when any one of the three drifts; `delegate-agy-6q1`'s exit-137 half is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agy subprocess -> `$STDERR_FILE` -> the shim | The shim shadows `gemini` on `PATH` for interactive shells, Octopus and Metaswarm; untrusted agy stderr reaches every one of those callers through this code. |
| agy subprocess -> `$STDERR_FILE` -> the bridge's generic-nonzero branch | Same untrusted input, second formatting site. |
| README -> operator's shell | An operator matches a printed string against the table; a paraphrase sends them looking for the wrong thing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | `printf` format strings, shim external-kill arm and bridge generic-nonzero arm | medium | mitigate | agy's stderr stays a `%s` argument at both new sites. Reproduced 2026-08-20: content of `100%s%d` renders as `1000` when spliced into a format and literally when passed as an argument. Pinned by the format-specifier scenario in EC03 and EC04. |
| T-03-06 | Information disclosure | shim external-kill message reaching non-agy-aware PATH callers | low | accept | Relaying agy's stderr is the requirement, and the shim's blast radius is box-wide by design (STATE.md). This plan changes only whether a separator precedes bytes that were already being relayed; it adds no new disclosure. Accepted. |
| T-03-07 | Repudiation | README quoting a message the code does not emit | medium | mitigate | EC05 pins the literal across both scripts and README, and is required to have been observed red before it is trusted. This is the exact drift that produced `delegate-agy-6q1`. |
| T-03-08 | Tampering | EC05's own scan going vacuous | medium | mitigate | Comment lines are filtered before every `grep -cF`, counts are asserted as exactly `1` rather than "at least one", and the case must be demonstrated failing under a one-character mutation before it is accepted. |
| T-03-05 | Supply chain | package installs | n/a | n/a | This plan installs no npm/pip/cargo package and adds no dependency; no legitimacy gate applies. |
</threat_model>

<verification>
- Full suite green: `bash tests/run-tests.sh` ends `FAIL=0`.
- `EC03`, `EC04`, `EC05` report `ok`; each was observed red before its corresponding change (EC05 via the deliberate one-character mutation), with the red output recorded in the SUMMARY.
- `T5`, `SH6`, `B3`, `S5`, `RB03` all still report `ok` and appear unchanged in `git diff`.
- The shim's and the bridge's external-kill lines are byte-equal under all four stderr scenarios.
- `grep -ic 'unbounded' README.md` returns `0`.
</verification>

<success_criteria>
Every site 03-CONTEXT.md D-01/D-02/D-03 names is guarded, the two entry points word the external kill identically, the one site that only looked broken is proven unchanged, and a single test fails if the literal drifts in either script or in README.
</success_criteria>

<output>
Create `.planning/phases/03-the-exit-code-contract/03-02-SUMMARY.md` when done. Record: the RED output for EC03 and EC04; EC05's mutation-red and reverted-green output; confirmation that the shim's generic-nonzero relay and the bridge's generic-nonzero JSON arm are untouched.
</output>
