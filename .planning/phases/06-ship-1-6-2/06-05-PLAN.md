---
phase: 06-ship-1-6-2
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-03", "06-04"]
files_modified:
  - README.md
autonomous: true
requirements: [R5, R6, R8, R11, S1, S2, S3, S4, S5]
beads: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Reading the files on `master` — not `git log` — shows every piece the `a001d0e` content revert removed is present again (Success Criterion 2)."
    - "The `### 1.6.2` release notes name each defect this release closes (Success Criterion 4, D-09)."
    - "The release notes state plainly that every existing installation must re-run the installer, because the pin only points forward (Success Criterion 4)."
  artifacts:
    - "README.md — `### 1.6.2` section extended with one bullet per closed defect, one closure note, and a re-run-the-installer notice"
    - ".planning/phases/06-ship-1-6-2/06-05-SUMMARY.md — the criterion-2 evidence record"
  key_links:
    - "Criterion 2 is a claim about file contents, so its checks must be content greps; plan 06-03 shifts `scripts/agy_bridge.sh` line numbers and any line-anchored check would rot."
    - "The release note is written after the fixes are green, so a bullet cannot describe a fix that did not land."
---

<objective>
Two release-gate deliverables that touch no code:

- **Success Criterion 2** — prove the `a001d0e` content revert is genuinely undone by reading files on `master`, not by reading history. A merge that restores history while leaving the working tree short is exactly the failure this criterion is written to catch.
- **Success Criterion 4 / D-09** — extend `README.md`'s `### 1.6.2` Changelog so it names each defect the release closes and states plainly that every existing installation must re-run the installer.

Purpose: 1.6.2 was held on a branch and content-reverted from `master` pending the follow-ups it surfaced. Those follow-ups are now closed by plans 06-01 through 06-04. This plan produces the two artifacts a reader needs to trust that: evidence the revert is undone, and notes that say what changed.
Output: a recorded criterion-2 verification and a finished 1.6.2 changelog.

This plan runs after every code fix is green precisely so its bullets describe what shipped rather than what was intended.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ship-1-6-2/06-CONTEXT.md
@.planning/phases/06-ship-1-6-2/06-RESEARCH.md
@.planning/phases/06-ship-1-6-2/06-PATTERNS.md
@.planning/phases/06-ship-1-6-2/06-01-SUMMARY.md
@.planning/phases/06-ship-1-6-2/06-02-SUMMARY.md
@.planning/phases/06-ship-1-6-2/06-03-SUMMARY.md
@.planning/phases/06-ship-1-6-2/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the a001d0e content revert is undone by reading files, not history</name>
  <files>(no repository file — evidence is recorded in this plan's SUMMARY)</files>
  <precondition>`git` resolves on PATH, the working tree is on `master` with plans 06-01, 06-03, and 06-04 merged, and the ref `fix/agy-bridge-resilience` still exists locally. If that branch has been pruned, halt and report rather than substituting a commit hash from memory.</precondition>
  <read_first>
    - .planning/phases/06-ship-1-6-2/06-RESEARCH.md — the "Criterion 2 verification" section, which lists the seven pieces `a001d0e` removed and where each is present again on `master`, each already re-verified once by direct read.
    - .planning/ROADMAP.md — Phase 6's Success Criterion 2, verbatim: the check is about files on `master`, explicitly not about `git log`.
    - .planning/phases/06-ship-1-6-2/06-VALIDATION.md — the Manual-Only Verifications row for criterion 2, which fixes the method as an ancestry check plus a content diff of the three files `a001d0e` touched.
    - .planning/phases/06-ship-1-6-2/06-03-SUMMARY.md — plan 06-03's diff to `scripts/agy_bridge.sh`, so the shifted line numbers are known before any check is written.
  </read_first>
  <action>
    Run and record two independent checks. Record the literal output of each in the SUMMARY, not a paraphrase — this is the evidence a later reader audits.

    Check A, ancestry: confirm `git merge-base --is-ancestor fix/agy-bridge-resilience master` succeeds, and record `git show --stat a001d0e -- scripts/ README.md`, which names the three files that revert touched: `README.md`, `scripts/agy_bridge.sh`, and `scripts/install.sh`. Note in the SUMMARY that `scripts/gemini_shim.sh` is deliberately absent from that list — the shim's bounding was fixed independently in Phase 1 under R11 and was never on the reverted branch, so its absence is not a gap.

    Check B, contents. This is the criterion. For each of the seven pieces `a001d0e` removed, assert it is present on `master` by grepping for its text, not by citing a line number. Line numbers in `scripts/agy_bridge.sh` shifted when plan 06-03 added D-07's branch, so any line-anchored check written from RESEARCH.md's citations would report a false negative. The seven:
    1. the `AGY_MODELS_TIMEOUT` default and its positive-integer validation guard, in `scripts/agy_bridge.sh`;
    2. the kill-after escalation on the model-fetch call — a `run_bounded` invocation carrying the fetch bound and a kill-after of 3, in `scripts/agy_bridge.sh`;
    3. the kill-after escalation on the delegation call — a `run_bounded` invocation carrying the delegation bound and a kill-after of 5, in `scripts/agy_bridge.sh`;
    4. the degraded-list error text stating the model list contains no `gemini-` ids and agy may be unauthenticated, in `scripts/agy_bridge.sh` (plan 06-03 makes this text appear twice; two matches is the expected count, not a duplicate to remove);
    5. the external-kill discrimination that distinguishes a 137 arriving before the bound elapsed from the script's own escalation, in `scripts/agy_bridge.sh`;
    6. the stale-pin registry comparison that exits 127 rather than running a superseded copy, in `scripts/install.sh` — two sites;
    7. the two-step, registry-comparison-only install prose in `README.md`'s install section.

    Write the checks as a single self-contained script that prints one PASS or FAIL line per item and a final verdict, then paste its full output into the SUMMARY. If any item reports FAIL, halt and report — do not proceed to Task 2 and do not adjust the check to make it pass. A genuine FAIL here means the release is not ready and the correct response is to say so.

    Change no file in this task. Criterion 2 is a verification, not a merge; `master` already carries the work and this task's job is to prove that by reading it.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor fix/agy-bridge-resilience master && grep -cE '^\[\[ "\$AGY_MODELS_TIMEOUT" =~|AGY_MODELS_TIMEOUT" =~' scripts/agy_bridge.sh && grep -cF 'run_bounded "$AGY_MODELS_TIMEOUT" 3 --' scripts/agy_bridge.sh && grep -cF 'run_bounded "$TIMEOUT" 5 --' scripts/agy_bridge.sh && grep -cF "no 'gemini-' ids" scripts/agy_bridge.sh && grep -cF '"$EXIT_CODE" -eq 137' scripts/agy_bridge.sh && grep -c 'exit 127' scripts/install.sh && grep -cF 'installed_plugins.json' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `git merge-base --is-ancestor fix/agy-bridge-resilience master` exits 0.
    - `grep -cF 'run_bounded "$AGY_MODELS_TIMEOUT" 3 --' scripts/agy_bridge.sh` is at least 1.
    - `grep -cF 'run_bounded "$TIMEOUT" 5 --' scripts/agy_bridge.sh` is at least 1.
    - `grep -cF "AGY_MODELS_TIMEOUT must be a positive integer" scripts/agy_bridge.sh` is at least 1.
    - `grep -cF "no 'gemini-' ids" scripts/agy_bridge.sh` is at least 3 — the degraded warning, the R9 error, and D-07's reuse.
    - `grep -cF '"$EXIT_CODE" -eq 137' scripts/agy_bridge.sh` is at least 1.
    - `grep -c 'exit 127' scripts/install.sh` is at least 2.
    - `grep -cF 'installed_plugins.json' README.md` is at least 1.
    - `git status --short` is empty of changes to `scripts/`, `tests/`, and `README.md` for this task.
    - The SUMMARY contains the verbatim output of `git show --stat a001d0e -- scripts/ README.md` and a per-item PASS line for all seven pieces.
  </acceptance_criteria>
  <reversibility rating="reversible">Read-only verification; no file is written outside this plan's SUMMARY.</reversibility>
  <done>All seven pieces `a001d0e` removed are proven present on `master` by content, the ancestry check passes, and the literal evidence is recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: D-09 — finish the 1.6.2 release notes</name>
  <files>README.md</files>
  <read_first>
    - README.md:405-416 — `## Changelog` at 405, `### 1.6.2` at 407, six existing bullets at 409-414, a blank line at 415, `### 1.6.1` at 416. Each bullet is one physical line. Read all six to match the voice: backtick-quoted identifiers, plain language, past-tense description of what changed, no ticket identifiers anywhere.
    - .planning/phases/06-ship-1-6-2/06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-03-SUMMARY.md, 06-04-SUMMARY.md — what actually shipped, in each plan's own words. Write the bullets from these, not from the plan objectives; a bullet must describe the landed fix.
    - .planning/ROADMAP.md — Success Criterion 4, verbatim.
    - .planning/phases/06-ship-1-6-2/06-CONTEXT.md — D-09, including the discretion note fixing bullet style.
    - README.md:81-114 — the install section the re-run notice should be consistent with, so the notice does not contradict the documented install path.
  </read_first>
  <action>
    Resolve D-09's bullet count explicitly first, and record the resolution in the SUMMARY. D-09 as locked reads "six newly-fixed items" but its parenthetical names only four (ltf, u1z, d4t, b7g). The count reconciles only one way: this phase fixed **five** defects, not four — the fifth is `sup`, the trap-preservation race, which D-09's parenthetical omits because it is discussed under D-08 rather than under D-09. Five fix bullets plus one bullet noting the two investigation closures (xfa and i43) is six items, matching the locked count exactly. Ship six new bullets. State this reading in the SUMMARY so the discrepancy is settled on the record rather than rediscovered.

    Insert a re-run notice immediately after the `### 1.6.2` heading, above the existing bullets, as a standalone emphasized line rather than a bullet — it is a notice about installation, not a defect entry, and criterion 4 asks for it to be stated plainly. It must say that every existing installation has to re-run `scripts/install.sh`, and give the reason the pin makes non-optional: the launchers exec a target pinned at install time and a superseded pin fails loud with exit 127 rather than silently running the old copy, so a shipped fix that is never re-installed sits on disk and never executes.

    Append the six new bullets after the last existing bullet at line 414, keeping the existing six untouched and their order unchanged. One physical line per bullet, matching the surrounding style. Do not touch `### 1.6.1` or `### 1.6.0` or any line at or below 416.

    The five fix bullets, one per defect, described in plain terms with no ticket identifiers:
    1. The `gemini` shim no longer swallows the token after an unrecognized long flag, so a prompt following an unknown `--flag` reaches agy instead of being consumed as that flag's value; an unknown flag that genuinely carries data must use the inline `--flag=value` form.
    2. A successful `agy models` call that returns nothing, with no cached list on disk, now reports the degraded/unauthenticated diagnostic instead of the generic fetch-failure message; the exit code and the untouched cache are unchanged.
    3. The bounded-execution helper now hands the host's `TERM`/`INT`/`HUP` handlers back before tearing down its own timer, closing a window in which a signal arriving during teardown left the caller's cleanup handler replaced.
    4. The static check that every `agy` invocation is a bounded call now also covers the repository's contract-check script, which invokes agy four times and was previously unscanned.
    5. A standing regression guard now states as one assertion that both model-list validators use the SIGPIPE-safe herestring form, so a one-sided edit to the pair fails with a message naming both sides.

    The sixth bullet records the two closures without ticket identifiers, naming both by subject: agy's per-run policy isolation was verified to bind at the bridge's actual invocation shape rather than being assumed from the CWD-binding comment, and a delegation call was observed dying on `SIGTERM` alone — which narrows, but did not remove, the kill-after escalation's stated rationale; the escalation was kept and the contradiction recorded in the project's decision log.

    Bullets 4 and 5 describe test-side work. Include them anyway: criterion 4 says the notes name each defect the release closes, and both were open tickets closed by this release. Say plainly in each that the change is to the project's checks rather than to shipped behavior, so a reader does not go looking for a runtime difference that is not there.
  </action>
  <verify>
    <automated>sed -n '/^### 1\.6\.2$/,/^### 1\.6\.1$/p' README.md | grep -c '^- '</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### 1\.6\.2$/,/^### 1\.6\.1$/p' README.md | grep -c '^- '` outputs `12` — the six pre-existing bullets plus six new ones.
    - `sed -n '/^### 1\.6\.2$/,/^### 1\.6\.1$/p' README.md | grep -ci 'install.sh'` is at least 1, and the matching text also contains the digits `127`.
    - `sed -n '/^### 1\.6\.2$/,/^### 1\.6\.1$/p' README.md | grep -c 'delegate-agy-'` outputs `0` — no ticket identifier appears in the release notes.
    - Within the `### 1.6.2` section, each of these distinct substrings matches at least once: `--flag=value`, `SIGPIPE`, `contract-check`, `TERM`, and `unauthenticated`.
    - `git diff -- README.md` shows no hunk at or below the `### 1.6.1` heading, and no modification to any of the six pre-existing 1.6.2 bullets.
    - `git diff --stat` for this task shows exactly one file changed: `README.md`.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation append inside one section; `git checkout README.md` restores it.</reversibility>
  <done>`### 1.6.2` names all five defects this release fixes plus the two investigation closures, carries the re-run-the-installer notice with its reason, uses no ticket identifiers, and leaves every earlier section untouched.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created or removed across all of Phase 06. Listed in every plan so the source-grounding pass can exclude them from drift verification regardless of which plan it reads.

**Created — new test-case IDs in `tests/run-tests.sh`:**
- `RB30` (plan 06-01, D-08) — deterministic trap-restore-window forcing case
- `SH16` (plan 06-03, D-04) — unknown long flag must not consume the next token
- `R9f` (plan 06-03, D-07) — zero-byte successful fetch with no cache reports the degraded diagnostic
- `IN02` (plan 06-04, D-05) — twin `grep -qxF` structural-equality guard

**Created — new shell variables and modes:**
- `_CC_NO_AGY` in `tests/contract-check.sh` (plan 06-04, D-06)
- `_agy_degraded_no_cache` in `scripts/agy_bridge.sh` (plan 06-03, D-07)
- `FAKE_AGY_MODELS_EMPTY` env mode in `tests/fake-agy.sh` (plan 06-03, D-07)
- Case-local supporting variables: `RB30_*` / `_rb30_*` (plan 06-01), `SH16_*` / `_sh16_*`, `R9F_*` / `_r9f_*` (plan 06-03), `IN02_BRIDGE_PATTERN`, `IN02_SHIM_PATTERN`, `IN02_BRIDGE`, `IN02_SHIM` (plan 06-04)

**Created — documentation:**
- Two new rows in `.planning/PROJECT.md` `## Key Decisions` (plan 06-02, D-02 and D-03)
- Six new bullets plus one re-run notice in `README.md` `### 1.6.2` (this plan, D-09)

**Removed:** none. No flag, function, variable, case ID, or file is deleted by this phase. `AGY_BIN` remains defined at `tests/contract-check.sh:413`; plan 06-04 replaces only the seven guard-clause *references* to it.

**Moved (not created):** the watchdog arm's `_rb_cancel_timer` call, in all three `run_bounded` copies (plan 06-01). Same symbol, new position.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped code → published release notes | The notes are the only channel telling an existing installation that it must act; an omission here is a silent non-delivery |
| branch history → working-tree contents | Criterion 2 exists because history and contents can disagree; the boundary is exactly that disagreement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Repudiation | `README.md` `### 1.6.2` release notes | low | mitigate | Criterion 4 requires each closed defect named. This plan's acceptance criteria assert a bullet count of 12 and grep for one distinctive substring per new bullet, so a silently dropped entry fails the task rather than shipping. The bullets are written from the four plan SUMMARYs, so a note cannot describe a fix that did not land. |
| T-06-13 | Tampering | the `a001d0e` content-revert verification | medium | mitigate | The criterion-2 check is written as content greps over the working tree rather than as line-number citations or a history inspection. This defeats both failure modes the criterion names: a merge that restores history while leaving contents short, and a check that rots because plan 06-03 shifted `scripts/agy_bridge.sh`'s line numbers. A FAIL halts the plan rather than being adjusted away. |
| T-06-14 | Information disclosure | release-note prose | low | accept | Public-facing text describing behavior changes. It names no path outside the already-published install locations, no credential, no internal hostname, and no user prompt content. Ticket identifiers are deliberately excluded, which also keeps the internal tracker unadvertised. |
| T-06-15 | Elevation of privilege | the re-run-the-installer notice | low | mitigate | The notice tells users to re-run `scripts/install.sh` from the plugin directory and explains the exit-127 stale-pin behavior, so a user who reads it re-installs from the documented source rather than following an unverified instruction from elsewhere. It is written to be consistent with the existing install section rather than introducing a second, competing procedure. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages. RESEARCH.md's Package Legitimacy Audit records "not applicable": the repo carries no `package.json`, `requirements.txt`, or `Cargo.toml`, and every dependency touched is pre-existing and already audited. No package-manager install task exists in any plan of this phase. |

ASVS level 1; blocking severity `high`. No `high` or `critical` finding in this plan.
</threat_model>

<verification>
1. The criterion-2 script reports PASS for all seven pieces, and its full output is in the SUMMARY.
2. `sed -n '/^### 1\.6\.2$/,/^### 1\.6\.1$/p' README.md | grep -c '^- '` outputs `12`.
3. `sed -n '/^### 1\.6\.2$/,/^### 1\.6\.1$/p' README.md | grep -c 'delegate-agy-'` outputs `0`.
4. `bash tests/run-tests.sh 2>&1 | tail -3` still reports `FAIL=0` — a documentation change must not disturb `CC05`, which holds the README's dated model fact to the captured fixture.
</verification>

<success_criteria>
- Criterion 2 is proven by file contents, with the evidence recorded verbatim.
- Criterion 4's two halves are both satisfied: every defect named, and the re-run requirement stated plainly with its reason.
- D-09's ambiguous "six" is resolved on the record rather than silently picked.
- No code file changed.
</success_criteria>

<output>
Create `.planning/phases/06-ship-1-6-2/06-05-SUMMARY.md` when done. It must contain the criterion-2 script's full literal output, the `git show --stat a001d0e` output verbatim, and the recorded resolution of D-09's bullet count. Plan 06-06's gate cites all three.
</output>
