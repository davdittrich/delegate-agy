---
beads_epic: delegate-agy-tmm
phase: 06-ship-1-6-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [S5, R11]
beads: [delegate-agy-xfa, delegate-agy-i43]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`delegate-agy-xfa` is closed as verified-and-documented, not silently dropped: the bridge's per-run GEMINI.md policy isolation was measured at the real invocation shape and the result carries its one-run caveat (D-02)."
    - "`delegate-agy-i43` is closed as a documented discrepancy: R11's -k escalation is kept, and the contradicting SIGTERM-alone delegation observation is recorded where a future reader will find it (D-03)."
    - "Neither closure changes a line of shipped code."
  artifacts:
    - ".planning/PROJECT.md — two new rows in `## Key Decisions`"
  key_links:
    - "Each bd closure comment must name the PROJECT.md row that carries its finding, or the verdict becomes unauditable once the ticket is closed."
    - "Plan 06-05's release note cites these two closures; the rows must exist before that note is written."
---

<objective>
Close the phase's two investigation tickets — `delegate-agy-xfa` (D-02) and `delegate-agy-i43` (D-03) — by recording what each investigation actually found in `.planning/PROJECT.md`'s `## Key Decisions` table, then closing the tickets against those rows.

Neither ticket is a defect with a fix. `xfa` asked whether the bridge's per-run policy isolation really binds; Phase 01.5-05's contract-check probe answered it. `i43` recorded a live observation that contradicts R11's stated `-k` rationale; the phase's decision is to keep the escalation and record the contradiction rather than act on one run. Both are closed on evidence, and criterion 1 requires the evidence to survive the closure.

Purpose: `bd list --status open` must contain no ticket surfaced by 1.6.2 work at tag time, and anything not fixed must be closed with a recorded reason. A ticket closed with no durable record is the failure mode this task exists to prevent.
Output: two rows in PROJECT.md and two closed tickets, each closure comment pointing at its row.

This plan runs in wave 1 alongside plan 06-01. It shares no file with any other plan in the phase.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ship-1-6-2/06-CONTEXT.md
@.planning/phases/06-ship-1-6-2/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the xfa and i43 findings as Key Decisions rows (D-02, D-03)</name>
  <beads-id>delegate-agy-tmm.3</beads-id>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md:69-83 — the `## Key Decisions` table. Header is `| Decision | Rationale | Outcome |` at line 71; the existing rows establish the shape: one sentence of decision, a rationale that names the ticket that forced it, and an Outcome cell opening with one of the glyphs `✓ Good`, `⚠️ Revisit`, or `✗ Superseded`.
    - .planning/phases/06-ship-1-6-2/06-PATTERNS.md — the `.planning/PROJECT.md` section, which fixes this row shape as the analog and states that D-02 needs the one-run caveat and D-03 needs the contradiction recorded.
    - .planning/phases/06-ship-1-6-2/06-CONTEXT.md — decisions D-02 and D-03 verbatim.
    - `bd show delegate-agy-xfa` — the original probe observation plus the 2026-08-20 comment carrying the `gemini-md-binds=verified` verdict and its ledger line.
    - `bd show delegate-agy-i43` — the rc=124 / elapsed=8s delegation observation and the `-k` rationale it contradicts.
    - scripts/agy_bridge.sh:478-480 — the inline comment whose claim ("agy ignores SIGTERM, observed: `timeout 25 agy models` still running after 3+ min") i43's finding narrows. Read it so the D-03 row cites the current line numbers, not the ticket's stale `:683-684`.
  </read_first>
  <action>
    Append exactly two rows to the `## Key Decisions` table in `.planning/PROJECT.md`. Match the existing row shape exactly — three pipe-delimited cells, Outcome opening with a glyph. Do not restructure the table, do not edit an existing row, do not add a column.

    Row 1 (D-02, `delegate-agy-xfa`). Decision: trust the bridge's per-run GEMINI.md policy isolation on directly measured evidence rather than on the CWD-binding comment alone. Rationale: the raw probe showed agy discovering GEMINI.md files across unrelated projects, which would have reduced `config/policies/*.md` to one context among several; Phase 01.5-05's `_cc_probe_gemini_md_binds` re-asked the question at the real bridge invocation shape (`--sandbox` plus `--add-dir WORK_DIR`), with a never-granted decoy directory carrying its own marked GEMINI.md and a per-run nonce whose locally computed cksum is the unforgeable discriminator for whether the forbidden shell tool actually ran. Outcome: `✓ Good` — verified against real agy 1.1.13 on 2026-08-20; the forbidden tool was declined cleanly, the computed cksum never appeared in the reply, and the decoy's marker never leaked. Carry the ledger line verbatim (`type=code policy=config/policies/code.md bound=120s rc=0 checksum_matched=no decoy_seen=no`) and state the caveat plainly in the same cell: one run, one prompt shape, one model, one agy version — evidence that the concern does not reproduce at the real invocation shape, not proof the restriction always binds.

    Row 2 (D-03, `delegate-agy-i43`). Decision: keep R11's `-k` SIGKILL escalation even though a real delegation call died on SIGTERM alone. Rationale: the escalation's stated basis is the 2026-08-19 observation that `timeout 25 agy models` was still running after three minutes — the *models* subcommand; Phase 01.5-05's probe drove a real *delegation* (a ~50000-word synthetic-essay prompt, `--type code`, `--add-dir WORK_DIR`) under `timeout -k 5 8` and got rc=124 at elapsed=8s, meaning the first-stage SIGTERM ended agy before the second stage ever fired. The two observations disagree because they exercised different subcommands, not because either is wrong. Outcome: `⚠️ Revisit` — the escalation stays, because one contradicting delegation run does not establish that agy reliably dies on SIGTERM, and dropping `-k` would reintroduce an unbounded hang on the path where the original observation still stands; note that the inline rationale at `scripts/agy_bridge.sh:478-480` is now narrower than what has been measured and should be reread if the models-subcommand observation is ever retested.

    Change nothing else in PROJECT.md. This task adds no shipped code and must not touch `scripts/`, `tests/`, or `README.md` — D-03 in particular is a recorded discrepancy, not a code change; rewriting the inline comment to match would retroactively weaken a shipped mechanism on the strength of a single run, which is exactly what Phase 01.5-05 declined to do.
  </action>
  <verify>
    <automated>grep -c '^| ' .planning/PROJECT.md && grep -F 'checksum_matched=no decoy_seen=no' .planning/PROJECT.md && grep -F 'rc=124' .planning/PROJECT.md && git diff --stat -- .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'checksum_matched=no decoy_seen=no' .planning/PROJECT.md` matches exactly one line.
    - `grep -F 'rc=124' .planning/PROJECT.md` matches exactly one line, and that line also contains the string `⚠️ Revisit`.
    - The xfa row's Outcome cell contains `✓ Good` and the substring `one agy version`.
    - The i43 row's Outcome cell contains `scripts/agy_bridge.sh:478-480`.
    - `git diff --stat` for this task shows exactly one file changed: `.planning/PROJECT.md`, with 2 insertions and 0 deletions.
    - `awk -F'|' '/^\| /{print NF}' .planning/PROJECT.md | sort -u` shows the two new rows carry the same field count as the pre-existing Key Decisions rows.
  </acceptance_criteria>
  <reversibility rating="reversible">Two appended markdown table rows in a planning document; `git checkout` restores the file.</reversibility>
  <done>PROJECT.md's `## Key Decisions` table carries one row for xfa's verified-with-caveat result and one for i43's kept-escalation-with-recorded-contradiction, both matching the existing row shape. No other file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Close delegate-agy-xfa and delegate-agy-i43 against their recorded rows</name>
  <beads-id>delegate-agy-tmm.4</beads-id>
  <files>(no repository file — bd issue state only)</files>
  <precondition>The `bd` CLI resolves on PATH and `bd show delegate-agy-xfa` returns the ticket; the local Dolt-backed issue database is reachable. If `bd` is unavailable, halt — do not hand-edit `.beads/issues.jsonl`, which is a passive export.</precondition>
  <read_first>
    - .planning/PROJECT.md — the two rows Task 1 just wrote; the closure comments quote from them, so read the committed text rather than reconstructing it.
    - ./CLAUDE.md — the Beads section: `bd` is the sole task backend, and the Memory Channel rule reserves `bd remember` for global invariants, so per-ticket findings go in `bd comment`, never in memory.
    - .planning/phases/06-ship-1-6-2/06-BEADS-RECALL.md — the matched-ticket list, to confirm no scope-matched ticket beyond these two belongs to this plan.
  </read_first>
  <action>
    For each of `delegate-agy-xfa` and `delegate-agy-i43`: add a closure comment with `bd comment` (not `bd remember`), then close the ticket with `bd close`.

    The xfa comment states the ticket is closed as verified-and-documented under Phase 6 D-02, names `.planning/PROJECT.md`'s `## Key Decisions` row as the durable record, and repeats the one-run caveat so a reader who finds the closed ticket first is not misled into treating it as a general guarantee.

    The i43 comment states the ticket is closed as a documented discrepancy under Phase 6 D-03, names the same table as the record, and states the disposition explicitly: `-k` escalation kept, contradiction recorded, no code changed.

    Do not close `delegate-agy-rod`, `delegate-agy-ltf`, `delegate-agy-u1z`, `delegate-agy-d4t`, `delegate-agy-b7g`, or `delegate-agy-sup` here. Those close in plan 06-06's gate, after their fixes are green — closing a fix ticket before its fix ships is exactly the drift criterion 1 is written to catch.

    Leave the deferred tickets alone. `delegate-agy-rdu`, `-abv`, `-e4i`, `-ps3.8`, and `-ps3.9` already carry status `deferred`, not `open`, so criterion 1 is already satisfied for them; reopening or re-deferring them adds churn and no information.

    Follow the repository's conservative git profile: do not commit, push, or run a Dolt remote sync as part of this task unless the executing workflow has already been granted that authority.
  </action>
  <verify>
    <automated>bd list --status open 2>&1 | grep -E 'delegate-agy-(xfa|i43)' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `bd list --status open 2>&1 | grep -cE 'delegate-agy-(xfa|i43)'` outputs `0`.
    - `bd show delegate-agy-xfa` reports status closed and its comment list contains a comment naming `.planning/PROJECT.md` and containing the substring `one agy version`.
    - `bd show delegate-agy-i43` reports status closed and its comment list contains a comment containing the substring `-k` and the word `kept`.
    - `bd list --status open 2>&1 | grep -cE 'delegate-agy-(ltf|u1z|d4t|b7g|sup|rod)'` outputs `6` — this task closed nothing it was not scoped to close.
    - `git status --short` shows no change under `scripts/`, `tests/`, or `README.md`.
  </acceptance_criteria>
  <reversibility rating="reversible">`bd reopen` restores either ticket; the comments remain as history either way.</reversibility>
  <done>Both investigation tickets are closed, each carrying a comment that points at the PROJECT.md row holding its finding. The six fix and epic tickets remain open for plan 06-06.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created or removed across all of Phase 06. Listed in every plan so the source-grounding pass can exclude them from drift verification regardless of which plan it reads.

**Created — new test-case IDs in `tests/run-tests.sh`:**
- `RB30` (plan 06-01, D-08) — deterministic trap-restore-window forcing case
- `SH16` (plan 06-03, D-04) — unknown long flag must not consume the next token
- `R9f` (plan 06-03, D-07) — zero-byte successful fetch with no cache reports the degraded diagnostic
- `IN02` (plan 06-04, D-05) — twin `grep -qxF` joint presence-and-form guard

**Created — new shell variables:**
- `_CC_NO_AGY` in `tests/contract-check.sh` (plan 06-04, D-06)
- `_agy_degraded_no_cache` in `scripts/agy_bridge.sh` (plan 06-03, D-07)
- Case-local supporting variables: `RB30_*` / `_rb30_*` (plan 06-01), `SH16_*`, `R9F_*` (plan 06-03), `IN02_BRIDGE_PATTERN`, `IN02_SHIM_PATTERN`, `IN02_BRIDGE`, `IN02_SHIM` (plan 06-04)

**Created — documentation:**
- Two new rows in `.planning/PROJECT.md` `## Key Decisions` (this plan, D-02 and D-03)
- New bullets in `README.md` `### 1.6.2` Changelog (plan 06-05, D-09)

**Removed:** none. No flag, function, variable, case ID, or file is deleted by this phase. `AGY_BIN` remains defined at `tests/contract-check.sh:413`; plan 06-04 replaces only the seven guard-clause *references* to it.

**Moved (not created):** the watchdog arm's `_rb_cancel_timer` call, in all three `run_bounded` copies (plan 06-01). Same symbol, new position.

<threat_model>
## Alternatives Considered

N/A — no mechanism choice: every task in this plan is a mechanical bash patch to existing, already-chosen code (reorder, extend an existing scan loop, restructure existing guard clauses, or add a test case mirroring an established sibling pattern) — no new library, algorithm, or architectural mechanism is being selected.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| investigation verdict → durable record | A finding that exists only in a closed ticket's body is effectively unreviewable at tag time; the boundary is between "measured" and "recorded" |
| bd issue database → release gate | Criterion 1 is evaluated from `bd list --status open`, so ticket state is release-gating input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Repudiation | bd ticket closure for `delegate-agy-xfa` and `delegate-agy-i43` | low | mitigate | Each closure carries a `bd comment` naming the `.planning/PROJECT.md` Key Decisions row that holds the finding, and the row itself carries the caveat. A reader who reaches the closed ticket first is told the limits of the evidence rather than inheriting a false guarantee. |
| T-06-11 | Tampering | premature closure of the five fix tickets | low | mitigate | This task's acceptance criteria assert that `ltf`, `u1z`, `d4t`, `b7g`, `sup`, and `rod` are still open afterwards, so a scope slip that closes a fix ticket before its fix ships fails the task rather than passing the release gate. |
| T-06-12 | Information disclosure | the `xfa` ledger line copied into `.planning/PROJECT.md` | low | accept | The ledger line contains a policy filename, a bound, and three boolean verdicts — no path outside the repository, no secret, no user prompt content. The nonce and cksum themselves are per-run and deliberately not copied. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages. RESEARCH.md's Package Legitimacy Audit records "not applicable": the repo carries no `package.json`, `requirements.txt`, or `Cargo.toml`, and every dependency touched is pre-existing and already audited. No package-manager install task exists in any plan of this phase. |

ASVS level 1; blocking severity `high`. No `high` or `critical` finding in this plan.
</threat_model>

<verification>
1. `bd list --status open` no longer lists `delegate-agy-xfa` or `delegate-agy-i43`, and still lists the other six.
2. `.planning/PROJECT.md`'s `## Key Decisions` table gained exactly two rows, both matching the pre-existing three-cell shape with a glyph-led Outcome.
3. `git status --short` shows no modification under `scripts/`, `tests/`, or `README.md`.
</verification>

<success_criteria>
- D-02 and D-03 are honoured as written: verified-and-documented and documented-discrepancy respectively, with the caveat and the contradiction both surviving the closure.
- The two closures move criterion 1 forward without touching shipped behavior.
- Nothing this plan does can conflict with plans 06-01, 06-03, 06-04, or 06-05; it owns one file no other plan writes.
</success_criteria>

<output>
Create `.planning/phases/06-ship-1-6-2/06-02-SUMMARY.md` when done. Quote both new PROJECT.md rows verbatim; plan 06-05's release note needs the xfa/i43 closure line and will source it from here.
</output>
