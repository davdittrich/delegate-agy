---
beads_epic: delegate-agy-6q3
phase: 02-model-list-handling-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/gemini_shim.sh
  - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh
autonomous: true
requirements: [S1, S4]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A successful `agy models` reply whose normalized id column (`cut -f1`) carries no line matching `^gemini-` is never written to `~/.cache/agy-bridge-models` by `scripts/gemini_shim.sh`'s `load_models()`: an existing cache file keeps both its bytes and its mtime, and no cache file is created where none existed (D-03, S4)."
    - "S4 is closed on both writers, not one: after this plan the same normalize-then-`^gemini-` test gates the write in `scripts/agy_bridge.sh` and in `scripts/gemini_shim.sh`, so neither tool can poison the file the other reads for up to 60 minutes (D-03, `delegate-agy-8ph`)."
    - "With a non-empty cache file present, a degraded-but-successful fetch makes `load_models()` fall back to that cache and emit the cached ids, taking the same route it already takes when the bounded fetch itself fails at `scripts/gemini_shim.sh:427` (D-04)."
    - "That fallback is silent. No stderr line is added to `scripts/gemini_shim.sh` on this path, preserving the degrade-quietly design recorded at `:424-426` and `:104-106`; the shim shadows `gemini` on PATH, so a warning here would land in every Octopus and Metaswarm log line (D-05)."
    - "With no cache file to fall back to, a degraded reply is unchanged from what shipped: `load_models()` returns 0, an unrecognized name still reaches agy verbatim, and `map_model`'s warning gate at `:459` stays quiet because the list carries no gemini ids. SH14's assertion is untouched (D-04, D-02)."
    - "edge:adjacency (explicit) — a reply row carrying a third tab-separated column and a reply row carrying a trailing tab with an empty second column both normalize through `load_models()`'s `cut -f1` at `:428` to the same bare id a plain two-column row yields, so `map_model`'s class matcher at `:447` sees identical input in all three shapes (S1 criterion 4, D-06)."
    - "edge:ordering (explicit) — `map_model`'s verbatim-live-id check at `:438` and its class resolution `grep -E '^gemini-[0-9.]+-<class>$' | sort -V | tail -1` at `:447` are byte-identical to what shipped; this plan changes which list reaches them, never how they pick. SH7, SH8 and SH9 are the proof."
    - "The `${HOME:-/nonexistent}` guard at `scripts/gemini_shim.sh:398` and the block-level `2>/dev/null || true` on the cache write are preserved exactly, and SH12's `HOME`-unset case still passes — criterion 1 is closed work this phase must not disturb and must not reopen (D-01). D-08's narrow exception below is scoped to exactly one line (`:459`) and does not touch this guard or reopen D-01 for any other reason."
    - "Both `grep -q '^gemini-'` degraded-list checks in `scripts/gemini_shim.sh` — the new write-gate this task adds and the existing shipped check in `map_model`'s warning gate at `:459`, which D-08 reopens as a narrow, user-approved exception to D-01/D-02 — use the herestring form (`grep -q '^gemini-' <<< \"$var\"`), never a pipe. Mirrors the identical fix landed in Plan 02-01 Task 1 for `scripts/agy_bridge.sh:515`; the empirical reproduction and full citation live in `02-CONTEXT.md` §D-08 (D-08, S1)."
    - statement: "An `agy models` reply that exits 0 with zero bytes never reaches the shim's write site at all, because the pre-existing `[[ -n \"$raw\" ]]` guard at `:413` already excludes it; the new gate sits inside that guard and narrows it further rather than replacing it."
      verification: backstop
  prohibitions:
    - "No one-sided fix: this plan is the second half of `delegate-agy-8ph`. Landing 02-01 without this plan leaves the requirement open, and this plan must not be descoped independently of it."
    - "No new stderr line, warning, or notice is added to `scripts/gemini_shim.sh` on the degraded path. The shim's silence there is a locked decision, and a shim warning is a Deferred Idea belonging to Phase 5."
    - "The 60-minute TTL (`find -mmin +60` at `scripts/gemini_shim.sh:406`) is not changed as a substitute for the write gate."
    - "The anchored matcher `^gemini-[0-9.]+-<class>$` at `:447`, the verbatim-id check at `:438`, and `map_model`'s warning gate at `:459` are not loosened or re-gated to accommodate any reply shape."
    - "A degraded reply never deletes or truncates the existing cache file."
    - "The D-04 fallback does not refresh the cache file's mtime."
    - "The atomic tmp-then-`mv` write plus `chmod 600` at `scripts/gemini_shim.sh:418-421` is not replaced by a direct redirect while the gate is added around it."
    - "The two scripts' model blocks are not factored into a shared sourced library. `scripts/gemini_shim.sh:378-386` records why the duplication is deliberate: this file installs as `~/.local/bin/gemini` and a missing helper would break `gemini` box-wide."
    - "D-08's herestring fix touches exactly two lines: the new write-gate and the existing `:459` check. It does not touch `scripts/gemini_shim.sh:447`'s `grep -E ... | sort -V | tail -1` class-resolution line (no early-close hazard) or `:438`'s `grep -qxF \"$m\"` exact-id validation (a different mechanism, not named by the user's directive)."
    - "The synthetic multi-column rows D-06 needs are never added to `tests/fixtures/agy-models.tsv`."
    - "`load_models()` never returns non-zero. Its contract at `:401-403` is that an unresolvable list degrades to pass-through, never to a hard error, and the new branch must not introduce a path that violates it under `set -euo pipefail`."
  assumptions:
    - "FLAGGED, UNRESOLVED — carried forward from 02-01: the S4 edge probe returned `unclassified`, so no automated edge predicate was derived for the two-independent-writers concurrency requirement. No test in this phase runs the bridge and the shim concurrently against the same cache path; the concurrency property rests on the tmp-then-`mv` rename being atomic, which this phase preserves but does not test."
    - "RESOLVED IN 02-01, NOT THIS PLAN'S WORK — criterion 3's `shown agy's own stderr` half, previously flagged as an open gap, was folded into 02-01 Task 2 during planning review: the bridge's existing stderr relay is relocated to fire unconditionally, closing the gap on the bridge side. This plan does not touch it — the shim has no `$_agy_err` to relay in the first place (`load_models()`'s fetch redirects agy's stderr straight to `2>/dev/null`, `scripts/gemini_shim.sh:412`), so there is nothing here to resolve or defer. No follow-up bd issue is needed for this; do not file one at phase close."
  artifacts:
    - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/gemini_shim.sh
    - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh
  key_links:
    - "The gate in `load_models()` and the gate in `scripts/agy_bridge.sh` are the same rule applied to the same cache path from two processes. If only one carries it, `delegate-agy-8ph` is not closed — the unguarded writer still poisons the guarded reader."
    - "`raw` being cleared in the new `elif` reaches the cached list through the shipped fallback at `scripts/gemini_shim.sh:427`. That one line is D-04's entire mechanism on this side; nothing new is added below it."
    - "`map_model`'s warning gate at `:459` is what keeps SH14 green when the list is empty rather than gemini-less — it tests the list for `^gemini-`, so an empty `LIVE_MODELS` and a garbage one are treated identically. The new branch relies on that and must not change it. Under D-08, `:459`'s test itself is converted to the herestring form (`grep -q '^gemini-' <<< \"$LIVE_MODELS\"`), but the test's semantics — what it returns for a given `LIVE_MODELS` — are unchanged, so this dependency still holds exactly as before."
    - "`AGY_FIXTURES_DIR` (`tests/fake-agy.sh:113-145`, tier 1, pinned by CC04a) is what lets SH15c drive a real multi-column `agy models` reply through `load_models()`'s fetch path instead of short-circuiting at the cache."
---

<objective>
Mirror the bridge's fix into the shim, and close S4.

`delegate-agy-8ph` forbids a one-sided fix in terms: both writers change together or neither does. Plan 02-01 proved the shape end to end on `scripts/agy_bridge.sh`; this plan applies the identical shape to `scripts/gemini_shim.sh`'s `load_models()`, which writes the same cache file, on the same 60-minute window, with the same atomic write. The two files are line-for-line mirrors of each other in this region by design, and `scripts/gemini_shim.sh:378-386` states why they are not factored into a shared library — so the fix is duplicated deliberately, not abstracted.

One thing does not mirror: the message. The bridge warns; the shim stays silent. That asymmetry is deliberate and already documented at `scripts/gemini_shim.sh:424-426` — this file shadows `gemini` for every PATH caller, so a warning on a normal degraded path would land in every Octopus and Metaswarm log line. D-05 locks it, and a shim warning is a Deferred Idea belonging to Phase 5.

One thing does mirror, added during planning review after Plan 02-01 was revised: D-08's herestring fix. Cross-AI review found the `printf | grep -q '^gemini-'` gate form is not fully `pipefail`-safe; this task's write-gate uses a herestring instead, and — as a narrow, user-approved exception to D-01/D-02, mirroring the one in Plan 02-01 — converts the existing shipped check at `map_model`'s warning gate (`:459`) to match.

Purpose: make the second writer as safe as the first, so one bad `agy models` reply cannot degrade the other tool for an hour.
Output: the gate and the fallback in `load_models()`, plus SH15, SH15b and SH15c in `tests/run-tests.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/dd/Gemini/delegate-agy/.planning/PROJECT.md
@/home/dd/Gemini/delegate-agy/.planning/ROADMAP.md
@/home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md
@/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-CONTEXT.md
@/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-PATTERNS.md
@/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-01-SUMMARY.md

**Source of truth:** `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2` on branch `fix/agy-bridge-resilience`. Every line number in this plan is from that worktree, and the `scripts/agy_bridge.sh` line numbers will have shifted by plan 02-01's edit — read the file, do not trust the arithmetic.
</context>

## Artifacts this phase produces

Full phase inventory is in `02-01-PLAN.md` §"Artifacts this phase produces". This plan contributes:

**New in `scripts/gemini_shim.sh`:**

- `ids` — a new local in `load_models()`, holding the `cut -f1`-normalized fetched list, computed once inside the existing non-empty-`raw` guard and read only by the D-03 gate. Declared on the existing `local` line at `:405`.
- The D-03 gate — a new `if` around the existing write block at `:418-421`. Not a new function.
- The D-04 arm — a new `elif` on that same `if`, clearing `raw`. No message, no new variable.

**New test case identifiers in `tests/run-tests.sh`:** `SH15`, `SH15b`, `SH15c`.

**No new literal on this side.** The shim adds no string. D-05's new warning belongs to the bridge alone and landed in plan 02-01.

**No new files, no new fake-agy knob, no shared library, no README change.**

## Alternatives Considered

**A. Whether the shim's fix is the bridge's fix duplicated, or the two are factored into one shared implementation.**

- **Duplicate the shape into `load_models()`, independently (chosen)**: LOCKED by D-03's framing and by the file's own recorded rationale. `scripts/gemini_shim.sh:378-386` states the model-resolution block is deliberately not factored into a sourced library because this script installs as `~/.local/bin/gemini` and shadows the real `gemini` for every caller on PATH — a missing helper would break `gemini` box-wide, and roughly twenty duplicated lines is the cheaper failure mode. Citation: `scripts/gemini_shim.sh:378-386`, read on `fix/agy-bridge-resilience` (2026); `02-PATTERNS.md` §"`scripts/gemini_shim.sh`'s `load_models()`" (2026).
  Decided by: maintenance — a shared file introduces a new runtime failure mode on the one script in this repo whose failures reach processes that never heard of this project.
- **Extract the fetch/gate/cache block into a sourced `lib/models.sh`**: the textbook answer to two near-identical blocks, and the only option that makes drift structurally impossible. Rejected for the reason above, and because the divergence this phase itself introduces — the bridge warns, the shim is silent — means the shared function would immediately need a caller-supplied message hook, which is a parameterized abstraction over two call sites. Citation: `02-CONTEXT.md` §D-05, the shim-stays-silent decision (2026); `scripts/gemini_shim.sh:424-426` (2026).
  Decided by: simplicity — one branch and one parameter's worth of abstraction for two call sites is not a saving.
- **A test that pins the two blocks byte-identical, `RB02`'s treatment of `run_bounded`**: keeps the duplication honest without a shared file. Rejected because the two blocks are *not* byte-identical and cannot be — different variable names (`_agy_models` against `raw`), different scope (top-level against function-local), and one carries a warning the other must not. RB02 works precisely because `run_bounded` is genuinely identical text. Citation: `tests/run-tests.sh:2032-2086` (RB02 and RB02m) (2026).
  Decided by: maintenance — a byte-comparison over text that legitimately differs is a test that must be weakened on its first true positive.

**B. What the shim does with a degraded reply when no cache exists.**

- **Leave `raw` holding the degraded reply and let `map_model` handle it, exactly as today (chosen)**: `map_model`'s warning gate at `:459` already tests `LIVE_MODELS` for a `^gemini-` line before warning, so a gemini-less list and an empty one are treated identically — the name passes through unchanged and quietly either way. Leaving `raw` alone therefore changes nothing observable while keeping the shim's branch shape a mirror of the bridge's, where the same choice is load-bearing because it preserves R8's message. Citation: `scripts/gemini_shim.sh:452-461` and `tests/run-tests.sh:1168-1187` (SH14), read on the worktree (2026); `02-01-PLAN.md` §"Alternatives Considered" B (2026).
  Decided by: LOC — zero extra branches, and the two files keep one shape between them.
- **Clear `raw` unconditionally on a degraded reply**: one condition shorter, and observably identical on this side — `load_models()` would emit nothing, `LIVE_MODELS` would be empty, and `map_model` would still pass the name through without warning. Rejected only because it breaks the mirror: the bridge cannot do this without turning R8 red, so adopting it here would leave the two blocks structurally different for no gain, and the next person to compare them would have to rediscover why. Citation: `scripts/agy_bridge.sh:502-517` (2026); `scripts/gemini_shim.sh:378-386` on the mirror being the maintenance contract (2026).
  Decided by: maintenance — the duplication only stays cheap while the two copies stay comparable.

**C. How SH15b proves the cache was preferred over the degraded reply.**

- **`FAKE_AGY_DUMP_ARGV` plus a cache-only model id, SH10's shape (chosen)**: seed the cache with `gemini-7.7-flash-high`, a version that exists in no fixture and no other case, then read the `--model` value agy actually received out of the argv dump. A resolved 7.7 can only have come from the cache. The knob and the `awk` extraction already exist and are already used by SH8, SH9, SH10 and SH14. Citation: `tests/run-tests.sh:1084-1097` (SH10) and `tests/fake-agy.sh:21-26` (the knob), read on the worktree (2026).
  Decided by: ecosystem — the harness already does this four times; a fifth costs nothing.
- **Asserting on the shim's own stdout**: simpler, no dump file. Rejected because the shim passes the resolved model to agy rather than printing it — stdout carries agy's reply, not the resolution — so this cannot observe the property at all. Citation: `scripts/gemini_shim.sh:548` (`LIVE_MODELS=$(load_models)`) and the delegation call site (2026).
  Decided by: simplicity — among options that can observe the resolution, this is not one.

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A degraded agy reply never reaches the cache file, from the shim either (D-03)</name>
  <beads-id>delegate-agy-6q3.4</beads-id>
  <reversibility rating="reversible">D-03 rates itself reversible: a single condition around an existing write, removable locally. D-08's herestring fix (folded in during planning review, touches this task's gate and the existing `:459` check) rates reversible too: two `<<<` tokens replacing two `|` pipe forms.</reversibility>
  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/gemini_shim.sh, /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-6q3.4` -->
</task>

<task type="auto" tdd="true">
  <name>Task 2: A degraded reply falls back to a good stale cache, silently (D-04, D-05)</name>
  <beads-id>delegate-agy-6q3.5</beads-id>
  <reversibility rating="reversible">D-04 rates itself reversible: a branch condition, not a new state machine.</reversibility>
  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/gemini_shim.sh, /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-6q3.5` -->
</task>

<task type="auto">
  <name>Task 3: Prove `cut -f1` normalizes an extra column and a trailing tab, on the shim (D-06)</name>
  <beads-id>delegate-agy-6q3.6</beads-id>
  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>
  <!-- beads: content synced to bd -- see `bd show delegate-agy-6q3.6` -->
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `agy` subprocess stdout → shim | Untrusted third-party CLI output crosses here at `scripts/gemini_shim.sh:412`. Same source, same lack of format control, as the bridge's boundary. |
| shim → `~/.cache/agy-bridge-models` | `load_models()` persists that output to the file `scripts/agy_bridge.sh` reads as authoritative for up to 60 minutes. This is the second half of the boundary `delegate-agy-8ph` names, and the half plan 02-01 could not close. |
| shim → every PATH caller of `gemini` | This script installs as `~/.local/bin/gemini` and shadows the real binary for Octopus, Metaswarm and interactive shells. Anything it writes to stderr, and any way it can fail, reaches processes that never heard of this project — the S3 blast radius. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | `scripts/gemini_shim.sh:418-421`, the cache write | medium | mitigate | Task 1's gate: normalize with `cut -f1` and require a `^gemini-` line before the tmp-then-`mv` write. Without this the phase leaves an unguarded writer against a guarded reader, which `delegate-agy-8ph` names explicitly as a non-fix. |
| T-02-07 | Denial of service | `load_models()` under `set -euo pipefail` | medium | mitigate | The function's contract at `:401-403` is that it never fails its caller — a non-zero return would abort `gemini` for every PATH caller, not just this plugin. The new gate and arm are both inside the existing `[[ -n "$raw" ]]` guard, the `return 0` at `:429` is unconditional and unmoved, and task 1's acceptance criteria assert both. SH12 and SH13 remain green as the runtime proof. |
| T-02-08 | Information disclosure | shim stderr reaching unrelated PATH callers | low | mitigate | No stderr line is added on the degraded path (D-05). Task 2's acceptance criteria assert the diff introduces no `WARNING`, no `NOTICE`, and no new `>&2` or `>&9` redirect, so a degraded agy cannot start emitting noise into every Octopus or Metaswarm log line. |
| T-02-09 | Tampering | `~/.cache/agy-bridge-models` as input to `--model` argv | medium | mitigate | Existing controls retained unchanged and asserted: `chmod 600` at `:421`, atomic tmp-then-`mv` at `:419-420`, the `${HOME:-/nonexistent}` path guard at `:398`, `cut -f1` at `:428`, the verbatim-id check at `:438` and the anchored class matcher at `:447`. The gate wraps the write block; it does not rewrite it. |
| T-02-10 | Denial of service | D-04's stale-cache fallback (task 2) | low | mitigate | The fallback reads through the shipped `cat` at `:427` and never writes, so the mtime stays at the last good write and the `find -mmin +60` window expires on its original schedule. SH15b asserts the mtime is still past the window after the call. |
| T-02-05 | Tampering | two independent writers, one cache path | low | accept | Carried from plan 02-01. S4's acceptance is that writes stay atomic, which the per-file `mv` rename satisfies; `delegate-agy-8ph` forbids substituting a TTL change, and a lock is a coordination mechanism this requirement did not ask for. Recorded as an untested property under `must_haves.assumptions`. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages and adds no dependency. The changed surface is two bash scripts and one test file, so the package-legitimacy gate has no rows to audit. |
</threat_model>

<verification>
From `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2`:

1. `bash tests/run-tests.sh` exits 0 with `FAIL=0` on its final line.
2. SH15, SH15b and SH15c all report `ok`, and so do R9, R9b and R9c from plan 02-01.
3. SH7, SH8, SH9, SH10, SH11, SH12, SH13, SH14, R1, R2, R3, R3b, R3c, R3d, R4, R5, R6, R7, R8, RB01, RB02, RB03, RB27, CC04a, CC04b and CC06 all still report `ok`.
4. `bash -n scripts/gemini_shim.sh` and `bash -n scripts/agy_bridge.sh` both exit 0.
5. Both writers carry the gate: the normalize-then-`^gemini-` test appears in the write path of `scripts/agy_bridge.sh` and of `scripts/gemini_shim.sh`. Confirm by reading both blocks side by side, not by a count — the two differ in variable names by necessity.
5b. Both writers use the herestring form for the gate (D-08): `grep -cF "grep -q '^gemini-' <<<" scripts/gemini_shim.sh` returns 2 (the new gate and the converted `:459` check), and no `printf ... | grep -q '^gemini-'` pipe form remains in `scripts/gemini_shim.sh`. `grep -cF "grep -qxF" scripts/gemini_shim.sh` returns 1 and `grep -cF "grep -E \"^gemini-" scripts/gemini_shim.sh` returns 1 — the exact-match and class-resolution lines are untouched.
6. `git diff --stat` for this plan (this wave's own commit) names exactly two files: `scripts/gemini_shim.sh` and `tests/run-tests.sh`. (The phase-wide diff also includes 02-01's `tests/fake-agy.sh` addition — that file is 02-01's surface, not this plan's; see item 7.)
7. `git diff -- tests/fixtures/ config/model-map.json README.md` is empty across the whole phase. `tests/fake-agy.sh` is deliberately excluded from this check — 02-01 Task 2 added one stderr literal to it (the folded-in criterion-3 fix); confirm instead that this plan's own diff does not touch `tests/fake-agy.sh`: `git diff --stat` for this plan's commit (item 6) already proves that by naming only two files.
8. Each RED observation (SH15's preservation half, SH15b, SH15c's mutation probe) is quoted in the SUMMARY.
</verification>

<success_criteria>
- `scripts/gemini_shim.sh` refuses to cache an `agy models` reply whose normalized id column carries no `^gemini-` line, with the same rule and the same shape as the bridge. SH15 pins both halves. Both the new write-gate and the existing `:459` check use the SIGPIPE-safe herestring form (D-08), not a pipe.
- S4 is closed on both writers. Neither tool can poison the other's model list, and neither did so by changing the TTL or adding a lock.
- A degraded reply with a usable stale cache present resolves the model off that cache, silently, without refreshing its mtime. SH15b pins all of it.
- A degraded reply with no cache present is unchanged: rc 0, name passed through verbatim, no warning. SH14 green.
- An extra column and a trailing tab both normalize through the shim's real fetch path, with `map_model`'s matchers byte-identical. SH15c pins it, completing criterion 4 on both entry points.
- The suite is green end to end. This plan's own diff is confined to the shim script and the test file; the phase's whole diff additionally includes 02-01's `tests/fake-agy.sh` addition (the folded-in criterion-3 fix), which is not this plan's surface.
</success_criteria>

<output>
Create `.planning/phases/02-model-list-handling-end-to-end/02-02-SUMMARY.md` when done.

Record in it: the three RED observations with their quoted failure details; a side-by-side note of how the shim's block differs from the bridge's and why each difference exists, since the two are a maintenance contract and the next reader will diff them; D-08's herestring fix, including that the existing `:459` check was also converted (a deliberate, narrow D-01/D-02 exception mirroring plan 02-01's `:515` conversion, not scope creep — cite D-08); and the one remaining flagged assumption from `must_haves.assumptions` verbatim (the untested concurrency property), so it reaches phase verification. The criterion-3 stderr gap is not this plan's concern — it was resolved in 02-01, not carried here; do not file a follow-up bd issue for it.
</output>
