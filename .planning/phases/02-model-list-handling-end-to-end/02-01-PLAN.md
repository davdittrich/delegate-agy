---
phase: 02-model-list-handling-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/agy_bridge.sh
  - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh
autonomous: true
requirements: [S1, S4]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A successful `agy models` reply whose normalized id column (`cut -f1`) carries no line matching `^gemini-` is never written to `~/.cache/agy-bridge-models` by `scripts/agy_bridge.sh`: an existing cache file keeps both its bytes and its mtime, and no cache file is created where none existed (D-03, S4)."
    - "With a non-empty cache file present, a degraded-but-successful fetch makes the bridge fall back to that cache and complete the delegation normally at rc 0, taking the same route the bridge already takes when the bounded fetch itself errors (D-04)."
    - "That fallback prints a WARNING on stderr naming the degraded reply as the cause, worded so an operator cannot confuse it with the fetch-failure warning at `scripts/agy_bridge.sh:494` — the degraded run's stderr carries the degraded-cause phrasing and neither of the two `_agy_why` phrasings assigned at `:489` and `:491` (D-05)."
    - "With no cache file to fall back to, a degraded reply still exits 2 carrying the shipped degraded-list message from `scripts/agy_bridge.sh:515-517`; R8's assertion is untouched and that message literal still exists in exactly one place in the file (D-04, D-02)."
    - "The D-04 fallback reads the cache through the existing `cat` at `scripts/agy_bridge.sh:504` and never rewrites it, so the file's mtime stays at its last good write and the existing `find -mmin +60` window expires on its original schedule — the stale window is closed, not widened (D-04)."
    - "edge:adjacency (explicit) — a reply row carrying a third tab-separated column and a reply row carrying a trailing tab with an empty second column both normalize through `cut -f1` to the same bare id a plain two-column row yields, so the anchored matchers at `scripts/agy_bridge.sh:524-525` see identical input in all three shapes (S1 criterion 4, D-06)."
    - "edge:ordering (explicit) — the auto-select rule `grep -E '^gemini-[0-9.]+-<class>$' | sort -V | tail -1` and the `grep -qxF` id validation at `:529` are byte-identical to what shipped; this phase changes which list reaches them, never how they pick, and R2/R3b/R3d/CC06 stay green as the proof."
    - "The `${HOME:-/nonexistent}` guard at `scripts/agy_bridge.sh:468` and the block-level `2>/dev/null || true` on the cache write are preserved exactly, and RB27's bare-`HOME` delegation still passes with empty stderr — criterion 1 is closed work this phase must not disturb and must not reopen (D-01)."
    - statement: "An `agy models` reply that exits 0 with zero bytes is degraded under the same rule as a `gemini-`-less reply, because `cut -f1` of an empty string yields no `^gemini-` line — so it is never written to the cache and a good stale cache is preferred over it."
      verification: backstop
  prohibitions:
    - "No one-sided fix: the write gate lands on both `scripts/agy_bridge.sh` and `scripts/gemini_shim.sh` within this phase. A bridge-only phase violates `delegate-agy-8ph`'s stated constraint."
    - "The 60-minute TTL (`find -mmin +60`) is not changed, widened, or narrowed as a substitute for the write gate."
    - "The anchored matchers `^gemini-[0-9.]+-flash-high$` and `^gemini-[0-9.]+-pro-high$` and the `grep -qxF` id validation are not loosened to accommodate any reply shape; the input is normalized instead."
    - "A degraded reply never deletes or truncates the existing cache file. Refusing to write is not licence to clear."
    - "The D-04 fallback does not refresh the cache file's mtime."
    - "The atomic tmp-then-`mv` write plus `chmod 600` at `scripts/agy_bridge.sh:481-484` is not replaced by a direct redirect while the gate is added around it."
    - "The degraded-list rule is not implemented twice: the write-time gate and the use-time check at `scripts/agy_bridge.sh:515` apply the same `cut -f1` then `^gemini-` test."
    - "No lockfile, `flock`, or cross-process coordination mechanism is introduced. S4's acceptance is that writes stay atomic, which the per-file `mv` rename already makes true."
    - "The synthetic multi-column rows D-06 needs are never added to `tests/fixtures/agy-models.tsv`, which D-14/D-14a reserve for captured evidence."
  assumptions:
    - "FLAGGED, UNRESOLVED — the S4 edge probe returned `unclassified`, so no automated edge predicate was derived for the two-independent-writers concurrency requirement. Everything this phase asserts is single-process and sequential: no test here runs the bridge and the shim concurrently against the same cache path. The concurrency property S4 names rests on the tmp-then-`mv` rename being atomic, which this phase preserves but does not test."
    - "FLAGGED, NOT FOLDED IN — criterion 3's `shown agy's own stderr` half is not satisfied on the degraded-but-successful path. `$_agy_err` is captured at `scripts/agy_bridge.sh:474` but surfaced only inside the fetch-FAILURE branch at `:498`, so a degraded rc-0 reply's stderr is discarded unread at `:500`. D-02 locks criterion 3 as already done and out of this phase's scope, so this plan does not change it. Raise as a follow-up bd issue rather than folding it in."
  artifacts:
    - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/agy_bridge.sh
    - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh
  key_links:
    - "The write-time gate around `scripts/agy_bridge.sh:481-484` and the use-time check at `:515` are one rule at two sites. If they ever disagree, the cache holds something the bridge then refuses to use, which is the 1.6.1 incident shape."
    - "`FAKE_AGY_MODELS_GARBAGE` (`tests/fake-agy.sh:164-168`) drives R9 and R9b. It is the only garbage-simulation knob and a second one would let the two halves drift."
    - "R9b's stale-cache seed reaches the model list through `_agy_models` being cleared and the existing `VALID_MODELS=$(cat \"$CACHE_FILE\")` fallback at `:504`. That shipped line is what makes D-04 work; nothing new is added there."
    - "`AGY_FIXTURES_DIR` (`tests/fake-agy.sh:113-145`, tier 1, pinned by CC04a) is what lets R9c drive a real multi-column `agy models` reply through the fetch path instead of short-circuiting at the cache."
---

<objective>
Close S4 on the bridge: one bad `agy models` reply can no longer be written to the cache the shim also reads, and a bridge that hits one now prefers a good stale list over failing.

This is the tracer half of the phase. It wires one path end to end — fake `agy models` reply, bounded fetch, degraded-list gate, cache file on disk, assertion in the suite — and proves the architecture before plan 02 mirrors it into `scripts/gemini_shim.sh`. The two scripts are deliberate mirrors (`scripts/gemini_shim.sh:380-385` states why they are not factored into a shared library), so whatever shape lands here is the shape that lands there.

Nothing here is speculative. `delegate-agy-8ph` names the bug, D-03 names the gate, D-04 names the fallback, and D-05 names the message. The gate reuses the degraded-list test already shipped at `scripts/agy_bridge.sh:515`; the fallback reuses the branch already shipped at `:493-497`. No new mechanism, no new state machine, no new dependency.

Purpose: stop the shared cache from being poisoned by a writer that had no business writing, and make a degraded reply survivable when a good list is already on disk.
Output: the gate and the fallback in `scripts/agy_bridge.sh`, plus R9, R9b and R9c in `tests/run-tests.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/dd/Gemini/delegate-agy/.planning/PROJECT.md
@/home/dd/Gemini/delegate-agy/.planning/ROADMAP.md
@/home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md
@/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-CONTEXT.md
@/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-PATTERNS.md

**Source of truth:** `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2` on branch `fix/agy-bridge-resilience`. Every line number in this plan is from that worktree. Never read or edit `master` — `PROJECT.md` and `STATE.md` both record that `master`'s files lag its history after the `a001d0e` content revert.
</context>

## Artifacts this phase produces

Full phase inventory, both plans. This plan delivers the bridge half and the shared inventory.

**No new files.** The phase modifies three existing files in place.

**New in `scripts/agy_bridge.sh` (this plan):**

- `_agy_ids` — a new shell variable holding the `cut -f1`-normalized fetched list, computed once inside the fetch-success branch and read only by the D-03 gate. Follows the `_agy_*` naming of `_agy_models`, `_agy_err`, `_agy_rc`, `_agy_why`.
- The D-03 gate — a new `if` around the existing write block at `:481-484`. Not a new function.
- The D-04 arm — a new `elif` on that same `if`, carrying the D-05 warning literal and clearing `_agy_models`.
- One new fixed string literal, used exactly once, inline like its sibling at `:494`:
  `WARNING: 'agy models' returned a list with no 'gemini-' ids (agy may be unauthenticated); using the stale cached list.`

**New in `scripts/gemini_shim.sh` (plan 02-02):** `ids` local in `load_models()`, the mirror gate, the mirror `elif`. No new literal — the shim stays silent on this path per D-05.

**New test case identifiers in `tests/run-tests.sh`:**

- This plan: `R9`, `R9b`, `R9c`.
- Plan 02-02: `SH15`, `SH15b`, `SH15c`.

**Modified in place, not new:** the trailing comment on R8 at `tests/run-tests.sh:501-503`, which currently states that the bridge caches the garbage reply. This phase reverses that behavior, so the comment becomes a stale claim and is corrected in task 1.

**Deliberately NOT produced,** so a reviewer does not read it as an omission:

- No shared helper across the two scripts. `scripts/gemini_shim.sh:380-385` records that the ~20 duplicated lines are deliberate, because the shim shadows `gemini` on PATH and a missing sourced library would break `gemini` box-wide.
- No static both-writers invariant test in the `RB01`/`RB02` mould. R9, R9b, SH15 and SH15b are behavioral and a one-sided regression turns two of them red, so a static scan on top would assert the same property a second time.
- No new knob in `tests/fake-agy.sh`. `FAKE_AGY_MODELS_GARBAGE` and `AGY_FIXTURES_DIR` already cover both new stimuli.
- No README troubleshooting row for the new warning. Its sibling at `:494` has none either, and D-05's convention is to quote a literal in that table only where a row for it already exists.

## Alternatives Considered

**A. How the degraded-list test is expressed at the write site.**

- **Normalize into a variable first, then a two-element `printf | grep -q` pipeline (chosen)**: mirrors `scripts/agy_bridge.sh:510` and `:515` exactly, so the write-time gate and the use-time check are one rule rather than two that can drift. It also keeps the pipeline two elements long under `set -o pipefail`, where a `grep -q` that closes the pipe on its first match can leave a middle `cut` dead of SIGPIPE and hand the whole pipeline status 141 — which the enclosing `if` would read as "degraded" for a perfectly good list. Citation: `scripts/agy_bridge.sh:8`, `:510`, `:515`, read on `fix/agy-bridge-resilience` (2026); GNU Bash Reference Manual §3.2.4 "Pipelines" on `pipefail` returning the rightmost non-zero status, bash 5.2 (2022) — <https://www.gnu.org/software/bash/manual/html_node/Pipelines.html>.
  Decided by: performance — the alternative's failure mode is a false degraded verdict on a good list, which is a worse outage than the bug being fixed.
- **One three-element pipeline, no temp variable**: a line shorter. Rejected for the `pipefail`/SIGPIPE hazard above. It is safe today only because a 14-row model list fits inside the 64 KiB default pipe buffer, so `cut` finishes writing before `grep -q` can close the read end — a correctness argument that depends on the size of agy's reply, which is exactly the thing S1 says this project does not control. Citation: `pipe(7)` on the 65536-byte default pipe capacity, Linux man-pages 6.9.1 (2024) — <https://man7.org/linux/man-pages/man7/pipe.7.html>; `tests/fixtures/agy-models.tsv`, 14 rows captured 2026-08-20.
  Decided by: performance — a latent, size-dependent false negative is not a saving.
- **Grepping the raw reply for `^gemini-` without normalizing**: shortest of all, and equivalent for every reply shape observed so far, since `^` anchors at line start and the id is column 1. Rejected because it states the degraded-list rule a second time in a second form: `:510` normalizes before `:515` greps, and two forms of one rule is precisely the drift S1 exists to prevent — the 1.6.1 incident was a matcher and an input format disagreeing. Citation: `.planning/REQUIREMENTS.md` §S1 origin (2026); `02-CONTEXT.md` §D-03, "reusing it, not inventing a second check" (2026).
  Decided by: maintenance — one rule that cannot drift beats one line saved.

**B. What happens to the degraded reply when there is no cache to fall back to.**

- **Leave `_agy_models` holding the degraded reply and let the shipped check at `:515-517` name it (chosen)**: the reply is still never written to disk, and the operator still gets the criterion-3 message that R8 pins, emitted from the one place that literal exists. Citation: `scripts/agy_bridge.sh:515-517` and `tests/run-tests.sh:497-501` (R8), read on the worktree (2026); `02-CONTEXT.md` §D-02 and §D-04, "with no stale cache to fall back to, behavior is unchanged" (2026).
  Decided by: LOC — zero new message literals and zero new branches for the no-cache path.
- **Clear `_agy_models` unconditionally on a degraded reply**: the most literal reading of "the degraded reply is discarded". Rejected on a mechanical ground, not a preference: with `VALID_MODELS` empty and no cache on disk, `:506` fires first and exits 2 with the generic retrieval-failure message *before* `:515` ever runs, so R8's degraded-list assertion goes red and criterion 3's distinct message is lost on the exact path it was written for. Citation: `scripts/agy_bridge.sh:502-517` (2026).
  Decided by: performance — an option that turns a shipped, passing assertion red is not viable.
- **Clear it unconditionally and emit the criterion-3 message from a second site**: restores R8 while keeping the literal discard. Rejected because it places one fixed literal in two places, which is what Phase 1's D-09/D-10 convention and RB03's "defined exactly once per script" count exist to prevent. Citation: `tests/run-tests.sh:2087-2110` (RB03's single-definition count); `.planning/phases/01-the-missing-timeout-decision/01-CONTEXT.md` §D-09/§D-10 (2026).
  Decided by: maintenance — a duplicated literal drifts on the first reword.

**C. How D-06's multi-column reply is produced.**

- **`AGY_FIXTURES_DIR` tier-1 override pointing at a scratch dir holding a synthetic `agy-models.tsv` (chosen)**: the knob already exists, already has a test proving tier 1 wins over the sibling tier (CC04a), drives a real `agy models` subprocess through the bridge's whole fetch → gate → normalize → match path rather than short-circuiting at the cache, and keeps the synthetic rows physically outside `tests/fixtures/agy-models.tsv`, which D-14/D-14a reserve for captured evidence. Citation: `tests/fake-agy.sh:113-145` (`_fake_fixture`'s three-tier resolution) and `:73-83` (the documented knob), `tests/run-tests.sh:4009-4026` (CC04a), read on the worktree (2026); `01.5-CONTEXT.md` §D-14/§D-14a (2026).
  Decided by: LOC — two lines of `mktemp -d` plus `printf` per case, against any option needing a code change.
- **A new `FAKE_AGY_MODELS_EXTRA` knob in `tests/fake-agy.sh`**: symmetrical with `FAKE_AGY_MODELS_GARBAGE`. Rejected as a second mechanism for something `AGY_FIXTURES_DIR` already does — `02-CONTEXT.md` says the existing knobs are reused rather than a second simulation mechanism added — and it would bury the synthetic row inside the fake, where a reader cannot tell it apart from captured output. Citation: `02-CONTEXT.md` §"Integration Points" (2026); `tests/fake-agy.sh:69-71` (2026).
  Decided by: LOC — zero new code against roughly six lines plus its documentation block.
- **Seeding the multi-column row straight into the cache file, R3c's shape**: cheapest of all, no subprocess at all. Rejected because it proves only the cache-read normalization at `:504` into `:510`, and R3c already proves exactly that. Criterion 4 is a statement about an `agy models` *reply*, so the fetch path is the one that has to be exercised. Citation: `tests/run-tests.sh:437-449` (R3c) (2026).
  Decided by: simplicity — among the options that can answer criterion 4, seeding the cache is not one.

## Tasks

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A degraded agy reply never reaches the cache file (D-03, tracer)</name>

  <precondition>The working tree is `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2` on branch `fix/agy-bridge-resilience`, and `bash tests/run-tests.sh` from that worktree currently exits 0 with `FAIL=0`. A suite that is already red makes the RED step of this task unattributable — halt and report rather than proceeding.</precondition>

  <reversibility rating="reversible">D-03 rates itself reversible: the gate is a single condition around an existing write, and removing it later is a local diff.</reversibility>

  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/agy_bridge.sh, /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>

  <read_first>
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/agy_bridge.sh` lines 460-520 in full — the `${HOME:-/nonexistent}` cache path at `:468`, the TTL check at `:470`, the `_agy_err` mktemp at `:474`, the bounded fetch at `:475-476`, the why-comment at `:477-480`, the unconditional write at `:481-484`, the fetch-failure branch at `:485-499`, the `VALID_MODELS` assembly at `:502-506`, the `cut -f1` normalization and its comment at `:507-510`, and the degraded-list check at `:512-517`.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 473-505 — R6, R7 and R8 in full, including R8's trailing comment and cleanup at `:501-504`.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 85-113 — `ok`, `bad` and `_run`.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/fake-agy.sh` lines 148-172 — the `models` subcommand's hang, fail and garbage branches.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-CONTEXT.md` §D-02, §D-03, §D-04 and §"Existing Code Insights".
    - `/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-PATTERNS.md` §"`scripts/agy_bridge.sh` fetch/cache block" and §"Shared Patterns".
  </read_first>

  <behavior>
    Written before the implementation, as the outcome matrix R9 encodes:
    - Garbage reply, no cache file on disk: nothing is created at `$HOME/.cache/agy-bridge-models`. The bridge still exits 2 and still prints the shipped degraded-list message, because `_agy_models` is untouched on this path and reaches the check at `:515`. This is R8's outcome with one extra assertion about the filesystem.
    - Garbage reply, a good cache file on disk: the file's bytes are unchanged after the call. This half is what a poisoned cache would break, and no existing case looks at the file at all.
    - Good reply: the write happens exactly as before — same `mkdir -p`, same tmp-then-`mv`, same `chmod 600`. R1, R2, R3b and R3c are the proof that this path did not move.
    - Failed fetch: untouched. The gate lives inside the success branch and cannot be reached when `run_bounded` returns non-zero. R5, R6 and R7 are the proof.
    - RED is expected on the second bullet only. Before the production edit, the garbage reply overwrites a good cache, so R9's cache-contents assertion fails while its exit-code assertions already pass.
  </behavior>

  <action>
Two edits, test first.

**RED — add R9 to `tests/run-tests.sh`, immediately after R8's cleanup at `:504`.** Use `_run`, `ok` and `bad` exactly as R6, R7 and R8 do; add no helper and no new harness.

R9 has two halves in one case, sharing one `ok`/`bad` pair and an accumulating detail string, the shape RB03 uses at `:2101-2144`.

First half — assert on absence. Remove `$HOME/.cache/agy-bridge-models`, run `bash "$BRIDGE" --type code -- "..."` under `FAKE_AGY_MODELS_GARBAGE=1` through `_run`, then assert three things: the captured rc is 2, the captured output still carries the shipped degraded-list phrase `no 'gemini-' ids`, and the cache path is still not a non-empty file. That third assertion is the new one; the first two exist so a regression in the no-cache path is caught here rather than only in R8.

Second half — assert on preservation. Seed the cache with one good two-column row using `printf '%s\t%s\n'` with `gemini-3.1-pro-high` and its display name, the way R6 does at `:475-477`, and age it with `touch -d '2 hours ago'` so the TTL check at `:470` forces a live fetch. Capture the file's exact contents into a variable before the call. Run the same garbage-reply invocation. Assert the file's contents after the call are byte-identical to the captured contents. Clean the cache up afterwards with `rm -f`, as R3, R6 and R8 all do, because `$HOME` is shared across the whole run.

Run the suite and record that R9 reports `FAIL` on the preservation half before any production code changes. A RED that was never observed is not a RED.

**GREEN — gate the write in `scripts/agy_bridge.sh`.**

Inside the fetch-success branch that opens at `:475`, before the existing `mkdir -p` at `:481`, assign a new variable `_agy_ids` from `$_agy_models` piped through `cut -f1` — the same normalization the file already performs at `:510`. Then wrap the three existing write statements at `:481-484` in an `if` whose condition pipes `$_agy_ids` through `grep -q '^gemini-'` — the same test the file already performs at `:515`. Indent those three statements one level; change nothing else about them. The `mkdir -p`, the braced tmp-then-`mv` with its block-level `2>/dev/null || true`, and the `chmod 600` all stay exactly as they are.

Keep the normalization in its own variable rather than folding `cut` into the gate's pipeline. Under this file's `set -o pipefail` at `:8`, a `grep -q` that closes the pipe on its first match can leave a middle `cut` killed by SIGPIPE, and the pipeline then reports 141 — which the `if` reads as a degraded list for a perfectly good one. The two-element form is the shape already proven at `:515`.

Leave `_agy_models` alone on this path. When the gate fails and no cache exists, the untouched degraded reply is what reaches `:515` and produces the message R8 pins; clearing it here would route the no-cache case into the generic retrieval-failure exit at `:506` instead. The stale-cache arm is task 2's work, not this one's.

Add a why-comment immediately above the gate, matching the convention every non-obvious guard in this file follows at `:477-480`, `:507-509` and `:512-514`. State why the check moved to write time: the fetched list is shared with `scripts/gemini_shim.sh` through one cache file with a 60-minute window, so a reply this bridge is about to reject is a reply the shim would read as authoritative for up to an hour. Do not restate the condition in prose; the code says what, the comment says why.

**Correct the stale comment on R8.** The trailing comment at `tests/run-tests.sh:501-503` states that the garbage fetch exits 0 so the bridge caches it, and that the cache is cleared for that reason. After this task that claim is false. Rewrite it to state that R9 now pins the cache as unwritten on this path, and that the `rm -f` remains as a cheap reset of the shared `$HOME` rather than as a poison cleanup. Keep the `rm -f` itself.
  </action>

  <verify>
    <automated>cd /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2 && bash tests/run-tests.sh > /tmp/p2-01-t1.log 2>&1; grep -q "^ok   - R9 " /tmp/p2-01-t1.log && grep -q "^ok   - R8 " /tmp/p2-01-t1.log && grep -q "^ok   - R6 " /tmp/p2-01-t1.log && grep -q "^ok   - R7 " /tmp/p2-01-t1.log && tail -1 /tmp/p2-01-t1.log | grep -q "FAIL=0"</automated>
  </verify>

  <acceptance_criteria>
    - The suite exits 0 with `FAIL=0` on the final line, and R9 reports `ok`.
    - R6, R7, R8, R1, R2, R3b, R3c, R3d and RB27 all still report `ok` — the fetch-failure branch, the no-cache fatal, the degraded-list message, the delegation paths and the bare-`HOME` regression are untouched. Per D-01, criterion 1 is closed work: if RB27 goes red here that is a regression in this task's edit to be investigated and fixed, never a reason to open new scope against `delegate-agy-30m`.
    - The RED observation is recorded in the SUMMARY: R9 reported `FAIL` on its cache-preservation half before the production edit, with the failure detail quoted.
    - `grep -c "mv \"\$CACHE_FILE.tmp.\$\$\" \"\$CACHE_FILE\"" scripts/agy_bridge.sh` returns 1 — the atomic write still exists exactly once and was wrapped, not rewritten.
    - `grep -v "^[[:space:]]*#" scripts/agy_bridge.sh | grep -cF "no 'gemini-' ids"` returns 1 — the degraded-list literal is still defined in exactly one non-comment place.
    - `grep -cF "mmin +60" scripts/agy_bridge.sh` returns 1 and its value is unchanged — the TTL was not touched.
    - `scripts/agy_bridge.sh` contains no `rm` or truncating redirect aimed at `$CACHE_FILE` outside the existing `mv` — a degraded reply does not clear the cache.
    - `bash -n scripts/agy_bridge.sh` exits 0.
  </acceptance_criteria>

  <done>A garbage `agy models` reply leaves `~/.cache/agy-bridge-models` exactly as it found it — absent if absent, byte-identical if present — while every shipped behavior around it, including the exit-2 degraded-list message on the no-cache path, is unchanged. R9 pins both halves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A degraded reply falls back to a good stale cache, and says so distinctly (D-04, D-05)</name>

  <reversibility rating="reversible">D-04 rates itself reversible: this is a branch condition, not a new state machine, and reverting to immediate-fail is a local diff. D-05 is the agent's discretion under an existing convention.</reversibility>

  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/agy_bridge.sh, /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>

  <read_first>
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/agy_bridge.sh` lines 485-506 — the fetch-failure branch in full: the `_agy_rc` capture at `:486`, the `_agy_models=""` at `:487`, the `_agy_why` assignments at `:488-492`, the stale-versus-no-cache split at `:493-497`, the agy-stderr relay at `:498`, and the `VALID_MODELS` fallback read at `:502-504` that this task's new arm depends on.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 473-486 — R6 in full, the exact stale-cache-plus-warning shape this task's R9b mirrors.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-CONTEXT.md` §D-04 and §D-05 in full, including D-05's constraint that the wording be distinct from the existing fetch-failure warning and that the shim stay silent.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01-the-missing-timeout-decision/01-CONTEXT.md` §D-09 and §D-10 — the fixed-literal convention D-05 defers to.
    - This plan's own §"Alternatives Considered" B, which records why the degraded reply is cleared only when a cache exists.
  </read_first>

  <behavior>
    - Garbage reply, a good stale cache on disk, `--verbose`: the bridge exits 0 and its verbose output names the model resolved from the cached row. Today it exits 2 without ever looking at the cache.
    - That same run prints a WARNING on stderr containing `returned a list with no 'gemini-' ids`. It does not contain either phrasing `_agy_why` can hold at `:489` and `:491`, so an operator reading stderr can tell a call that failed from a call that succeeded and answered with garbage.
    - The cache file survives the call byte-identical, and it survives it still older than the 60-minute window — the fallback reads, it does not rewrite, so the next call past the window still refetches on schedule.
    - Garbage reply, no cache: unchanged from task 1. Exit 2 with the shipped degraded-list message. R8 and R9's first half are the proof.
    - Failed fetch, stale cache: unchanged. R6 is the proof, and its warning text must not have moved.
    - RED is expected on the whole of R9b: before the production edit, the run exits 2 and no such warning exists anywhere in the file.
  </behavior>

  <action>
Test first again.

**RED — add R9b to `tests/run-tests.sh`, immediately after R9.** Build it as R6 at `:473-486` is built, changing exactly one thing: drive the fetch with `FAKE_AGY_MODELS_GARBAGE=1` instead of `FAKE_AGY_MODELS_FAIL=1`, so agy succeeds and answers with a list carrying no gemini ids.

Seed the cache with the same single good two-column `gemini-3.1-pro-high` row R6 uses, age it with `touch -d '2 hours ago'`, and capture the file contents into a variable before the call. Invoke through `_run` with `FAKE_AGY_STDOUT="ok"` and `--type code --verbose`, so the delegation succeeds and the verbose line carrying the resolved model is emitted.

Assert six things through one accumulating detail string:
  - the captured rc is 0;
  - the captured output contains `model=gemini-3.1-pro-high`, which can only have come from the cached row;
  - the captured output contains `returned a list with no 'gemini-' ids`;
  - the captured output does NOT contain the substring `timed out after`;
  - the captured output does NOT contain the substring `exited `;
  - the cache file after the call is byte-identical to the captured contents, and `find` with `-mmin +60` still matches it, proving the fallback did not refresh its mtime.

The two absence assertions are what make the warning genuinely distinct rather than merely new. `_run` merges stdout and stderr, which is what R6 relies on and is sufficient here; the warning's stream is already fixed by the file's convention.

Clean the cache up with `rm -f` afterwards.

Run the suite and record that R9b reports `FAIL` before the production edit.

**GREEN — add the stale-cache arm in `scripts/agy_bridge.sh`.**

Extend the `if` task 1 built around the write block with an `elif` testing `[[ -s "$CACHE_FILE" ]]` — the same test the fetch-failure branch already uses at `:493`. Inside that arm, do exactly two things, in this order: echo the warning to stderr, then set `_agy_models` to the empty string.

The warning is this exact literal, written inline at its single use site the way its sibling at `:494` is, not hoisted into a constant:

`WARNING: 'agy models' returned a list with no 'gemini-' ids (agy may be unauthenticated); using the stale cached list.`

It shares its prefix and its trailing clause with the sibling at `:494` on purpose — same event class, same consequence — and differs in the middle clause, which names the cause. That middle clause is the substring R9b greps for, and it does not occur in the degraded-list message at `:516` either, whose wording is `contains no 'gemini-' ids`.

Clearing `_agy_models` is the whole of D-04's mechanism. The already-shipped lines at `:502-504` then find `VALID_MODELS` empty and read the cache file, and everything downstream — normalization at `:510`, the degraded-list check at `:515`, auto-select at `:524-525` — runs against the cached list exactly as it does on a fetch-failure fallback. Add no branch below `:499`, no new variable, and no second read of the cache file.

Do not add an `else` arm. The remaining case — degraded reply, no cache — is deliberately the untouched one, for the reason recorded in §"Alternatives Considered" B, and adding an arm there is what turns R8 red.

Do not touch `:498`'s agy-stderr relay or move it out of the failure branch. That the degraded path does not surface `$_agy_err` is a known, recorded gap listed under `must_haves.assumptions`; D-02 scopes criterion 3 out of this phase, so it is raised as a follow-up, not fixed here.

Add a why-comment above the `elif`, in the file's existing style, stating why a successful-but-degraded fetch is treated as a failed one for this call: a list with no gemini ids cannot resolve a model, so a good list already on disk is strictly better than exiting, and the reply that caused it is the one thing that must not be persisted.
  </action>

  <verify>
    <automated>cd /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2 && bash tests/run-tests.sh > /tmp/p2-01-t2.log 2>&1; grep -q "^ok   - R9b " /tmp/p2-01-t2.log && grep -q "^ok   - R9 " /tmp/p2-01-t2.log && grep -q "^ok   - R6 " /tmp/p2-01-t2.log && grep -q "^ok   - R8 " /tmp/p2-01-t2.log && tail -1 /tmp/p2-01-t2.log | grep -q "FAIL=0"</automated>
  </verify>

  <acceptance_criteria>
    - The suite exits 0 with `FAIL=0`, and R9b reports `ok`.
    - R6, R7, R8, R9, R3, R3b, R3c and R3d all still report `ok`.
    - The RED observation for R9b is recorded in the SUMMARY with its failure detail quoted.
    - `grep -v "^[[:space:]]*#" scripts/agy_bridge.sh | grep -cF "returned a list with no 'gemini-' ids"` returns 1 — one definition, one use site.
    - `grep -v "^[[:space:]]*#" scripts/agy_bridge.sh | grep -cF "; using the stale cached list."` returns 2 — the new warning and its untouched sibling at `:494`, and no third.
    - `grep -cF "mmin +60" scripts/agy_bridge.sh` still returns 1 with its value unchanged.
    - `bash -n scripts/agy_bridge.sh` exits 0.
    - The diff against the task-1 tree touches only the fetch/cache block; no line at or below `:502` in the original numbering is modified.
  </acceptance_criteria>

  <done>A bridge that gets a gemini-less reply while a usable list sits on disk completes the delegation off that list, and prints a warning an operator can tell apart from a fetch failure at a glance. With no list on disk it fails exactly as it always did.</done>
</task>

<task type="auto">
  <name>Task 3: Prove `cut -f1` normalizes an extra column and a trailing tab, on the bridge (D-06)</name>

  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>

  <read_first>
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/fake-agy.sh` lines 96-146 — `_fake_fixture` in full: the three-tier resolution, the `grep -v '^#'` header strip, and the loud non-zero on a zero-row fixture.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 4009-4026 — CC04a, the existing pattern for building a scratch fixture dir and pointing `AGY_FIXTURES_DIR` at it with a distinguishable synthetic row.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 401-455 — R2, R3b, R3c and R3d, the existing model-resolution assertions this case sits beside.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/scripts/agy_bridge.sh` lines 507-531 — the `cut -f1` normalization, the degraded-list check, and the two anchored auto-select matchers this case exercises without changing.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/02-model-list-handling-end-to-end/02-CONTEXT.md` §D-06 in full, including the prohibition on adding synthetic rows to the captured fixture.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/fixtures/agy-models.tsv` — the real two-column shape the synthetic rows extend. Read it; do not edit it.
  </read_first>

  <action>
Add R9c to `tests/run-tests.sh` immediately after R9b. Test-only: no production file changes in this task, and none are needed — `cut -f1` is already column-count-agnostic and D-06 exists to prove that rather than to change it.

Build one scratch fixture directory per case with `mktemp -d` under `$SANDBOX`, and write a synthetic `agy-models.tsv` into it with `printf`. Three lines: a first line beginning with `#` marking the file as synthetic and not captured evidence, which `_fake_fixture` strips; a three-column row built with a `%s\t%s\t%s\n` format, id `gemini-9.4-flash-high`, a display name, and a third column value; and a trailing-tab row built with a `%s\t\n` format, id `gemini-9.3-pro-high`, leaving column two empty.

Pick ids that appear nowhere else — not in `tests/fixtures/agy-models.tsv`, not in R6's or SH10's seeds. A resolved `gemini-9.4-flash-high` can then only have come from the three-column row, which is what makes the case capable of failing. Keep the two ids in different classes so the version sort in one class cannot mask the other.

Run the bridge twice through `_run`, each time with `AGY_FIXTURES_DIR` set to the scratch dir and `FAKE_AGY_STDOUT="ok"`, and each time with the cache removed first so the TTL check at `scripts/agy_bridge.sh:470` forces a live fetch rather than a cache read. First run `--type search --verbose`, which auto-selects the flash-high class, and assert rc 0 and `model=gemini-9.4-flash-high` in the output. Then run `--type code --verbose`, which auto-selects the pro-high class, and assert rc 0 and `model=gemini-9.3-pro-high`.

Report both through one `ok`/`bad` pair with an accumulating detail string naming which shape failed.

Remove the cache file afterwards with `rm -f`, and remove the scratch dir with `rm -rf`. This matters more here than in R9 or R9b: the synthetic rows are well-formed gemini ids, so the write gate passes and they really are cached. Leaving them behind would hand every later case a model list containing versions that do not exist.

Do not add these rows to `tests/fixtures/agy-models.tsv`. That file is the captured record of what real agy 1.1.13 emitted on 2026-08-20, and D-14/D-14a keep captured evidence and synthetic payloads in separate files precisely so nobody later mistakes one for the other. The `#` header line on the scratch fixture is the second guard on the same point.
  </action>

  <verify>
    <automated>cd /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2 && bash tests/run-tests.sh > /tmp/p2-01-t3.log 2>&1; grep -q "^ok   - R9c " /tmp/p2-01-t3.log && grep -q "^ok   - R2 " /tmp/p2-01-t3.log && grep -q "^ok   - CC04b " /tmp/p2-01-t3.log && tail -1 /tmp/p2-01-t3.log | grep -q "FAIL=0"</automated>
  </verify>

  <acceptance_criteria>
    - The suite exits 0 with `FAIL=0`, and R9c reports `ok`.
    - R9c is proven capable of failing: temporarily change the three-column row's id in the scratch fixture to something the search matcher cannot match, observe R9c report `FAIL`, revert. Record the observation in the SUMMARY.
    - `git diff --stat` for this task shows `tests/run-tests.sh` as the only changed file.
    - `git diff -- tests/fixtures/agy-models.tsv` is empty — the captured fixture was not touched.
    - `git diff` on the two auto-select matcher lines in `scripts/agy_bridge.sh` is empty; they are byte-identical to their pre-phase state. The input was normalized, the matchers were not loosened.
    - After a full suite run, every id in `$HOME/.cache/agy-bridge-models` also appears in `tests/fixtures/agy-models.tsv` — R9c's synthetic list did not leak past its own cleanup.
  </acceptance_criteria>

  <done>An `agy models` reply carrying a third column, and one carrying a trailing tab with an empty display name, each resolve a model through the bridge's real fetch path, with the anchored matchers untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `agy` subprocess stdout → bridge | Untrusted, unvalidated third-party CLI output crosses here at `scripts/agy_bridge.sh:475-476`. Format, content and size are all outside this project's control — S1 exists because it changed once already. |
| bridge → `~/.cache/agy-bridge-models` | The bridge persists that output to a file a *second, independent* process (`scripts/gemini_shim.sh`) reads as authoritative for up to 60 minutes. This is the boundary `delegate-agy-8ph` is filed against. |
| cache file → `--model` argv | Cached content becomes a command-line argument to a subprocess at `scripts/agy_bridge.sh:673`, via auto-select at `:524-525` or validation at `:529`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `scripts/agy_bridge.sh:481-484`, the cache write | medium | mitigate | D-03's gate (task 1): normalize with `cut -f1` and require a `^gemini-` line before the tmp-then-`mv` write, so a degraded or unauthenticated reply cannot be persisted where the shim reads it. Local availability impact only — a poisoned id is still anchored-matched at `:524-525` and exact-matched at `:529`, so it cannot become an arbitrary argv value. |
| T-02-02 | Tampering | `~/.cache/agy-bridge-models` as input to `--model` | medium | mitigate | Existing controls retained unchanged and asserted in task 1's acceptance criteria: `chmod 600` at `:484`, atomic tmp-then-`mv` at `:482-483`, the `${HOME:-/nonexistent}` path guard at `:468`, `cut -f1` normalization at `:510`, and `grep -qxF` exact-id validation at `:529`. The gate wraps this block; it does not rewrite it. |
| T-02-03 | Denial of service | D-04's stale-cache fallback (task 2) | low | mitigate | The fallback reads through the shipped `cat` at `:504` and never writes, so the mtime stays at the last good write and the `find -mmin +60` window expires on its original schedule — the stale window is closed rather than widened. R9b asserts the mtime is still past the window after the call. A cache poisoned by an older build is still caught by the use-time check at `:515`. |
| T-02-04 | Information disclosure | agy stderr relayed at `:498` | low | accept | Unchanged by this phase and required by criterion 3 as the only auth or network diagnostic available. Prefixed and written to stderr only, never stdout, never the JSON envelope. |
| T-02-05 | Tampering | two independent writers, one cache path | low | accept | S4's acceptance is that writes stay atomic, which the per-file `mv` rename already satisfies. `delegate-agy-8ph` explicitly forbids substituting a TTL change, and adding a lock is a new coordination mechanism this requirement did not ask for. Recorded as an untested property under `must_haves.assumptions`. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages and adds no dependency. The changed surface is two bash scripts and one test file, so the package-legitimacy gate has no rows to audit and no `[ASSUMED]`/`[SUS]` checkpoint is required. |
</threat_model>

<verification>
From `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2`:

1. `bash tests/run-tests.sh` exits 0 with `FAIL=0` on its final line.
2. R9, R9b and R9c all report `ok`.
3. R1, R2, R3, R3b, R3c, R3d, R5, R6, R7, R8, RB01, RB02, RB03, RB27, CC04a, CC04b and CC06 all still report `ok` — nothing shipped moved.
4. `bash -n scripts/agy_bridge.sh` exits 0.
5. `git diff --stat` names exactly two files: `scripts/agy_bridge.sh` and `tests/run-tests.sh`.
6. `git diff -- tests/fixtures/ tests/fake-agy.sh scripts/gemini_shim.sh` is empty — no fixture was edited, no new fake knob was added, and the shim is plan 02-02's surface.
7. Each RED observation (R9's preservation half, R9b, and R9c's mutation probe) is quoted in the SUMMARY with the failure detail the suite printed.
</verification>

<success_criteria>
- `scripts/agy_bridge.sh` refuses to cache an `agy models` reply whose normalized id column carries no `^gemini-` line, and R9 pins both the absent-cache and the preserved-cache halves.
- A degraded reply with a usable stale cache present completes at rc 0 off that cache, with a warning distinct from the fetch-failure warning, and without refreshing the cache's mtime. R9b pins all of it.
- A degraded reply with no cache present is unchanged: exit 2 with the shipped degraded-list message, R8 green.
- An extra column and a trailing tab both normalize through the real fetch path, with the anchored matchers byte-identical. R9c pins it.
- The suite is green end to end, and the diff touches only the two files this plan owns.
</success_criteria>

<output>
Create `.planning/phases/02-model-list-handling-end-to-end/02-01-SUMMARY.md` when done.

Record in it: the three RED observations with their quoted failure details; the exact warning literal as it landed; the decision recorded in §"Alternatives Considered" B about why the no-cache path leaves `_agy_models` untouched, since a future reader will otherwise read it as an inconsistency; and the two flagged assumptions from `must_haves.assumptions` verbatim, so the criterion-3 stderr gap and the untested concurrency property survive into the phase verification rather than being lost here.
</output>
