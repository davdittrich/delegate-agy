---
phase: 05-the-shim-s-failure-mode-contract
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
  - tests/run-tests.sh
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [S3]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README's hung-agy row names both `agy-bridge` and the `gemini` shim and gives a one-line reason for the JSON-envelope divergence, taken from `scripts/gemini_shim.sh:686-688` versus `scripts/agy_bridge.sh:748-757`."
    - "README's unparseable-model-list row names both entry points and gives a one-line reason for the bridge-exits-2 / shim-degrades-quietly divergence, per CONTEXT D-03."
    - "README's model-name row states that the `gemini` shim passes an unrecognized name through to agy unchanged with a stderr warning while `agy-bridge` refuses it with exit 2, and gives the one-line reason — the shim shadows the system `gemini` for every PATH caller."
    - "No row of the four-mode contract states a divergence without a reason, and no row states sameness where the shipped scripts diverge."
    - "FM01 fails when any of the five contract rows loses an entry-point name, loses its disposition clause, loses its quoted literal, or is duplicated."
    - "The shim's model-passthrough warning literal is byte-identical between `scripts/gemini_shim.sh` and README's row; changing one without the other fails FM01."
    - "edge:S3/adjacency — the hung-agy (exit 124) and external-kill (exit 137) rows stay two separate rows with distinct causes; the fold never merges them into one timeout row, because the 137 row's advice is that raising the bound will not help."
    - "REQUIREMENTS.md's S3 traceability row reports met, naming the four contract rows, FM01, and the per-row proof IDs."
    - "`bash tests/run-tests.sh` exits 0 with FAIL=0, and no file under `scripts/` is modified by this phase."
  artifacts:
    - "README.md — Troubleshooting rows for hung agy, unparseable model list, and unrecognized model name, rewritten in place"
    - "tests/run-tests.sh — FM01 extended with three further anchors and one provenance pin against scripts/gemini_shim.sh"
    - ".planning/REQUIREMENTS.md — S3 traceability row closed"
  key_links:
    - "README's hung-agy row <-> the shim's timeout arm having no JSON branch (`scripts/gemini_shim.sh:686-688`) while the bridge's does (`scripts/agy_bridge.sh:748-757`)"
    - "README's model-name row <-> `scripts/gemini_shim.sh:485-493` (pass through with warning) versus `scripts/agy_bridge.sh:560-562` (refuse with exit 2)"
    - "README's unparseable-list row <-> `scripts/agy_bridge.sh:546-550` (exit 2) versus the shim having no matching check (already pinned by EC06's zero-in-shim count)"
  prohibitions:
    - "MUST NOT state a behavior in the contract table that the shipped scripts do not actually produce — no aspirational wording, and no smoothing a genuine shim/bridge divergence into claimed parity. [flagged: unverified]"
    - "MUST NOT remove or weaken README's existing Security-section claims (pinned exec target, comparison-only registry read) as a side effect of this phase's folding. [flagged: unverified]"
---

<objective>
State, for each remaining failure mode, what the `gemini` shim does to a caller that has never heard of agy, what `agy-bridge` does instead, and why they differ — then close S3.

Purpose: ROADMAP criteria 1, 3 and 4 for the three rows where the two entry points genuinely diverge, per CONTEXT D-01 and D-03. Plan 05-01 established the table shape and the FM01 gate on the two rows where they agree.
Output: three rewritten README rows, FM01 extended to all five contract rows, S3 closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-the-shim-s-failure-mode-contract/05-CONTEXT.md
@.planning/phases/05-the-shim-s-failure-mode-contract/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hung agy — say why the two entry points' timeout output differs</name>

  <files>tests/run-tests.sh, README.md</files>

  <read_first>
    - scripts/gemini_shim.sh:680-720 — the shim's post-call arms. The timeout arm at :686-688 writes one line to stderr and exits 124 for every output mode; the empty-output arm at :709-716 is where the shim's only JSON error envelope lives. Read both before writing the reason, so the reason is what the code does rather than a guess.
    - scripts/agy_bridge.sh:735-757 — the bridge's external-kill arm and its timeout arm. Line 748-757 is the JSON-versus-text fork the shim has no counterpart for.
    - README.md:231 — the current exit-124 row. It already names the divergence; what it does not carry is the reason.
    - README.md:232 — the exit-137 row. Leave it as its own row: its advice (raising the bound will not help) is only true for an external kill, so merging it into the timeout row would mislead.
    - tests/run-tests.sh — the FM01 block from plan 05-01, and EC06's block at :2584-2685 which already pins the exit-124 text literal in both scripts and README and pins the JSON literal to the bridge with a zero count in the shim. Do not duplicate EC06's literal pins in FM01.
  </read_first>

  <behavior>
    - FM01 fails when README carries zero, or more than one, line containing the exit-124 text literal.
    - FM01 fails when that row's line does not contain `agy-bridge`, or does not contain `gemini`, or contains neither `because` nor `identical`.
    - FM01 still passes for the two rows plan 05-01 pinned.
  </behavior>

  <action>
    Extend FM01 with a third anchor, `_FM_ANCHOR_HANG='ERROR: agy timeout after '`, written verbatim in the test as the others are. Apply the same exactly-one-line count and the same three row checks. Do not re-assert the literal against the two scripts — EC06 owns that pin.

    Run the suite and record FM01's failing detail. Then rewrite README's exit-124 row in place at `README.md:231`, keeping every literal it already quotes byte-identical, and add the missing one-line reason for the divergence: the bridge forks on `--json` at its timeout arm and wraps the timeout in its error envelope, while the shim's timeout arm sits ahead of any envelope and reports on stderr with exit 124 for every output mode, so a JSON consumer of the shim gets no envelope for a timed-out call at all. Say it in one line in the row's existing prose voice; do not add a column and do not add a second table.

    Leave the exit-137 row at `README.md:232` alone. It states sameness already and its cause is distinct from a timeout.
  </action>

  <reversibility rating="reversible">One README row and three test assertions.</reversibility>

  <verify>
    <automated>bash tests/run-tests.sh 2>&1 | tail -3</automated>
    <automated>bash tests/run-tests.sh 2>&1 | grep -E '^(ok|FAIL) +- (FM01|EC05|EC06)'</automated>
    <automated>grep -cF 'ERROR: agy timeout after ' README.md</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` exits 0 and its final line reads `FAIL=0`.
    - FM01, EC05 and EC06 all report `ok`.
    - `grep -cF 'ERROR: agy timeout after ' README.md` returns `1`.
    - The README line matching that literal contains both `agy-bridge` and `gemini`, and contains `because` or `identical`.
    - README still carries a separate exit-137 row: `grep -cF 'possible OOM or external kill' README.md` returns `1`.
    - `git diff --name-only -- scripts/ | wc -l` returns `0`.
    - The FM01 detail string observed before the README edit is recorded in the SUMMARY.
  </acceptance_criteria>

  <done>The hung-agy row names both entry points and states why their timeout output differs, sourced from the two arms in the shipped scripts; the exit-137 row remains distinct; FM01 gates all three rows so far.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The two model-list divergences — bridge refuses, shim keeps the caller running</name>

  <files>tests/run-tests.sh, README.md</files>

  <read_first>
    - scripts/agy_bridge.sh:546-563 — two separate refusals in this window: a model list with no `gemini-` ids exits 2 at :546-550, and an unknown `--model` exits 2 at :560-562. Both are the bridge's side of a divergence this task documents.
    - scripts/gemini_shim.sh:480-495 — the shim's counterpart. It passes an unresolvable name through unchanged and emits the warning at :493. Read the comment at :485-492 — the reason this task states is argued there.
    - README.md:229-230 — the model-name row and the degraded-model-list row as they stand.
    - README.md:336-350 — the model-mapping table's pass-through row and the paragraph below it. The fact is already stated here in prose; this task makes it a contract row in the Troubleshooting table, which means the two statements must not contradict each other.
    - tests/run-tests.sh:1307-1333 — SH9, which pins that an unrecognized name still reaches agy unchanged. This is the row's cited proof; add no new provocation.
    - tests/run-tests.sh:1424-1484 — SH14 and SH15, the shim's degraded-list behavior. Cited proof for the other row.
    - tests/run-tests.sh — the FM01 block as extended by Task 1.
  </read_first>

  <behavior>
    - FM01 fails when README carries zero, or more than one, line containing the degraded-model-list literal.
    - FM01 fails when README carries zero, or more than one, line containing the shim's model-passthrough warning literal.
    - FM01 fails when the shim's passthrough warning literal is absent from `scripts/gemini_shim.sh`.
    - FM01 fails when either of those two rows does not name both entry points, or carries neither `because` nor `identical`.
  </behavior>

  <action>
    Extend FM01 with two further anchors, written verbatim in the test: `_FM_ANCHOR_LIST="agy model list contains no 'gemini-' ids; agy may be unauthenticated"` and `_FM_ANCHOR_PASSTHRU='did not resolve against the agy model list; passing it through unchanged'`. Apply the exactly-one-line count and the three row checks to each. For the passthrough anchor only, also assert it appears in `$ROOT/scripts/gemini_shim.sh` — that is the second provenance pin this phase adds, and nothing in the suite ties that warning to README today. EC06 already pins the degraded-list literal to the bridge and to README, so do not restate it.

    Run the suite and record FM01's failing detail. Then make two in-place row edits.

    At `README.md:230`, keep the row's quoted literal byte-identical and add the missing one-line reason for the divergence, per CONTEXT D-03: the bridge is invoked deliberately by a caller that knows it, so refusing loudly with exit 2 is the useful answer, while the shim is installed as `gemini` on PATH and a list-fetch problem there would break callers that never asked for agy — so it degrades quietly and lets the name through.

    At `README.md:229`, re-key the row so it states the contract rather than only agy's own error. Keep the existing quoted `Error: invalid model selection` text byte-identical. Add: the `gemini` shim never rejects a name it merely does not recognize — it passes the name through to agy unchanged and warns on stderr with the passthrough literal above, which is why the error you see is agy's own; `agy-bridge` differs, refusing an unknown `--model` itself with exit 2 before agy is called. State the same one-line reason as the row above: the shim shadows the system `gemini` for every PATH caller. Make sure this row and the prose at `README.md:336-350` agree.

    Do not reorder rows, do not add a table, do not touch anything under `scripts/`.
  </action>

  <reversibility rating="reversible">Two README rows and four test assertions; no shipped behavior changes.</reversibility>

  <verify>
    <automated>bash tests/run-tests.sh 2>&1 | tail -3</automated>
    <automated>bash tests/run-tests.sh 2>&1 | grep -E '^(ok|FAIL) +- (FM01|SH9|SH14|SH15|EC06)'</automated>
    <automated>grep -cF 'did not resolve against the agy model list; passing it through unchanged' README.md scripts/gemini_shim.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` exits 0 and its final line reads `FAIL=0`.
    - FM01, SH9, SH14, SH15 and EC06 all report `ok`.
    - The third command reports `1` for README.md and at least `1` for scripts/gemini_shim.sh.
    - Each of the five contract rows' matched README lines contains both `agy-bridge` and `gemini`, and contains `because` or `identical` — assert this by running FM01, which now covers all five.
    - `git diff --name-only -- scripts/ | wc -l` returns `0`.
    - The FM01 detail string observed before the two README edits is recorded in the SUMMARY.
  </acceptance_criteria>

  <done>Both model-list divergences are stated with reasons, the shim's passthrough warning is pinned in both directions, and FM01 gates all five contract rows.</done>
</task>

<task type="auto">
  <name>Task 3: Close S3 and prove the phase changed no shipped code</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md:61-64 — S3's statement and acceptance, which the new status must be written against.
    - .planning/REQUIREMENTS.md:85-93 — the Traceability table, including S3's current row reporting open. Read the neighbouring met rows (R5, R6, S1, S4) for the wording convention: what shipped, in which plan, pinned by which test IDs.
    - README.md:220-236 — the finished Troubleshooting table, read end to end as an operator would.
    - README.md:254-258 — the Security section, to confirm this phase moved nothing out of it.
  </read_first>

  <action>
    Rewrite S3's status cell in the Traceability table from open to met, following the neighbouring rows' convention. Name the four failure-mode rows now carrying the contract, the fifth row covering an unrecognized model name, the plan each landed in, `FM01` as the gate, and the per-row proof IDs RB03, EC06, SH14, SH9 and I16. Record the one honest ceiling: FM01 checks that each row names both entry points and carries a disposition marker, which is a shape check — that the stated reason is a true reason is settled by review, not by grep.

    Do not restate the ticket column; `cy5` stays as it is.

    Then confirm the phase's blast radius: the working tree's changed files are README.md, tests/run-tests.sh and .planning files only.
  </action>

  <reversibility rating="reversible">A planning-document status cell.</reversibility>

  <verify>
    <automated>bash tests/run-tests.sh 2>&1 | tail -3</automated>
    <automated>git diff --name-only -- scripts/ | wc -l</automated>
    <automated>git diff --name-only | sort</automated>
    <human-check>Read README.md:220-236 top to bottom as an operator who has never heard of agy. For each of the four failure modes — hung agy, unparseable model list, missing dependency, superseded pin — plus the unrecognized-model-name row: does the row tell you what `gemini` does to your caller? Where it says the two entry points differ, is the stated reason one you would accept, or is it filler placed to satisfy a grep?</human-check>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` exits 0 and its final line reads `FAIL=0`.
    - `git diff --name-only -- scripts/ | wc -l` returns `0`.
    - `git diff --name-only` lists only README.md, tests/run-tests.sh and paths under .planning/.
    - REQUIREMENTS.md's S3 row no longer begins with `open` and names FM01 plus all five proof IDs.
    - README:258's Security paragraph is unchanged: `git diff -U0 README.md | grep -c '^[-+].*comparison-only'` returns `0`.
  </acceptance_criteria>

  <done>S3 reports met with its evidence and its stated ceiling; the phase's diff touches no shipped script.</done>
</task>

</tasks>

## Alternatives Considered

N/A — no mechanism choice

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> README | An operator reads the Troubleshooting table to decide what a `gemini` failure means. A row that claims parity where the shipped scripts diverge sends them at the wrong entry point. |
| PATH caller -> `gemini` shim | Unchanged by this plan. The shim's pass-through-on-unrecognized-name behavior is documented here, not altered; `scripts/` is read-only for this phase. |

No package-manager installs occur in this phase, so no `T-05-SC` supply-chain row applies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | README.md divergence rows | medium | mitigate | FM01 requires every contract row to name both entry points and carry a disposition marker, so a rewrite that quietly claims parity fails the suite; Task 3's human-check covers the half a grep cannot judge. |
| T-05-06 | Spoofing | `gemini` shim's pass-through of an unrecognized model name | low | accept | Documented, not changed: the shim forwards an unknown name to agy, which rejects it. Accepted because a PATH-shadowing binary that hard-rejects input it merely does not recognize is the larger failure (requirement S3's own origin), and SH9 pins the behavior. |
| T-05-07 | Information Disclosure | README.md:254-258 Security paragraph | medium | mitigate | Task 3 gates on a diff check that the comparison-only claim is untouched; nothing is folded out of the Security section. |
| T-05-08 | Tampering | scripts/ (out of scope, guarded) | low | mitigate | Every task gates on `git diff --name-only -- scripts/` being empty. |
</threat_model>

## Artifacts this phase produces

See plan `05-01-PLAN.md`, section "Artifacts this phase produces", for the full phase-wide list. This plan contributes:

- `README.md` — three Troubleshooting rows rewritten in place: hung agy (`:231`), unparseable model list (`:230`), unrecognized model name (`:229`). The exit-137 row (`:232`) is deliberately left untouched and stays a separate row.
- `tests/run-tests.sh` — `FM01` extended with three anchors (`_FM_ANCHOR_HANG`, `_FM_ANCHOR_LIST`, `_FM_ANCHOR_PASSTHRU`) and one new provenance pin against `scripts/gemini_shim.sh`. No new test ID beyond FM01.
- `.planning/REQUIREMENTS.md` — S3's traceability row moved from open to met.

## Multi-Source Coverage Audit

The phase-wide audit lives in `05-01-PLAN.md`. Every GOAL, REQ and CONTEXT item is COVERED; there is no RESEARCH source for this phase. No item is MISSING.

<verification>
- `bash tests/run-tests.sh` exits 0 with `FAIL=0`.
- FM01 covers five README rows and two script-to-README provenance pins.
- RB03, EC05, EC06, SH9, SH14, SH15 and I16 all still pass.
- `git diff --name-only -- scripts/` is empty for the whole phase.
- REQUIREMENTS.md S3 reports met.
</verification>

<success_criteria>
- Every row where the shim and the bridge differ states why, in one line, sourced from the shipped code.
- No row states sameness where the scripts diverge, and none diverges without a reason.
- An unrecognized model name is documented as reaching agy unchanged through the shim, with `agy-bridge`'s contrasting refusal named in the same row.
- FM01 is the gate on all of it; S3 is closed with its ceiling stated.
</success_criteria>

<output>
Create `.planning/phases/05-the-shim-s-failure-mode-contract/05-02-SUMMARY.md` when done. Record each RED observation verbatim, and the human-check verdict from Task 3.
</output>
