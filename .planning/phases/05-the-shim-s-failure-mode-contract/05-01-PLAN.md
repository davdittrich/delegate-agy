---
phase: 05-the-shim-s-failure-mode-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - tests/run-tests.sh
autonomous: true
requirements: [S3]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README's Troubleshooting table carries a missing-dependency row that names both `agy-bridge` and the `gemini` shim and states they behave identically there."
    - "README's Troubleshooting table carries a superseded-pin row that names both `agy-bridge` and the `gemini` shim and states they behave identically there."
    - "A new `FM01` block in tests/run-tests.sh fails when either of those two rows loses an entry-point name, loses its disposition clause, loses its quoted literal, or is duplicated."
    - "The superseded-pin refusal literal is byte-identical between `scripts/install.sh`'s generated-wrapper heredoc and README's row; changing one without the other fails FM01."
    - "FM01 fails when any test cited as a row's proof (RB03, EC06, SH14, SH9, I16) no longer exists as a test label in tests/run-tests.sh."
    - "edge:S3/empty — each row's proof is cited by test ID (a grep-able label), never by line number, so renumbering tests/run-tests.sh cannot rot a citation."
    - "edge:S3/ordering — the four failure-mode rows keep their current relative order in README's table; this plan's diff changes row content only, never row position."
    - "`bash tests/run-tests.sh` exits 0 with FAIL=0 after the edits, and no file under `scripts/` is modified by this plan."
  artifacts:
    - "README.md — Troubleshooting rows for missing dependency and superseded pin, rewritten in place"
    - "tests/run-tests.sh — new `FM01` block placed with the other README-provenance tests"
  key_links:
    - "README's missing-dependency row literal <-> the `WARNING:` string both shipped scripts define (already pinned by RB03; FM01 adds the row-shape half)"
    - "README's superseded-pin row literal <-> the message `scripts/install.sh`'s generated wrapper actually prints (new pin, previously unpinned in either direction)"
    - "FM01's cited test IDs <-> the blocks that prove each row's code path (RB03, EC06, SH14, SH9, I16)"
  prohibitions:
    - "MUST NOT state a behavior in the contract table that the shipped scripts do not actually produce — no aspirational wording, and no smoothing a genuine shim/bridge divergence into claimed parity. [flagged: unverified]"
    - "MUST NOT remove or weaken README's existing Security-section claims (pinned exec target, comparison-only registry read) as a side effect of folding the superseded-pin contract into the Troubleshooting table. [flagged: unverified]"
---

<objective>
Prove the failure-mode contract table's shape end-to-end on the two rows where `agy-bridge` and the `gemini` shim behave identically — missing dependency and superseded pin — and pin that shape with a new test so neither row can go stale silently.

Purpose: ROADMAP criteria 1 and 2 for the two same-for-both rows, per CONTEXT D-01 and D-02. Criterion 3's harder half (rows that diverge, and why) is plan 05-02's job.
Output: a `FM01` block in tests/run-tests.sh and two rewritten README rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-the-shim-s-failure-mode-contract/05-CONTEXT.md
@.planning/phases/05-the-shim-s-failure-mode-contract/05-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — one failure-mode row (missing dependency) is contract-pinned</name>

  <files>tests/run-tests.sh, README.md</files>

  <read_first>
    - tests/run-tests.sh:2498-2555 — RB03. This is the block shape to copy: expected bytes are written as literals in the test, never extracted from the file under assertion. Read its scope comment at :2528-2549 in full — RB03 also refuses README any occurrence of the reversed bounding claim it bans there, and that ban applies to every word you add to README in this plan.
    - tests/run-tests.sh:2584-2685 — EC06. Read its literal-definition preamble (:2594-2597) and its `ok`/`bad` closing pair; FM01 goes immediately after this block ends and before EC07's comment.
    - tests/run-tests.sh:88-100 — the `ok`/`bad` helpers and the PASS/FAIL counters FM01 must report through.
    - README.md:220-236 — the Troubleshooting table as it stands, including the missing-dependency row and the generic-nonzero paragraph that closes the table.
    - README.md:292-315 — the `#### Bounding without timeout/gtimeout` section. The one-line reason the new row states is already argued here; take it from this text, do not invent one.
  </read_first>

  <behavior>
    - FM01 fails when README carries zero, or more than one, line containing the missing-dependency warning literal.
    - FM01 fails when that row's line does not contain `agy-bridge`.
    - FM01 fails when that row's line does not contain `gemini`.
    - FM01 fails when that row's line contains neither `because` nor `identical`.
    - FM01 passes once the row names both entry points and states that they behave identically there.
  </behavior>

  <action>
    Write the RED first, run the suite, and record what FM01 reported before touching README.

    Add a new `FM01` block to tests/run-tests.sh immediately after EC06's closing `fi` and before EC07's leading comment. Follow RB03's shape exactly: `FM01_OK=1`, `FM01_DETAIL=""`, accumulate failures into `FM01_DETAIL`, close with a matched `ok`/`bad` pair whose first argument starts with `FM01 ` followed by a space. Label it for what it asserts: the failure-mode contract table names both entry points per row and states sameness or a reason.

    Define `_FM_README="$ROOT/README.md"` and one anchor literal, written verbatim in the test rather than read out of README: `_FM_ANCHOR_DEP='WARNING: timeout/gtimeout not found -- bounding agy with the bash watchdog fallback'`. Assert `grep -cF` over `$_FM_README` for that anchor returns exactly `1`. Capture the matched line into `_fm_row` with `grep -F ... | head -1` and assert three things about it with bash pattern tests: it contains `agy-bridge`, it contains `gemini`, and it contains either `because` or `identical`. Record which check failed in `FM01_DETAIL` — RB03's `readme:` prefix convention.

    Run `bash tests/run-tests.sh` and confirm FM01 reports FAIL with a detail naming the missing entry-point name. That is the RED.

    Then rewrite README's missing-dependency row in place at `README.md:234`. Keep its quoted `WARNING:` literal byte-identical — RB03 greps README for that exact string and EC05/EC06 grep for the neighbouring rows' literals, so a re-typed literal breaks a shipped test. Add to the row's second column a clause naming `agy-bridge` and the `gemini` shim explicitly and stating they behave identically here, with the one-line reason drawn from README's own bounding section: both route every agy call through the same shared `run_bounded` helper, so a host without coreutils changes which kill you get, never whether the call is bounded. Do not reorder rows. Do not add a second table. Do not touch anything under `scripts/`.

    Run the suite again and confirm FM01 reports ok and FAIL=0. That is the GREEN.
  </action>

  <precondition>`bash tests/run-tests.sh` exits 0 and prints `FAIL=0` on the tree as found, before the first edit — otherwise a FM01 red is not attributable to this task.</precondition>

  <reversibility rating="reversible">A README row and a test block; reverting is a text edit with no migration and no published-contract break.</reversibility>

  <verify>
    <automated>bash tests/run-tests.sh 2>&1 | tail -3</automated>
    <automated>bash tests/run-tests.sh 2>&1 | grep -E '^(ok|FAIL) +- (FM01|RB03|EC05|EC06)'</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` exits 0 and its final line reads `FAIL=0`.
    - The second automated command lists FM01, RB03, EC05 and EC06 and every one of those lines begins with `ok`.
    - `grep -cF 'WARNING: timeout/gtimeout not found -- bounding agy with the bash watchdog fallback' README.md` returns `1`.
    - The README line matching that anchor contains both `agy-bridge` and `gemini`, and contains `because` or `identical`.
    - `git diff --name-only -- scripts/ | wc -l` returns `0`.
    - The RED observation is recorded in the plan's SUMMARY: the exact FM01 detail string the suite printed before the README edit.
  </acceptance_criteria>

  <done>FM01 exists, was observed failing against the pre-edit README, and now passes; the missing-dependency row names both entry points and states sameness with a reason; the whole suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fold the superseded-pin row into the same table and pin its literal to the installer</name>

  <files>tests/run-tests.sh, README.md</files>

  <read_first>
    - scripts/install.sh:155-180 — the generated wrapper's registry-comparison arm. Line 162 is the message an operator actually sees on a superseded pin; line 163 is its second line. Read the surrounding heredoc so you can tell which bytes are literal and which are wrapper-time expansions.
    - README.md:226 — the current superseded-pin Troubleshooting row.
    - README.md:254-258 — the Security section. Its paragraph at :258 states the pinned-exec-target and comparison-only-registry claims; those claims stay where they are. This task moves no prose out of the Security section — it adds the shim-versus-bridge contract statement to the Troubleshooting row.
    - tests/run-tests.sh:4330-4456 — I16. It exercises `agy-bridge` only, through wrappers written by the same `install.sh` run that also wrote the `gemini` wrapper from the same `write_wrapper()` call. That shared-function fixture is the evidence CONTEXT D-02 accepts for this row. Note what I16 does not do: it never greps README, and it matches versions by substring, so a reworded installer message leaves I16 green.
    - tests/run-tests.sh — the FM01 block you wrote in Task 1.
  </read_first>

  <behavior>
    - FM01 fails when README carries zero, or more than one, line containing the superseded-pin refusal literal.
    - FM01 fails when that row's line does not contain `agy-bridge`, or does not contain `gemini`, or contains neither `because` nor `identical`.
    - FM01 fails when the same refusal literal is absent from `scripts/install.sh`.
    - FM01 fails when any of the five cited proof IDs — RB03, EC06, SH14, SH9, I16 — is no longer present as a test label in tests/run-tests.sh.
  </behavior>

  <action>
    Extend FM01; do not start a second block.

    Add `_FM_ANCHOR_PIN='is installed, but this launcher is pinned to'` as a second literal written verbatim in the test. Assert it appears in `$ROOT/scripts/install.sh` (that is the new provenance half — nothing in the suite currently ties the installer's message to README, in either direction) and that `grep -cF` over README returns exactly `1`. Apply the same three row checks Task 1 established to the matched README line. Refactor the two anchors into one loop over an anchor list if that reads cleaner, but keep the per-anchor detail strings distinguishable in `FM01_DETAIL`.

    Add the citation-rot guard: an array holding the five proof IDs `RB03 EC06 SH14 SH9 I16`, and for each, assert tests/run-tests.sh contains an `ok` or `bad` call whose first argument begins with that ID followed by a space. Match the label-call form, not a bare occurrence of the ID — the array line itself contains all five IDs as data and must not satisfy its own check. Above the array, write the row-to-proof mapping as a comment: missing dependency proven by RB03, hung agy by EC06, unparseable model list by SH14 and EC06, unrecognized model name by SH9, superseded pin by I16. Cite by ID only; no line numbers, which renumber.

    Run the suite and confirm FM01 reports FAIL against the pre-edit README. Then rewrite README's superseded-pin row at `README.md:226` so it names `agy-bridge` and the `gemini` shim explicitly and states they behave identically here, with the one-line reason: both wrappers are written by the same `write_wrapper()` call in the installer, so the refusal is one code path with two call sites rather than two behaviors. Keep the row's quoted refusal fragment byte-identical to what `scripts/install.sh:162` prints. Leave README:258 as it stands. Do not reorder rows and do not touch anything under `scripts/`.
  </action>

  <reversibility rating="reversible">Row text and test assertions only.</reversibility>

  <verify>
    <automated>bash tests/run-tests.sh 2>&1 | tail -3</automated>
    <automated>bash tests/run-tests.sh 2>&1 | grep -E '^(ok|FAIL) +- (FM01|I16)'</automated>
    <automated>grep -cF 'is installed, but this launcher is pinned to' README.md scripts/install.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` exits 0 and its final line reads `FAIL=0`.
    - The FM01 and I16 lines both begin with `ok`.
    - The third command reports `1` for README.md and at least `1` for scripts/install.sh.
    - The README line matching the refusal literal contains both `agy-bridge` and `gemini`, and contains `because` or `identical`.
    - Deleting any one of the five cited proof IDs' `ok`/`bad` label makes FM01 report FAIL — demonstrate this once on `SH14`, record the FM01 detail string in the SUMMARY, then restore the label by retyping the exact line back (never `git checkout`, `restore`, `reset` or `stash`), and confirm `git status --porcelain` matches what it showed before the mutation.
    - README:258's Security paragraph is unchanged: `git diff -U0 README.md | grep -c '^[-+].*comparison-only'` returns `0`.
    - `git diff --name-only -- scripts/ | wc -l` returns `0`.
  </acceptance_criteria>

  <done>The superseded-pin row states the shim-and-bridge contract with a reason, its literal is pinned in both directions against the installer, the five row-proof citations cannot rot silently, and the suite is green.</done>
</task>

</tasks>

## Alternatives Considered

N/A — no mechanism choice

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> README | An operator reads the Troubleshooting table to decide what a `gemini` failure means. A row that misstates shipped behavior sends them at the wrong cause. |
| installer -> generated wrapper -> PATH caller | Unchanged by this plan. `scripts/` is read-only here; the plan only pins README's quotation of what that path already prints. |

No package-manager installs occur in this phase, so no `T-05-SC` supply-chain row applies and no package-legitimacy checkpoint is warranted.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | README.md Troubleshooting rows | low | mitigate | FM01 pins each row's quoted literal and requires both entry points be named, so a doc edit that misstates shipped behavior fails the suite instead of shipping. |
| T-05-02 | Information Disclosure | README.md:258 Security paragraph | medium | mitigate | Task 2 reads :254-258 before editing, folds nothing out of it, and gates on a diff check that the comparison-only claim is untouched. |
| T-05-03 | Repudiation | test-citation rot in tests/run-tests.sh | low | mitigate | FM01 asserts each cited proof ID still exists as a test label, and the guard is demonstrated red by deliberate mutation before it is trusted. |
| T-05-04 | Tampering | scripts/ (out of scope, guarded) | low | mitigate | Every task gates on `git diff --name-only -- scripts/` being empty, so a doc phase cannot silently alter a shipped code path. |
</threat_model>

## Artifacts this phase produces

Produced by this plan:

- `README.md` — two Troubleshooting rows rewritten in place: the missing-dependency row (`:234`) and the superseded-pin row (`:226`). No new section, no second table, no row reordering.
- `tests/run-tests.sh` — one new test block, ID `FM01`, placed after EC06 and before EC07. New test IDs added by this plan: **FM01**. No other test ID is added, renamed or removed.

Produced by plan 05-02 (listed here so the phase's artifact set is complete in one place):

- `README.md` — three further Troubleshooting rows rewritten in place: hung agy (`:231`), unparseable model list (`:230`), unrecognized model name (`:229`).
- `tests/run-tests.sh` — FM01 extended with three more anchors and one further provenance pin against `scripts/gemini_shim.sh`. Still no new test ID beyond FM01.
- `.planning/REQUIREMENTS.md` — S3's traceability row moved from open to met.

## Multi-Source Coverage Audit

Covers the whole phase's plan set (05-01 and 05-02).

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | ROADMAP Phase 5 goal — an operator can read what `gemini` does to a caller that has never heard of agy, for every way this plugin fails | 05-01 T1-T2, 05-02 T1-T2 |
| GOAL | Criterion 1 — one table, four failure modes, bridge behavior alongside | 05-01 T1 (missing dep), 05-01 T2 (superseded pin), 05-02 T1 (hung agy), 05-02 T2 (unparseable list) |
| GOAL | Criterion 2 — every row has a test | 05-01 T1-T2 (FM01 row-shape + literal pins + citation-rot guard), 05-02 T1-T2 (remaining anchors) |
| GOAL | Criterion 3 — every divergence states why, no unexplained divergence | 05-02 T1-T2 (the three divergent rows); 05-01 states sameness on the two identical rows |
| GOAL | Criterion 4 — unrecognized model name passes through, never hard-rejected | 05-02 T2 |
| REQ | S3 — shim defects must not escape into unrelated PATH callers | 05-01, 05-02; closed in 05-02 T3 |
| RESEARCH | none — research was skipped for this phase by user choice | n/a |
| CONTEXT | D-01 — fold the contract into README's existing Troubleshooting table, never a parallel table | 05-01 T1-T2, 05-02 T1-T2 (every task states "rewrite in place, do not reorder, no second table") |
| CONTEXT | D-02 — cite I16 for the superseded-pin row; no new runtime provocation against the `gemini` wrapper's exit-127 path | 05-01 T2 (cites I16, adds a static literal pin only) |
| CONTEXT | D-03 — the shim-quiet / bridge-loud divergence on a degraded model list is final; document it, change no code | 05-02 T2 |

No item is MISSING. Exclusions applied: CONTEXT's Deferred Ideas (a quiet-shim signal; a direct exit-127 test against the `gemini` wrapper) are out by user decision, and every task gates on `scripts/` being untouched.

## Deviation from CONTEXT worth the developer's attention

CONTEXT D-01 names four failure modes. Reading the shipped code for this plan turned up a fifth divergence that ROADMAP criterion 4 already requires the table to carry, and that CONTEXT assumed was parity: `scripts/agy_bridge.sh:560-562` hard-rejects an unknown `--model` with exit 2, while `scripts/gemini_shim.sh:485-493` passes the same name through to agy with a stderr warning. The planning brief described criterion 4 as "already true and already pinned by SH9 — this plan's job is to make that fact visible in the table", which holds; what it did not say is that the row must state a divergence rather than sameness. 05-02 T2 handles it. No code changes either way.

<verification>
- `bash tests/run-tests.sh` exits 0 with `FAIL=0`.
- FM01 appears in the suite output and passes.
- RB03, EC05, EC06 and I16 still pass — the README edits did not disturb a shipped literal pin.
- `git diff --name-only -- scripts/` is empty.
</verification>

<success_criteria>
- Two of the four failure-mode rows name both entry points and state sameness with a reason.
- FM01 was observed red before each row edit and green after.
- The superseded-pin literal is pinned in both README and `scripts/install.sh`.
- No shipped script changed.
</success_criteria>

<output>
Create `.planning/phases/05-the-shim-s-failure-mode-contract/05-01-SUMMARY.md` when done. Record the two RED observations verbatim (the FM01 detail strings), and the mutation-proof detail string from Task 2's citation-rot demonstration.
</output>
