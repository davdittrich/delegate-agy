---
phase: 1.5
slug: contract-check-against-a-real-agy
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-19
---

# Phase 1.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `01.5-RESEARCH.md` §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — hand-rolled bash. `tests/run-tests.sh` carries its own `ok()`/`bad()`/`PASS`/`FAIL` bookkeeping (`:71-82`) |
| **Config file** | none — the harness is a single executable file |
| **Quick run command** | `bash tests/run-tests.sh` — no case-filtering flag exists in this harness, so quick and full are the same command |
| **Full suite command** | `bash tests/run-tests.sh` |
| **Estimated runtime** | Minutes, by design. Several cases drive `fake-agy.sh` into a 300s SIGTERM-ignoring hang behind a 30s outer bound (SH11 shape) — that latency is load-bearing and must not be "optimised" away |
| **Baseline** | `PASS=123 FAIL=0` at Phase 1 close (STATE.md). Any new red is this phase's |

**Second entry point, deliberately not part of the above:** `bash tests/contract-check.sh` exercises the **real** `agy`, spends quota, and is never invoked by the unit suite (D-01/D-02). Per D-05 it is **not** a gate — its verdicts inform, they do not block.

---

## Sampling Rate

- **After every task commit:** `bash tests/run-tests.sh` (only granularity available)
- **After every plan wave:** `bash tests/run-tests.sh`, full run
- **Before `/gsd-verify-work`:** full suite green
- **Max feedback latency:** one full-suite run — there is no shorter signal in this harness
- **At least once before the phase is called done:** one manual `bash tests/contract-check.sh` against the real binary. The unit suite can only prove the check's *shape*; criteria 1, 3 and 4 are claims about `agy`, and a phase that never ran the check against `agy` has verified its proxy rather than its deliverable

---

## Per-Task Verification Map

Task IDs are assigned by the planner; rows below map each success criterion and derived decision to the evidence that settles it. The planner fills `Task ID` / `Plan` / `Wave` as tasks are created.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 01.5-01 | 1 | S5 — criterion 1 (tracer: `agy-version-shape`, D-09 assumption 7) | T-01.5-01, T-01.5-03, T-01.5-06 | Ledger never reports pass for an assumption it did not ask; an internal 124 stays in the evidence, never in `$?` | manual (real binary) + unit for ledger shape | `bash tests/contract-check.sh`; header + row + exit ∈ {0,10,11} | ❌ W0 | ⬜ pending |
| 01-T2 | 01.5-01 | 1 | D-03 third `run_bounded` copy | T-01.5-04 | Three extracted blocks byte-identical | unit (RB02 widened via `_rb_extract`, `:1208`) | `bash tests/run-tests.sh` → `ok - RB02` | ✅ RB02 exists (`:1976-1997`) — extend | ⬜ pending |
| 01-T3 | 01.5-01 | 1 | S5 — criterion 2, shape (b): no `agy` on PATH (D-08b) | T-01.5-05 | Bounded exit; exit 10; assumption named; no verified row | unit (fully automatable) | `bash tests/run-tests.sh` → `ok - CC01` | ❌ W0 | ⬜ pending |
| 02-T1 | 01.5-02 | 2 | S5 — criterion 2, shape (a): SIGTERM-ignoring hang (D-08a) | T-01.5-08 | Returns inside its own `-k`; exit 10, not 124 from the outer net; elapsed `-lt 25` | unit (fake-only, SH11 shape) | `bash tests/run-tests.sh` → `ok - CC02` | ❌ W0 | ⬜ pending |
| 02-T2 | 01.5-02 | 2 | D-02 isolation invariant | T-01.5-05, T-01.5-07 | Suite never reaches a real `agy` through the check; scan cannot pass vacuously | unit, static scan + mutation (RB01/RB01m shape) | `bash tests/run-tests.sh` → `ok - CC03`, `ok - CC03m` | ❌ W0 | ⬜ pending |
| 03-T1 | 01.5-03 | 3 | S5 — criterion 1 (`models-format`, `non-gemini-rows`) + D-13 preflight | T-01.5-02, T-01.5-09 | Preflight gates all billed calls; ragged rows reported, never normalized away | manual (real binary) for content; unit for suite stability | `bash tests/contract-check.sh`; `bash tests/run-tests.sh` → `FAIL=0` | ❌ W0 | ⬜ pending |
| 03-T2 | 01.5-03 | 3 | S5 — criterion 3 (`invalid-model.txt` fixture, D-09 assumption 4) | T-01.5-01, T-01.5-03, T-01.5-01b | Probe never sets the skip-permissions flag; fixture read before commit | manual for content; source assertion for the flag absence | `grep -c AGY_SKIP_PERMISSIONS tests/contract-check.sh` = 0 | ❌ W0 | ⬜ pending |
| 04-T1 | 01.5-04 | 4 | D-14a fixture plumbing at the 5 `cp` sites | T-01.5-11 | RB27's bare `env -i` gains neither `HOME` nor `AGY_PLUGIN_DIR`; a missing `fixtures/` is FATAL | unit (behavior-preserving) | `bash tests/run-tests.sh` → `FAIL=0` | ❌ W0 | ⬜ pending |
| 04-T2 | 01.5-04 | 4 | S5 — criterion 3 (fixtures regenerate the fake, D-14/D-15) + D-15a repair | T-01.5-04, T-01.5-10, T-01.5-13 | Fake never emits an empty model list; R2/R4 derive, never re-pin; nothing sources a fixture | unit (fake-only) | `bash tests/run-tests.sh` → `FAIL=0`, `ok - R2`, `ok - R4`, `ok - S6` | ✅ R2 (`:354-360`), R4 (`:460-477`) exist — modify | ⬜ pending |
| 04-T3 | 01.5-04 | 4 | D-14a tier resolution + derivation edges | T-01.5-10, T-01.5-12, T-01.5-13 | Each tier proven individually; loud failure provably distinct from an empty list | unit (fake-only) | `bash tests/run-tests.sh` → `ok - CC04a/b/c`, `ok - CC06` | ❌ W0 | ⬜ pending |
| 05-T1 | 01.5-05 | 5 | S5 — criterion 1 (`gemini-md-binds`, D-10) + criterion 3 (R6 fixture, D-11) | T-01.5-03, T-01.5-01, T-01.5-01b, T-01.5-02 | Skip-permissions flag absent at source; free argv half gates the spend | manual (billed, real binary); source assertion for the flag | `bash tests/contract-check.sh`; `grep -c 'dangerously-skip-permissions' tests/contract-check.sh` = 0 | ❌ W0 | ⬜ pending |
| 05-T2 | 01.5-05 | 5 | S5 — criterion 1 (`sigterm-ignored`, D-12; `model-arg-accepts`, D-09 assumption 2) | T-01.5-14, T-01.5-06 | Elapsed-vs-bound discrimination; watchdog arm routed to `unverified`, never read as a verdict | manual (billed, timing-dependent) | `bash tests/contract-check.sh`; read the two rows | ❌ W0 | ⬜ pending |
| 05-T3 | 01.5-05 | 5 | S5 — criterion 1 (complete ledger) + D-12a gap statement | T-01.5-06 | Eight rows every run, fixed order; every gap named; `11` outranks `10` | manual (the phase's actual deliverable) | `bash tests/contract-check.sh`; row count = 8 | ❌ W0 | ⬜ pending |
| 06-T1 | 01.5-06 | 6 | S5 — criterion 4 (README states the dated fact, D-17) | T-01.5-16, T-01.5-19 | README states the ledger's finding, not the prediction; RB03's negative half survives | unit (no live agy) | `bash tests/run-tests.sh` → `ok - RB03`; `grep -ciF unbounded README.md` = 0 | ❌ W0 | ⬜ pending |
| 06-T2 | 01.5-06 | 6 | D-16 README ↔ fixture-header provenance | T-01.5-17, T-01.5-18 | Literals defined once in the test file; fixed-string matching; non-vacuity floor | unit (no live agy — RB03 shape, `:2019-2073`) | `bash tests/run-tests.sh` → `ok - CC05` | ❌ W0 | ⬜ pending |
| 06-T3 | 01.5-06 | 6 | D-18 doc corrections + D-05 recorded omission | T-01.5-20 | No tracked document claims S5 is unverified; Phase 1.5 criteria byte-unchanged | source assertion for REQUIREMENTS.md greps; human read for prose | `grep -cF 'open, externally blocked' .planning/REQUIREMENTS.md` = 0 | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/contract-check.sh` — does not exist; the phase's core deliverable
- [ ] `tests/fixtures/` — directory and contents (model list, invalid-model rejection, R6 empty-success) do not exist
- [ ] `tests/fake-agy.sh` fixture-reading rewrite with D-14a's tiered lookup — the canned block still sits at `:106-114`
- [ ] `run-tests.sh` D-02 case — isolation-invariant static scan
- [ ] `run-tests.sh` D-08 cases (a)/(b) — hang and absent-binary paths, SH11's outer-`timeout` shape
- [ ] `run-tests.sh` D-16 case — README-to-fixture-header provenance, RB03 shape
- [ ] `_rb_extract`/RB02 widened to a third file — mechanical extension
- [ ] Framework install: **none** — the harness is self-contained bash

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| What `agy models` actually emits; whether `--model` accepts ids, display names or both; whether the per-run `GEMINI.md` binds | S5 criterion 1 | Only the real binary can answer. The unit suite is forbidden from reaching it (D-02) | `bash tests/contract-check.sh`; read the assumption ledger |
| Fixture **content** accuracy (as opposed to format) | S5 criterion 3 | A fixture is only correct if it matches what `agy` emitted; the suite can check shape, not truth | Re-run the check; confirm captured fixtures match the ledger's recorded evidence |
| The rc=0-empty-stdout permission-gate response | S5 criterion 3 (R6 fixture) | Not reliably reproducible on demand — D-11 routes non-reproduction to *unverified*, so this fixture may legitimately not be produced | Run the check; if the row reports *unverified*, record it as a known gap rather than a failure |
| Whether `agy` ignores SIGTERM | D-12 / R11's rationale | Timing-dependent against a live service; no authoritative source exists (research round) | Run the check; read the SIGTERM row and what it records having attempted |
| REQUIREMENTS.md prose no longer claiming S5 is unverified | S5 criterion 4 / D-18 | English prose; a lint was considered and declined (false-positive surface) | Read `.planning/REQUIREMENTS.md` §S5 and the traceability row |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency = one full-suite run (no shorter signal exists in this harness)
- [ ] One manual `bash tests/contract-check.sh` run recorded against the real binary
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
