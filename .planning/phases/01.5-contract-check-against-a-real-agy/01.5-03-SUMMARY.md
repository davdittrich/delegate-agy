---
phase: 01.5-contract-check-against-a-real-agy
plan: 03
subsystem: testing
tags: [bash, contract-testing, agy, run_bounded, test-harness]

# Dependency graph
requires:
  - phase: 01.5-02
    provides: "tests/contract-check.sh's tracer, RB02 widened to three run_bounded copies, CC01/CC02 ('could not ask' for absent and hung agy), CC03/CC03m (isolation self-scan)"
provides:
  - "_cc_preflight: runs bounded agy --version and bounded agy models exactly once per run, gating every later probe (D-13) -- the two cheapest calls decide whether any expensive call is attempted"
  - "_cc_probe_models_format: D-09 assumption 1, verdict from TAB-shape re-derivation, never from a remembered list"
  - "_cc_probe_non_gemini_rows: D-09 assumption 5, re-derives the shipped anchored matchers (cut -f1, grep -E '^gemini-[0-9.]+-<class>$' | sort -V | tail -1) verbatim over the live list"
  - "_cc_probe_invalid_model_rejection: D-09 assumption 4, reproduces the shipped bridge's real invocation (minus skip-permissions, never referenced) against a permanently non-existent model id"
  - "tests/fixtures/agy-models.tsv and tests/fixtures/invalid-model.txt: real captured bytes with provenance headers, both read by a human before commit"
affects: [01.5-04, 01.5-05, 01.5-06, phase-2]

# Actuals (#2632)
actuals:
  tokens: 4100
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Preflight-once, reuse-everywhere: agy --version and agy models are each called exactly once per run (never per-probe), with captures kept in a persistent scratch dir cleaned up via a single EXIT trap -- three probes (agy-version-shape, models-format, non-gemini-rows) all read from those two captures instead of re-fetching"
    - "Verdict-from-derivation, never from equality: models-format counts TABs, non-gemini-rows re-runs the shipped grep -E ... | sort -V | tail -1 matchers verbatim, invalid-model-rejection checks whether the rejection names the id WE supplied -- none compares agy's output against a remembered string"
    - "RED/GREEN split for shell-tooling behavior with no separate test file: RED commits the new probe function bodies unwired (ledger doesn't show the new row -- an honest failing state), GREEN adds the one-line call in the fixed probe-running order (ledger shows it, fixture lands) -- applied to both tasks since tdd=\"true\" was set but neither task adds assertions to tests/run-tests.sh (deferred to 01.5-04+)"
key-files:
  created:
    - tests/fixtures/agy-models.tsv
    - tests/fixtures/invalid-model.txt
  modified:
    - tests/contract-check.sh

key-decisions:
  - "agy models hangs indefinitely with no stdin redirect (observed live, matching scripts/agy_bridge.sh's own comment 'agy ignores SIGTERM ... observed: timeout 25 agy models still running after 3+ min'), but resolves in under 2s with </dev/null. The bridge's own fetch already redirects stdin this way at :476; _cc_preflight's models call reproduces that redirect exactly -- omitting it would have made the preflight itself the thing that hangs."
  - "models-format and non-gemini-rows consume the SAME single agy models capture that _cc_preflight fetches, rather than each calling agy models independently. This is what makes 'It performs two bounded, non-generating calls' literally true for the whole run (not per-probe), and is why _cc_probe_agy_version_shape was rewired to stop calling run_bounded itself and instead read the preflight's already-captured --version output (the plan's explicit reuse instruction)."
  - "invalid-model-rejection's verdict derives from whether the combined stdout+stderr contains the exact impossible-model literal WE constructed (gemini-99.9-flash-high), not from matching 'Available models:' or any fixed agy wording. This satisfies the re-derive-not-assert principle while still being a legitimate structural check: the literal only appears in agy's own output if agy is genuinely reacting to the id we asked for."
  - "Chose gemini-99.9-flash-high as the impossible id, distinct from R3d's tests/run-tests.sh:400 literal (gemini-9.9-pro-high) -- different version segment AND different class -- so the two synthetic cases stay independently readable and neither test's fixture/log output could be confused with the other's."
  - "Used config/policies/code.md as the GEMINI.md source (an existing, tracked, shipped policy file) rather than a bespoke test-only policy, per the plan's explicit instruction that the probe must exercise real shipped configuration."

patterns-established:
  - "_cc_preflight's persistent scratch dir + single EXIT trap: the first place in contract-check.sh where a capture needs to outlive one probe function (models-format AND non-gemini-rows both read it) -- prior probes each managed their own throwaway mktemp -d, rm -rf pair."

requirements-completed: [S5]

coverage:
  - id: D1
    description: "D-13 preflight: bounded agy --version and bounded agy models run exactly once, gating every later probe -- a preflight failure routes every probe to unverified with zero spend"
    requirement: S5
    verification:
      - kind: integration
        ref: "manual: sanitized-PATH-no-agy run of tests/contract-check.sh -- exit 10, all rows unverified, 'billed delegations: 0'"
        status: pass
    human_judgment: false
  - id: D2
    description: "models-format (D-09 assumption 1) and non-gemini-rows (D-09 assumption 5) answered against the real agy 1.1.13, verdict re-derived from shape/matcher logic, not a remembered list"
    requirement: S5
    verification:
      - kind: integration
        ref: "manual: bash tests/contract-check.sh against real agy -- models-format verified (14 rows, 1 TAB each), non-gemini-rows verified (3 non-gemini rows correctly excluded by both anchored matchers)"
        status: pass
    human_judgment: false
  - id: D3
    description: "invalid-model-rejection (D-09 assumption 4) answered against the real agy 1.1.13, reproducing the shipped bridge invocation minus skip-permissions, which is absent from the file entirely"
    requirement: S5
    verification:
      - kind: integration
        ref: "manual: bash tests/contract-check.sh against real agy -- invalid-model-rejection verified, rc=1, rejection quotes the requested id and lists Available models: in display names"
        status: pass
      - kind: other
        ref: "grep -c AGY_SKIP_PERMISSIONS tests/contract-check.sh == 0; grep -c dangerously-skip-permissions tests/contract-check.sh == 0"
        status: pass
    human_judgment: false
  - id: D4
    description: "tests/fixtures/agy-models.tsv and tests/fixtures/invalid-model.txt captured with provenance headers, read by a human before commit for account identifiers/tokens/emails/absolute home paths"
    requirement: S5
    verification:
      - kind: manual_procedural
        ref: "cat of both fixtures, reviewed inline before staging -- see key-decisions and this SUMMARY's body"
        status: pass
    human_judgment: true
    rationale: "Content-safety review of live, third-party CLI error text is a judgment call about what counts as sensitive; the automated grep checks (headers, Available models: presence) are necessary but not sufficient on their own."
  - id: D5
    description: "Full unit suite (tests/run-tests.sh) stays green with no consumer of the new fixtures yet, and no path under scripts/ or config/ is touched by this plan"
    verification:
      - kind: unit
        ref: "bash tests/run-tests.sh -- PASS=130 FAIL=0 (re-run after every commit in this plan)"
        status: pass
      - kind: other
        ref: "git diff --name-only d5c773a..HEAD -- tests/contract-check.sh, tests/fixtures/agy-models.tsv, tests/fixtures/invalid-model.txt only"
        status: pass
    human_judgment: false

duration: 45min
completed: 2026-08-20
status: complete
---

# Phase 1.5 Plan 3: Preflight, models-format, non-gemini-rows, invalid-model-rejection Summary

**`tests/contract-check.sh` now preflights once (bounded `agy --version` + bounded `agy models`) and answers three more D-09 assumptions against the real `agy 1.1.13` -- what `agy models` emits (14 well-formed rows), whether the shipped anchored matchers exclude the 3 non-gemini rows, and how agy rejects a model id that cannot exist -- landing `tests/fixtures/agy-models.tsv` and `tests/fixtures/invalid-model.txt` as real captured bytes, at zero billed delegations.**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-08-20T04:45:00+02:00 (approx.)
- **Completed:** 2026-08-20T05:29:05+02:00 (last task commit)
- **Tasks:** 2
- **Files modified:** 1 (`tests/contract-check.sh`); 2 fixtures created

## Accomplishments
- `_cc_preflight` added: runs bounded `agy --version` and bounded `agy models` exactly once per invocation (not once per probe), storing both captures in a scratch dir cleaned up by a single `EXIT` trap. `_cc_probe_agy_version_shape` was rewired to consume the preflight's `--version` capture rather than calling `run_bounded` a second time, matching the plan's explicit reuse instruction.
- `_cc_probe_models_format` (D-09 assumption 1) and `_cc_probe_non_gemini_rows` (D-09 assumption 5) added, both reading the single preflight `agy models` capture. Against real `agy`: 14 rows, every row carries exactly one TAB (`verified`); 3 non-gemini rows (`claude-*`, `gpt-oss-*`) present, both shipped anchored matchers (`grep -E '^gemini-[0-9.]+-flash-high$'` / `'^gemini-[0-9.]+-pro-high$'` | `sort -V | tail -1`) correctly select only `gemini-`-prefixed ids (`verified`). `tests/fixtures/agy-models.tsv` written with the standard header, byte-identical data rows, and a display name (`Gemini 3.7 Flash (High)`) exercising the encoding edge (space + parenthesis in the display column, zero spaces in the extracted id column).
- `_cc_probe_invalid_model_rejection` (D-09 assumption 4) added: assembles a scratch `GEMINI.md` from `config/policies/code.md` exactly as the shipped bridge does, invokes `agy` with the bridge's own flag surface (`--print "$AGY_POINTER" --sandbox --model gemini-99.9-flash-high --add-dir "$WORK_DIR"`, `< /dev/null`) with the skip-permissions branch entirely absent from the file. Real agy rejects the impossible id CLI-side (rc=1, instant, no generation), quoting the id back verbatim and listing `Available models:` in display names -- the live evidence that the id-vs-display-name split is not accidental. `tests/fixtures/invalid-model.txt` written and read before commit.
- All four rows now print in fixed order (`agy-version-shape`, `models-format`, `non-gemini-rows`, `invalid-model-rejection`), closed by one trailing summary reporting `billed delegations: 0` throughout this plan.
- Preflight-failure proof (manual, sanitized-PATH holding every tool this script needs except `agy`): `bash tests/contract-check.sh` exits 10, all rows read `unverified` naming "no agy on PATH", and the summary still says `billed delegations: 0`.

## Task Commits

Each task followed a RED (probe defined, not wired) then GREEN (wired in, fixture captured) split on `fix/agy-bridge-resilience` in the persistent worktree `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2`:

1. **Task 1 RED:** `3909aa7` -- `test(01.5-03): preflight + models-format/non-gemini-rows probes defined, not wired`
2. **Task 1 GREEN:** `96b048b` -- `feat(01.5-03): wire in models-format and non-gemini-rows (GREEN), capture agy-models.tsv`
3. **Task 2 RED:** `05c0916` -- `test(01.5-03): invalid-model-rejection probe defined, not wired`
4. **Task 2 GREEN:** `39b76cc` -- `feat(01.5-03): wire in invalid-model-rejection (GREEN), capture invalid-model.txt`

**Plan metadata:** this commit, on `master` (docs: complete 01.5-03 plan)

## Files Created/Modified
- `tests/contract-check.sh` -- `AGY_MODELS_TIMEOUT` knob (validate-and-correct, matching `scripts/agy_bridge.sh:39-46`'s default and regex); `_cc_preflight`; rewired `_cc_probe_agy_version_shape`; new `_cc_probe_models_format`, `_cc_probe_non_gemini_rows`, `_cc_probe_invalid_model_rejection`; probe-running section extended to the fixed four-row order
- `tests/fixtures/agy-models.tsv` -- real `agy models` output, 14 rows, provenance header
- `tests/fixtures/invalid-model.txt` -- real rejection message for `gemini-99.9-flash-high`, provenance header

## Decisions Made
See `key-decisions` in frontmatter: `agy models` requiring `</dev/null` (discovered live), the single-fetch-reused-by-three-probes preflight design, the derive-not-assert verdict for `invalid-model-rejection`, the R3d-distinct impossible-model literal, and the choice of `code.md` as the exercised policy file.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `agy models` hangs indefinitely without `</dev/null`**
- **Found during:** Task 1, live precondition check before writing any code
- **Issue:** `timeout 30 agy models` (no stdin redirect) never returned; `agy models </dev/null` returned in under 2s. `scripts/agy_bridge.sh:476` already redirects stdin this way for its own fetch, confirming this is a known, already-mitigated shape, not new agy behavior.
- **Fix:** `_cc_preflight`'s `agy models` call includes `< /dev/null`, exactly matching the bridge's own invocation.
- **Files modified:** `tests/contract-check.sh` (part of the design, not a separate patch)
- **Verification:** preflight completes in ~1.7s against the real binary; confirmed via direct manual `timeout 30 agy models` (hangs) vs `timeout 30 agy models </dev/null` (returns) before writing the probe.

---

**Total deviations:** 1 auto-fixed (1 blocking, discovered via direct precondition testing before implementation)
**Impact on plan:** None on scope -- the fix is exactly the redirect the plan's own read-first pointer (`scripts/agy_bridge.sh` lines 463-532) already models; omitting it would have made the preflight itself hang.

## Issues Encountered
None beyond the auto-fixed stdin-redirect issue above.

## User Setup Required
None -- `agy` was already installed, on PATH, and authenticated (version 1.1.13), confirmed live before Task 1 began (`agy --version` -> `1.1.13`; `agy models </dev/null` -> 14-row list) per this plan's `user_setup` precondition.

## Next Phase Readiness
- Two of the three fixtures this phase's D-09 probe set can capture by hand now exist on disk (`agy-models.tsv`, `invalid-model.txt`), both provenance-headed and human-read before commit. Nothing consumes them yet -- the fake still emits its own canned list and the suite stays green, exactly as the plan's objective specifies. Plan 01.5-04 performs the fake-agy swap against `agy-models.tsv`.
- D-13's preflight now gates every future billed probe (plan 01.5-05's three remaining D-09 assumptions): a single `agy --version` + `agy models` pair decides whether any billed call is attempted at all, and the trailing summary's `billed delegations` count is proven honest at both the always-0 end (this plan) and the increment-on-contradiction end (the `invalid-model-rejection` probe's own accept-and-bill branch, exercised in code but not hit live since real agy correctly rejects the impossible id).
- `S5` in `.planning/REQUIREMENTS.md`'s traceability table still reads "Phase 7 | open, externally blocked" -- unchanged by this plan, same as 01.5-01 and 01.5-02 left it. `requirements mark-complete S5` was run as part of state sync and correctly no-op'd (the row's current text doesn't match the tool's `Pending`/`Gaps Found` trigger), so no incorrect "Complete" status was written. `D-18`'s correction is still plan 01.5-06's scope.
- No blockers for 01.5-04.

---
*Phase: 01.5-contract-check-against-a-real-agy*
*Completed: 2026-08-20*

## Self-Check: PASSED
- FOUND: tests/fixtures/agy-models.tsv
- FOUND: tests/fixtures/invalid-model.txt
- FOUND: .planning/phases/01.5-contract-check-against-a-real-agy/01.5-03-SUMMARY.md
- FOUND: commits 3909aa7, 96b048b, 05c0916, 39b76cc (fix/agy-bridge-resilience)
