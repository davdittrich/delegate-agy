---
phase: 01.5-contract-check-against-a-real-agy
plan: 06
type: execute
wave: 6
depends_on: ["01.5-05"]
files_modified:
  - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/README.md
  - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh
  - /home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md
  - /home/dd/Gemini/delegate-agy/.planning/ROADMAP.md
autonomous: true
requirements: [S5]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README states as fact what `--model` accepts — naming both model forms the ledger's aggregated `model-arg-accepts` row actually tested — carrying the `agy` version and the verification date taken from `tests/fixtures/agy-models.tsv`'s header, and every wording implying the behavior is assumed or unverified is removed in the same change (D-17, criterion 4)."
    - "README names `tests/contract-check.sh`, states its three exit codes beside R5's troubleshooting table, and states that running it spends real quota with an upper bound on billed delegations (D-07, D-13, D-17)."
    - "A `run-tests.sh` case defines the version and date literals exactly once in the test file and asserts them present in README **and in the one fixture that backs the README claim being pinned** — `tests/fixtures/agy-models.tsv`, the model list the `--model` acceptance fact rests on — so editing one without the other goes red with no live agy involved (D-16)."
    - "CC05 does not require every fixture under `tests/fixtures/` to carry one identical version and date. Fixtures are captured by four probes across three plans and may legitimately be recaptured independently; requiring global equality would either block a valid partial recapture or pressure a false provenance stamp into a file that was not captured at that moment (F8)."
    - "A short required-fixture manifest, `_CC_REQUIRED_FIXTURES`, names the fixtures that must exist; each must carry a well-formed provenance header (`# agy-version: ` and an ISO `# captured: `, once each), which is a shape assertion rather than an equality one. `empty-success.txt` is deliberately absent from the manifest — D-11 makes it conditional (F8)."
    - "That case also pins an absence: README's pre-phase claim that the model name must be an exact string is gone, so a reverted or reintroduced stale claim is caught rather than only a missing new one (D-16, RB03's negative half)."
    - "REQUIREMENTS.md's S5 constraint line and its `S5 | Phase 7 | open, externally blocked` traceability row are both corrected in this change — the phase is 1.5, not 7, and the premise is disproven. A sentence the change falsifies is fixed in the same change, never filed as a follow-up (D-18)."
    - "ROADMAP §Phase 6 records that its release gate is deliberately not widened to include the contract check, so the omission reads as a decision rather than an oversight (D-05)."
    - "No tracked document claims the assumption is unverified after this plan (criterion 4)."
    - "edge:encoding (delivered here) — the provenance literals are matched with fixed-string `grep -qF`, so a drifted hyphen, quote or parenthesis is caught rather than absorbed by a pattern."
  artifacts:
    - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/README.md
    - /home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md
  key_links:
    - "`tests/fixtures/agy-models.tsv`'s header ↔ README's dated fact ↔ CC05's literals. Three copies of one string, pinned by a case that needs no live agy — the same rule Phase 1's D-10 set for the bound warning and the 124 marker. The other required fixtures are held to header *shape* only, so an independent recapture is not forced to carry a borrowed timestamp (F8)."
    - "REQUIREMENTS.md S5 ↔ `delegate-agy-9qp`. The ticket closes when these two corrections land."
---

<objective>
Make the documents state what the check found, and pin them to the evidence.

Criterion 4 inverted when the phase moved from 7 to 1.5: README no longer disclaims the assumption, it states the verified fact carrying the `agy` version and the date it was verified against. D-16 supplies the mechanism — the version and date are fixed literals defined once, quoted verbatim in README, and asserted against the fixture headers by a case that runs with no live agy, so a document edited without its evidence goes red in seconds rather than at the next quota spend.

D-18's two stale claims in REQUIREMENTS.md are corrected here rather than filed. This follows Phase 1's D-17/D-18 precedent exactly: a sentence the change falsifies is fixed in the same change. `delegate-agy-9qp` closes when they land.

D-05's omission is recorded where the gate is written, so a later reader finds a decision rather than an oversight.

One honesty rule governs the whole plan: **write what the ledger recorded, not what this phase expected.** The planning record predicts `--model` accepts both ids and display names. If plan 01.5-05's `model-arg-accepts` row says otherwise, README states what the check found and the SUMMARY flags the divergence from criterion 4's wording for the user's call. Documenting a prediction as a verified fact would reproduce, in a new place, the exact defect this phase exists to eliminate.

**On an `unverified` `model-arg-accepts` row** *(review round; codex proposed a phase gate blocking criterion-4-complete unless the row is conclusive — recorded here as a decision rather than adopted).* The review round's F6 fix removed the likeliest cause: the row now aggregates a free id observation with the display-name call, so it reports `unverified` only when a billed call genuinely did not happen — quota exhausted, agy unreachable, or the free argv gate refusing the spend. In that case README must **not** state the behavior as fact, because it is not one; the task writes what the ledger shows and the SUMMARY states plainly that criterion 4 is not met and why. That is a phase-completion judgement for the user at `/gsd-verify-work`, made with the evidence in front of them, not a gate this plan can encode — an unreachable agy is exactly the condition D-05 refuses to let block a release, and encoding it here would put the same dependency in a different place. What this plan guarantees is that the gap is *visible*: an unverified row, a named gap in the ledger's closing line, and an explicit SUMMARY statement.

Purpose: close the loop from evidence to document, mechanically.
Output: an updated README, case CC05, corrected REQUIREMENTS.md, and a recorded Note under ROADMAP Phase 6.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/dd/Gemini/delegate-agy/.planning/ROADMAP.md
@/home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md
@/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-CONTEXT.md
@/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-RESEARCH.md
@/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-05-SUMMARY.md

**Source of truth:** code and README live in `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2` on `fix/agy-bridge-resilience`. `.planning/` does **not** exist inside that worktree — it lives only in the main tree at `/home/dd/Gemini/delegate-agy/.planning/`. Task 3 edits absolute paths outside the worktree; tasks 1 and 2 edit inside it.
</context>

## Artifacts this phase produces

Full inventory in `01.5-01-PLAN.md` §"Artifacts this phase produces". This plan contributes:

- **New README anchor:** `### Contract check` under `## Troubleshooting`, anchor `#contract-check`
- **New test case id:** `CC05` (README ↔ fixture-header provenance, positive and negative halves)
- **New literals defined once in `tests/run-tests.sh`:** `_CC_AGY_VERSION`, `_CC_CAPTURE_DATE`, `_CC_DEAD_EXACT_STRING` (the pre-phase claim whose absence is pinned), `_CC_PINNED_FIXTURE` (the single fixture whose header must equal README's literals), and `_CC_REQUIRED_FIXTURES` (the manifest of fixtures that must exist and carry a well-formed header). The last two are the review round's F8 fix.
- **Modified documents:** `README.md` (`:37` model-resolution paragraph, the `Model name rejected` troubleshooting row, the `## File layout` block, a new `### Contract check` section), `.planning/REQUIREMENTS.md` (S5 constraint line, S5 traceability row), `.planning/ROADMAP.md` (Phase 6 Note, and the Phase 1.5 Plans list)

## Alternatives Considered

**How README is held to the captured evidence.**

- **A static `run-tests.sh` case matching fixed literals defined once in the test file (chosen)**: RB03's exact shape at `tests/run-tests.sh:2019-2073`, including its negative half asserting the absence of a reversed claim. LOCKED by D-16. Citation: `01.5-RESEARCH.md` §"Pattern 3: README-literal-matches-fixture-literal provenance", quoting RB03's own tautology-avoidance comment verbatim (2026); `01-CONTEXT.md` §D-10, the fixed-literals-defined-once rule this applies (2026).
  Decided by: performance — the mechanism detects a drifted document on the next suite run, in seconds, with no network and no quota.
- **The check comparing live `agy --version` against README on every run**: catches real-world drift, not just editorial drift. Considered as the provenance mechanism and left out of the binding by D-16: it detects nothing until someone spends quota, so it cannot be what holds the document to the evidence. Worth adding later as an extra ledger row. Citation: `01.5-CONTEXT.md` §D-16 and §Deferred Ideas (2026).
  Decided by: performance — a check that only fires when a human pays cannot be the enforcement layer.
- **Generating the README section from the fixture headers at build time**: makes drift structurally impossible. Rejected because this repository has no build step for README and adding one to keep two strings in sync is a generator plus a check that the generator ran, where a `grep -qF` is one line. Citation: `01.5-PATTERNS.md` §"No Analog Found" recording that no codegen precedent exists in this repo (2026).
  Decided by: LOC.

**How widely CC05's provenance equality reaches** *(added by the review round — finding F8; the case as first written required every file under `tests/fixtures/` to carry one byte-identical version and capture date)*.

- **Equality against the one fixture that backs the pinned claim, plus a shape-only header assertion over a short required-fixture manifest (chosen)**: README's dated fact is about what `--model` accepts, and the evidence for it is the captured model list, so `tests/fixtures/agy-models.tsv` is what the literals are pinned to. The manifest (`agy-version.txt`, `agy-models.tsv`, `invalid-model.txt`) must exist and each entry must carry a well-formed provenance header, which keeps a deleted or unstamped fixture red without forcing four independently-captured files to share one timestamp. Citation: `01.5-CONTEXT.md` §D-16, which requires README's version and date to match "the fixture headers" that back the claim, and §D-11/§D-12a, which make `empty-success.txt` explicitly conditional (2026); `tests/run-tests.sh:2019-2073`, RB03's per-literal pinning discipline, read on this tree (2026).
  Decided by: maintenance — a probe may legitimately be re-run alone (a retry, an exhausted quota, an agy version bump mid-phase), and a rule that turns a truthful partial recapture red teaches contributors to hand-edit provenance headers, which is worse than no rule.
- **Global equality across every file matching `tests/fixtures/*` (the original wording)**: strongest possible coupling, and it covers a fixture added later automatically. Rejected because the four fixtures are produced by four probes across plans 01, 03 and 05 at potentially different times, and because it directly contradicts this phase's own conditional-fixture contract — `empty-success.txt` may legitimately not exist at all, so a glob-driven equality assertion is either vacuous on it or red because of it. Citation: `01.5-CONTEXT.md` §D-11, the conditional R6 fixture (2026).
  Decided by: maintenance — a mechanism whose correct outcome is sometimes "hand-edit the evidence" is not an enforcement mechanism.
- **Dropping the fixture side entirely and pinning README against the check's live output**: no coupling problem at all. Rejected for the reason D-16 already gave and §"How README is held to the captured evidence" records above: a check that only fires when a human spends quota cannot be the enforcement layer. Citation: `01.5-CONTEXT.md` §D-16 (2026).
  Decided by: performance — the whole point of CC05 is detecting drift in seconds with no network.

**Where the stale-claim removal is enforced.**

- **A negative `grep -qF` on one specific dead literal, inside CC05 (chosen)**: precisely the shape RB03 already uses for the deleted startup fatal, which it pins across both scripts and README. Citation: `tests/run-tests.sh:2047-2073` read on this tree (2026); `01.5-RESEARCH.md` §Pattern 3's note that RB03's negative half is "equally load-bearing" (2026).
  Decided by: simplicity — one fixed string, zero false positives, and it fails the moment the claim returns.
- **A lint case grepping every tracked document for hedging-shaped claims**: would stop the stale claim from ever reappearing anywhere. Considered under D-18 and declined: its false-positive surface is English prose across README, PROJECT.md, REQUIREMENTS.md and every phase record. Revisit only if the claim actually reappears. Citation: `01.5-CONTEXT.md` §Deferred Ideas (2026).
  Decided by: maintenance — a lint that cries wolf across prose gets disabled, and a disabled lint is worse than none.
- **Extending the scan to `.planning/REQUIREMENTS.md`**: would let CC05 pin D-18's corrections too. Structurally impossible here and correctly so: `.planning/` lives in the main tree while this suite runs from the worktree, and RB03's own scope comment records that the suite must also pass from a release tarball containing no `.planning/` at all. Citation: `tests/run-tests.sh:2064-2068`, verbatim scope comment read on this tree (2026).
  Decided by: ecosystem — the artifact under test ships without the directory, so the test cannot depend on it; REQUIREMENTS.md's correction is verified by human read, as `01.5-VALIDATION.md` §Manual-Only Verifications already records.

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: README states the verified fact, names the check, and prices it</name>

  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/README.md</files>

  <read_first>
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/README.md` lines 27-45 — the `## Type routing` table and the paragraph at `:37` beginning "The Model column is a rule, not a pin", which is the sentence D-17 amends.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/README.md` lines 220-235 — the whole `## Troubleshooting` table, including the `Model name rejected` row and every exit-code row R5 owns. The new exit codes go beside these, and the `Model name rejected` row is the one the invalid-model probe either confirms or contradicts.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/README.md` lines 299-332 — the `### Model name mapping` section, which already states that names resolve against the live list; the verified fact must not contradict it.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/README.md` lines 363-382 — the `## File layout` block, where the new file is named.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 2019-2074 — RB03 in full. Two of its assertions constrain what may be written here: it requires README to quote two operator-visible literals verbatim, and it pins the **absence** of one specific word from README case-insensitively. Read the `grep -qiF` line at `:2072` and treat the word it names as forbidden in any text added by this task.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/fixtures/agy-models.tsv` — its header lines specifically. This is the fixture that backs README's `--model` claim and the one CC05 pins the literals against (F8); quote the version and date from **this** file, not from whichever fixture is read first.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/fixtures/invalid-model.txt` — the captured rejection, which the `Model name rejected` troubleshooting row is rewritten from.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-05-SUMMARY.md` — the recorded ledger. **What this task writes comes from there, not from the planning record's prediction.**
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-CONTEXT.md` §D-07, §D-13, §D-16, §D-17.
  </read_first>

  <action>
Amend the model-resolution paragraph at `README.md:37`. It currently points the reader at `agy models` for current ids and says nothing about what `--model` will accept. Replace the hedge with the fact the ledger recorded, stated plainly, carrying the `agy` version string and the ISO capture date taken verbatim from **`tests/fixtures/agy-models.tsv`'s header** — that fixture is the evidence for this specific claim, and CC05 pins the pair against it alone (F8). Both appear as fixed literals; write them once here and once in the test file, and nowhere else in README.

**Write what the ledger says about both model forms.** Plan 01.5-05's `model-arg-accepts` row now aggregates two real observations — a bare id and a display name, each actually submitted to agy — so the row's evidence names both values and states which forms were accepted. Reproduce that distinction in README rather than collapsing it: if only one form was accepted, README says which one, and if either observation was missing the row reads `unverified` and the next paragraph applies.

**Write what the ledger says.** If the `model-arg-accepts` row reported `verified` for both ids and display names, say both. If it reported `contradicted`, say what it found instead, and flag in the SUMMARY that criterion 4's wording anticipated a different answer so the user can decide whether the roadmap criterion needs amending. Do not write the prediction.

Update the `Model name rejected` troubleshooting row. Its current advice tells the reader an exact string is required, which the invalid-model probe either confirms or contradicts. Rewrite it from the captured `tests/fixtures/invalid-model.txt`: what agy actually says when a model is not recognised, and what the operator should do. The old advice's wording is the literal CC05's negative half will pin as absent, so remove it entirely rather than softening it.

Add a `### Contract check` subsection immediately after the `## Troubleshooting` table, so its exit codes sit beside R5's. It states, in this order: that `tests/contract-check.sh` exists, is repo-only and is run with `bash tests/contract-check.sh` from a clone; that it interrogates the real binary and **spends real quota**, up to three billed delegations per full run, with the actual count printed in the ledger header; that it is deliberately not part of the unit suite and not a release gate, so an agy outage never blocks a tag; and its three exit codes with what each means, explicitly noted as disjoint from the exit codes in the table above so no reader confuses a verdict with a bridge exit.

Add `tests/contract-check.sh` and `tests/fixtures/` to the `## File layout` block, each with a one-line description in the same style as the entries around them.

Three constraints on the prose you add, each mechanical:
  - Do not introduce the word RB03 pins as absent from README at `tests/run-tests.sh:2072`. Read that line before writing; the check is case-insensitive and fixed-string, and a single occurrence anywhere in README turns RB03 red.
  - Do not remove or paraphrase either literal RB03 requires README to quote verbatim (`_RB_WARN_LITERAL` and `_RB_NOTE_LITERAL` at `:2026-2027`). They live in the `## Troubleshooting` table and in the bounding subsection; leave both byte-identical.
  - Do not write any sentence stating or implying that the model-argument behavior is assumed, untested or unverified. Criterion 4 forbids it, and removing such wording is part of this same change.
  </action>

  <verify>
    <automated>cd /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2 && grep -qF "$(grep '^# agy-version: ' tests/fixtures/agy-models.tsv | sed 's/^# agy-version: //')" README.md && grep -qF "$(grep '^# captured: ' tests/fixtures/agy-models.tsv | sed 's/^# captured: //')" README.md && grep -c 'tests/contract-check.sh' README.md | awk '$1>=2{exit 0}{exit 1}' && [ "$(grep -ciF unbounded README.md)" = 0 ] && bash tests/run-tests.sh 2>&1 | tail -3 | grep -q 'FAIL=0'</automated>
  </verify>

  <acceptance_criteria>
    - README contains the `agy` version string from `tests/fixtures/agy-models.tsv`'s header, byte-identical: `grep -cF '<version>' README.md` is at least `1`.
    - README contains the ISO capture date from that same header, byte-identical.
    - README names the check at least twice — once in the new section, once in `## File layout`: `grep -c 'tests/contract-check.sh' README.md` is at least `2`.
    - README states all three exit codes in the new section, and says they are disjoint from the codes in the troubleshooting table above.
    - README states the quota cost with an upper bound: the new section contains the phrase `billed delegation` and a number.
    - README states the check is not a release gate.
    - `grep -ciF unbounded README.md` is `0` — RB03's negative half survives the edit.
    - `grep -cF 'exact string required' README.md` is `0` — the pre-phase claim is gone, not softened.
    - RB03 stays green: `bash tests/run-tests.sh` prints `ok   - RB03`.
    - No sentence in README states or implies the model-argument behavior is assumed or unverified — confirmed by reading `## Type routing`, `## Troubleshooting` and `### Model name mapping` in full and recording that read in the SUMMARY.
    - `bash tests/run-tests.sh` ends `FAIL=0`.
  </acceptance_criteria>

  <done>README states the recorded fact with its version and date, names and prices the check, documents its exit codes beside R5's, and carries no wording left implying the behavior is unverified — with RB03 still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CC05 — the document cannot drift from the evidence without going red</name>

  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>

  <read_first>
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 2019-2074 — RB03 in full: the tautology-avoidance comment explaining why expected bytes are written in the test file rather than extracted from one file and grepped for in another; the once-per-script definition count with its comment filter; the two positive README greps; the negative half pinning a deleted literal across both scripts and README; and the scope comment at `:2064-2068` explaining why `.planning/` is checked by neither.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/README.md` — as task 1 left it.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/fixtures/` — every fixture and its header lines.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-CONTEXT.md` §D-16, §D-18.
  </read_first>

  <behavior>
    - With README and `tests/fixtures/agy-models.tsv`'s header agreeing, and every manifest fixture present and stamped: CC05 passes.
    - Changing the version string in README alone: CC05 fails, naming README.
    - Changing the version string in `agy-models.tsv`'s header alone: CC05 fails, naming that fixture.
    - Changing the capture date in either of those two places alone: CC05 fails.
    - Changing the version or date in `agy-version.txt` or `invalid-model.txt` alone: CC05 **passes**. Those fixtures are recapturable independently and do not back README's `--model` claim; only their header *shape* is asserted. This is the F8 fix, and it must be stated in the case's comment so the looseness reads as a decision rather than an oversight.
    - Stripping the provenance header from any manifest fixture: CC05 fails, naming that fixture — the shape assertion still holds everywhere.
    - Reintroducing the pre-phase "exact string required" advice anywhere in README: CC05 fails on its negative half.
    - Deleting any manifest fixture entirely: CC05 fails rather than passing vacuously.
    - `tests/fixtures/empty-success.txt` absent: CC05 passes. D-11 makes that fixture conditional, and the closing line of the check's own ledger is what reports its absence.
    - CC05 makes no network call, invokes no `agy` real or fake, and adds no measurable time to the suite.
  </behavior>

  <action>
Add a section header `echo "== the docs are held to the captured evidence (CC05) =="` and place CC05 under it, near RB03 so the two provenance cases read together.

Define the expected bytes **in the test file**, exactly as RB03 does and for the reason its comment gives: extracting a string from one file and grepping the other file for it is a tautology that passes whatever the strings become. Five values, each assigned once:
  - `_CC_AGY_VERSION` — the `agy` version string as it appears after `# agy-version: ` in `tests/fixtures/agy-models.tsv`'s header;
  - `_CC_CAPTURE_DATE` — the ISO date as it appears after `# captured: ` in that same header;
  - `_CC_DEAD_EXACT_STRING` — the pre-phase troubleshooting advice task 1 removed, pinned as an absence;
  - `_CC_PINNED_FIXTURE` — the one fixture path the two literals are pinned against, `tests/fixtures/agy-models.tsv`;
  - `_CC_REQUIRED_FIXTURES` — the manifest: `agy-version.txt`, `agy-models.tsv`, `invalid-model.txt`.

Copy RB03's comment discipline: state above the literals why they are written here rather than derived, and state that this case is the seam between captured evidence and the document quoting it.

**Two assertions of different strengths, deliberately.** *(Review round, finding F8 — this case previously required every fixture header to carry one byte-identical version and date, which over-couples four independently-captured files and collides with `empty-success.txt` being conditional.)*

  - **Equality, over `_CC_PINNED_FIXTURE` only.** Assert it carries `# agy-version: $_CC_AGY_VERSION` exactly once and `# captured: $_CC_CAPTURE_DATE` exactly once, and assert README carries both literals. That is the whole coupling: README's dated fact is a claim about what `--model` accepts, and the captured model list is its evidence. Use `grep -qF` / `grep -cF` throughout — a drifted hyphen, quote or parenthesis must be caught, not absorbed by a pattern.
  - **Shape, over every entry in `_CC_REQUIRED_FIXTURES`.** Assert each named file exists, carries exactly one line matching `^# agy-version: ` followed by something non-empty, and exactly one line matching `^# captured: ` followed by an ISO date. No cross-file equality. A probe may legitimately be re-run alone — a retry, an exhausted quota, an agy version bump mid-phase, all of which D-11 and D-12a already anticipate — and a rule that turns a truthful partial recapture red would teach the next contributor to hand-edit a provenance stamp, which is the one thing this case exists to prevent.

Write the reasoning above into the case's comment. A reader who finds only `agy-models.tsv` pinned must find the decision, not conclude the other two were forgotten.

Do **not** enumerate `tests/fixtures/*` with a glob. `empty-success.txt` is conditional by D-11 and may legitimately not exist, so a glob makes the case either vacuous on it or red because of it; the manifest is the explicit alternative, and adding a fixture that backs a new README claim is then a deliberate one-line edit here.

Non-vacuity. Assert the manifest has at least three entries and that **every** entry resolves to an existing file. A manifest whose files are all missing would clear each per-file assertion by vacuous iteration and report success for the wrong reason — the same hole RB01's occurrence floor exists to close, and the reason existence is asserted separately from content.

Negative half. Assert `$_CC_DEAD_EXACT_STRING` appears nowhere in README. RB03 already demonstrates why this half is load-bearing: every other assertion here is positive, so a stale sentence reintroduced *beside* a correct one would keep the suite green.

Scope. Check README and `tests/fixtures/` only. Do not add `.planning/REQUIREMENTS.md` or any other path under `.planning/` — that directory lives in the main tree while this suite runs from the worktree, and the suite must also pass from a release tarball that contains none of it. RB03's scope comment at `:2064-2068` states exactly this; add a matching comment here so the omission reads as a decision. REQUIREMENTS.md's correction is verified by human read, as the validation record already specifies.

Report through the existing `ok`/`bad` helpers with a detail string naming which file and which literal failed.
  </action>

  <verify>
    <automated>cd /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2 && bash tests/run-tests.sh 2>&1 | tee /tmp/rt.out | grep -E '^(ok|FAIL) +- +CC05' ; ! grep -q '^FAIL' /tmp/rt.out && tail -3 /tmp/rt.out | grep -q 'FAIL=0'</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` prints `ok   - CC05` and ends `FAIL=0`.
    - The literals are defined once each: `grep -c '_CC_AGY_VERSION=' tests/run-tests.sh` is `1`, `grep -c '_CC_CAPTURE_DATE=' tests/run-tests.sh` is `1`, `grep -c '_CC_DEAD_EXACT_STRING=' tests/run-tests.sh` is `1`, `grep -c '_CC_PINNED_FIXTURE=' tests/run-tests.sh` is `1`, `grep -c '_CC_REQUIRED_FIXTURES=' tests/run-tests.sh` is `1`.
    - Matching is fixed-string: the CC05 region contains `grep -qF` or `grep -cF` and contains no unanchored `grep -E` over README.
    - **F8 — the equality assertion is narrow and the manifest is explicit.** The manifest names exactly the three unconditional fixtures and does not name the conditional one: `sed -n '/_CC_REQUIRED_FIXTURES=/,/)/p' tests/run-tests.sh | grep -c 'empty-success'` is `0`, and the same range names three `.tsv`/`.txt` entries. The CC05 region contains no `fixtures/*` glob expansion. The version/date equality is applied to `$_CC_PINNED_FIXTURE` and to README only.
    - Non-vacuity: the manifest carries at least three entries and every entry is asserted to exist before its header is read.
    - The region does not reference `.planning`: `sed -n '/CC05/,/^fi$/p' tests/run-tests.sh | grep -c '\.planning'` is `0`.
    - Mutation proofs, each run manually and recorded in the SUMMARY, each reverted after: changing the version in README alone turns CC05 red naming README; changing it in `agy-models.tsv`'s header alone turns CC05 red naming that fixture; adding the dead literal back to README turns CC05 red on the negative half; deleting any manifest fixture turns CC05 red rather than passing; stripping the provenance header from `invalid-model.txt` turns CC05 red on the shape assertion.
    - Counter-proof that the narrowing is real, run manually and recorded in the SUMMARY: changing the capture date in `agy-version.txt`'s header alone leaves CC05 **green**. Under the pre-revision global-equality rule this run was red, which is the over-coupling F8 identified.
    - CC05 adds no live-agy dependency: the region invokes neither `$CONTRACT_CHECK` nor any `agy`.
  </acceptance_criteria>

  <done>Editing README's version or date without editing the model-list fixture's header — or reintroducing the removed claim, or deleting or unstamping a required fixture — turns the suite red in seconds with no live agy involved; the case is proven able to fail on five distinct drifts and proven *not* to fire on a legitimate independent recapture of a fixture that backs no README claim.</done>
</task>

<task type="auto">
  <name>Task 3: The requirement stops claiming it cannot be satisfied</name>

  <files>
    /home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md
    /home/dd/Gemini/delegate-agy/.planning/ROADMAP.md
  </files>

  <read_first>
    - `/home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md` lines 73-78 — S5's acceptance, origin and the Constraint line, which is the first of D-18's two stale claims.
    - `/home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md` lines 81-97 — the whole Traceability table including S5's row, which is the second, plus the 2026-08-19 correction note at `:97` about `delegate-agy-62x`.
    - `/home/dd/Gemini/delegate-agy/.planning/REQUIREMENTS.md` line 88 — R11's traceability row, as the precedent for how much detail a corrected row carries and how a qualifier is recorded rather than hidden.
    - `/home/dd/Gemini/delegate-agy/.planning/ROADMAP.md` §"Phase 6: Ship 1.6.2" — the four success criteria, criterion 3 in particular ("both suites pass on that tree"), which is the gate D-05 declines to widen.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-05-SUMMARY.md` — the recorded ledger, which supplies what S5's status line can now truthfully say.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-CONTEXT.md` §D-05, §D-18.
  </read_first>

  <action>
These files live in the **main tree**, not in the worktree: use the absolute paths above. `.planning/` is absent from `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2` entirely, which is also why CC05 cannot pin these edits and why the validation record lists them as human-read verifications.

**REQUIREMENTS.md, S5's Constraint line** — the single `- Constraint:` bullet at the end of the S5 section, which you read at `:73-78`. It asserts two things: that the requirement is unsatisfiable on this machine, and that the binary is unresponsive and answers every call with the timeout code. Both halves are disproven — the 2026-08-19 probe answered in seconds, and this phase produced a full ledger against a live binary. Replace the whole bullet. It should record what is now true: the requirement is satisfied by `tests/contract-check.sh`; the check is repo-only and spends quota; and the "could not ask" path remains a required, tested capability rather than the expected outcome, because agy was unreachable a week ago and may be again. Do not simply delete the bullet — a Constraint that quietly vanishes leaves a reader wondering whether it was resolved or forgotten, and none of the phrasing it carries today may survive anywhere in the file.

**REQUIREMENTS.md, the S5 traceability row** — the last data row of the table you read at `:81-97`. Its Phase column names the pre-insertion phase number and its Status column carries a blocked-on-an-external-dependency verdict with the same timeout claim. Correct the Phase column to 1.5 and rewrite the Status column entirely from what the ledger supports, following R11's row at `:88` as the precedent for detail: which criteria are met, by what artifact, and any qualifier a reader would otherwise have to reconstruct. If any assumption reported `contradicted` or `unverified`, name it in the row — S5's acceptance is that the check *reports* which assumptions hold, not that they all hold, so an unverified row is not a failure of S5, but a reader must be able to see it without opening the ledger. No fragment of the old Status text may remain.

**REQUIREMENTS.md's ticket columns.** `delegate-agy-9qp` closes when these corrections land, and `delegate-agy-xfa` resolves on the ledger's `gemini-md-binds` verdict whichever way it went. Reflect both in the row and its surrounding notes; do not close tickets from this task — the beads sync step owns that.

**ROADMAP.md, Phase 6.** Add a `**Note**:` beneath Phase 6 recording D-05: its criterion-3 gate covers `bash tests/run-tests.sh` and the installer suite, and is deliberately **not** widened to include `bash tests/contract-check.sh`, because making a release gate depend on a live third-party binary means an agy outage blocks a tag. The check's verdicts inform the release; they do not gate it. Write it as a Note beneath the phase rather than editing a success criterion — the roadmap's own convention at the top of the Phases section puts rationale in Notes, and amending a criterion is the user's call.

Do not edit ROADMAP criterion 1 of Phase 1.5. The review round proposed rewording it to match D-02's narrowing; that proposal is sound but is an open item for the user, not for this task. D-02 records the narrowing and its reasoning, and CC03's comment repeats it where a reader will hit it.

Read every remaining occurrence of S5 across `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md` and `.planning/ROADMAP.md` and confirm none still claims the assumption is unverified or that the requirement is externally blocked. Criterion 4 says *no document* may claim it. Record the files read and the result in the SUMMARY.
  </action>

  <verify>
    <automated>cd /home/dd/Gemini/delegate-agy && [ "$(grep -cF 'cannot be satisfied on the current machine' .planning/REQUIREMENTS.md)" = 0 ] && [ "$(grep -cF 'open, externally blocked' .planning/REQUIREMENTS.md)" = 0 ] && [ "$(grep -cE '^\| S5 \| Phase 7 ' .planning/REQUIREMENTS.md)" = 0 ] && grep -qE '^\| S5 \| Phase 1\.5 ' .planning/REQUIREMENTS.md && grep -qF 'contract-check.sh' .planning/ROADMAP.md</automated>
    <human-check>Read `.planning/REQUIREMENTS.md` §S5 and its traceability row end to end, then the Phase 6 Note in `.planning/ROADMAP.md`. Confirm no sentence claims S5 is unverified, blocked, or unsatisfiable; confirm the row's status matches what the ledger actually recorded, including any contradicted or unverified assumption; and confirm the Phase 6 Note reads as a decision rather than an oversight. `01.5-VALIDATION.md` lists this as a manual-only verification because a prose lint was considered under D-18 and declined for its false-positive surface.</human-check>
  </verify>

  <acceptance_criteria>
    - `grep -cF 'cannot be satisfied on the current machine' .planning/REQUIREMENTS.md` is `0`.
    - `grep -cF 'agy is unresponsive' .planning/REQUIREMENTS.md` is `0`.
    - `grep -cF 'open, externally blocked' .planning/REQUIREMENTS.md` is `0`.
    - `grep -cE '^\| S5 \| Phase 7 ' .planning/REQUIREMENTS.md` is `0` and `grep -cE '^\| S5 \| Phase 1\.5 ' .planning/REQUIREMENTS.md` is `1`.
    - S5 still carries a Constraint line — it is rewritten, not deleted: the S5 section contains a line beginning `- Constraint:`.
    - The S5 traceability row names the artifact (`tests/contract-check.sh`) and names any assumption that reported `contradicted` or `unverified`.
    - `.planning/ROADMAP.md` §Phase 6 contains a `**Note**:` mentioning `contract-check.sh` and stating it does not gate the release.
    - ROADMAP Phase 1.5's success criteria are byte-unchanged: `git -C /home/dd/Gemini/delegate-agy diff .planning/ROADMAP.md` shows no edit inside the Phase 1.5 criteria block.
    - No remaining occurrence of S5 in `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md` or `.planning/ROADMAP.md` claims the assumption is unverified — the files read and the result recorded in the SUMMARY.
    - `bash tests/run-tests.sh` in the worktree still ends `FAIL=0` — these edits are outside it and must not affect it.
  </acceptance_criteria>

  <done>REQUIREMENTS.md records S5 as satisfied by a named artifact under Phase 1.5, with any unsettled assumption visible in the row; the two stale claims are gone; Phase 6's un-widened gate is recorded as a decision; and no tracked document claims the assumption is unverified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `tests/fixtures/*` headers → README prose | Two copies of one provenance string; drift between them is invisible without a mechanism. |
| worktree → main tree | `.planning/` is outside the worktree and outside every test's reach; its correctness rests on human read alone. |
| README → operators | README is the only place an operator learns that running the check costs money. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.5-16 | Repudiation | README stating a *predicted* fact as verified — documenting the planning record's expectation rather than the ledger's finding, reproducing in a new place the exact defect this phase exists to eliminate | high | mitigate | Task 1 reads `01.5-05-SUMMARY.md` as its source and is instructed to write the recorded verdict even when it contradicts criterion 4's anticipated wording, flagging the divergence for the user. CC05 then pins the version and date against the fixture headers, so the claim cannot outlive its evidence silently. `security_block_on: high` satisfied by a mechanism plus a demonstrated red. |
| T-01.5-17 | Tampering | A provenance case that passes vacuously — a manifest whose files are all absent, or a pattern match absorbing a drifted character | medium | mitigate | CC05 asserts the manifest carries at least three entries and that **every** entry exists before its header is read (RB01's occurrence-floor discipline applied to an explicit list rather than a glob), uses fixed-string `grep -qF`/`-cF` throughout, and carries five demonstrated-red mutation proofs including a deleted fixture and an unstamped one. |
| T-01.5-24 | Repudiation | A provenance rule strict enough that its correct outcome is sometimes "hand-edit the evidence" — global version/date equality forced onto four independently-captured fixtures, pressuring a false stamp into a file that was not captured at that moment | medium | mitigate | Byte equality is scoped to the single fixture backing the pinned README claim; every other required fixture gets a header **shape** assertion only, so a legitimate partial recapture stays green while a missing or unstamped fixture still goes red. The narrowing is proven by a counter-mutation (recapturing `agy-version.txt` alone leaves CC05 green) and its reasoning is written into the case's comment so it reads as a decision. |
| T-01.5-18 | Tampering | A stale claim reintroduced beside a correct one, leaving every positive assertion green | medium | mitigate | CC05's negative half pins the absence of the removed advice, exactly as RB03 pins the deleted startup fatal across both scripts and README. |
| T-01.5-19 | Information Disclosure | README understating the cost of running the check, so an operator spends quota unknowingly | medium | mitigate | The new `### Contract check` section states the spend and its upper bound, and the ledger header prints the actual count on every run; both are acceptance criteria. |
| T-01.5-20 | Tampering | An edit to `.planning/ROADMAP.md` silently altering a success criterion while adding the Phase 6 Note | medium | mitigate | The Note is added beneath the phase per the roadmap's own stated convention; an acceptance criterion asserts the Phase 1.5 criteria block is byte-unchanged; and the review round's proposed criterion-1 rewording is explicitly left as an open item for the user. |
| T-01.5-SC | Tampering | Supply chain — npm/pip/cargo installs | low | accept | No package manager involved. `01.5-RESEARCH.md` §"Package Legitimacy Audit" records Not Applicable for the phase; no install task exists in this plan. |

</threat_model>

<verification>
- `bash tests/run-tests.sh` ends `FAIL=0` with `ok - CC05` and `ok - RB03` both present.
- `grep -ciF unbounded README.md` is `0` and `grep -cF 'exact string required' README.md` is `0`.
- The `agy` version and capture date appear byte-identically in README and in `tests/fixtures/agy-models.tsv`'s header; every fixture named in `_CC_REQUIRED_FIXTURES` exists and carries a well-formed provenance header.
- `.planning/REQUIREMENTS.md` carries neither of D-18's two stale claims, and its S5 row names Phase 1.5.
- `.planning/ROADMAP.md` §Phase 6 carries the D-05 Note; Phase 1.5's criteria are unchanged.
</verification>

<success_criteria>
- README states the verified behavior as fact with the `agy` version and the date, and no tracked document claims the assumption is unverified.
- A document edited without its evidence — or an evidence file edited without its document — goes red in one suite run, with no live agy involved.
- The check is named, priced, and its exit codes documented beside R5's; its exclusion from the release gate is recorded as a decision.
</success_criteria>

<output>
Create `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-06-SUMMARY.md` when done.
</output>
