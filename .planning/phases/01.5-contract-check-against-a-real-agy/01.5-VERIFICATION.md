---
phase: 01.5-contract-check-against-a-real-agy
verified: 2026-08-20T05:46:23Z
status: passed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 1.5: Contract check against a real agy Verification Report

**Phase Goal:** An operator can ask the real binary which assumptions hold and get either a recorded answer or an honest "could not ask" — and the later phases build on the recorded answers rather than on hypotheses.
**Verified:** 2026-08-20T05:46:23Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | One command, separate from the unit suite and never invoked by it, exercises the real `agy` and reports which assumptions hold — at minimum `agy models` output, `--model` id/display-name acceptance, and whether a per-run `GEMINI.md` binds against a competing `GEMINI.md` elsewhere | ✓ VERIFIED | `tests/contract-check.sh` (1170 lines) exists, is not referenced by `tests/run-tests.sh` as a driven-under-test target for the unit suite (CC03/CC03m statically prove this — see Key Link Verification). Seven `_cc_probe_*` functions implement all seven D-09 assumptions: `agy-version-shape`, `models-format`, `non-gemini-rows`, `invalid-model-rejection`, `gemini-md-binds`, `sigterm-ignored`, `model-arg-accepts` (`_CC_ASSUMPTIONS` array, contract-check.sh:1132). `_cc_probe_gemini_md_binds` (contract-check.sh:805-935) constructs a `decoy_dir` with its own `GEMINI.md` outside the granted `--add-dir`, and checks the reply for the decoy's unforgeable marker — directly testing the "GEMINI.md in other projects" concern (`delegate-agy-xfa`). A real run recorded in 01.5-05-SUMMARY.md's Appendix shows all seven rows populated with real agy 1.1.13 evidence. |
| 2 | Run against an `agy` that hangs or is absent, the check exits within its own bound with a distinct "unverified" status, names the unresolved assumption, never reports pass; this path is required and tested | ✓ VERIFIED | `CC01` (absent `agy`, no binary anywhere on PATH) and `CC02` (SIGTERM-ignoring hung `agy`, 300s sleep) both present in `tests/run-tests.sh` and both pass in a live run of the full suite (`ok - CC01`, `ok - CC02`, confirmed below). CC01 asserts exit 10, elapsed strictly under an external 30s wrapper. CC02 asserts exit 10 inside `contract-check.sh`'s own internal bound (~7s observed, not the fake's 300s sleep), never a `verified` row. Exit-code computation (contract-check.sh:1147-1169) is over scoring counters only, computed once at the end, never re-parsed from stdout. |
| 3 | Captured output lands in `tests/` fixtures the fake agy can regenerate from — at minimum the `id<TAB>display name` model list, the invalid-model rejection (with its "Available models:" display-name list), and the rc=0-empty-stdout response R6 exists to catch | ✓ VERIFIED (with a stated, pre-authorized honest gap) | `tests/fixtures/agy-models.tsv` (14 real rows, each exactly one TAB, e.g. `gemini-3.7-flash-high\tGemini 3.7 Flash (High)`) and `tests/fixtures/invalid-model.txt` (real agy rc=1 rejection of `gemini-99.9-flash-high`, followed by an `Available models:` list in display names) both exist, carry provenance headers (`agy-version: 1.1.13`, `captured: 2026-08-20`), and are read at runtime by `tests/fake-agy.sh` via `_fake_fixture` (three-tier resolution, fails loud on total-resolution-failure or a zero-row fixture — CC04a/b/c). The third named fixture, `tests/fixtures/empty-success.txt` (the rc=0-empty-stdout / R6 shape), does **not** exist because the headless permission gate did not fire on the one live run attempted. This is not a silent omission: `01.5-CONTEXT.md` §D-11/§D-12a and `01.5-VALIDATION.md` (line 89) explicitly pre-declare this exact outcome as an acceptable "known gap, stated as one" rather than a failure — the probe is designed to route non-reproduction to `unverified` via the non-scoring `_cc_row_note`, and the real run's closing gap line names the missing fixture by hand (`# gap: empty-success-capture (unverified, capture attempt) -- empty-success.txt not written`). This is the phase goal's own "honest could not ask" outcome applied to a fixture rather than a full probe. |
| 4 | README states the verified behavior as fact — `--model` accepts both ids and display names — carrying the `agy` version and verification date; the check re-confirms it; no document claims the assumption is unverified | ✓ VERIFIED | `README.md:37` states, as fact: "`--model` accepts both a bare id (e.g. `gemini-3.1-pro-high`) and a display name (e.g. `Gemini 3.7 Flash (High)`)... verified against `agy` 1.1.13 (captured 2026-08-20)". The `Model name rejected` troubleshooting row (`README.md:229`) is rewritten verbatim from the captured `invalid-model.txt` fixture. `CC05` (`tests/run-tests.sh`, near RB03) pins README's dated claim to `tests/fixtures/agy-models.tsv`'s header by byte equality and passes in a live run (`ok - CC05`). `grep -niE "unverified\|not yet verified\|hypothes"` over README.md returns no match against the `--model` claim. `.planning/REQUIREMENTS.md`'s S5 row reads `met`, no longer `open, externally blocked`; `PROJECT.md:61` states the corrected, dated fact; no remaining document (README, REQUIREMENTS.md, PROJECT.md, ROADMAP.md) claims the assumption is unverified. |

**Score:** 4/4 truths verified (0 present, behavior-unverified)

**Known, honestly-reported gaps (not verification failures — the phase goal explicitly permits an honest "could not ask"):**
- `sigterm-ignored` (D-09 assumption 6) came back **contradicted** against the real binary: agy died on SIGTERM alone (rc=124 at the 8s bound), contradicting `scripts/agy_bridge.sh`'s R11 `-k` escalation rationale. Filed as `delegate-agy-i43` (OPEN, P2), explicitly deferred as phase-boundary work per the plan's own scope rule. `REQUIREMENTS.md`'s S5 row and README both state this plainly rather than smoothing it over — confirmed by direct read.
- `empty-success-capture` (the R6 fixture attempt) reported `unverified` — the headless permission gate did not reproduce on the one live run. Pre-declared as an acceptable outcome in `01.5-CONTEXT.md` §D-11/D-12a and `01.5-VALIDATION.md`. Re-running the check is the stated mechanism for a future capture.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `tests/contract-check.sh` | Repo-only operator tool, third `run_bounded` copy, seven-assumption ledger, disjoint exit codes | ✓ VERIFIED | 1170 lines. Not shipped (`scripts/install.sh` does not reference it — confirmed by CC03's static scan). Byte-identical `run_bounded` block confirmed independently via `diff` against `scripts/agy_bridge.sh:47-352` and `scripts/gemini_shim.sh` — no output, 306 lines each. |
| `tests/fixtures/agy-version.txt` | Captured `agy --version`, provenance header | ✓ VERIFIED | Exists, header carries `agy-version: 1.1.13`, `captured: 2026-08-20`, raw data line `1.1.13`. |
| `tests/fixtures/agy-models.tsv` | Real `agy models` capture, `id<TAB>display name` shape | ✓ VERIFIED | 14 data rows + 3-line header, each row exactly one TAB, read directly from disk. |
| `tests/fixtures/invalid-model.txt` | Real rejection message, `Available models:` in display names | ✓ VERIFIED | Header + real agy stderr/stdout: `Error: invalid model selection...` followed by `Available models:` and a display-name list. |
| `tests/fixtures/empty-success.txt` | Conditional (D-11) — written only if the headless permission gate fires | N/A (not written) | Correctly absent per the pre-declared conditional-fixture contract (`01.5-CONTEXT.md` §D-11, `01.5-06-PLAN.md`'s explicit rejection of a glob-driven equality assertion over this exact fixture). |
| `tests/fake-agy.sh` | Reads `agy-models.tsv` at runtime via `_fake_fixture`, no independent model list | ✓ VERIFIED | `models` branch calls `_fake_fixture agy-models.tsv`; `grep -c 'Gemini 3.6 Flash (High)' tests/fake-agy.sh` (the old canned literal) is not present in the source. |
| `README.md` §Contract check | Names/prices the check, documents 0/10/11 exit codes | ✓ VERIFIED | `README.md:236-247`, disjoint exit-code table present, priced "up to 3 billed delegations per run". |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `tests/contract-check.sh`'s `run_bounded` copy | `scripts/agy_bridge.sh:47-352` | Byte-for-byte identity, pinned by widened `RB02` | ✓ WIRED | `diff` of the extracted `# --- BEGIN/END run_bounded ---` blocks across all three files (`agy_bridge.sh`, `gemini_shim.sh`, `contract-check.sh`) produces no output; `RB02` in `tests/run-tests.sh:2050-2064` compares all three programmatically and passes live. |
| `tests/run-tests.sh` (unit suite) | `tests/contract-check.sh` | Never invoked for a live probe against real `agy`; only invoked under a fully-replaced sanitized PATH holding no real `agy` (CC01) or the fake (CC02) | ✓ WIRED (isolation proven, not just asserted) | `CC03`'s static two-region source scan of `tests/run-tests.sh`'s own text (`_cc_check_scan`) proves every `$CONTRACT_CHECK` invocation in the production region runs under a sanitized PATH or is a non-invocation static read; `CC03m`'s five mutation probes prove the scan can actually fail on five distinct shapes. Both pass live. |
| `tests/fake-agy.sh`'s `models` branch | `tests/fixtures/agy-models.tsv` | `_fake_fixture agy-models.tsv`, three-tier resolution | ✓ WIRED, DATA FLOWS | `AGY_FIXTURES_DIR=tests/fixtures bash tests/fake-agy.sh models` (per 01.5-04-SUMMARY.md, and structurally confirmed by reading the function body) emits the fixture's 14 rows verbatim; `CC04a/b/c` pin each resolution tier and the loud-failure path live. |
| `tests/run-tests.sh`'s `R2`/`R4`/`CC06` | `tests/fixtures/agy-models.tsv` via `_cc_expect_model` | Derive-not-pin: `grep -E '^gemini-[0-9.]+-<class>$' \| sort -V \| tail -1` over the live fixture | ✓ WIRED, DATA FLOWS | `_cc_expect_model` reads the real fixture by default; a version-sort or anchoring regression is mutation-proved to turn `CC06` red (per 01.5-04-SUMMARY.md, code inspected and consistent). |
| `README.md`'s dated `--model` claim | `tests/fixtures/agy-models.tsv`'s header | `CC05`, byte equality on the pinned fixture, header-shape assertion on the rest | ✓ WIRED | `CC05` passes live (`ok - CC05 README's dated --model fact matches the captured fixture header, and the pre-phase claim stays gone`). A document or its evidence edited alone would go red in one suite run with no live agy involved (per 01.5-06-SUMMARY.md's six mutation/counter-proofs, code inspected and consistent with the live pass observed). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full unit suite never touches real `agy`, all CC cases pass | `bash tests/run-tests.sh` (run once, from repo root of the worktree) | `PASS=135 FAIL=0`, real elapsed 3m13s. Includes `ok - CC01`, `ok - CC02`, `ok - CC03`, `ok - CC03m`, `ok - CC04a/b/c`, `ok - CC05`, `ok - CC06`, plus 128 pre-existing cases (R/S/B/SH/RB/I prefixes) all green. | ✓ PASS |
| `run_bounded` triple-copy byte-identity | `diff` of the three extracted `# --- BEGIN/END run_bounded ---` blocks (bridge, shim, contract-check.sh) | No output — byte-identical, 306 lines each | ✓ PASS |
| Fixture provenance and shape | Direct read of `tests/fixtures/{agy-version.txt,agy-models.tsv,invalid-model.txt}` | Headers present, TSV shape confirmed (one TAB per row), rejection message matches the "Available models:" claim README makes | ✓ PASS |
| README states the verified fact, no unverified wording remains | `grep`/read of `README.md` model-resolution paragraph and troubleshooting table | Fact stated with version + date; old `exact string required` claim absent | ✓ PASS |

No live billed run against real `agy` was performed by this verification (the phase's own 01.5-05-SUMMARY.md documents the one required live run and its billed cost; re-running it would incur additional real quota spend with no new evidence, since the ledger's declared row order and exit-code logic are independently confirmed by source read and the unit suite's scratch-copy proofs). A real `agy` 1.1.13 binary is present on this host's PATH (`/usr/bin/agy`), confirming the environment could reproduce the live run if desired, but re-spending quota was judged unnecessary for goal verification.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| S5 | 01.5-01 through 01.5-06 (all six plans declare `requirements: [S5]`) | "Verifiable against a real `agy`" | ✓ SATISFIED | `.planning/REQUIREMENTS.md:93` — S5 row reads `Phase 1.5 \| xfa \| met`, naming all six verified assumptions and the one contradicted one plainly. No orphaned requirements: S5 is the only requirement ID this phase's plans declare, and it is the only one REQUIREMENTS.md maps to Phase 1.5. |

### Anti-Patterns Found

None. `grep -n -E "TBD\|FIXME\|XXX"` and `grep -n -iE "TODO\|HACK\|PLACEHOLDER\|not yet implemented\|coming soon"` over `tests/contract-check.sh`, `tests/fake-agy.sh`, and `README.md` return no matches (the only `XXXXXX`-shaped hits are `mktemp -d` template suffixes, not debt markers).

The phase's own code review (`01.5-REVIEW.md`, `status: issues_found`) recorded 3 warnings and 1 info item, no blockers or criticals:
- **WR-01**: three probe scratch directories clean up only on the happy path, not on interrupt (leaks under `/tmp` on Ctrl-C). Does not affect goal achievement — a hygiene issue, not a correctness one.
- **WR-02**: `gemini-md-binds`'s `verified` (declined) verdict is reached by absence-of-evidence (no cksum, no raw nonce, no fabrication) rather than positive proof of a decline. A real limitation of black-box behavioral testing, not a stub or a fabricated result — the row's evidence text states exactly what was and wasn't observed.
- **WR-03**: `tests/run-tests.sh` (the actual unit-suite entry point) and `tests/fake-agy.sh` are never named in README's "File layout"/testing guidance. A documentation completeness gap, not a goal-achievement blocker — `tests/contract-check.sh` (the phase's actual deliverable) is correctly and prominently documented.
- **IN-01**: `RUN_BOUNDED_KILLED` is written but never read in `contract-check.sh` (dead output, informational only).

None of these change the phase-goal verdict; they are pre-existing findings from the phase's own review gate, correctly classified as non-blocking there and reconfirmed as non-blocking here.

### Human Verification Required

None. All four ROADMAP success criteria are verifiable from source, from independently-reproduced diffs, and from a live (non-billed) run of the full unit suite. No visual, real-time, or subjective-quality judgment is required for this phase's deliverable (a CLI operator tool and documentation).

### Gaps Summary

No gaps block phase completion. Two pre-declared, honestly-reported outcomes exist and are documented above: the `sigterm-ignored` contradiction (ticketed, deferred by design) and the `empty-success-capture` non-reproduction (pre-authorized by the phase's own `01.5-CONTEXT.md`/`01.5-VALIDATION.md` as an acceptable "known gap, stated as one" — exactly the phase goal's "honest could not ask" outcome, applied to a fixture rather than a full assumption). Both are visible in the ledger's own closing gap line and in `REQUIREMENTS.md`, not hidden.

One non-blocking loose end outside this phase's code scope: `delegate-agy-9qp` is still `OPEN` in `bd` though 01.5-06-SUMMARY.md states its correction has landed and only the beads-sync step remains to close it — a tracker-hygiene item, not a code or documentation gap.

---

_Verified: 2026-08-20T05:46:23Z_
_Verifier: Claude (gsd-verifier)_
