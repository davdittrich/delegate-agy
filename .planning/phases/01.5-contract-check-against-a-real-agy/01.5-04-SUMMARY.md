---
phase: 01.5-contract-check-against-a-real-agy
plan: 04
subsystem: testing
tags: [bash, test-harness, fixtures, contract-testing, agy]

# Dependency graph
requires:
  - phase: 01.5-03
    provides: "tests/fixtures/agy-models.tsv (14-row real agy capture) and tests/fixtures/invalid-model.txt, both provenance-headed and human-read before commit"
provides:
  - "tests/fake-agy.sh's models branch reads tests/fixtures/agy-models.tsv at runtime via _fake_fixture -- the fake carries no independent model list and cannot disagree with the fixture"
  - "_fake_fixture NAME: three-tier fixture resolution (AGY_FIXTURES_DIR, dirname \"$0\"/fixtures, $AGY_PLUGIN_DIR/tests/fixtures) that fails loud and non-zero, naming every path tried, on total resolution failure or a zero-row fixture -- never a silent empty list"
  - "_cc_fixtures_beside DIR: plumbs tests/fixtures/ beside all five cp \"$HERE/fake-agy.sh\" sites in tests/run-tests.sh, FATAL on a missing source directory"
  - "_cc_expect_model CLASS [FIXTURE_PATH]: derives the expected model id with the shipped grep|sort -V|tail -1 rule, defaulting to the real fixture; R2 and R4 use it with no path, CC06 drives it with synthetic lists via its second parameter (F3)"
  - "CC04a/b/c: each fixture-resolution tier proven individually (tier 1 beats tier 2, tier 2 after a copy, no-tier and zero-row both fail loud with separately-captured streams)"
  - "CC06: the shipped derivation pinned at version-sort precision (3.10 over 3.7), $-anchored prefix adjacency, and determinism -- both mutation-tested red"
affects: [01.5-05, 01.5-06, phase-2]

# Actuals (#2632)
actuals:
  tokens: 15400
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Runtime-fixture-read over codegen or a heredoc: the fake has no copy of the model list to drift from, closing the exact drift vector the 1.6.1 incident exposed (D-14)"
    - "Derive-not-pin for every model-id assertion: R2, R4, and CC06 all route through _cc_expect_model rather than comparing against a literal, so a recapture cannot leave a stale expectation passing silently (D-15a)"
    - "Fixtures travel with the binary, never with a widened environment: five cp sites gained a paired _cc_fixtures_beside call; RB27's deliberately bare env -i and _install_in's whitelist stayed untouched (D-14a)"
    - "Repo-local RED/GREEN idiom for shell test-harness changes with no separate test file (established in 01.5-03, reused here): RED commits new helper functions unwired (no behavior change, suite green throughout); GREEN wires them in and repairs whatever the swap makes red, in the same commit"

key-files:
  created: []
  modified:
    - tests/fake-agy.sh
    - tests/run-tests.sh

key-decisions:
  - "_fake_fixture's failure-diagnostic always names all three candidate paths, even when a tier's governing env var (AGY_FIXTURES_DIR, AGY_PLUGIN_DIR) is unset -- built as three fixed candidate-description strings computed up front, not conditionally appended only when a tier was actually attempted. Without this, an unset AGY_PLUGIN_DIR would silently drop tier 3 from the diagnostic, and CC04c's 'three named paths' assertion would be unmeetable by construction."
  - "_cc_expect_model's own docstring named 'CC06' inline while CC06 didn't exist yet (written during task 2). Task 3's own acceptance checks use `sed -n '/CC06/,/^rm -rf/p'` to scope assertions to CC06's region; the earlier mention made that range start at the docstring instead of the real section, silently widening (or truncating) every region-scoped grep. Reworded the docstring to describe the caller without the literal case id -- found and fixed while verifying task 3's own acceptance criteria (Rule 1)."
  - "CC06's ok/bad messages originally said 'sort -V precision' -- literal text, not code, but the region-scoped acceptance check `grep -c 'sort -V'` over CC06's block can't distinguish a descriptive string from an actual invocation. Reworded to 'version-sort precision' so the check target (no reimplementation of the rule inside the case) is what the grep actually measures."

patterns-established:
  - "Region-scoped acceptance checks (sed -n '/CASE_ID/,/^rm -rf/p') are load-bearing on case naming discipline: any earlier prose mention of a case id anywhere above its own section silently corrupts the boundary. Applies to every future CC/RB case added to this file."

requirements-completed: [S5]

coverage:
  - id: D1
    description: "tests/fake-agy.sh's models branch reads the captured fixture (tests/fixtures/agy-models.tsv) instead of emitting a hand-written eight-id list -- the fake cannot disagree with what real agy was observed to emit (D-14, D-15)"
    requirement: S5
    verification:
      - kind: unit
        ref: "bash tests/run-tests.sh -- PASS=134 FAIL=0, including R1-R8, SH1-SH14, RB01-RB29, I1-I18"
        status: pass
      - kind: manual_procedural
        ref: "AGY_FIXTURES_DIR=tests/fixtures bash tests/fake-agy.sh models emits 14 rows matching tests/fixtures/agy-models.tsv verbatim; grep -c 'Gemini 3.6 Flash (High)' tests/fake-agy.sh == 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "_fake_fixture's three-tier resolution (AGY_FIXTURES_DIR, dirname \"$0\"/fixtures, $AGY_PLUGIN_DIR/tests/fixtures), each tier proven individually, and total-failure/zero-row-fixture both proven loud and non-zero, never a silent empty list"
    requirement: S5
    verification:
      - kind: unit
        ref: "CC04a (tier 1 over tier 2, distinguishable synthetic variants), CC04b (tier 2 after a copy, RB27's shape reduced), CC04c (no tier + zero-row fixture, streams captured separately) -- all ok in tests/run-tests.sh"
        status: pass
      - kind: other
        ref: "Mutation proof: _fake_fixture's no-dir branch changed to `return 0` (fall through silently) turns CC04c red (rc=0, no diagnostic named); reverted, suite returns to FAIL=0"
        status: pass
    human_judgment: false
  - id: D3
    description: "R2 and R4 -- the two cases the fake-swap makes red by construction -- now derive their expected model id from the fixture with the shipped rule via _cc_expect_model, never a pinned literal; config/model-map.json and S6 untouched"
    requirement: S5
    verification:
      - kind: unit
        ref: "R2, R4 both ok in tests/run-tests.sh against the real fixture (newest flash-high: gemini-3.7-flash-high); S6 ok, unmodified; git diff --name-only lists no path under config/"
        status: pass
    human_judgment: false
  - id: D4
    description: "CC06 pins the shipped grep|sort -V|tail -1 derivation at its version-ordering (two-digit minor), $-anchored prefix-adjacency, and determinism edges, driving the byte-identical helper via its own second parameter rather than reimplementing the rule"
    requirement: S5
    verification:
      - kind: unit
        ref: "CC06 ok in tests/run-tests.sh: gemini-3.10-flash-high beats gemini-3.7-flash-high; gemini-3.8-flash-high unaffected by a longer gemini-3.8-flash-high-extra id present or absent; two runs over the same list agree"
        status: pass
      - kind: other
        ref: "Mutation proof: _cc_expect_model's sort -V changed to plain sort turns CC06 red on the precision assertion (lexical order picks 3.7 over 3.10); reverted, suite returns to FAIL=0"
        status: pass
    human_judgment: false
  - id: D5
    description: "No case beyond R2/R4 went red from the fixture swap -- R3/R3b/R3c/R3d/R6/SH7-SH14 all self-seed synthetic ids into the cache and stay unaffected by what the fake's models branch emits"
    verification:
      - kind: unit
        ref: "Full suite before (PASS=130 FAIL=0, canned list) and after (PASS=134 FAIL=0, fixture-backed + 4 new cases) -- exactly R2 and R4 needed repair, matching the plan's prediction"
        status: pass
    human_judgment: false

duration: 30min
completed: 2026-08-20
status: complete
---

# Phase 1.5 Plan 4: Fake agy reads the captured fixture; the suite's model-list drift vector is closed Summary

**`tests/fake-agy.sh`'s `models` branch now reads `tests/fixtures/agy-models.tsv` at runtime through a three-tier, loud-on-failure resolver instead of emitting a hand-written eight-id list, and every model-id assertion in `tests/run-tests.sh` (`R2`, `R4`, plus the new `CC06`) derives from that fixture with the shipped `sort -V` rule rather than pinning a literal -- closing the exact drift vector that caused the 1.6.1 incident, with `PASS=134 FAIL=0` and two mutation-tested red proofs.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-08-20T05:29:00+02:00 (approx., following 01.5-03's last commit)
- **Completed:** 2026-08-20T05:55:51+02:00 (last task commit)
- **Tasks:** 3
- **Files modified:** 2 (`tests/fake-agy.sh`, `tests/run-tests.sh`)

## Accomplishments
- `_cc_fixtures_beside DIR` added to `tests/run-tests.sh` and called after all five `cp "$HERE/fake-agy.sh" ... agy` sites (`_purebin()`, `_fresh_home()`, I10's nopy dir, RB27's bare-env dir, and the suite-level sandbox setup), FATAL on a missing `tests/fixtures/` source. RB27's `env -i PATH=...` and `_install_in`'s whitelist are unchanged -- the fixtures travel with the binary, not through a widened environment. This task changed no behavior (verified: PASS=130 FAIL=0, canned list still emitted).
- `_fake_fixture NAME` added to `tests/fake-agy.sh`: resolves `tests/fixtures/NAME` through `AGY_FIXTURES_DIR` (tier 1), `$(dirname "$0")/fixtures` (tier 2), `$AGY_PLUGIN_DIR/tests/fixtures` (tier 3), reads it as data only (`grep -v '^#'`, never sourced/eval'd), and fails loud -- non-zero exit, all three candidate paths named on stderr -- on total resolution failure or a fixture with zero data rows. The `models` branch's canned `printf` block (eight hand-written ids) is replaced with a call to `_fake_fixture agy-models.tsv`.
- `_cc_expect_model CLASS [FIXTURE_PATH]` added to `tests/run-tests.sh`: derives the newest matching id with the byte-identical shipped rule (`grep -E '^gemini-[0-9.]+-<class>$' | sort -V | tail -1`), defaulting to the real fixture. `R2` and `R4` -- the two cases the fixture swap makes red by construction, since the real capture's newest `flash-high` is `gemini-3.7-flash-high` versus the old canned list's `gemini-3.6-flash-high` -- now call it with no path, so a recapture can never leave a stale pin passing silently. Both case names/comments dropped their version-number prose.
- No other case went red from the swap: `R3`/`R3b`/`R3c`/`R3d`/`R6`/`SH7`-`SH14` all self-seed synthetic ids into the shared cache and never compare against the fake's live list, exactly matching the plan's prediction. Full suite after the swap: `PASS=130 FAIL=0`.
- `CC04a`/`CC04b`/`CC04c` added: each fixture-resolution tier proven individually with distinguishable synthetic fixture content (`CC04a` proves tier 1 wins over tier 2 specifically, not merely that some tier resolved), `CC04b` reduces RB27's shape to its resolution question, and `CC04c` proves both "no tier resolves" and "a zero-row fixture" fail loud -- non-zero exit, byte-empty stdout, three named paths -- captured through separate stdout/stderr files rather than the merging `_run`.
- `CC06` added: pins the shipped derivation at version-sort precision (a synthetic `3.10` id beats a `3.7` one), `$`-anchored prefix adjacency (a longer id present or absent never changes which shorter id is selected), and determinism (two runs over the same list agree) -- reached entirely through `_cc_expect_model`'s own second parameter over synthetic lists written into the sandbox, never by reimplementing the rule inside the case.
- Two mutation proofs run manually and reverted: `_cc_expect_model`'s `sort -V` swapped for plain `sort` turns `CC06` red on the precision assertion; `_fake_fixture`'s no-tier branch swapped to fall through silently (`return 0`, no diagnostic) turns `CC04c` red. Both reverted; suite returns to green.
- Final suite: `PASS=134 FAIL=0` (130 prior cases unchanged + `CC04a`, `CC04b`, `CC04c`, `CC06`).

## Task Commits

Each task followed the RED/GREEN split established in 01.5-03 (RED = new helper functions defined, not wired in, no observable behavior change; GREEN = wired in, whatever the swap breaks repaired in the same commit) on `fix/agy-bridge-resilience` in the persistent worktree `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2`:

1. **Task 1 (no-behavior-change, plumbing):** `031e627` -- `test(01.5-04): plumb tests/fixtures/ beside every copied fake agy (D-14a)`
2. **Task 2 RED:** `a4ee0b0` -- `test(01.5-04): _fake_fixture and _cc_expect_model defined, not wired (RED)`
3. **Task 2 GREEN:** `729002c` -- `feat(01.5-04): fake agy reads the captured fixture; R2/R4 derive, not pin (GREEN)`
4. **Task 3:** `1ba2d4e` -- `test(01.5-04): CC04 pins the three resolution tiers and the loud failure; CC06 pins the derivation's edges`

**Plan metadata:** this commit, on `master` (docs: complete 01.5-04 plan)

## Files Created/Modified
- `tests/fake-agy.sh` -- `AGY_FIXTURES_DIR` documented in the header block; `_fake_fixture NAME` added (three-tier resolution, loud-on-failure, data-only read); `models` branch's canned eight-id `printf` block replaced with `_fake_fixture agy-models.tsv`. `--version` branch untouched (SH12 asserts it verbatim).
- `tests/run-tests.sh` -- `_cc_fixtures_beside DIR` added and called after all five `cp` sites; `_cc_expect_model CLASS [FIXTURE_PATH]` added; `R2` and `R4` repaired to derive via `_cc_expect_model flash-high`; `CC04a`/`CC04b`/`CC04c`/`CC06` sections added at end of file.

## Decisions Made
See `key-decisions` in frontmatter: always naming all three candidate paths in `_fake_fixture`'s diagnostic regardless of which env vars were set (required for CC04c's "three named paths" assertion to be meetable), and the two self-referential-wording fixes to `_cc_expect_model`'s docstring and CC06's assertion messages, both discovered while verifying task 3's own region-scoped acceptance checks.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `_cc_expect_model`'s docstring named "CC06" before CC06's section existed, corrupting task 3's own region-scoped acceptance checks**
- **Found during:** Task 3, while running the plan's own literal acceptance-check commands (`sed -n '/CC06/,/^rm -rf/p' tests/run-tests.sh | grep -c 'sort -V'`) against the newly-written CC06 case
- **Issue:** Task 2's `_cc_expect_model` docstring (written before CC06 existed) said "...while CC06 (task 3) drives this same byte-identical derivation...". `sed`'s `/CC06/,/^rm -rf/` range starts at the *first* line matching `/CC06/` in the file -- that docstring mention, not CC06's actual `echo "== ... (CC06) =="` header dozens of lines later. The region-scoped `sort -V` count (expected `0`, meaning no reimplementation of the rule inside the case) instead scanned from the docstring through the middle of the file and returned `3`, a false failure of the acceptance check even though the actual code was correct.
- **Fix:** Reworded the docstring to describe the caller ("task 3's derivation-edges case") without the literal string "CC06", so the first `/CC06/` match in the file is CC06's own section header.
- **Files modified:** `tests/run-tests.sh`
- **Verification:** `sed -n '/CC06/,/^rm -rf/p' tests/run-tests.sh | grep -v '^[[:space:]]*#' | grep -c 'sort -V'` now returns `0`; full suite re-run, `PASS=134 FAIL=0`.
- **Committed in:** `1ba2d4e` (part of task 3's commit)

**2. [Rule 1 - Bug] CC06's own `ok`/`bad` messages contained the literal string "sort -V", also tripping its own region-scoped negative check**
- **Found during:** Task 3, same acceptance-check run as above
- **Issue:** After fixing deviation 1, the region-scoped `sort -V` count still returned `2` -- CC06's own descriptive assertion messages ("CC06 shipped derivation: sort -V precision, ...") are plain text, not code, but the check cannot distinguish a description from an invocation.
- **Fix:** Reworded to "version-sort precision" in both the `ok` and `bad` message strings.
- **Files modified:** `tests/run-tests.sh`
- **Verification:** Region-scoped count returns `0`; suite still `PASS=134 FAIL=0`.
- **Committed in:** `1ba2d4e` (part of task 3's commit)

---

**Total deviations:** 2 auto-fixed (2 bugs, both discovered while directly running this plan's own literal acceptance-check commands against the newly-written code, both self-contained to the new CC06/`_cc_expect_model` text with no functional-code impact)
**Impact on plan:** None on scope. Both fixes are wording-only corrections to comments and assertion messages so the plan's own literal acceptance-check commands measure what they intend to measure; no runtime behavior changed. The underlying `_fake_fixture` and `_cc_expect_model` logic was correct throughout -- confirmed by the two mutation proofs, which turned the intended case red both before and after the wording fixes.

## Issues Encountered
None beyond the two auto-fixed wording issues above.

## User Setup Required
None.

## Next Phase Readiness
- The suite's fake now tracks captured real `agy` output end to end: `tests/fake-agy.sh` has no independent model list, every model-id assertion (`R2`, `R4`, `CC06`) derives from `tests/fixtures/agy-models.tsv` with the shipped rule, and a change in what `agy models` emits can no longer leave the suite green without also updating the fixture.
- `S5` in `.planning/REQUIREMENTS.md`'s traceability table still reads "Phase 7 | open, externally blocked" -- unchanged by this plan, matching 01.5-01/02/03's handling. `requirements mark-complete S5` will be run as part of state sync and is expected to no-op again (row text doesn't match the tool's trigger patterns); full closure is 01.5-05/06's scope (remaining D-09 assumptions and the README statement).
- No blockers for 01.5-05.

---
*Phase: 01.5-contract-check-against-a-real-agy*
*Completed: 2026-08-20*

## Self-Check: PASSED
- FOUND: tests/fake-agy.sh
- FOUND: tests/run-tests.sh
- FOUND: .planning/phases/01.5-contract-check-against-a-real-agy/01.5-04-SUMMARY.md
- FOUND: commits 031e627, a4ee0b0, 729002c, 1ba2d4e (fix/agy-bridge-resilience)
