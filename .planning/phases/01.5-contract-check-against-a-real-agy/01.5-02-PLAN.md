---
phase: 01.5-contract-check-against-a-real-agy
plan: 02
type: execute
wave: 2
depends_on: ["01.5-01"]
files_modified:
  - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh
autonomous: true
requirements: [S5]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Run against an `agy` that ignores SIGTERM and sleeps 300s, `bash tests/contract-check.sh` returns inside its own `-k` escalation, exits 10, names the assumption it could not settle, and never reports a verified row (D-08a, criterion 2)."
    - "Every invocation of `tests/contract-check.sh` that appears anywhere in `tests/run-tests.sh`'s own source runs with `PATH` fully replaced by a sanitized directory that holds no real `agy` — asserted by a static scan of the harness against itself (D-02)."
    - "The isolation scan cannot pass vacuously: it fails when it matches zero invocations, and its mutation probes prove it reports an invocation that reaches the ambient PATH (D-02, RB01/RB01m shape)."
    - "edge:boundary (delivered here) — CC02 measures elapsed wall time against the external `timeout 30` wrapper and asserts it strictly below the wrapper's bound, so 'returned' is distinguished from 'was reaped by the net'."
  artifacts:
    - /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh
  key_links:
    - "CC02 ↔ `fake-agy.sh`'s `FAKE_AGY_VERSION_HANG` (`tests/fake-agy.sh:117-121`) — the hang mode Phase 1 already built; no new fake mode is needed."
    - "CC03's scan ↔ every `\"$CONTRACT_CHECK\"` occurrence in `tests/run-tests.sh` — the seam that keeps criterion 1's real property (no live binary during automated testing) enforceable as the suite grows."
---

<objective>
Complete criterion 2's required "could not ask" path and pin the isolation invariant that lets the suite drive the check at all.

D-08 names two shapes and this plan lands the second one plus the invariant that makes both legal. CC02 covers the hang — the path the `-k` escalation exists for and the one criterion 2 names first; CC01 already covered the absent binary in plan 01.5-01. CC03 then enforces D-02: criterion 1's "never invoked by it" is narrowed to **the unit suite never reaches a real `agy` through the check**, because the literal reading collides head-on with criterion 2, which requires the "could not ask" path to be *tested* — and testing it means driving the command.

That narrowing is a reinterpretation of a success criterion, so it is recorded here in the plan as well as in D-02, and a reviewer reading criterion 1 literally finds the reasoning rather than a violation. The suite gains the ability to name the file and loses, by assertion, any path to the live binary.

Purpose: make the check's failure path a tested deliverable rather than a hope, and make the isolation property mechanically enforced rather than conventional.
Output: cases CC02, CC03 and CC03m in `tests/run-tests.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/dd/Gemini/delegate-agy/.planning/ROADMAP.md
@/home/dd/Gemini/delegate-agy/.planning/STATE.md
@/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-CONTEXT.md
@/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-PATTERNS.md
@/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-01-SUMMARY.md

**Source of truth:** `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2` on `fix/agy-bridge-resilience`. Never read `master`.
</context>

## Artifacts this phase produces

The full phase-wide inventory is declared once in `01.5-01-PLAN.md` §"Artifacts this phase produces". This plan contributes:

- **New test case ids:** `CC02` (hung-`agy` "could not ask"), `CC03` (isolation static scan), `CC03m` (the scan's mutation probes)
- **New shell functions in `tests/run-tests.sh`:** none — CC03 reuses the `_rb_agy_scan`-style inline scan shape rather than adding a helper with one caller
- **New env knobs:** none
- **New files:** none

## Alternatives Considered

**Outer-bound shape for the "could not ask" hang case.**

- **SH11's external `timeout N` wrapper (chosen)**: `_run OUT RC timeout 30 bash "$CONTRACT_CHECK" …`, exactly the shape at `tests/run-tests.sh:1060`. Citation: `01.5-RESEARCH.md` §"Pitfall 3: T4/T5/SH11 are not one uniform outer-timeout pattern" (2026), which corrects D-08's own premise after re-reading all three cases.
  Decided by: performance — a regression to an unbounded probe inside `contract-check.sh` fails this case in ~30s instead of stalling the suite for the fixture's full 300s sleep, a 10× feedback-latency difference on the exact failure this case exists to catch.
- **T4/T5's internal `--timeout N` plus a wall-clock assertion**: no external wrapper; the case leans on the bounded script's own bound (`tests/run-tests.sh:618-639`). Rejected on a correctness argument, not a preference: a check whose own bound has regressed cannot be the thing that bounds the test of its own bound, so this shape is structurally unable to fail on the regression it is supposed to catch. Citation: `01.5-RESEARCH.md` §Pitfall 3, `[VERIFIED: tests/run-tests.sh:618-628,630-639,1046-1069,169-179]` (2026); GNU Coreutils Manual §`timeout` invocation, coreutils 9.7 (2025) — <https://www.gnu.org/software/coreutils/manual/html_node/timeout-invocation.html>.
  Decided by: performance — this shape's worst case is an unbounded stall of the whole suite, where the chosen shape's worst case is a 30s failure.
- **`_run_sanitized`'s `"$_TIMEOUT_NET" --foreground -k 5 30` net**: already exists, already whitelist-enforced (`tests/run-tests.sh:169-179`). Not rejected so much as *composed*: CC02 uses the sanitized PATH machinery for isolation and adds SH11's explicit `timeout 30` on top, because the suite-level net is documented at `:131-145` as "a suite-level safety net, never the mechanism under test" and reusing it as the assertion's bound would blur exactly that line. Citation: GNU Coreutils Manual §`timeout` invocation, coreutils 9.7 (2025) — <https://www.gnu.org/software/coreutils/manual/html_node/timeout-invocation.html>, for `--foreground` and `-k` semantics.
  Decided by: simplicity — one explicit wrapper the case owns, rather than overloading a shared net with a second contract.

**Enforcement shape for the isolation invariant.**

- **Static source scan of the harness against itself, RB01's shape (chosen)**: `tests/run-tests.sh:1899-1975` already does exactly this for R11's bounding invariant, complete with an occurrence floor (`RB01_TOTAL -lt 1` ⇒ fail) and RB01m's four mutation probes including the two shapes that walked past an earlier per-line version. Citation: `01-CONTEXT.md` §D-12 "static scan plus runtime proof" (2026); `01.5-CONTEXT.md` §D-02 (2026).
  Decided by: maintenance — the invariant survives every future case anyone adds, without that author needing to know the rule exists.
- **A runtime guard inside `contract-check.sh` that refuses to run when a marker env var is set by the harness**: a test-only override living in the artifact under test. Rejected outright: Phase 1's D-15 set the standing rule that no test-only override may exist in code that ships or is audited, and this would put the suite's convenience inside the deliverable. Citation: `01-CONTEXT.md` §D-15 (2026).
  Decided by: simplicity — zero new surface in the deliverable versus one branch plus a documented knob.
- **Convention plus code review**: no mechanism, a note in the harness header. Rejected because the failure mode is silent and delayed: a future case that reaches a real binary spends quota and passes green. Citation: `01.5-CONTEXT.md` §Specific Ideas, "criterion 1 is satisfied more strictly than written, not more loosely" (2026).
  Decided by: maintenance — an unenforced convention decays at the first contributor who has not read it.

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: CC02 — a hung agy does not hang the operator who ran the check</name>

  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>

  <read_first>
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` — the CC01 block added by plan 01.5-01, at the end of the file under the `== the check answers 'could not ask' (CC01, CC02) ==` header. CC02 is its sibling and shares its sanitized-PATH plumbing.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 1046-1069 — SH11 in full: the `timeout 30` wrapper, the `_SH11_START` / `_SH11_ELAPSED` pair, and the comment explaining why rc=124 from the outer bound IS the failure signal.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 96-180 — `_purebin()` and `_run_sanitized()`, including the verbatim fd-9 file-capture rationale at :152-168.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/fake-agy.sh` lines 27-32 and 116-122 — `FAKE_AGY_VERSION_HANG` and `FAKE_AGY_MODELS_HANG`, the SIGTERM-ignoring hang modes Phase 1 built, and the `--version` dispatch branch they gate.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-CONTEXT.md` §D-02, §D-08, §D-12a.
  </read_first>

  <action>
Add CC02 immediately after CC01, under the same section header.

Drive `bash "$CONTRACT_CHECK"` through `_purebin()`'s sanitized directory as the entire `PATH` — the fake `agy` is already copied there as `agy` by `_purebin()` at `:118`, so this is the fake-present counterpart to CC01's fake-absent case. Set `FAKE_AGY_VERSION_HANG=1` so the fake traps SIGTERM and sleeps 300s on the `--version` path, which is the only probe the check makes at this point in the phase. Set `AGY_CONTRACT_TIMEOUT=2` so the check's own bound fires quickly.

Wrap the whole invocation in an external `timeout 30`, SH11's shape at `:1060` — not T4's internal-flag shape at `:628`. Capture stdout through a file and read it back, never through a command substitution, for the fd-9 reason stated verbatim at `:152-168`: the check opens fd 9 on its own original stderr, the bounded child inherits what it is given, and under `$(...)` a surviving descendant holds the capture pipe open and turns a ~10s red case into a ~5min one.

Record `date +%s` either side and compute `_CC02_ELAPSED`.

Assert all five of:
  - exit status is exactly `10` — the distinct unverified code. Not `0`. Not `124`: a 124 here would mean the outer net reaped the check, i.e. the check's own bound did not fire, which is the regression this case exists to catch;
  - stdout carries a row whose first field is `agy-version-shape` and whose second field is `unverified`;
  - that row's evidence is non-empty and mentions the bound or the rc the internal `run_bounded` returned — D-07's rule that an internal 124 lands in the evidence and never in `$?`, and D-12a's rule that an unverified row records what was attempted;
  - no line in stdout has `verified` as its second field — criterion 2's "never reports pass" asserted in the ledger text, not only in `$?`;
  - `_CC02_ELAPSED` is strictly less than 25, mirroring SH11's own margin against its 30s wrapper, so "returned on its own" is distinguished from "was reaped by the net".

The fake's hang mode records no PID file, so nothing needs reaping here beyond what the harness `cleanup` trap at `:50-59` already does. Do not switch to a fork-hang mode to get PID recording — that changes what is under test.

Finish with `rm -f "$HOME/.cache/agy-bridge-models"`, the suite's standing cache-hygiene convention.
  </action>

  <verify>
    <automated>cd /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2 && bash tests/run-tests.sh 2>&1 | tee /tmp/rt.out | grep -E '^(ok|FAIL) +- +CC0[12]' ; ! grep -q '^FAIL' /tmp/rt.out</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` prints `ok   - CC02` and ends `FAIL=0`.
    - The CC02 region sets `FAIL`-free hang mode: `sed -n '/CC02/,/rm -f/p' tests/run-tests.sh | grep -c 'FAKE_AGY_VERSION_HANG=1'` is `1`.
    - The CC02 region wraps with an external bound: it contains `timeout 30` and contains no `--timeout` flag passed to `contract-check.sh`.
    - The CC02 region asserts `-eq 10` and asserts elapsed with `-lt 25`.
    - The CC02 region captures through a file: it contains no `$(` command substitution wrapping the `bash "$CONTRACT_CHECK"` invocation.
    - Whole-run wall clock stays under 3 minutes on this host — CC02 must add roughly seconds, not the fake's 300s sleep.
    - Mutation proof, run manually and recorded in the SUMMARY: temporarily changing `contract-check.sh`'s `--version` probe to call `"$AGY_BIN" --version` directly instead of through `run_bounded` makes CC02 fail with rc=124 from the outer wrapper and an elapsed at the 30s ceiling; reverting restores green in seconds.
  </acceptance_criteria>

  <done>A SIGTERM-ignoring `agy` makes the check exit 10 within its own bound, with `agy-version-shape` named `unverified`, its evidence recording the attempt, and no verified row anywhere — and CC02 catches a regression to an unbounded probe in ~30s rather than ~5min.</done>
</task>

<task type="auto">
  <name>Task 2: CC03 — the suite proves it can never reach a real agy through the check</name>

  <files>/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh</files>

  <read_first>
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 1811-1975 — the whole RB01 block: the section header, `_rb_agy_segments`, `_rb_agy_scan` with its `grep -o` occurrence counting, RB01's `bash -n` guard and occurrence floor, and RB01m's four mutation probes with the comment naming the two shapes that walked past the earlier per-line scan.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` lines 96-180 — `_purebin()` and `_run_sanitized()`, so the scan's notion of "sanitized" matches what the suite actually builds.
    - `/home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2/tests/run-tests.sh` — the CC01 and CC02 blocks added by plan 01.5-01 and task 1 above: these are the two invocation sites the scan must find and clear.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-CONTEXT.md` §D-02 in full, including its reasoning about why criterion 1's literal reading collides with criterion 2.
    - `/home/dd/Gemini/delegate-agy/.planning/phases/01-the-missing-timeout-decision/01-CONTEXT.md` §D-12 and §D-15 — the static-scan-plus-runtime-proof shape and the sanitized-PATH pattern this reuses.
  </read_first>

  <action>
Add a section header `echo "== the suite never reaches a real agy through the check (CC03) =="` and place CC03 and CC03m under it.

Write CC03 as a scan of `tests/run-tests.sh`'s **own source** — the harness reading itself, the same self-referential shape RB01 uses on the shipped scripts. Lead the case with a comment that records D-02's narrowing explicitly: criterion 1 says "separate from the unit suite and never invoked by it"; that is enforced here as *the unit suite never reaches a real `agy` through the check*, because the literal reading collides with criterion 2, which requires the "could not ask" path to be tested and testing it means driving the command. State that the roadmap wording itself is an open item for the user, not something this case edits.

Scan mechanics, mirroring `_rb_agy_scan`'s discipline rather than inventing new machinery:
  - strip comment lines before counting, so a comment naming `$CONTRACT_CHECK` cannot inflate or clear the count — RB01m's `commented` probe exists because that exact hole was real;
  - count **occurrences** with `grep -o`, not lines: two invocations on one logical line must count as two, which is the `twoonone` hole RB01m found;
  - an occurrence is any expansion of the harness's own variable in any form — `"$CONTRACT_CHECK"`, `${CONTRACT_CHECK}`, bare `$CONTRACT_CHECK` — plus any literal `contract-check.sh` path used as a command. Matching only the doubly-quoted form would let a rewrite walk straight past a scan still reporting zero violations. The assignment line itself carries no `$` and is not an occurrence;
  - an occurrence is **cleared** only when its logical line also sets `PATH` to a sanitized directory for that invocation — i.e. the line carries a `PATH=` assignment whose value derives from `_purebin` and does not append `:$PATH`. An occurrence with no such prefix, or one whose `PATH` value ends in `:$PATH`, is a violation. Do not accept `_run_sanitized` as a blanket clearing token without also allowing the direct `PATH="$(_purebin)"` form both CC01 and CC02 use; recognise both explicitly.

Add the occurrence floor, copying RB01's `RB01_TOTAL -lt 1` guard verbatim in spirit: if the scan matches zero occurrences at all, the case FAILS with a detail saying so. A scan that matched nothing would report zero violations for the wrong reason, and a rename would empty it silently.

Write CC03m as the mutation half, following `_rb01m_probe` at `:1946-1961`. Copy `tests/run-tests.sh` into a scratch dir per probe, append one line, run the same scan helper against the copy, and assert the expected verdict. Four probes, chosen to cover the holes RB01m found the hard way:
  - `ambient` — an invocation with no PATH prefix at all. Expect a violation.
  - `appended` — an invocation whose PATH value ends in `:$PATH`, so the host's real `agy` stays reachable. Expect a violation.
  - `commented` — a comment line mentioning the variable. Expect no violation.
  - `twoonone` — one logical line carrying a sanitized invocation and an ambient one. Expect a violation, proving the scan counts occurrences rather than lines.
Assert the occurrence floor on every probe, not only the clean ones, exactly as `_rb01m_probe` does.

Remove the scratch dir when finished, and do not leave a mutated copy of the harness behind.
  </action>

  <verify>
    <automated>cd /home/dd/Gemini/delegate-agy/.worktrees/agy-1.6.2 && bash tests/run-tests.sh 2>&1 | tee /tmp/rt.out | grep -E '^(ok|FAIL) +- +CC03' ; ! grep -q '^FAIL' /tmp/rt.out</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-tests.sh` prints `ok   - CC03` and `ok   - CC03m`, and ends `FAIL=0`.
    - The scan is non-vacuous at run time: CC03's detail string on a forced failure reports an occurrence count of at least `2` (CC01 and CC02), and the case fails when that count is `0`.
    - CC03m declares exactly four probes; `sed -n '/CC03m/,/^rm -rf/p' tests/run-tests.sh | grep -c '_cc03m_probe '` is `4`.
    - The scan filters comments: `sed -n '/CC03/,/^rm -rf/p' tests/run-tests.sh | grep -c "grep -v '\^\[\[:space:\]\]\*#'"` is at least `1`.
    - The scan counts occurrences not lines: the CC03 region contains a `grep -o` (not merely `grep -c` over raw lines).
    - Mutation proof beyond CC03m, run manually and recorded in the SUMMARY: appending a real ambient `bash "$CONTRACT_CHECK"` line to the live `tests/run-tests.sh` turns CC03 red; removing it restores green.
    - No case in the suite invokes `contract-check.sh` with the ambient PATH: after this task, CC03 passing IS that assertion.
  </acceptance_criteria>

  <done>Every `contract-check.sh` invocation in the harness is proven to run under a sanitized PATH with no real `agy`, the scan is proven capable of failing on four distinct shapes including the two that defeated an earlier per-line scan, and the occurrence floor stops a rename from emptying it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `run-tests.sh` → `PATH` | Whether the `agy` a case reaches is the fake or the real binary decides whether the case measures anything. |
| `run-tests.sh` → its own source | CC03 trusts a text scan of the harness to stand in for a runtime property; a scan that can pass vacuously is worse than no scan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.5-05 | Spoofing | A real `agy` reachable on `PATH` during a `contract-check.sh` invocation from the suite — billed quota spent by an automated test, and an isolation assertion that measures nothing | high | mitigate | CC03's static scan requires a full-replacement sanitized `PATH` at every occurrence, rejects a `:$PATH` suffix explicitly, carries an occurrence floor, and is proven able to fail by CC03m's four probes. `security_block_on: high` is satisfied by a mechanism with a demonstrated red, not by convention. |
| T-01.5-07 | Tampering | A vacuous scan — zero matches reported as zero violations, or a per-line count masking a second occurrence on the same line | high | mitigate | Occurrence floor copied from RB01 (`RB01_TOTAL -lt 1` ⇒ fail), `grep -o` occurrence counting rather than line counting, comment stripping before counting, and the `twoonone` and `appended` mutation probes that pin exactly the two holes an earlier per-line scan let through. |
| T-01.5-08 | Denial of Service | A regression to an unbounded probe stalling the suite for the fixture's 300s sleep instead of failing | medium | mitigate | CC02's external `timeout 30` wrapper plus a strict `-lt 25` elapsed assertion — SH11's shape, chosen over T4/T5's internal-bound shape precisely because the latter cannot fail on this regression. |
| T-01.5-06 | Repudiation | An `unverified` row that records no attempt (D-12a) | low | mitigate | CC02 asserts the row's evidence is non-empty and mentions the bound or rc. |
| T-01.5-SC | Tampering | Supply chain — npm/pip/cargo installs | low | accept | No package manager involved; `01.5-RESEARCH.md` §"Package Legitimacy Audit" records Not Applicable for the whole phase. No install task exists in this plan. |

</threat_model>

<verification>
- `bash tests/run-tests.sh` ends `FAIL=0` with `ok - CC01`, `ok - CC02`, `ok - CC03` and `ok - CC03m` all present.
- Total suite wall clock has not grown by minutes — CC02's hang is bounded at 2s internally and 30s externally.
- `git diff --name-only` for this plan lists only `tests/run-tests.sh`.
</verification>

<success_criteria>
- Both of D-08's shapes — hang and absent — are tested, each asserting the distinct unverified exit code, the named assumption, recorded evidence, and elapsed under the bound.
- D-02's invariant is mechanically enforced and demonstrated red on four distinct violation shapes.
- No live `agy` is reachable from `bash tests/run-tests.sh` by any path.
</success_criteria>

<output>
Create `/home/dd/Gemini/delegate-agy/.planning/phases/01.5-contract-check-against-a-real-agy/01.5-02-SUMMARY.md` when done.
</output>
