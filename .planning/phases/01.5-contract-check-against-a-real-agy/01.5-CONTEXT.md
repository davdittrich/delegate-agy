# Phase 1.5: Contract check against a real agy - Context

**Gathered:** 2026-08-19
**Status:** Ready for planning
**Reviewed:** 2026-08-19 by agy (Gemini Pro, repo access) — see `01.5-REVIEWS.md`. Findings folded in as D-12a, D-14a, D-15a, and corrections to D-03 and Existing Code Insights.
**Researched:** 2026-08-19 — see `01.5-RESEARCH.md`. Three further errors in this record were found and corrected: D-14a's copy-site count and the `env -i` whitelist, the `fake-agy.sh` models-block citation (a second master-vs-branch slip), and D-08's outer-timeout precedent (SH11's shape, not T4/T5's). Two constraints added: no `AGY_SKIP_PERMISSIONS=1` in D-10's probe, and a three-delegation budget in D-13.

<domain>
## Phase Boundary

One repo-local command, `tests/contract-check.sh`, interrogates the **real** `agy` binary about the assumptions this plugin is built on and emits an assumption ledger: each assumption is *verified*, *contradicted*, or *unverified*, with the evidence it rests on. Its captured output lands in `tests/` fixtures that `tests/fake-agy.sh` reads at runtime, so the suite's fake tracks real output instead of a hypothesis. README and REQUIREMENTS.md then state the verified behavior as fact, carrying the `agy` version and the date it was verified against.

**In scope:** `tests/contract-check.sh` (new); `tests/fixtures/` (new); `tests/fake-agy.sh`'s canned `models` block plus its fixture-resolution tiers (D-14a); the six `fake-agy.sh` copy sites in `tests/run-tests.sh` and `_install_in`'s `env -i` whitelist, which must carry the fixtures with the binary; the suite cases that go red when the real list replaces the canned one — R2 (`:356`) and R4 (`:473`) — repaired by deriving the expected id from the fixture (D-15a); the `run-tests.sh` cases that pin the isolation invariant, the unverified path, and README-to-fixture provenance; README's model-name statement and the line naming the check; REQUIREMENTS.md's S5 constraint line and traceability row; tickets `delegate-agy-xfa` and `delegate-agy-9qp`.

**Out of scope:** every shipped script and config — `scripts/agy_bridge.sh`, `scripts/gemini_shim.sh`, `scripts/install.sh`, `scripts/uninstall.sh`, and `config/model-map.json` are not edited by this phase (the `run_bounded` block is *copied out of* the scripts, not changed in them; the model map already maps aliases to classes on the branch and needs no `gemini-3.7-*` keys — D-15a). Acting on what the check finds: a contradicted row becomes a ticket for the phase that owns the behavior (S1 → Phase 2, R5/R6 → Phase 3, S3 → Phase 5), not a fix here. Phase 6's release gate is deliberately not widened — see D-05.

</domain>

<decisions>
## Implementation Decisions

### Harness — where the check lives and how it stays out of the suite

- **D-01:** The check is `tests/contract-check.sh`, repo-only. It is not shipped, not installed, and gets no launcher. Criterion 3 already puts the fixtures under `tests/`, so capture and consumption share a directory. An operator with a clone runs `bash tests/contract-check.sh`; an operator with only an installation cannot run it, and that is accepted. — **Reversibility:** reversible — promoting it to a shipped script later is additive, but it would grow Phase 4's installer surface and Phase 5's failure-mode table, which is why it is not done now.

- **D-02:** Criterion 1's "never invoked by it" is enforced as **the unit suite never reaches a real `agy` through the check**, not as "the suite never names the file". The literal reading collides head-on with criterion 2, which requires the "could not ask" path to be *tested* — and testing it means driving the command. What criterion 1 protects is that `bash tests/run-tests.sh` never depends on a live, billed, network-bound binary; that property is what gets asserted. A `run-tests.sh` case scans its own source and asserts every `contract-check.sh` invocation runs under a **sanitized PATH** holding `fake-agy.sh` and no real `agy` — the same D-15 pattern Phase 1 used for the coreutils-less branch, and the same static-scan shape as RB01. The check's real-agy probes stay unreachable from the suite. — **Reversibility:** reversible — the invariant is one test case; tightening or loosening it is local. Recorded explicitly because the narrowing is a *reinterpretation of a success criterion*, and a later reader must find the reasoning rather than the conflict.

- **D-03:** The check bounds its own `agy` calls with a **third verbatim copy** of `run_bounded`, carrying the same `# --- BEGIN run_bounded ---` / `# --- END run_bounded ---` markers. Phase 1's D-08 byte-identical test widens from two files to three. Two facts settled by the review round: the block is **~306 lines** (`scripts/agy_bridge.sh:47-352`, `scripts/gemini_shim.sh:64-369`), a materially larger verbatim copy than a casual reading suggests; and `_rb_extract` (`tests/run-tests.sh:1208` on the branch) already takes a file argument, so widening RB02 (`:1976-1993`) from two extractions to three is mechanical, not a rewrite. This is chosen over extracting the helper to a sourced file — that would cost `gemini_shim.sh` its standalone property, which is the whole reason D-08 chose duplication — and over hard-requiring coreutils, which would make the check unrunnable on exactly the hosts Phase 1 existed to serve, and would leave the bash-watchdog path with no live exercise anywhere. — **Reversibility:** costly — undoing it means either a third, divergent bounding mechanism in a repo whose Phase 1 exists to eliminate exactly that, or the shared-file extraction D-08 declined. Both re-open a settled decision.

- **D-04:** Output is an **assumption ledger, not a PASS/FAIL run**. One line per assumption: name, verdict, and the evidence it rests on. The header carries the `agy` version, the capture date, and how many billed calls the run made. It deliberately does not reuse `run-tests.sh`'s `ok`/`bad` bookkeeping or its `PASS=n FAIL=n` summary, so nobody mistakes a green line for a unit-test pass or wires it into a gate by muscle memory. — **Reversibility:** reversible.

- **D-05:** Phase 6's "both suites pass" gate is **not** widened to include the contract check. Making a release gate depend on a live third-party binary means an agy outage blocks a tag. The check's verdicts inform the release; they do not gate it. State this where the gate is written, so the omission reads as a decision rather than an oversight. — **Reversibility:** reversible.

### Verdict — what the check reports and how "could not ask" is proven

- **D-06:** Every assumption carries one of **three** states: *verified* (asked and agy agrees with what the repo believes), *contradicted* (asked and agy disagrees), *unverified* (could not ask). The run's exit code preserves the distinction: `0` when every assumption is verified, one distinct non-zero code when any assumption is unverified, another when any is contradicted. Collapsing the latter two into a single non-zero loses exactly the distinction criterion 2 was written to preserve — "agy was unreachable" and "agy contradicts the docs" demand different responses. Criterion 2's "never reports pass" therefore holds in `$?`, not only in the ledger text. — **Reversibility:** costly — the codes get documented next to R5's table and an operator may script against them; changing them later is a doc-plus-caller change, though no external consumer exists yet.

- **D-07:** The check's exit codes are **disjoint from R5's** `2` / `3` / `124` / `127` / `137`, so no reader confuses a check verdict with a bridge exit and Phase 3 does not inherit a contract it did not write. A bounded probe that times out internally still sees `124` from `run_bounded`; that becomes an *unverified* row in the ledger, never the check's own exit status. Both the numbers and this mapping are stated in README alongside R5's table. — **Reversibility:** reversible.

- **D-08:** The "could not ask" path is tested from `run-tests.sh`, driving `contract-check.sh` under a sanitized PATH in **two** shapes: (a) `fake-agy.sh` in its SIGTERM-ignoring hang mode, and (b) no `agy` on PATH at all. Each case asserts the distinct unverified exit code, that the ledger **names the assumption** it could not settle, and `_ELAPSED` under the bound. **Copy SH11's shape specifically, not T4/T5's** *(research round correction)*: only SH11 (`tests/run-tests.sh:1059`) wraps the invocation in an external `timeout 30 bash …`, which is what makes a regression to unbounded fail in 30s instead of stalling for the fixture's 300s sleep. T4 (`:614`) and T5 lean on the *bridge's own* `--timeout` flag plus a wall-clock elapsed assertion — a shape the contract check cannot borrow, because a check whose own bound has regressed cannot be the thing that bounds the test of its own bound. The absent-binary case alone is not sufficient: the hang is the path the `-k` bound exists for, and it is the one criterion 2 names first. — **Reversibility:** reversible.

### Probes — which assumptions get asked, and what each costs

- **D-09:** Seven assumptions. Criterion 1's three — what `agy models` emits; whether `--model` accepts ids, display names, or both; whether a per-run `GEMINI.md` in the working directory actually binds — plus four that earn their place: the **invalid-model rejection** (criterion 3 names its fixture by hand, and it is the evidence that the id-vs-display-name answer is not accidental); the **non-`gemini-` rows** in the live list (`claude-sonnet-4-6`, `claude-opus-4-6-thinking`, `gpt-oss-120b-medium` — the fake emits none, so no test has ever seen a list the anchored `^gemini-[0-9.]+-<class>$` matchers must *exclude*, and Phase 2's S1 work rests on that); **whether agy ignores SIGTERM** (PROJECT.md flags it as explicitly not established, and R11's whole `-k` escalation rests on one untested observation); and the **`agy --version` string shape** (the ledger header and README's dated claim both quote it, so its format is load-bearing for criterion 4).

- **D-10:** The `GEMINI.md`-binding assumption is probed **through the real bridge** with a policy that forbids a tool, asserting the tool is actually refused. Not the bare-CWD probe: that reproduces an observation already recorded on `delegate-agy-xfa` and leaves the ticket's actual question — whether the `--sandbox` and `--add-dir WORK_DIR` the bridge additionally passes constrain discovery — unanswered. This is the phase's one structurally necessary billed delegation. — **Reversibility:** reversible.

- **D-11:** R6's `rc=0`-with-empty-stdout case is probed **deliberately**, by driving agy into the headless permission gate that produced it live on 2026-08-19. If the gate fires, the evidence is captured as a fixture; if agy answers instead, that row reports *unverified*. Opportunistic capture is rejected because the fixture criterion 3 names by hand might then simply never be produced, with nothing reporting its absence. Hand-seeding it from the ticket comment is rejected because it yields a fixture nothing in the repo can regenerate — the exact property criterion 3 asks for. — **Reversibility:** reversible.

- **D-12:** The SIGTERM assumption is probed by issuing a **genuinely slow real delegation with a short bound**, signalling at the bound, and observing whether agy dies on SIGTERM or requires the SIGKILL escalation. Signalling the `models` call instead is cheaper but tests a subcommand that may not share the delegation path's process tree, so a clean death there would not license R11's `-k` on delegations. If agy answers before the bound fires, the row reports *unverified* — a wrong answer here would retroactively justify or condemn a shipped mechanism. — **Reversibility:** reversible.

- **D-12a — an assumption that can only ever report *unverified* has not been settled.** *(Review round, F3.)* D-11 and D-12 both route a non-reproducing probe to *unverified*, which is honest but is not an answer. Both must therefore specify their attempt concretely enough to be repeatable and to be *criticised*: D-12 states the payload shape and the exact bound it signals at (a large synthetic-output request against a strict short bound, chosen to maximise the chance of catching agy in flight); D-11 states the tool and the gate it drives at. Where a probe reports *unverified*, the ledger row records **what was attempted**, not only that it failed — otherwise the next operator cannot tell a genuine non-reproduction from a badly-chosen payload. Criterion 3 names the R6 fixture by hand, so a permanently-unverified D-11 is a known gap in this phase's deliverable and must be stated as one rather than discovered later.

- **D-13:** A **preflight** runs first: bounded `agy --version` and `agy models`, both cheap and non-generating. If either fails, every delegation-dependent assumption reports *unverified* immediately and **no billed call is made**. If both pass, the full probe set runs and the ledger header states how many billed calls it made. **Budget up to three billed delegations, not one** *(research round, open question 2)*: D-10's policy probe, D-11's permission-gate probe, and D-12's slow delegation may each cost one. The count belongs in README per D-17, so an operator knows the price before running it. A tiered `--full` flag was rejected: the default run would then be unable to satisfy criteria 1 and 3, making the useful mode the non-default one. A confirmation prompt was rejected as it makes the check non-scriptable without adding a `--yes` surface. — **Reversibility:** reversible.

### Fixtures — how captured output reaches the fake

- **D-14:** Captured output lands in `tests/fixtures/` and **`fake-agy.sh` reads the fixture files at runtime** rather than carrying a canned `printf` block. One source of truth, no codegen step, and no drift test needed — the fake cannot disagree with the fixture because it has no independent copy. Accepted costs: the fake stops being a single self-contained file, and a missing or unreadable fixture becomes a new failure mode the fake must handle explicitly (fail loudly and name the missing file — never silently emit nothing, which is the degraded-list shape S1 exists to catch). — **Reversibility:** costly — reverting to a standalone fake means reinstating the canned block plus the drift test this decision avoids, and every case that reads a fixture would need rewriting.

  - **D-14a — the fake is copied, so the fixtures must travel with it.** *(Review round, F1 — the decision as first written would have broken the suite. Counts corrected by the research round; see `01.5-RESEARCH.md`.)* `fake-agy.sh` is not run in place. On the branch it has **eleven** touch points, of which the ones that move it away from `tests/` are what matter:
    - **5 `cp` sites**, none with a `fixtures/` sibling — `tests/run-tests.sh:62` (`$SANDBOX/bin/agy`), `:118` (`$d/agy`), `:2724` (`$h/bin/agy`, `_fresh_home`), `:2919` (`$IH/nopy/agy`), `:3305` (`$RB27_BIN/agy`).
    - **2 `exec` recorder shims** — `:311`, `:532` — which exec `$HERE/fake-agy.sh` and so keep a `tests/`-relative path.
    - **1 in-place invocation** — `:1165` (`bash "$HERE/fake-agy.sh"`).
    - **3 sites passing the path as an argument** to inline scripts — `:1722`, `:2543`, `:2703`.

    Only the five `cp` sites break a `dirname "$0"` scheme; the other six keep a `tests/`-relative path and are already satisfied by it. **`_install_in` runs under `env -i` whose whitelist is exactly `HOME`, `PATH`, `AGY_PLUGIN_DIR` — no fixtures variable** — so the override cannot reach it unless the whitelist is widened, and that is the single step most likely to be forgotten. Resolution is therefore a **tiered lookup**: `AGY_FIXTURES_DIR` if set and a directory → `$(dirname "$0")/fixtures` → `$AGY_PLUGIN_DIR/tests/fixtures`. Every copy site either copies `fixtures/` beside the binary or exports `AGY_FIXTURES_DIR` — and for `_install_in`, that means adding it to the `env -i` whitelist explicitly, which is the step that gets forgotten. If no tier resolves, the fake **fails loudly naming the paths it tried**; it never emits an empty list, because an empty `agy models` is the degraded-list shape S1 exists to catch and would make the failure look like a passing test of the wrong thing.

- **D-15:** The fake's current **8 two-segment gemini ids are replaced wholesale** by the captured 14-line list, non-`gemini-` rows included. Whatever goes red gets fixed. Keeping the old list beside the real one, or adding a per-case override to select the old shape, would leave the suite passing against a list agy stopped emitting — which is the originating 1.6.1 bug's exact shape and the hypothesis this phase exists to kill. Any assertion that breaks was asserting against output the real binary does not produce. — **Reversibility:** costly — the churn lands across the ~89-case suite in one change; splitting it later means re-introducing a second model list.

  - **D-15a — which cases go red, and how they are repaired.** *(Review round, F2.)* The live list carries `gemini-3.7-flash-high`, so `agy_bridge.sh:153`'s `grep -E '^gemini-[0-9.]+-flash-high$' | sort -V | tail -1` selects 3.7. Two branch cases assert 3.6 and go red: **R2** (`tests/run-tests.sh:356`, bridge auto-select) and **R4** (`:473`, the shim resolving `-m flash` through the live list). **S6** (`:255-275`) does *not* — it reads `config/model-map.json` alone, asserting class values and a required key subset, and is unaffected by the fixture. Both red cases are repaired by **deriving the expected id from the fixture** rather than re-pinning them to 3.7; a hard-coded 3.7 is the same defect one release later. `config/model-map.json` is **not edited** in this phase: on the branch it already maps aliases to *classes* (`pro-high`, `flash-high`, …), and the class resolves through the live list, so no `gemini-3.7-*` key is needed and the phase boundary's "no shipped artifact is edited" holds.

- **D-16:** Each fixture carries a header recording the **`agy` version and the capture date**. README quotes both verbatim, as fixed literals defined once — the same rule Phase 1's D-10 set for the bound warning and the 124 marker. A `run-tests.sh` case asserts README's version/date strings match the fixture headers, so editing one without the other goes red **with no live agy involved**. The check additionally comparing the live `agy --version` against README was considered and left out of the binding mechanism: it catches real-world drift but detects nothing until someone spends quota, so it cannot be what holds the document to the evidence. — **Reversibility:** reversible.

### Documentation obligations (part of this phase, not a follow-up)

- **D-17:** README states as fact that `--model` accepts **both** ids and display names, carrying the agy version and verification date from D-16's fixture headers. Any wording implying the behavior is assumed or unverified is removed in the same change. README also names `tests/contract-check.sh`, its exit codes (D-07), and that it spends real quota.

- **D-18:** REQUIREMENTS.md carries two stale claims that criterion 4's "No document may claim the assumption is unverified" forbids: S5's constraint line ("cannot be satisfied on the current machine — agy is unresponsive and returns 124") and the traceability row `S5 | Phase 7 | open, externally blocked (agy returns 124 on every call)`. Both are corrected here — the phase is 1.5, not 7, and the premise is disproven. This follows Phase 1's D-17/D-18 precedent: a sentence the change falsifies is fixed in the same change, never filed as a follow-up. `delegate-agy-9qp` closes when these land; `delegate-agy-xfa` resolves with the ledger's verdict on D-10, whichever way it goes.

### Claude's Discretion

- **Exact exit-code numbers** — subject to D-07 (disjoint from R5's five) and D-06 (unverified distinguishable from contradicted). Any consistent choice is fine as long as README states it.
- **Fixture file layout and naming** under `tests/fixtures/`, and the header format that carries version and date — subject to D-16's assertion being writable against it.
- **Ledger line format** — subject to D-04 (name, verdict, evidence; header carries version, date, billed-call count) and D-08 (the unverified row must name the assumption).
- **The forbidden tool and prompt wording** for D-10's policy probe — subject to using the real bridge and a policy shaped like the existing `config/policies/*.md` files, not a bespoke one-off, **and subject to the research round's constraint: the probe must not set `AGY_SKIP_PERMISSIONS=1`.** Combining `--sandbox` with `--dangerously-skip-permissions` has an open upstream sandbox-bypass report, and a probe run that way would be measuring the bypass rather than the policy. (Research confidence MEDIUM — web-sourced, not read directly; the constraint holds regardless of the bug's current status, because the probe's whole point is the shipped configuration.)
- **Whether the seven probes run in a fixed order** and whether a contradicted early row short-circuits later ones. Default to running all seven and reporting all seven; a partial ledger is less useful than a complete one.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase intent and requirements
- `.planning/ROADMAP.md` §"Phase 1.5: Contract check against a real agy" — the four success criteria, and the Note recording why the phase moved from 7 to 1.5 and what the 2026-08-19 probe already established
- `.planning/ROADMAP.md` §"Phase 2: Model-list handling, end to end" §"On the Phase 1.5 dependency" — why criterion 4's "byte-identical to what shipped" needs this phase's fixtures
- `.planning/ROADMAP.md` §"Phase 5" Note, §"Phase 6" — the two downstream consumers; Phase 6's gate is the one D-05 declines to widen
- `.planning/REQUIREMENTS.md` §S5 — the requirement, and the two stale claims D-18 corrects
- `.planning/REQUIREMENTS.md` §R5, §R6, §R11, §S1 — the contracts a *contradicted* row would land against
- `.planning/PROJECT.md` §Context, §Constraints — the corrected agy-is-live paragraph, and the explicit "what the probe did **not** establish" sentence that D-12 exists to settle

### Prior-phase decisions this phase inherits
- `.planning/phases/01-the-missing-timeout-decision/01-CONTEXT.md` §D-08 — the verbatim-duplication-plus-identity-test rule D-03 extends to a third file
- `.planning/phases/01-the-missing-timeout-decision/01-CONTEXT.md` §D-10 — fixed literals defined once, pinned by a test, quoted verbatim in README; D-16 applies it to the provenance strings
- `.planning/phases/01-the-missing-timeout-decision/01-CONTEXT.md` §D-15 — the sanitized-PATH pattern D-02 and D-08 both reuse, and the "no test-only override in shipped scripts" rule
- `.planning/phases/01-the-missing-timeout-decision/01-CONTEXT.md` §D-12 — static scan plus runtime proof, the shape D-02's isolation case follows

### Code under change or under copy
- `tests/run-tests.sh` — the ~89-case harness; cases T4/T5/SH11 for the outer-bound pattern D-08 reuses, RB01 for the static-scan shape D-02 follows, and the `rm -f` cache-hygiene convention any new case must honour
- `tests/fake-agy.sh:85` (the `models)` case label) and `:106-114` (the canned 8-id `printf` block) **on the branch** — what D-14/D-15 replace with a fixture read. *(An earlier draft cited `:41-51`; that is the block's location on `master`, the same master-vs-branch trap as F5. Corrected in the research round.)*
- `tests/fake-agy.sh:1-30` — the header comment block documenting every `FAKE_AGY_*` knob; a fixture-path knob follows the same naming rule
- `scripts/agy_bridge.sh:145-159` — the `cut -f1` normalization and both anchored matchers the model-list assumptions are checked against
- `scripts/agy_bridge.sh` `# --- BEGIN run_bounded ---` block — the source of D-03's third verbatim copy (read on the branch, not `master`)
- `config/model-map.json` — **read it on the branch, not `master`.** On `master` it maps aliases to display names; on `fix/agy-bridge-resilience` it maps aliases to **classes** (`pro-high`, `flash-high`, …) which the shim resolves through the live list, so the shim now passes an **id** (`tests/run-tests.sh:473` asserts exactly that, and `:255-275` pins the class-value invariant). The id-vs-display-name probe still earns its place — `README.md:37` documents `--model MODEL_ID`, and the invalid-model rejection renders its `Available models:` list in display names — but the rationale is *what agy accepts*, not *what the shim sends*. **This entry corrected a factual error in the first draft of this record (review round, F5).**
- `config/policies/*.md` — the per-type tool restrictions whose authority D-10 tests

### Documentation under change
- `README.md:37` — the model-resolution paragraph D-17 amends with the verified fact
- `README.md:221` — the "Model name rejected / exact string required" troubleshooting row, which the invalid-model probe either confirms or contradicts
- `README.md` §Troubleshooting — where D-07's exit codes are documented alongside R5's table

### Tracker
- `delegate-agy-xfa` (P1, open) — the `GEMINI.md`-discovery bug; D-10 is the bounded test its description asks for, and it also records the live `rc=0`-empty-stdout observation D-11 probes
- `delegate-agy-9qp` (P2, open) — carries the 2026-08-19 probe evidence in its comment; closes when D-17 and D-18 land
- `delegate-agy-62x` (closed, never verified) — the id-vs-display-name question; criterion 1 answers it with evidence rather than assertion

### Working tree
- `.worktrees/agy-1.6.2` on `fix/agy-bridge-resilience` — where the 1.6.2 content actually lives; `master` content-reverted it at `a001d0e`. Judge state by reading files, never by the commit graph.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`run_bounded` with its BEGIN/END markers** — already duplicated in both shipped scripts and locked by a byte-identical test; D-03 makes the check its third consumer, so the check bounds agy by exactly the mechanism it is auditing.
- **The sanitized-PATH scratch dir** (Phase 1 D-15) — a directory holding only the fake and no `timeout`/`gtimeout`; D-02 and D-08 reuse it to keep the real binary out of the suite.
- **`fake-agy.sh`'s SIGTERM-ignoring hang mode** — built in Phase 1 for the runtime bound proofs; D-08's hang case needs no new fixture mode.
- **The outer-`timeout` case pattern** (T4/T5/SH11, `tests/run-tests.sh:940-948`) — wraps a hang-driving invocation so a regression to unbounded fails in ~30s instead of stalling ~300s, and asserts `_ELAPSED` alongside exit code.
- **`FAKE_AGY_DUMP_ARGV`** — observational argv capture already in the fake; useful for asserting what the bridge actually passed during D-10's policy probe.

### Established Patterns
- **`set -u`, never `set -e`, in test files** — the check is a test-side artifact and inherits this; most probe bodies deliberately invoke something expected to fail.
- **Shared `$HOME` across the whole suite run** — any new case writing `$HOME/.cache/*` must `rm -f` before returning, or every later case sees corrupted state. The model cache at `$HOME/.cache/agy-bridge-models` is the specific hazard for anything touching model resolution.
- **`_run` merges stderr into stdout** — a probe that must prove something reached stderr specifically (the ledger's evidence lines, the unverified marker) captures the streams separately rather than using `_run`.
- **Numbered case prefixes with letter suffixes for variants** — new cases follow the existing convention rather than inventing a prefix; the isolation scan is lint-shaped rather than behavior-shaped and its name should say so.
- **Validate-and-correct, not reject, for optional knobs** — the project's standing rule; the check is a maintainer tool rather than a box-wide shim, so it may be stricter, but its bound knobs should still not silently disable a bound.

### Integration Points
- **`tests/fixtures/` ← `fake-agy.sh`** — the new read path D-14 creates; the missing-fixture failure mode is the seam that must fail loudly rather than emit an empty list. **Six copy sites** carry the fake away from that directory (`tests/run-tests.sh:62`, `:118`, `:2724`, `:2919`, `:3305`) and `_install_in`'s `env -i` strips the override variable — D-14a is the contract each of them must satisfy.
- **Fixture headers ← README strings** — D-16's assertion is the seam between captured evidence and the document quoting it. **`RB03` (`tests/run-tests.sh:2019-2073`) is the existing precedent to copy**: it already matches README's troubleshooting table against the operator-visible literals defined in the scripts, and asserts each literal is defined exactly once. D-16's provenance case should be RB03-shaped rather than newly invented.
- **The ledger's verdicts → Phases 2, 3, 5** — a contradicted row becomes a ticket for the phase owning that behavior, not a fix here.
- **`run-tests.sh` → `contract-check.sh` under sanitized PATH** — the only permitted invocation shape (D-02), and the thing the isolation scan asserts.

</code_context>

<specifics>
## Specific Ideas

- **The 2026-08-19 probe already predicts four of the seven rows** (recorded verbatim in `delegate-agy-9qp`'s comment): `agy models` returns 14 lines of `id<TAB>display name` in under 30s; live ids are `gemini-3.7/3.6/3.5-flash-{high,medium,low}` and `gemini-3.1-pro-{high,low}` plus `claude-sonnet-4-6`, `claude-opus-4-6-thinking`, `gpt-oss-120b-medium`; `--model` accepts both ids and display names; a bogus name gives `rc=1`, "invalid model selection … is not recognized as a known model or custom model in settings", then an `Available models:` list rendered in **display names**. The check must **re-derive** these, not assert them — a probe that hard-codes the expected answer and checks agy agrees is a fixture, not a contract check. The recorded evidence is the reviewer's baseline, not the check's input.

- **Criterion 1 is satisfied more strictly than written, not more loosely.** Its phrase "never invoked by it" is narrowed by D-02 to "the suite never reaches a real agy through it". The suite gains the ability to name the file — and loses, by assertion, any path to the live binary. Record this in the plan so a reviewer reading the criterion literally finds the reasoning instead of a violation.

- **This record was reviewed adversarially by agy (Gemini Pro, with repo access) on 2026-08-19 before planning**, the same round Phase 1 ran. Full review and per-finding verification: `01.5-REVIEWS.md`. Four findings were folded in — D-14a (the fake is copied to six locations with no fixtures sibling; the decision as first written would have broken the installer and sandbox suites), D-15a (R2 and R4 go red; S6 does not; `config/model-map.json` stays untouched), D-12a (a permanently-unverified probe is a stated gap, not an answer), and D-03's size/extraction facts. One finding was **corrected rather than accepted**: the reviewer bundled S6 with R2/R4, but S6 reads `config/model-map.json` alone and is fixture-independent. One error the reviewer did not catch was found during verification and fixed: the `config/model-map.json` entry under Existing Code Insights described `master`'s display-name mapping, not the branch's class mapping (F5).

- **A contradicted row is a success, not a failure of this phase.** The phase delivers a truthful answer; it does not deliver agreement. If the `GEMINI.md` probe shows the bridge's per-type restriction does not bind, that is `delegate-agy-xfa` confirmed and a Phase 5 ticket — the check did its job.

</specifics>

<deferred>
## Deferred Ideas

- **Shipping the check as `scripts/agy-contract-check.sh` with an installed launcher** — considered and declined (D-01). It would let an installed user diagnose their own agy, but grows Phase 4's installer surface and Phase 5's failure-mode table, and it would have to write fixtures into a repo the user may not have. Revisit if a user-reported model-name failure is ever hard to reproduce from a clone.

- **A lint case grepping every tracked doc for "unverified"-shaped claims** — considered under D-18 and declined. It would stop the stale claim from ever reappearing, but its false-positive surface is English prose across README, PROJECT.md, REQUIREMENTS.md and every phase record. Revisit only if the claim actually reappears after this phase.

- **Widening Phase 6's "both suites pass" gate to include the contract check** — explicitly declined (D-05). Recorded here so the omission is findable as a decision.

- **The contract check comparing live `agy --version` against README on every run** — considered as the provenance mechanism and left out of the binding (D-16); a static suite case holds the document to the fixture instead. Worth adding as an extra ledger row later if agy versions start moving faster than the fixtures.

- **Amending ROADMAP.md criterion 1's wording to match D-02** *(review round, F6)* — the reviewer proposed rewriting "separate from the unit suite and never invoked by it" as something closer to "never exercising a live binary during automated testing", so the roadmap does not contradict the decision. The proposal is sound, but editing a success criterion is a roadmap change requiring the user's call and `gsd-tools`, not a silent edit from a review step. D-02 records the narrowing and its reasoning; a reviewer reading criterion 1 literally will find it there. **Open item for the user, not for the planner.**

- **A lint case asserting no test hard-codes a model id absent from the fixture** — offered alongside D-15a and not taken. It would stop a future capture from leaving a stale pin passing silently. Revisit if the fixture is ever recaptured and a pin is found to have rotted.

</deferred>

---

*Phase: 1.5-Contract check against a real agy*
*Context gathered: 2026-08-19*
