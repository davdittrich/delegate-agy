---
phase: 04-installer-and-launcher-surface
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/install.sh
  - scripts/uninstall.sh
  - tests/run-tests.sh
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [R8, S2]
beads: [delegate-agy-4bp, delegate-agy-4xn]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`bash scripts/install.sh` and `bash scripts/uninstall.sh` run with no `HOME` in the environment print a stated precondition error naming HOME and exit 1, instead of bash's own `HOME: unbound variable` at the first expansion. (D-06)"
    - "Neither script falls back to a default home path when `HOME` is unset — a silent redirect would install into a path the user never sees and, on a shared host, a guessable one. Refusal is the decided behavior. (D-06, 04-RESEARCH.md Security Domain)"
    - "The precondition sits immediately after the existing refuse-root check in each file, so it precedes every `$HOME` expansion: `install.sh:58, 228, 253, 254, 256` and `uninstall.sh:20, 60, 61` (freshly verified line numbers — the ticket's own citations are stale by +5 in install.sh). (D-06)"
    - "With `AGY_SETUP_PATCH_ALIASES=1` set, a real recursive `gemini` alias present in an rc file, and no `python3` on PATH, `install.sh` exits 0, both wrappers exist on disk, exactly one fail-open warning naming the alias-patch feature is on stderr, and the rc file is byte-identical to its pre-run content. (D-01)"
    - "The python3 presence check is evaluated once per `install.sh` run, before the `for RC in ...` loop at `install.sh:228`, not once per matched rc file. (D-01)"
    - "The existing per-rc-file dry-run advisory (`install.sh:234-236`) still fires for every matched rc file when the flag is off, regardless of whether python3 is present — the new guard does not swallow it. (D-01, resolving 04-RESEARCH.md's Open Question 1)"
    - "The new warning literal is NOT pinned in README or in an RB03-style provenance test; one suite case is the whole verification surface for it. (D-02)"
    - "R8 and S2 are closed by citing `tests/run-tests.sh` I16 (`:3980-4105`), I17 (`:4107-4194`) and I18 (`:4197-4239`) as shipped evidence, with no new registry fixtures added and no re-verification of the comparison logic. (D-07)"
    - "`scripts/install.sh`'s `write_wrapper` registry-comparison heredoc (`:85-182`) is not modified by this phase — the exact-key `sed` window and the `^[0-9]+(\\.[0-9]+)*$` version regex that R8 and S2 rest on are untouched. (D-07)"
  artifacts:
    - "scripts/install.sh — a HOME precondition line after the refuse-root check, and a single python3 presence check before the rc-alias loop carrying a `_alias_patch_py3_ok` flag"
    - "scripts/uninstall.sh — the mirrored HOME precondition line after its refuse-root check"
    - "tests/run-tests.sh — cases `I19` (python3-absent alias patch), `I20` (install.sh, HOME unset) and `I20b` (uninstall.sh, HOME unset)"
    - ".planning/REQUIREMENTS.md — R8 and S2 traceability rows moved to met, citing I16/I17/I18"
  key_links:
    - "the HOME precondition's position -> every `$HOME` expansion below it: if it is inserted after `install.sh:58` instead of after the refuse-root block at `:37-41`, the crash it exists to prevent still happens and the new test still fails"
    - "`_alias_patch_py3_ok` -> the loop's existing `python3 -` invocation at `install.sh:239`: the flag is what makes the single hoisted check actually skip the patch, and without wiring it the guard warns and then crashes anyway"
    - "`I19`'s `nopy`-style curated PATH -> a whitelist of the binaries install.sh needs: a blocklist would let a future `command -v` call silently find the real host's python3 and turn the case vacuous"
  prohibitions:
    - "Neither script may substitute a default path for an unset `HOME`. A guessable install destination on a shared host is a tampering surface, and the user never sees where their files went."
    - "The rc-alias patch must not rewrite a user's shell rc file on any path that does not have the explicit `AGY_SETUP_PATCH_ALIASES=1` consent flag set; the new guard skips the patch, it never relaxes the consent gate."
    - "No existing assertion in tests/run-tests.sh may be weakened, deleted, or made conditional to accommodate this plan's changes."
    - "The registry-comparison logic in `write_wrapper` must not be edited, refactored, or 'improved' while this plan is in `install.sh` — R8 and S2 are being closed on the evidence that it is unchanged."
---

<objective>
Close `delegate-agy-4bp` (both installer scripts die on an opaque `HOME: unbound variable` instead of stating the precondition) and `delegate-agy-4xn` (the rc-alias-patch branch calls `python3` with no `command -v` guard, so an installer that has already written both wrappers can hard-fail afterwards), then close R8 and S2 on their existing shipped evidence.

Purpose: ROADMAP criterion 4 is "no install path can abort after the wrappers are written." Both bugs are exactly that shape — one aborts before anything is written with a message that names bash's mechanism rather than the actual requirement, the other aborts after `~/.local/bin/gemini` already shadows the real binary box-wide. Neither is hypothetical: `env -i`, container entrypoints and `User=`-less systemd units all reach the first, and any host without python3 reaches the second.

Output: two one-line preconditions, one hoisted dependency guard, three new regression cases, and R8/S2 closed in REQUIREMENTS.md.

**TDD note.** `workflow.tdd_mode` is enabled. Tasks 1 and 2 carry `tdd="true"` with `<behavior>` blocks: write the new case against the unfixed script, observe `FAIL -` in the full-suite output, then apply the fix. See plan 04-01's objective for why this phase uses task-level TDD rather than one `type: tdd` plan per fix.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-installer-and-launcher-surface/04-CONTEXT.md
@.planning/phases/04-installer-and-launcher-surface/04-PATTERNS.md
@.planning/phases/04-installer-and-launcher-surface/04-VALIDATION.md
</context>

## Artifacts this phase produces

Symbols and identifiers **created by this plan** — none exist on the current tree, so exclude them from drift and source-grounding verification:

| Artifact | File | Kind |
|----------|------|------|
| `_alias_patch_py3_ok` | `scripts/install.sh` | new shell variable (`0`/`1`) carrying the hoisted python3 presence result into the rc-alias loop |
| `I19` | `tests/run-tests.sh` | new test case id (python3-absent rc-alias patch fails open) |
| `I20` | `tests/run-tests.sh` | new test case id (`install.sh`, HOME unset) |
| `I20b` | `tests/run-tests.sh` | new test case id (`uninstall.sh`, HOME unset) |
| `I19_RC`, `I20_RC`, `I20B_RC`, `_RCHASH_I19_BEFORE`, `_RCHASH_I19_AFTER` | `tests/run-tests.sh` | new driver-local shell variables |

No new files. No `COVERAGE.md`: the API-coverage detector returned `{"detected":false,"signals":[]}` for this phase — there is no external API or SDK integration here.

## Alternatives Considered

### D-01 — where the python3 presence check goes

| Rank | Option | Performance | Simplicity / LOC | Ecosystem | Maintenance |
|------|--------|-------------|------------------|-----------|-------------|
| **1 (chosen)** | One check before the `for RC` loop, gated on the consent flag, carrying a flag into the loop | Best — one `command -v` per run instead of one per matched rc file | +4 lines, one new variable | matches `install.sh:279-283` and `:260-264` exactly | Best — the warning fires exactly once per run, so the operator sees one line, not three |
| 2 | Check inside the loop, per matched rc file | Up to 3 `command -v` calls and up to 3 duplicate warnings | +3 lines, no new variable | same analog | Worse — repeats an unchanging fact, and explicitly overridden by D-01 |
| 3 | Check inside the existing flag-off arm at `install.sh:234` | Same as 2 | Fewest lines | none | Worst — inverts the logic; that arm is the dry-run path, which does not call python3 at all |

Citations: `scripts/install.sh:279-283` (`_register_tokensave`'s shipped fail-open guard, the wording and shape template) and `scripts/install.sh:260-264` (`_agy_detect`'s silent variant) — both in-repo, both shipped; [Bash Scripting Best Practices for Reliable Automation — oneuptime.com, 2026-02-13](https://oneuptime.com/blog/post/2026-02-13-bash-best-practices/view) (in-window) for the hoisted `check_dependencies()` pattern.
**Decided by:** user, locked as D-01 ("guard once, before the loop"), with 04-RESEARCH.md's Open Question 1 resolved in favour of its own recommended reading — see Task 2's action for the exact structure and why the dry-run advisory stays untouched.

### D-06 — how the unset-`HOME` precondition is expressed

| Rank | Option | Performance | Simplicity / LOC | Ecosystem | Maintenance |
|------|--------|-------------|------------------|-----------|-------------|
| **1 (chosen)** | `[[ -n "${HOME:-}" ]] || { echo "ERROR: ..." >&2; exit 1; }` | Identical | 1 line | matches the refuse-root check directly above it in both files | Best — the message names the requirement, and the shape is already the file's idiom |
| 2 | `: "${HOME:?ERROR: HOME is not set...}"` | Identical | Shortest, POSIX-portable | native | Worse — bash prefixes its own `script: line N:` framing, and `install.sh`'s own header commits to `[[ ]]` over `[ ]` |
| 3 | A default-path fallback for an unset `HOME` | Identical | 0 extra lines | native | **Forbidden** — installs into a path the user never sees; on a shared host the destination is guessable (Tampering, see threat register) |

Citations: `scripts/install.sh:37-41` and `scripts/uninstall.sh:15-19` (the in-repo refuse-root analogs); [5 Methods to Check If an Environment Variable is Set in Bash — LinuxSimply](https://linuxsimply.com/bash-scripting-tutorial/variables/usage/check-if-environment-variable-is-set/); [Bash Scripting Best Practices for Reliable Automation — oneuptime.com, 2026-02-13](https://oneuptime.com/blog/post/2026-02-13-bash-best-practices/view) (in-window, required-env-var validation).
**Decided by:** the ticket's own bead-lock, adopted verbatim by D-06. Option 3 is the anti-pattern the lock names explicitly; note the *generated wrapper* legitimately uses a fallback for the opposite reason (it must degrade to silence at every invocation) and that is not this file.

### D-02, D-07 — verification scope

`N/A — no mechanism choice.` D-02 declines an RB03-style provenance pin for the new warning; D-07 cites existing coverage rather than adding fixtures. Neither selects a mechanism, and both are user overrides recorded in 04-CONTEXT.md.

<!-- planner-discipline-allow: python3 not found -->
<!-- The literal above appears in Task 2's action because it is the shape constraint the new
     warning must match (`install.sh:280-282`). The only negative grep for it targets README.md,
     which no task in this plan writes, so there is no comment-echo risk. -->

## Flagged Assumptions (spec-less edge-probe fallback)

This phase has no `SPEC.md`. The deterministic edge-probe engine was run against R8 and S2 and returned three rows, **all `unresolved`** — none auto-resolved by an explicit or backstop verification. Per the fallback protocol these become explicit flagged assumptions, never `must_haves.truths` and never silent drops. Each is cross-referenced below against D-07's position that criteria 1-3 are already shipped and covered by I16/I17/I18. **None of them spawns a task**; the user declined new registry fixtures (D-07), and manufacturing work against an already-answered probe would be busywork.

| # | Req | Probe | Disposition |
|---|-----|-------|-------------|
| A1 | R8 | "What is the result for empty, single-element, or null input?" | **Answered by shipped coverage.** `I17` (`tests/run-tests.sh:4107-4194`) exercises the empty-array, compact and semi-compact registry shapes and asserts each degrades to silence. `I16` cases (b)/(c) (`:3980-4105`) cover the absent and unparseable registry. The single-element case (only our own entry, no neighbour) is strictly easier than `I16`'s adversarial shape, which places a lookalike marketplace entry immediately beside ours. No gap. |
| A2 | R8 | "Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form?" | **Answered by construction, with a named residue.** The registry key match is a literal byte comparison inside `write_wrapper`'s `sed` window, and the version is matched against `^[0-9]+(\.[0-9]+)*$`, which is ASCII-only by definition. `I18` (`:4197-4239`) covers a non-trivial byte (an apostrophe) in the plugin cache path. **Residue, flagged not closed:** a registry key differing only by Unicode normalization form (NFC vs NFD) would fail the exact-key match and the launcher would degrade to silence. That is the safe failure direction and satisfies S2's own acceptance ("any parse failure degrades to silence, never to a false refusal"), so it is recorded as an accepted assumption rather than a defect. |
| A3 | S2 | unclassified — review manually | **Answered by shipped coverage.** S2's stated acceptance is (i) extraction bounded to our own entry, (ii) no neighbour misattribution, (iii) parse failure degrades to silence. `I17` covers (i) and (iii) across three registry shapes; `I16`'s lookalike-adjacent fixture covers (ii). Verified this session that `write_wrapper`'s extraction window is still the anchored `sed` range and not a `grep -A6`. No gap. |

**One observation surfaced while reading D-05's synced content, recorded here rather than acted on:** the branch-tip `.md` files' new *primary* path-lookup one-liner uses `grep -A6` over `installed_plugins.json` — the same shape S2's origin note names as the historical hazard. It is not an R8/S2 violation: that one-liner prints a path for the human to read and approve ("Check it looks right, then run:"), it never feeds a launcher decision, and the launcher's own read is the anchored `sed` window in `write_wrapper`. Flagged for the record; D-05 locks the sync as byte-exact, so changing it here would be an unreviewed divergence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State the HOME precondition in both installer scripts (D-06)</name>
  <files>scripts/install.sh, scripts/uninstall.sh, tests/run-tests.sh</files>
  <read_first>
    - `scripts/install.sh:30-60` — the `set -euo pipefail` line, the refuse-root check at `:37-41` (the insertion anchor), the plugin-root resolution, and the first `$HOME` expansion at `:58` (`BIN_DIR="$HOME/.local/bin"`)
    - `scripts/uninstall.sh:10-25` — the mirrored refuse-root check at `:15-19` and the first `$HOME` expansion at `:20`
    - `tests/run-tests.sh:3759-3778` — `I6` (install.sh refuse-root) and `I6b` (uninstall.sh refuse-root); this is the "same behavior, two scripts, two ids" precedent CONTEXT.md's discretion note points at, and the harness shape to copy
    - `tests/run-tests.sh:3668-3684` — `_fresh_home` / `_install_in`, the `env -i` isolation harness every `I*` case uses
    - `tests/run-tests.sh:86` — `export HOME="$SANDBOX/home"`; the suite sets HOME globally, which is why these two cases must use `env -i` with no `HOME` key at all
    - `.planning/phases/04-installer-and-launcher-surface/04-PATTERNS.md` section "D-06: HOME precondition"
  </read_first>
  <behavior>
Write `I20` and `I20b` first, against the unfixed scripts, and observe both red.

  - `I20`: `env -i PATH=<curated dir> AGY_PLUGIN_DIR="$ROOT" bash "$INSTALL"` — **no `HOME=` key in the environment at all**, which is what distinguishes these from every existing case. Expect: rc == 1; stderr contains an `ERROR:` line naming `HOME`; combined output does NOT contain `unbound variable`; no wrapper was written anywhere.
  - `I20b`: the same against `"$UNINSTALL"`. Expect the same three assertions.
  - Both cases must be red before the fix for the right reason: the pre-fix failure is bash's own `HOME: unbound variable`, which is precisely what the `unbound variable` negative assertion catches.
  </behavior>
  <action>
**RED.** Add `I20` and `I20b` to `tests/run-tests.sh` immediately after `I6b` (`tests/run-tests.sh:3778`), following `I6`/`I6b`'s exact harness shape but swapping the `SUDO_USER="someone"` trigger for an environment with no `HOME` key. Two ids, not one, per `I6`/`I6b`'s own per-script convention. Capture combined stdout+stderr into `$SANDBOX/last-install.log` / `$SANDBOX/last-uninstall.log` as the neighbouring cases do. Run the full suite, observe `FAIL - I20` and `FAIL - I20b`, and record the exact `unbound variable` text seen. Commit as `test(04-02): add failing I20/I20b for the unset-HOME precondition`.

**GREEN.** Insert the precondition — locked verbatim by D-06, not discretionary — into both scripts, immediately after the closing `fi` of the refuse-root check (`scripts/install.sh:41` and `scripts/uninstall.sh:19`) and before any `$HOME` expansion:

`[[ -n "${HOME:-}" ]] || { echo "ERROR: HOME is not set; run as a normal user with a home directory." >&2; exit 1; }`

Same single line, same message, in both files. Placement matters more than anything else in this task: the freshly verified `$HOME` expansion sites it must precede are `install.sh:58, 228, 253, 254, 256` and `uninstall.sh:20, 60, 61`. Use these numbers, not `delegate-agy-4bp`'s own citations (`:223, :248, :249, :251`), which are stale by a consistent +5 lines against the current file. Note `install.sh:222` contains an escaped `\$HOME` inside a double-quoted `echo` printed for the user to paste — it is never expanded by `install.sh` and is not a crash site; leave it alone.

Do NOT introduce a default-path fallback for an unset `HOME`. The generated wrapper's own heredoc does use one internally, for the opposite reason (it must degrade to silence on every invocation); copying that shape into these two scripts is the anti-pattern the ticket's bead-lock forbids.

Re-run the full suite; `I20` and `I20b` must both report `ok`. Commit as `fix(04-02): state the HOME precondition in install.sh and uninstall.sh`.
  </action>
  <verify>
    <automated>bash tests/run-tests.sh > /tmp/agy-p4-p2t1.log 2>&1; echo "suite-rc=$?"; grep -E '^(PASS=|ok   - I20|FAIL - )' /tmp/agy-p4-p2t1.log; env -i PATH=/usr/bin:/bin bash scripts/install.sh 2>&1 | tail -2; env -i PATH=/usr/bin:/bin bash scripts/uninstall.sh 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - The git log shows a `test(04-02): ...I20/I20b...` commit preceding the `fix(04-02): ...HOME precondition...` commit, and the SUMMARY quotes the `unbound variable` message observed at the red commit.
    - `env -i PATH=/usr/bin:/bin bash scripts/install.sh; echo rc=$?` prints `rc=1` and its stderr contains `ERROR: HOME is not set`. Same for `scripts/uninstall.sh`.
    - Neither script's output contains `unbound variable` on that run.
    - Source assertion, both files: `grep -c 'HOME is not set' scripts/install.sh` == 1 and `grep -c 'HOME is not set' scripts/uninstall.sh` == 1.
    - Ordering assertion, both files: the line number of the `HOME is not set` guard is greater than the line number of `refusing to run as root` and less than the line number of `BIN_DIR="$HOME/.local/bin"`. Check with `grep -n` on all three literals per file.
    - `bash tests/run-tests.sh` output contains `ok   - I20 ` and `ok   - I20b ` and reports `FAIL=0` with `PASS` >= 160.
    - `git diff` for the `fix(...)` commit touches exactly two files and adds exactly one line to each.
  </acceptance_criteria>
  <done>Both scripts refuse with a stated, HOME-naming error and exit 1 when `HOME` is unset, `I20`/`I20b` were red before the guard and green after, and the full suite reports `FAIL=0`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the rc-alias patch's python3 dependency once, before the loop (D-01, D-02)</name>
  <files>scripts/install.sh, tests/run-tests.sh</files>
  <read_first>
    - `scripts/install.sh:226-250` — the whole consent-gated rc-alias-patch block: the `if [[ -n "$_real_gemini" ]]; then` gate at `:227`, the `for RC in ...` loop at `:228`, the per-file dry-run advisory at `:234-236`, and the unguarded `python3 - "$RC" "$_real_gemini" <<'PY'` heredoc at `:239`
    - `scripts/install.sh:279-283` — `_register_tokensave`'s shipped fail-open guard; the wording and shape template D-01 says to mirror
    - `scripts/install.sh:260-264` — `_agy_detect`'s silent python3-absent variant, for contrast
    - `scripts/install.sh:200-206` — the `write_wrapper` calls, which prove both wrappers are already on disk by the time the rc-alias block runs
    - `scripts/install.sh:328-348` — the tokensave block; under `env -i` (no tty) `DO_REG=0` and `_register_tokensave` never runs, which is why `I19` can assert `python3 not found` appears exactly once
    - `tests/run-tests.sh:3794-3825` — `I8` (dry-run, with its `cksum`-style no-op detection idiom) and `I8b` (real recursive alias + flag set); `I19` composes `I8b`'s setup
    - `tests/run-tests.sh:3862-3881` — `I10`, the `nopy` curated-PATH recipe (a whitelist of the binaries `install.sh` needs, python3 deliberately absent); `I19` composes this PATH
  </read_first>
  <behavior>
Write `I19` first, against the unfixed `install.sh`, and observe it red.

  - Setup: `I8b`'s scenario (a real `gemini` in a separate bin dir so `_real_gemini` is non-empty, a matching `alias gemini='...'` recursive alias written into `$IH/.bashrc`, `AGY_SETUP_PATCH_ALIASES=1`) run against `I10`'s `nopy` curated PATH, which symlinks every binary `install.sh` needs **except** `python3`.
  - Expect after the fix: `install.sh` exits 0; `$IH/.local/bin/agy-bridge` and `$IH/.local/bin/gemini` both exist; the log contains exactly one line matching `python3 not found`, and that line also mentions the alias patch (case-insensitive); `$IH/.bashrc` is byte-identical to its pre-run content, checked by a checksum taken before and after, using `I8`'s existing no-op-detection idiom; no `*.bak-agy-*` backup was created beside the rc file.
  - Pre-fix red: `install.sh` aborts non-zero at the unguarded `python3 -` call under `set -e`, so the exit-0 and rc-untouched assertions both fail.
  </behavior>
  <action>
**RED.** Add case `I19` after `I8b` (`tests/run-tests.sh:3825`), composing the two source patterns named in `<read_first>`. Use the `nopy` recipe as a whitelist, not a blocklist — a future new `command -v` call in `install.sh` must fail loud in this case rather than silently resolving against the real host. Take the rc-file checksum before and after the run. Run the full suite, observe `FAIL - I19`, and record the pre-fix rc and the tail of `$SANDBOX/last-install.log`. Commit as `test(04-02): add failing I19 for the python3-absent rc-alias patch`.

**GREEN.** Insert a single guard immediately after `if [[ -n "$_real_gemini" ]]; then` at `scripts/install.sh:227` and before the `for RC in ...` loop at `:228`. Structure — this is 04-RESEARCH.md Open Question 1 resolved in favour of its own recommended reading, adopted here explicitly so the executor does not have to choose:

1. Initialise `_alias_patch_py3_ok=1`.
2. When `[[ "${AGY_SETUP_PATCH_ALIASES:-0}" == "1" ]]` **and** `! command -v python3 >/dev/null 2>&1`, set `_alias_patch_py3_ok=0` and echo one `WARNING:` line to stderr.
3. Inside the loop, at the point where the flag is already known true (immediately after the existing `if [[ "${AGY_SETUP_PATCH_ALIASES:-0}" != "1" ]]; then ... continue; fi` arm at `:234-236`), `continue` when `_alias_patch_py3_ok` is `0`, so neither the `cp -f "$RC" "$RC.bak-agy-$(_ts)"` backup at `:238` nor the `python3 -` heredoc at `:239` runs.

Why this reading and not the alternative: the two spatial anchors D-01 names ("after the flag gate is confirmed true" and "before the loop starts") do not coincide in the current code, because the flag is read per rc file inside the loop. Reading the flag once at the hoist point is a *read*, not a restructure — it leaves the existing per-file dry-run advisory at `:234-236` firing for every matched rc file when the flag is off, exactly as today, and it fires the new warning exactly once per run rather than once per matched file. Do not hoist the flag's *use* out of the loop; do not delete or reword the dry-run advisory.

Warning wording is Claude's Discretion, subject to two constraints: it mirrors `_register_tokensave`'s shape at `:280-282` (a `WARNING:` prefix, the phrase `python3 not found`, a named skipped feature, and a fail-open marker) and it names the alias-patch feature specifically so `I19`'s single-line assertion can distinguish it from the tokensave warning. Per D-02, do NOT add a README mention, a shared literal constant, or an RB03-style provenance test for it — none of `install.sh`'s existing sibling warnings are pinned that way, and one suite case is the whole verification surface.

Re-run the full suite; `I19` must report `ok` and `I8`/`I8b`/`I10` must stay `ok`. Commit as `fix(04-02): guard the rc-alias patch's python3 dependency, fail open once per run`.
  </action>
  <verify>
    <automated>bash tests/run-tests.sh > /tmp/agy-p4-p2t2.log 2>&1; echo "suite-rc=$?"; grep -E '^(PASS=|ok   - I19|ok   - I8|ok   - I10|FAIL - )' /tmp/agy-p4-p2t2.log; grep -n '_alias_patch_py3_ok\|command -v python3' scripts/install.sh</automated>
  </verify>
  <acceptance_criteria>
    - The git log shows `test(04-02): ...I19...` preceding `fix(04-02): ...rc-alias patch's python3...`, and the SUMMARY records the pre-fix exit code observed at the red commit.
    - Source assertion: `grep -c '_alias_patch_py3_ok' scripts/install.sh` == 3 (initialise, set-to-0, test-in-loop) and `grep -c 'command -v python3' scripts/install.sh` == 3 (the new guard plus the two pre-existing ones at `:261` and `:280`).
    - Ordering assertion: the line number of the new `command -v python3` occurrence is greater than the line of `if [[ -n "$_real_gemini" ]]; then` and less than the line of `for RC in "$HOME/.bashrc"`.
    - The existing dry-run advisory is untouched: `grep -c '(dry-run) set AGY_SETUP_PATCH_ALIASES=1' scripts/install.sh` == 1.
    - Per D-02, the new literal is not pinned anywhere else: `grep -c 'python3 not found' README.md` == 0.
    - `bash tests/run-tests.sh` output contains `ok   - I19 ` and still contains `ok   - I8 `, `ok   - I8b `, `ok   - I10 `; it reports `FAIL=0` with `PASS` >= 161.
    - `git diff` for the `fix(...)` commit touches only `scripts/install.sh`.
  </acceptance_criteria>
  <done>With the consent flag set and no python3 on PATH, `install.sh` exits 0 with both wrappers written, one warning naming the alias patch, and the rc file bit-for-bit unchanged; `I19` was red before the guard and green after; the suite reports `FAIL=0`.</done>
</task>

<task type="auto">
  <name>Task 3: Close R8 and S2 on their shipped evidence, and close the two beads (D-07)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` sections R8 and S2 — both traceability rows currently read as shipped-on-branch rather than met
    - `tests/run-tests.sh:3980-4105` (`I16`), `:4107-4194` (`I17`), `:4197-4239` (`I18`) — the three adversarial registry cases D-07 says to cite rather than duplicate. Read them to confirm no regression against the current file content; D-07 permits re-opening only if a planning-time or execution-time re-read finds an actual regression.
    - `scripts/install.sh:85-182` — `write_wrapper`'s registry-comparison heredoc: the anchored `sed` extraction window and the numeric version regex R8 and S2 rest on. Confirm it is unmodified by this phase's diff.
    - `.planning/ROADMAP.md` section "Phase 4" — the five success criteria and the Requirement Coverage table
    - This plan's "Flagged Assumptions" section above — A1, A2 and A3 must be reflected in the closure note, not silently dropped
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md`'s R8 and S2 entries to record them as met, with the evidence being the three shipped cases rather than new work:
- R8: cite `tests/run-tests.sh` `I16` (`:3980-4105`, cases a-e: stale pin vs install registry to exit 127, absent and bad registry silent, exec target pinned to the install-time literal), `I17` (`:4107-4194`) and `I18` (`:4197-4239`, apostrophe in the plugin cache path). Add that criterion 5's docs-tier half — the published fallback one-liner reaching its validating `case` on every registry reply shape — is closed by plan `04-01`'s `I21`/`I21b`.
- S2: cite `I17` for the bounded extraction window across the empty-array, compact and semi-compact registry shapes, and `I16`'s lookalike-adjacent fixture for no-cross-plugin-misattribution.

Record, in the same edit, the one named residue from this plan's flagged-assumption pass (A2): a registry key differing only by Unicode normalization form would fail the exact-key match and degrade to silence. State it as an accepted assumption in the safe failure direction, satisfying S2's own "degrades to silence, never a false refusal" acceptance — not as an open defect and not as a closed one. Do not add a test for it; the user declined additional registry fixtures (D-07).

Add no new registry fixtures. Do not edit `scripts/install.sh`'s `write_wrapper` heredoc, `tests/run-tests.sh`'s `I16`/`I17`/`I18`, or any other file in this task.

Then close `delegate-agy-4bp` (Task 1) and `delegate-agy-4xn` (Task 2), each with a comment naming the commit that closed it and the case id that guards it.
  </action>
  <verify>
    <automated>bash tests/run-tests.sh > /tmp/agy-p4-p2t3.log 2>&1; echo "suite-rc=$?"; grep -E '^(PASS=|ok   - I1[678]|FAIL - )' /tmp/agy-p4-p2t3.log; git diff --name-only HEAD~1 HEAD; bd show delegate-agy-4bp 2>&1 | grep -i status; bd show delegate-agy-4xn 2>&1 | grep -i status</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s R8 and S2 entries each cite `I16`, `I17` and/or `I18` by id and by line range, and R8 additionally cites `I21`/`I21b` for criterion 5's docs-tier half.
    - The A2 residue (Unicode-normalization key mismatch degrading to silence) appears verbatim in the S2 entry as an accepted assumption.
    - `git diff --name-only HEAD~1 HEAD` lists exactly `.planning/REQUIREMENTS.md`.
    - Unchanged-evidence assertion: `git diff --name-only <phase-base>..HEAD -- scripts/install.sh` shows the file changed, but `git diff <phase-base>..HEAD -- scripts/install.sh | grep -c '^[+-].*reg_key_re_sq\|^[+-].*\[0-9\]+(\\\.\[0-9\]+)\*'` == 0 — the registry-comparison heredoc is untouched by this phase.
    - `bash tests/run-tests.sh` shows `ok   - I16 `, `ok   - I17 `, `ok   - I18 ` and reports `FAIL=0`.
    - `bd show delegate-agy-4bp` and `bd show delegate-agy-4xn` both report closed.
  </acceptance_criteria>
  <done>R8 and S2 read as met in REQUIREMENTS.md on shipped evidence, the A2 residue is recorded as an accepted assumption, no registry fixture or comparison logic was touched, and both beads are closed.</done>
</task>

</tasks>

<threat_model>
ASVS enforcement level: **1**. Block on severity: **high**. Grounded in 04-RESEARCH.md "Security Domain" (V5 Input Validation, V10 Malicious Code / Untrusted Data, V12 Files and Resources) and extended with this plan's own surface.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the process environment → `install.sh` / `uninstall.sh` | `HOME` is untrusted input from the caller's environment (`env -i`, a container entrypoint, a `User=`-less systemd unit) and determines every write destination both scripts use. |
| the user's shell rc files → `install.sh`'s python3 rewriter | The installer mutates files the user owns and did not hand it; the consent flag is the only authorization. |
| `installed_plugins.json` → the generated wrapper | Registry-supplied strings cross into a version comparison and a printed repin command. **Not modified by this plan** — recorded because Task 3 closes R8/S2 on the evidence that it is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | `install.sh` / `uninstall.sh` write destinations under an unset `HOME` | high | mitigate | D-06's explicit precondition refuses outright at `install.sh:~42` / `uninstall.sh:~20`, before every `$HOME` expansion. A default-path fallback is prohibited by this plan's `must_haves.prohibitions` because a guessable destination on a shared host is a plantable path. `I20`/`I20b` assert rc=1, the stated message, and the absence of `unbound variable`. |
| T-04-06 | Denial of Service | an install that aborts after `~/.local/bin/gemini` already shadows the real binary | high | mitigate | D-01's fail-open guard skips the rc-alias patch instead of aborting; `I19` asserts exit 0 with both wrappers present. Blast radius is why this is high rather than medium: the shim shadows `gemini` for every PATH caller, so a half-finished install leaves the box in a worse state than no install. |
| T-04-07 | Tampering | the user's `.bashrc` / `.zshrc` / `.bash_aliases` | medium | mitigate | The `AGY_SETUP_PATCH_ALIASES=1` consent gate is unchanged; the new guard only adds a skip path. `I19` asserts the rc file is byte-identical after a python3-absent run and that no `.bak-agy-*` file was created — i.e. the skip is a true no-op, not a backup-then-fail. |
| T-04-08 | Information Disclosure | the new fail-open warning on stderr | low | accept | The warning names a missing local binary and a skipped feature; it carries no path, credential, or registry-supplied string. Accepted, consistent with the two sibling warnings already shipped at `install.sh:281` and `:285`. |
| T-04-09 | Tampering / Elevation of Privilege | registry-supplied path or version string reaching `exec` or a printed re-run command | high | mitigate | Already shipped and unmodified: exact-key `sed` window plus the `^[0-9]+(\.[0-9]+)*$` version regex in `write_wrapper` (`install.sh:85-182`), covered by `I16`/`I17`/`I18`. This plan's mitigation is the prohibition against editing that heredoc plus Task 3's unchanged-evidence assertion. |
| T-04-SC | Tampering | npm / pip / cargo installs | n/a | accept | Zero package-manager installs in this phase's scope; the Package Legitimacy Gate is not triggered (04-RESEARCH.md "Package Legitimacy Audit": not applicable — `bash`, `python3`, `git`, `sed` and `grep` are all pre-existing system dependencies already used by these scripts). |
</threat_model>

<verification>
- `bash tests/run-tests.sh` reports `FAIL=0` after every task commit (04-VALIDATION.md sampling rate; the harness has no subset selector, so the full suite is the only granularity). Pre-phase baseline `PASS=153 FAIL=0`; after plan 04-01 `PASS` >= 158; after this plan `PASS` >= 161.
- `env -i PATH=/usr/bin:/bin bash scripts/install.sh` and the same for `scripts/uninstall.sh` each exit 1 with a stated HOME error and no `unbound variable` anywhere in their output.
- No file outside `files_modified` appears in this plan's diff. In particular `scripts/agy_bridge.sh` and `scripts/gemini_shim.sh` are untouched — this phase owns the installer, the generated wrapper and the docs only (04-CONTEXT.md, Out of scope).
- `write_wrapper`'s registry-comparison heredoc is byte-identical to its pre-phase content.
- Existing cases `I6`, `I6b`, `I8`, `I8b`, `I10`, `I16`, `I17`, `I18` all still report `ok`.
</verification>

<success_criteria>
- `delegate-agy-4bp` and `delegate-agy-4xn` are both closed.
- Both installer scripts refuse with a stated error when `HOME` is unset; neither substitutes a default path.
- `AGY_SETUP_PATCH_ALIASES=1` on a python3-less host completes the install with one warning and an untouched rc file.
- R8 and S2 read as met in REQUIREMENTS.md, on I16/I17/I18 evidence, with the A2 normalization residue recorded as an accepted assumption.
- Full suite `FAIL=0`, `PASS` >= 161.
- Five commits: a `test(...)`/`fix(...)` pair per code fix, plus the requirements closure.
</success_criteria>

<output>
Create `.planning/phases/04-installer-and-launcher-surface/04-02-SUMMARY.md` when done. Record, per the TDD gate rules: the exact `FAIL - I19` / `FAIL - I20` / `FAIL - I20b` lines and the pre-fix `unbound variable` text observed at each `test(...)` commit; the chosen warning wording and why it satisfies D-01's "names the alias-patch feature specifically"; the final `PASS=N FAIL=0` figure; and the disposition of flagged assumptions A1, A2, A3.
</output>

