---
beads_epic: delegate-agy-5r9
phase: 04-installer-and-launcher-surface
plan: 03
type: execute
gap_closure: true
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - .claude/commands/agy-setup.md
  - .claude/commands/agy-uninstall.md
  - scripts/install.sh
  - tests/run-tests.sh
autonomous: true
requirements: [R8]
beads: [delegate-agy-5r9.7, delegate-agy-5r9.8, delegate-agy-5r9.9]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Neither `agy-setup.md`'s nor `agy-uninstall.md`'s CLI-fallback block executes anything by itself. It resolves `$RESOLVED`, validates its shape, and prints it. A second, separate, explicit `bash \"$RESOLVED\"` line — outside the validating block — is what actually runs the installer/uninstaller."
    - "Feeding the fallback block a `claude plugin list --json` reply where a lookalike `agy-delegate@`-prefixed entry from a different marketplace is listed first no longer results in that entry's script executing merely by running the resolve/validate block. Executing anything requires the reader to run the second, separate `bash \"$RESOLVED\"` line themselves, after reading the printed path."
    - "A `claude plugin list --json` reply that is empty or not valid JSON degrades the fallback block to the same `ERROR: refusing to resolve ...` arm as a zero-match reply, with no Python traceback — even under `bash -euo pipefail`."
    - "`scripts/install.sh`'s python3-absent rc-alias-patch warning fires only when at least one rc file actually contains a recursive `alias gemini=` entry — never on a fresh `HOME` with no rc files, or rc files with no such alias, even with `AGY_SETUP_PATCH_ALIASES=1` set and `python3` absent."
    - "Neither `agy-setup.md` nor `agy-uninstall.md` contains the literal placeholder token `<that-path>` passed directly as a `bash` argument anywhere under a 'copy-paste this' heading. Every command a reader is told to paste is genuinely paste-able as written, using a real shell variable captured in the preceding step."
  artifacts:
    - "agy-setup.md / agy-uninstall.md — fallback block's `case` success arm prints (not execs) the resolved path; a new, separate fenced command below it runs `bash \"$RESOLVED\"`; the `python3 -c` expression wraps `json.load` in a try/except degrading to the existing empty-string default"
    - "agy-setup.md / agy-uninstall.md — the primary flow's step 1 captures the printed install path into a real variable (`AGY_PATH`) instead of leaving `<that-path>` as a placeholder; step 2 and the opt-in variants (`AGY_SETUP_REGISTER_TOKENSAVE=1`, `AGY_SETUP_PATCH_ALIASES=1`) use `\"$AGY_PATH\"`"
    - "scripts/install.sh — the python3-presence guard's warning moves from unconditional-before-the-loop to conditional-on-a-recursive-alias-actually-found-in-the-current-rc-file"
    - "tests/run-tests.sh — `I21`/`I21b` redesigned to assert the two-step contract (resolve+validate does not exec; a separate explicit `bash \"$RESOLVED\"` does) instead of one-shot auto-exec; new case `I19b` (python3-absent guard, no rc file has a recursive alias -> no warning line); new case `I22` (negative grep: no `<that-path>` token remains as a `bash` argument in either doc)"
  key_links:
    - "This gap was found by the phase-04 deep code review (`04-REVIEW.md`, findings CR-01/CR-02/WR-01/WR-02/IN-01) — not by the phase's own must_haves, which never claimed the fallback block's single-paste auto-exec was reviewed for marketplace identity. The fix must not weaken the existing `case` path-pattern/regular-file check (T-04-04 in `04-01-PLAN.md`'s threat model, which explicitly limited that check to shape-only and never claimed it as an identity guarantee) — it adds a human-review gate in front of execution, it does not change what the pattern match itself proves."
    - "`_md_extract`'s anchors (`RESOLVED=\"$(claude plugin list` .. `esac`) do not move — only the content inside that range changes (the success arm no longer execs). The separate `bash \"$RESOLVED\"` line this plan adds lives immediately after that range in the doc, in its own fenced block, and needs its own small extraction anchor in the test."
    - "I19's existing scenario (python3 absent + a REAL recursive alias present, from I8b's fixture) already proves the warning fires correctly when there IS something to patch — it is not touched by this plan. I19b is the missing negative case: python3 absent + NO recursive alias anywhere -> no warning."
  flagged_assumptions:
    - "The two-step split changes UX (the reader must run a second line to actually install), not the shipped installer/uninstaller scripts themselves — `scripts/install.sh` and `scripts/uninstall.sh` are out of scope for CR-01/CR-02/IN-01, only the two `.md` docs are."
    - "A registry key differing only by Unicode normalization form is still out of scope here (already recorded as accepted residue A2 in `04-02-PLAN.md`) — this plan's fix is about requiring human review before exec, not about making the identity match itself stronger."
  prohibitions:
    - "Do not weaken, reorder, or bypass the `case` statement's path-pattern match or its regular-file check in either doc's fallback block."
    - "Do not add BEGIN/END sentinel comments, fixture markers, or other harness scaffolding to the `.md` files — they remain user-facing copy-paste docs."
    - "Do not touch `scripts/install.sh`'s `write_wrapper` registry-comparison heredoc, or `tests/run-tests.sh`'s `I16`/`I17`/`I18` — unrelated, already-shipped, unaffected by this plan's scope."
    - "Do not weaken any existing assertion in `tests/run-tests.sh` to accommodate this plan's changes — redesign `I21`/`I21b`'s assertions to match the new two-step contract, do not delete coverage."
---

<objective>
Close three findings the phase-04 deep code review (`04-REVIEW.md`) surfaced outside this phase's own stated scope, in the same two doc files and one script 04-01/04-02 already touched:

- **`delegate-agy-5r9.7`** (CR-01, CR-02, IN-01 — Critical + Info, same code block): both command docs' CLI-fallback one-liner resolves **and bash-execs** the first `claude plugin list --json` entry whose `id` merely starts with `agy-delegate@`, with no pause for the reader to check the path first. A lookalike plugin from a different marketplace, listed first, gets executed. Reproduced end-to-end by the reviewer (`EVIL_RAN`). The same block's `json.load` also has no exception handling for a non-JSON reply, aborting with a raw Python traceback under `set -euo pipefail` instead of reaching the friendly `ERROR:` branch.
- **`delegate-agy-5r9.8`** (WR-01 — Warning): `scripts/install.sh`'s just-added python3-presence guard (04-02) warns "skipping the recursive-gemini rc alias patch" even when no rc file has a recursive alias to skip — a false positive that can mislead a user into thinking their setup needs attention when it doesn't.
- **`delegate-agy-5r9.9`** (WR-02 — Warning): this phase's doc rewrite introduced `<that-path>` bracket-placeholder syntax under a literal "copy-paste this" heading. `<`/`>` are shell redirection metacharacters; pasted verbatim, `bash <that-path>/scripts/install.sh` fails as "redirect stdin from a file named that-path," not "run the installer."

Per this project's standing rule that findings discovered by review are blockers, not deferred work, this plan closes all three in the current phase before phase 04 is marked complete.

**TDD note.** `workflow.tdd_mode` is enabled. Tasks 1 and 2 carry `tdd="true"`: the RED observation is "run the whole suite; the new/changed case(s) report `FAIL -`," then GREEN. Task 3 (the doc-only placeholder fix) adds one cheap negative-grep case rather than a full behavioral harness, since there is no runtime mechanism to exercise — only doc text.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-installer-and-launcher-surface/04-REVIEW.md
@.planning/phases/04-installer-and-launcher-surface/04-01-PLAN.md
@.planning/phases/04-installer-and-launcher-surface/04-02-PLAN.md
@.planning/phases/04-installer-and-launcher-surface/04-CONTEXT.md
</context>

## Why this plan exists (provenance)

Not part of the original phase-04 scope or `04-CONTEXT.md`'s locked decisions. Discovered by the `gsd-code-reviewer` deep pass spawned after 04-01 and 04-02 both shipped, reading `.claude-plugin/plugin.json`, both command docs, both installer scripts, and `tests/run-tests.sh` together. Full findings, reproductions, and reviewer-proposed fix directions are in `04-REVIEW.md` (CR-01, CR-02, WR-01, WR-02, IN-01) — read it before starting; this plan's tasks operationalize its "Fix" sections into TDD-disciplined tasks, they do not restate the analysis.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split the CLI-fallback's resolve/validate step from its exec step in both docs, and make the JSON parse degrade safely (CR-01, CR-02, IN-01)</name>
  <beads-id>delegate-agy-5r9.7</beads-id>
  <reversibility rating="reversible">Doc text plus a redesigned test assertion; revert restores the prior one-paste block and prior test.</reversibility>
  <files>.claude/commands/agy-setup.md, .claude/commands/agy-uninstall.md, tests/run-tests.sh</files>
  <read_first>
    - `04-REVIEW.md` sections "CR-01", "CR-02", "IN-01" — the exact reproduction, the exact current block, and the reviewer's proposed fix
    - `.claude/commands/agy-setup.md` and `.claude/commands/agy-uninstall.md` — the current fallback block in each (post 04-01, the `python3 -c 'import sys,json;print(next(...))'` one-liner feeding a `case`)
    - `tests/run-tests.sh` — `_md_extract` and `_md_fallback_case` (the shared I21/I21b harness) — read the whole function body before changing it; understand exactly what Tests 1-4 currently assert and why (the comments cite M1-M5 from `04-01-PLAN.md`)
    - The primary (non-fallback) flow directly above the fallback block in each doc — it already uses a "print the path, tell the reader to check it, then a separate command to run it" shape. The fallback fix should read like the natural extension of that same idiom to the CLI-resolved path, not a new pattern.
  </read_first>
  <behavior>
Redesign `I21`/`I21b` first, against the unfixed docs, and observe them fail for the right reason (the current one-shot block DOES auto-exec, which is exactly what the new assertion forbids).

  - **Test A (no auto-exec on a hostile reply):** feed the block a payload where a lookalike `agy-delegate@evil-marketplace` entry (with an `installPath` pointing at a marker that would print a distinct `EVIL_TOKEN` if executed) is listed before the legitimate entry. Run ONLY the extracted resolve/validate range (the same `_md_extract` anchors as today) under `bash -euo pipefail -c`. Assert: rc == 0, the output contains the resolved path (so the reader can see what they're about to run), and the output does **not** contain `EVIL_TOKEN` or any marker output at all — nothing was executed by this step alone, regardless of which entry `next()` picked.
  - **Test B (the doc's own second command line exists, is exactly right, and actually works end-to-end):** this test must prove properties of the DOC'S OWN TEXT, not a command the test harness invents independently — a doc that ships with the exec line missing entirely must fail this test even though Test A would still pass. Both docs already contain a THIRD, pre-existing, legitimate mention of the substring `bash "$RESOLVED"` — the opt-in-variants section's "e.g. `AGY_SETUP_REGISTER_TOKENSAVE=1 bash "$RESOLVED"`.)" illustration for CLI-fallback users (agy-setup.md line 84, agy-uninstall.md line 65), untouched by this task — so a whole-file *substring* count is not a safe way to detect "did the new standalone line get added." Use an exact-whole-line match instead: the new line this task adds is `bash "$RESOLVED"` alone on its own line inside its own fenced block, while both the pre-fix embedded form (`    && bash "$RESOLVED" \`) and the "e.g." prose mention carry leading/trailing text on the same line, so neither is a whole-line match. Sub-checks, all against the live file `$doc`: (i) `grep -xc 'bash "$RESOLVED"' "$doc"` (exact whole-line match, whole file) == 1 — this alone discriminates all three cases (pre-fix embedded: 0; correct fix: 1; fix with the new line simply missing: 0 — never colliding with the pre-existing prose mention, which is never a whole-line match). (ii) Extract that one exact-matching line via `grep -xm1 'bash "$RESOLVED"' "$doc"` (redundant with (i) once count==1, but keeps the two checks independently readable) — then, using the same big-payload fixture as today's Test 1+2 (oversized multi-match reply, legitimate entry first), run the resolve/validate range, parse the resolved path out of its own printed `Resolved: ...` line, export it as `RESOLVED`, and execute the DOC'S EXTRACTED line text (not a hand-written `bash "$RESOLVED"` in the test) against that export. Assert that run's output contains the marker's token and its own path — the full path is still mechanically correct end-to-end, and this is now proven by running the actual shipped text, not a stand-in for it.
  - **Test C (single-match parity):** same as today's Test 3 — the resolve/validate range's printed output for a single-match reply is byte-identical to Test B's first-run resolve/validate output for the multi-match reply (same first `installPath`).
  - **Test D (zero-match, and now also invalid-JSON):** same as today's Test 4 (zero-match -> `ERROR:` arm, rc 0, no traceback) PLUS a new fixture: the stub `claude` prints empty output (simulating `claude plugin list --json` itself failing). Assert the resolve/validate range still exits 0, still reaches an `ERROR:` arm, and still produces no `Traceback`/`IndexError`/`StopIteration`/`JSONDecodeError` in its output.
  </behavior>
  <action>
**RED.** Redesign `_md_fallback_case` (shared by `I21` and `I21b`) to implement Tests A-D above in place of the current Tests 1-4. Keep the fixture-construction style already established (mktemp sandbox, stub `claude` cat-ing a payload file, marker scripts printing a unique token plus `$0`), but:
  - Add a distinct `EVIL_TOKEN_<id>` marker at the lookalike entry's `installPath` for Test A, separate from the legitimate marker used by Tests B/C.
  - Add an empty-payload fixture for Test D's new invalid-JSON case.
  - Capture the resolve/validate range's own printed `$RESOLVED` value from its stdout (it must print the resolved path on the success arm — see GREEN below).
  - For Test B, extract the doc's own second-line text with an exact whole-line `grep -x` (NOT a substring `grep -c`/`grep -F` over the whole file) — both docs already carry a pre-existing, legitimate "e.g. ... bash \"$RESOLVED\"\"" mention inside opt-in-variants prose (agy-setup.md:84, agy-uninstall.md:65, untouched by this task) that a substring whole-file count would collide with, wrongly reporting the new line present when it is missing, or missing when it is present. `grep -x` requires the ENTIRE line to equal `bash "$RESOLVED"` with nothing else on it, which the prose mention and the pre-fix embedded form both fail (each carries extra text on the same line) and only the new standalone line satisfies. Do not let Test B pass by constructing its own `bash "$RESOLVED"` invocation independently of what the doc actually contains; that was an earlier revision's gap (a doc that shipped with no second exec line at all would still have passed).

Run the full suite, observe `FAIL - I21` and `FAIL - I21b` against the current (unfixed, one-shot-exec) doc blocks — the current blocks fail Test A because they DO execute the lookalike marker when it's the resolved match. Record the exact pre-fix behavior observed (which marker ran) in the SUMMARY. Commit as `test(04-03): redesign I21/I21b for the two-step resolve-then-exec contract`.

**GREEN.** In both `.claude/commands/agy-setup.md` and `.claude/commands/agy-uninstall.md`, edit the fallback block:
  1. Change the `python3 -c` expression to wrap `json.load(sys.stdin)` in a `try`/`except` that falls back to an empty list on any exception, keeping the same `next(..., "")` selection and empty-string default on zero matches (IN-01). Keep it inline in the same `-c` argument style already used (embedded newlines inside the single-quoted argument are fine — this is already a multi-line paste block).
  2. Change the `case` statement's success arm from `[[ -f "$RESOLVED" ]] && bash "$RESOLVED" || echo "ERROR: ..."` to `[[ -f "$RESOLVED" ]] && echo "Resolved: $RESOLVED" || echo "ERROR: resolved installer '$RESOLVED' is not a regular file — is agy-delegate installed?" >&2`. Do not touch the `case`'s path patterns or the `*) ... ;;` refusal arm — those are the check this plan must not weaken.
  3. Immediately below that block, in its own new fenced `bash` block, add: `bash "$RESOLVED"`, with one line of prose above it telling the reader to check the printed `Resolved: ...` line before running it — mirroring the "check it looks right, then run" phrasing already used for the primary flow directly above.

Re-run the full suite; `I21` and `I21b` must both report `ok`. Commit as `fix(04-03): require an explicit second step to exec the CLI-fallback's resolved path, and make its JSON parse fail closed to the existing empty-result branch`.
  </action>
  <verify>
    <automated>bash tests/run-tests.sh > /tmp/agy-p4-p3t1.log 2>&1; echo "suite-rc=$?"; grep -E '^(PASS=|FAIL=|FAIL - )|I21' /tmp/agy-p4-p3t1.log; for f in .claude/commands/agy-setup.md .claude/commands/agy-uninstall.md; do echo "== $f =="; INRANGE=$(sed -n '/^RESOLVED="\$(claude plugin list/,/^esac$/p' "$f" | grep -c 'bash "$RESOLVED"'); EXACT=$(grep -xc 'bash "$RESOLVED"' "$f"); echo "inrange=$INRANGE exact_line_whole_file=$EXACT"; grep -n 'bash "\$RESOLVED"' "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - The git log shows a `test(04-03): ...I21/I21b...` commit preceding the `fix(04-03): ...` commit, and the SUMMARY records which marker (`EVIL_TOKEN_*` or the legitimate one) the pre-fix block executed when it auto-exec'd Test A's hostile fixture.
    - Region-scoped negative gate, both files: `sed -n '/^RESOLVED="\$(claude plugin list/,/^esac$/p' <file>` contains no `bash "$RESOLVED"` as a substring (the resolve/validate range itself never execs) — `grep -c 'bash "\$RESOLVED"'` over that range == 0.
    - Exact-whole-line gate, both files, whole file (NOT a substring count — both docs carry a pre-existing, legitimate, untouched "e.g. ... bash \"$RESOLVED\"" prose mention in the opt-in-variants section that a substring count would wrongly catch): `grep -xc 'bash "$RESOLVED"' <file>` == 1. This single check discriminates all three states correctly: pre-fix (the line is embedded as `    &amp;&amp; bash "$RESOLVED" \` — not a whole-line match — count 0), a correct fix (the new standalone line — count 1), and a broken fix where the new line was never added (still 0, and it stays 0 regardless of the pre-existing prose mention, which never whole-line-matches either).
    - The extracted line (`grep -xm1 'bash "$RESOLVED"' <file>`) is exactly `bash "$RESOLVED"` — no trailing redirect, flag, or suppressed-error variant (this is what the exact-match grep already enforces; this bullet exists to make explicit that its content, not just its count, is verified).
    - Test B in the SUMMARY explicitly states it executed the line extracted from the doc (not a literal the test wrote itself) and that this run produced the marker's token and path.
    - The `case`'s path patterns and refusal arm are byte-identical to pre-fix: `grep -c 'agy-delegate/\*/scripts/\(un\)\?install.sh' <file>` unchanged from before this task, and `grep -c '^\s*\*) echo "ERROR: refusing to'` == 1 in each file.
    - `bash tests/run-tests.sh` output contains lines matching `ok .*I21 ` and `ok .*I21b` and reports `FAIL=0`, with `PASS` == plan 04-02's final figure (158) + 0 net (same two case ids, redesigned in place — count unchanged unless a genuinely new case id was added for this task, which it was not).
    - `git diff HEAD~1 -- scripts/install.sh scripts/uninstall.sh` is empty (this task touches docs and the test file only).
  </acceptance_criteria>
  <done>Both docs' fallback blocks resolve, validate, and print without executing; a separate explicit line execs the validated path; the JSON parse degrades to the existing empty-result branch on any exception; `I21`/`I21b` prove all four properties; the full suite reports `FAIL=0`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Only warn about the python3-absent rc-alias guard when a recursive alias was actually found (WR-01)</name>
  <beads-id>delegate-agy-5r9.8</beads-id>
  <reversibility rating="reversible">A guard relocated inside the existing loop, plus one new test case; revert restores the prior unconditional-before-loop warning.</reversibility>
  <files>scripts/install.sh, tests/run-tests.sh</files>
  <read_first>
    - `04-REVIEW.md` section "WR-01" — the exact reproduction and the reviewer's proposed fix
    - `scripts/install.sh:226-250` — the whole consent-gated rc-alias-patch block this plan's predecessor (04-02) added the `_alias_patch_py3_ok` guard to: the unconditional check at `:227-230` (before the `for RC in ...` loop), and the loop body's existing `grep -qE ... alias gemini=` match branch where the dry-run advisory and the `_alias_patch_py3_ok` skip both currently live
    - `tests/run-tests.sh` — locate `I19` via `grep -n 'I19\b' tests/run-tests.sh`; read its full fixture (curated `nopy` PATH, `I8b`'s real-recursive-alias setup) — this plan does not touch `I19`, only adds a sibling
    - `tests/run-tests.sh:3668-3684` — `_fresh_home`, the isolation harness `I19b` will reuse for a from-scratch `HOME` with no rc files at all
  </read_first>
  <behavior>
Write `I19b` first, against the unfixed guard, and observe it red.

  - Setup: a **fresh** `HOME` with no `.bashrc`, `.zshrc`, or `.bash_aliases` at all (or rc files that exist but contain no recursive `alias gemini=` line), a real `gemini` at `$IH/otherbin/gemini` (so `_real_gemini` is non-empty), `AGY_SETUP_PATCH_ALIASES=1`, and `I19`'s `nopy` curated PATH (python3 absent).
  - Expect after the fix: `install.sh` exits 0, and the captured log contains **no** line matching `python3 not found` — there was nothing to patch, so nothing is skipped, so nothing should be warned about.
  - Pre-fix red: the unconditional guard at `:227-230` fires as soon as `_real_gemini` is set and python3 is absent, regardless of whether the loop below ever finds a matching rc file — so the warning appears despite there being nothing to skip.
  </behavior>
  <action>
**RED.** Add case `I19b` to `tests/run-tests.sh` immediately after `I19`, reusing `I19`'s `nopy` curated PATH and `_fresh_home` isolation, but with no rc file carrying a recursive alias (either no rc files at all, or rc files present with unrelated content — pick whichever is the smaller diff against `I19`'s existing fixture-building code). Assert rc == 0 and `grep -qi 'python3 not found' <captured log>` is FALSE.

Run the full suite, observe `FAIL - I19b`, and record the exact false-positive warning line observed. Commit as `test(04-03): add failing I19b for the python3-absent guard's false-positive warning`.

**GREEN.** Move the python3-presence check from unconditional-before-the-loop to conditional-on-a-match, inside the loop's existing `if grep -qE "^alias gemini='[^']* gemini'\$" "$RC" ...; then` branch, per `04-REVIEW.md`'s WR-01 fix direction:

1. Replace the current unconditional block at `:227-230` (`_alias_patch_py3_ok=1; if [[ "${AGY_SETUP_PATCH_ALIASES:-0}" == "1" ]] && ! command -v python3 ...; then _alias_patch_py3_ok=0; echo "WARNING: ..."; fi`) with just `_alias_patch_py3_ok=1` before the loop — the flag still exists and still gates the `continue` inside the loop (04-02's ordering assertion B, which this task must not break), but the `command -v python3` check and its warning move.
2. Inside the loop, immediately after the existing `if grep -qE ... alias gemini= ...; then` match is confirmed (i.e., there IS something to patch) and the flag is confirmed true, add the `command -v python3` check there: if patch-alias consent is on and python3 is absent, set `_alias_patch_py3_ok=0` and emit the warning **once** (track a `_alias_patch_py3_warned` flag so a second matching rc file doesn't repeat it — matching 04-02's original "one warning per run" intent, now correctly scoped to "one warning per run in which there was something to warn about").
3. Keep the existing `continue`-on-flag-zero check exactly where 04-02 put it (after the dry-run advisory's own `continue`, before the `cp -f` backup) — this task does not change *that* ordering, only *when the flag gets set to 0 and warned about* in the first place.

Re-run the full suite; `I19b` must report `ok`, and `I19` (the positive case: alias present, warning correctly fires) must stay `ok`. Commit as `fix(04-03): only warn about the python3-absent rc-alias guard once a recursive alias is actually found`.
  </action>
  <verify>
    <automated>bash tests/run-tests.sh > /tmp/agy-p4-p3t2.log 2>&1; echo "suite-rc=$?"; grep -E '^(PASS=|FAIL=|FAIL - )|I19\b|I19b' /tmp/agy-p4-p3t2.log; grep -n '_alias_patch_py3_ok\|_alias_patch_py3_warned\|command -v python3\|for RC in\|alias gemini=' scripts/install.sh</automated>
  </verify>
  <acceptance_criteria>
    - The git log shows `test(04-03): ...I19b...` preceding `fix(04-03): ...python3-absent guard...`, and the SUMMARY records the exact false-positive warning line observed at the red commit.
    - Ordering assertion (the check moved into the loop, past the match): the line number of the (moved) `command -v python3` occurrence is greater than the line of `if grep -qE "\^alias gemini=` and less than the line of `cp -f "\$RC" "\$RC.bak-agy-`.
    - The existing skip-ordering guarantee from 04-02 is unchanged: the line number of the `_alias_patch_py3_ok` test that triggers `continue` is still greater than the dry-run advisory's `continue` and less than `cp -f "\$RC" "\$RC.bak-agy-`.
    - `bash tests/run-tests.sh` output contains lines matching `ok .*I19b` and `ok .*I19 ` (both must pass — the positive and negative cases) and reports `FAIL=0`, with `PASS` == plan 04-02's final figure (158) + 1.
    - The `fix(...)` commit touches only `scripts/install.sh`.
  </acceptance_criteria>
  <done>The python3-absent guard warns only when a recursive alias was actually found and would have been patched; `I19b` proves the false-positive is gone while `I19` proves the true-positive still fires; the full suite reports `FAIL=0`.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the `&lt;that-path&gt;` placeholder with a real captured shell variable in both docs (WR-02)</name>
  <beads-id>delegate-agy-5r9.9</beads-id>
  <reversibility rating="reversible">Doc text only; revert restores the prior placeholder.</reversibility>
  <files>.claude/commands/agy-setup.md, .claude/commands/agy-uninstall.md, tests/run-tests.sh</files>
  <read_first>
    - `04-REVIEW.md` section "WR-02" — the exact reproduction and the reviewer's two proposed fix options; this task takes option (a), capturing the path into a real variable
    - Confirmed this session by `grep -n '<that-path>'` on both files: `agy-setup.md` has **7** occurrences (lines 45, 71, 78, 93 — bare `bash <that-path>/...` commands; lines 81, 83, 96 — prose parentheticals explaining the placeholder). `agy-uninstall.md` has **4** (lines 36, 60 — commands; lines 63, 64 — prose). All 11 must be gone after this task, not just the command lines — a whole-file `grep -c '<that-path>'` is the acceptance gate, and it does not distinguish command occurrences from prose ones.
    - The fallback block's own `$RESOLVED` variable idiom (same file, section just below) — the primary flow's new variable should read as a sibling of it, not an unrelated new convention
  </read_first>
  <action>
Change the primary flow's step 1 in both docs from a bare pipeline to one that captures its result into a real shell variable, e.g.:

```bash
AGY_PATH="$(grep -A6 '"agy-delegate@' ~/.claude/plugins/installed_plugins.json \
  | sed -n 's/.*"installPath"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' | head -1)"
echo "$AGY_PATH"
```

(mirroring the existing `sed -n ... | head -1` extraction verbatim — only the capture-into-a-variable-and-echo wrapping is new). Apply the identical pattern in `agy-uninstall.md` (its own `AGY_PATH` capture in that file's step 1 — the two docs are separate files with separate shell sessions, so each needs its own declaration, not a shared one). Then replace **every** remaining `bash <that-path>/scripts/install.sh` (or `uninstall.sh`) command — including the opt-in variants (`AGY_SETUP_REGISTER_TOKENSAVE=1`, `AGY_SETUP_PATCH_ALIASES=1`, `AGY_UNINSTALL_TOKENSAVE=1`) — with `bash "$AGY_PATH/scripts/install.sh"` (or `uninstall.sh`), so each command is genuinely paste-able as written in the same shell session as step 1.

Update **every** prose parenthetical that currently explains `<that-path>` as a placeholder (agy-setup.md lines 81/83/96; agy-uninstall.md lines 63/64 — see `read_first` for the confirmed full list) to instead describe `$AGY_PATH` as the variable set in step 1, and to say that CLI-fallback users should use `$RESOLVED` in its place (unchanged advice, just no longer describing a placeholder token that doesn't survive a literal paste). Missing any one of these leaves a stray `<that-path>` that the whole-file acceptance grep below will catch — but do the full sweep in the action, don't rely on the gate to find it for you.

Do not touch the fallback block this task's sibling task (Task 1) owns — `$RESOLVED` there is untouched by this task.

Add case `I22` to `tests/run-tests.sh`: a plain negative grep (no sandbox/exec needed — this is pure doc text) asserting, for BOTH `.claude/commands/agy-setup.md` and `.claude/commands/agy-uninstall.md`: `grep -c '<that-path>'` == `0`, `grep -c 'AGY_PATH='` >= `1` (the variable is actually declared in that file, not just referenced), and `grep -c 'bash "\$AGY_PATH'` >= `1` (the replacement variable is actually used to run something, not just declared). Run the full suite; `I22` must report `ok`. Commit as `fix(04-03): replace the <that-path> doc placeholder with a captured $AGY_PATH variable in both docs`.
  </action>
  <verify>
    <automated>bash tests/run-tests.sh > /tmp/agy-p4-p3t3.log 2>&1; echo "suite-rc=$?"; grep -E '^(PASS=|FAIL=|FAIL - )|I22' /tmp/agy-p4-p3t3.log; for f in .claude/commands/agy-setup.md .claude/commands/agy-uninstall.md; do echo "== $f =="; grep -c '<that-path>' "$f"; grep -c 'AGY_PATH=' "$f"; grep -n 'bash "\$AGY_PATH' "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<that-path>' .claude/commands/agy-setup.md` == 0 and `grep -c '<that-path>' .claude/commands/agy-uninstall.md` == 0 (whole-file — covers both command lines and prose parentheticals, all 11 confirmed occurrences).
    - `grep -c 'AGY_PATH=' .claude/commands/agy-setup.md` >= 1 AND `grep -c 'AGY_PATH=' .claude/commands/agy-uninstall.md` >= 1 (each file declares its own variable — they are separate shell sessions, not a shared one).
    - Every `bash ... scripts/install.sh` / `scripts/uninstall.sh` invocation outside the fallback block's own `$RESOLVED` line now reads `bash "$AGY_PATH/scripts/...` in BOTH files — spot-checked via `grep -n 'bash "\$AGY_PATH'` returning at least one hit per opt-in variant plus the primary Install/Uninstall command, in each file independently.
    - `bash tests/run-tests.sh` output contains a line matching `ok .*I22` and reports `FAIL=0`, with `PASS` == plan 04-02's final figure (158) + 2 (I19b from Task 2, I22 from this task).
    - `git diff HEAD~1 -- scripts/install.sh scripts/uninstall.sh` is empty (doc-only task).
  </acceptance_criteria>
  <done>Neither doc contains the bare `&lt;that-path&gt;` placeholder as a `bash` argument; every copy-paste command in both docs is genuinely paste-able using a real captured variable; `I22` proves it; the full suite reports `FAIL=0`.</done>
</task>

</tasks>

<verification>
- `bash tests/run-tests.sh` reports `FAIL=0` after every task commit. This plan ends at plan 04-02's final figure (158) + 2 net new cases (`I19b`, `I22`; `I21`/`I21b` are redesigned in place, not added).
- Neither `.claude-plugin/plugin.json`, `scripts/install.sh`'s `write_wrapper` heredoc, nor `tests/run-tests.sh`'s `I16`/`I17`/`I18` appear in this plan's diff.
- Existing cases `I8`, `I8b`, `I10`, `I16`, `I17`, `I18`, `I19`, `I20`, `I20b` all still report `ok`.
- `git diff --name-only <plan-base>..HEAD` lists only `.claude/commands/agy-setup.md`, `.claude/commands/agy-uninstall.md`, `scripts/install.sh`, and `tests/run-tests.sh`.
</verification>

<success_criteria>
- `delegate-agy-5r9.7`, `delegate-agy-5r9.8`, and `delegate-agy-5r9.9` are all closed.
- Neither doc's CLI-fallback block executes anything by itself; a separate explicit line does; a lookalike-marketplace entry can no longer be silently bash-exec'd by the resolve/validate step alone.
- The fallback's JSON parse degrades to the existing empty-result branch on any exception, with no traceback.
- `scripts/install.sh`'s python3-absent rc-alias warning fires only when there is something to skip.
- Neither doc contains the `&lt;that-path&gt;` placeholder as a `bash` argument; every copy-paste command is genuinely paste-able.
- Full suite `FAIL=0` at plan 04-02's final figure (158) + 2.
- Six commits: a `test(...)`/`fix(...)` pair per task.
</success_criteria>

<output>
Create `.planning/phases/04-installer-and-launcher-surface/04-03-SUMMARY.md` when done. Record: the exact `FAIL -` lines and pre-fix reproductions observed at each `test(...)` commit (which marker Test A's hostile fixture ran pre-fix; the exact false-positive warning line pre-fix); the final `PASS=N FAIL=0` figure; and confirmation that all three beads (`delegate-agy-5r9.7/.8/.9`) are closed with commit references.
</output>